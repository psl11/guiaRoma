---
phase: 06-derivados-de-datos-b-squeda-y-ruta-del-d-a
plan: 05
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - tests/parity/search-route.spec.ts
autonomous: false
requirements: [FEAT-03, FEAT-09]
nyquist_compliant: true

must_haves:
  truths:
    - "On the built /guiaRoma/ site: typing <2 chars keeps the dropdown closed; ≥2 chars opens it with ≤8 .search-result rows; a no-match query shows 'Sin resultados'"
    - "Clicking a search result adds .highlight to the target card and does NOT change the URL hash (navigateToCard / preventDefault — SC#2)"
    - "Each day's route button is visible only with ≥2 stops, shows 'Ver ruta del día (N paradas)', and its href is a https://www.google.com/maps/dir/?api=1&travelmode=walking… URL (asserted by structure, never fetched)"
    - "A human confirms search + route parity against the live index.html in light/dark × mobile/desktop"
  artifacts:
    - path: "tests/parity/search-route.spec.ts"
      provides: "Self-contained Playwright spec (build + serve under /guiaRoma/, port base 5740) covering dropdown behavior, result→navigation, and route-button visibility/label/href"
  key_links:
    - from: "tests/parity/search-route.spec.ts"
      to: "the built .output/public served under /guiaRoma/"
      via: "self-contained generate + cpSync + pnpm dlx serve (mirror of modes.spec.ts/navigation.spec.ts)"
      pattern: "guiaRoma|STATIC_PORT|pnpm.*generate"
---

<objective>
Prove the search + route behavior on the real built SSG site with a self-contained Playwright spec `tests/parity/search-route.spec.ts` (FEAT-03 + FEAT-09), then get a human paridad sign-off against the live `index.html`.

Purpose: The unit tests (06-01/06-02) prove the pure logic; this plan proves the wired behavior end-to-end on the generated static site — the dropdown thresholds/limits, result→`navigateToCard` (`.highlight` + hash unchanged), and the route button's visibility/label/href. It mirrors the established self-contained parity spec pattern (`modes.spec.ts`, `navigation.spec.ts`) that builds and serves under `/guiaRoma/` rather than the golden's live `index.html` webServer. The human checkpoint is the final paridad gate for the phase's user-visible surfaces (Phase 8 does the pixel-diff later).
Output: `tests/parity/search-route.spec.ts` (green under `pnpm test:golden`) + an approved human verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-RESEARCH.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-PATTERNS.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-VALIDATION.md

<depends_on_note>
Depends on BOTH 06-03 (route button in DaySection) and 06-04 (SearchBox wired into TheHero) — the spec asserts both surfaces on the built site, so both UI plans must be merged first. No prior SUMMARY re-read required; the spec targets the rendered DOM (`#search`, `.search-results.show`, `.search-result`, `.day-route-btn`).
</depends_on_note>

<memory_note>
Project memory: run via sequential execution (`use_worktrees=false` — already set in config). Serve the build under a `guiaRoma/` SUBDIR and open `/guiaRoma/` (serving `.output/public` at root = blank page). The bootstrap below already does this (mkdtemp → `guiaRoma/` subdir → cpSync → serve).
</memory_note>

<interfaces>
<!-- The self-contained Playwright bootstrap to copy verbatim. Extracted from modes.spec.ts:1-110. -->
Bootstrap pieces to copy VERBATIM from tests/parity/modes.spec.ts (lines 1-110):
- imports (`spawn`/`spawnSync`/`cpSync`/`existsSync`/`mkdtempSync`/`mkdirSync`/`rmSync`/`tmpdir`/`join`/`expect`/`test`)
- `EXPECTED_HYDRATION_MSG = /Hydration completed but contains mismatches/i`
- `OUTPUT_DIR = join(process.cwd(), '.output', 'public')`
- `waitForServer(url, timeoutMs)` (polls fetch until status > 0)
- `killGroup(proc)` (SIGTERM the detached group)
- `ensureBuild()` (`pnpm generate` if `.output/public/index.html` missing)
- the `beforeAll` (mkdtemp → `guiaRoma/` subdir → cpSync → `spawn('pnpm', ['dlx','serve','-l', PORT, previewRoot], { detached:true })` → `waitForServer`) and `afterAll` (killGroup + rm temp)
- the `gotoHydrated(page)` helper (goto → `waitForLoadState('networkidle')` → assert a hydrated signal)

Port collision: modes.spec.ts uses 5700, navigation.spec.ts uses 5720 → use a NEW base:
```typescript
const STATIC_PORT = 5740 + Number(process.env.TEST_WORKER_INDEX ?? 0)
const STATIC_URL = `http://localhost:${STATIC_PORT}/guiaRoma/`
```
Console-error gate (verbatim from modes/navigation): tolerate ONLY `EXPECTED_HYDRATION_MSG`, fail on any other console error.
navigateToCard `.highlight` + hash assertion (mirror navigation.spec.ts:221-229):
```typescript
await expect(page.locator(`#${slug}`)).toHaveClass(/\bhighlight\b/)
expect(new URL(page.url()).hash).not.toBe(`#${slug}`)   // navigateToCard does preventDefault (D-03)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/parity/search-route.spec.ts — dropdown + result→navigation + route button</name>
  <files>tests/parity/search-route.spec.ts</files>
  <read_first>
    - tests/parity/modes.spec.ts:1-110 — copy the ENTIRE bootstrap verbatim (imports, `EXPECTED_HYDRATION_MSG`, `OUTPUT_DIR`, `waitForServer`, `killGroup`, `ensureBuild`, `beforeAll`/`afterAll`, `gotoHydrated`). Adapt only the port base and the hydrated-signal assertion.
    - tests/parity/navigation.spec.ts — the analog for the `.highlight` + hash-unchanged assertion (the `navigateToCard` parity check) and the console-error gate usage; reuse its result-click → `.highlight` → `expect(hash).not.toBe('#slug')` shape.
    - app/components/SearchBox.vue (from 06-04) — confirm the rendered selectors: `#search` input, `#search-results.search-results` (toggles `.show`), `.search-result` rows, the `Sin resultados` empty state.
    - app/components/DaySection.vue (from 06-03) — confirm `.day-route-btn` selector, the label text format, and that it lives inside `.day-stats`.
    - content/trips/roma/days/*.yml — to pick a known day for the route-button assertion (e.g. viernes = 6 stops → button visible, label "Ver ruta del día (6 paradas)").
    - 06-VALIDATION.md §Per-Task Verification Map — the exact behaviors this spec must cover (≥2 opens / max 8 / "Sin resultados" / result→`.highlight` / button visibility+label+href).
  </read_first>
  <action>
    Create `tests/parity/search-route.spec.ts` as a SELF-CONTAINED spec (NOT using the golden webServer, which serves the OLD live index.html). Copy the modes.spec.ts bootstrap verbatim; set `STATIC_PORT = 5740 + worker`. Header doc-comment: cite the analogs, state it covers FEAT-03 dropdown behavior + result→navigation (SC#2) and FEAT-09 route-button visibility/label/href (SC#3), tolerates ONLY the color-mode hydration message, and does not rebaseline the golden.
    `gotoHydrated(page)`: goto `STATIC_URL` → `waitForLoadState('networkidle')` → assert a hydrated signal (the page is interactive). Use a robust signal such as the first pace-btn being `.active` (as modes.spec does) OR typing into `#search` and expecting `.search-results.show`.
    Required test cases (each registers the console-error gate via `page.on('console', …)` tolerating only `EXPECTED_HYDRATION_MSG`, and fails if `consoleErrors.length` > 0 at the end):
    1. Dropdown threshold: fill `#search` with 1 char → `#search-results` does NOT have `.show` (closed). Fill with ≥2 chars matching a real monument (e.g. "Pante" → Pantheon) → `.search-results.show` visible with ≥1 `.search-result`.
    2. Max 8: fill with a broad ≥2-char query that matches many monuments and assert `page.locator('.search-result').count()` is ≤ 8.
    3. Empty state: fill with a ≥2-char query that matches nothing (e.g. "zzzqqq") → the dropdown shows the `Sin resultados` text (assert `#search-results` contains text "Sin resultados" and there are 0 `.search-result` rows).
    4. Result → navigation (SC#2): fill a query, click the first `.search-result`, then assert the target card (`#<slug>` read from the result's `data-card`/href) gets `.highlight` (`toHaveClass(/\bhighlight\b/)`) AND `new URL(page.url()).hash` is NOT `#<slug>` (navigateToCard preventDefault). Use `.click()` (the dropdown row is in-viewport).
    5. Route button (FEAT-09): on a known day (viernes), assert its `.day-route-btn` is visible, its text matches `/Ver ruta del día \(\d+ paradas\)/` (and for viernes specifically "(6 paradas)"), and its `href` matches `/^https:\/\/www\.google\.com\/maps\/dir\/\?api=1&travelmode=walking/`. NEVER fetch the href.
    Scope guards (do NOT assert): no keyboard-nav, no enriched rows, no pixel/screenshot diff (Phase 8 owns visual-diff). Keep it behavior-only.
  </action>
  <acceptance_criteria>
    - `tests/parity/search-route.spec.ts` is self-contained (its own `beforeAll` build+serve under `/guiaRoma/`, port base 5740) and does NOT rely on `playwright.config.ts`'s webServer.
    - The spec asserts: <2 chars closed; ≥2 chars opens `.search-results.show`; `.search-result` count ≤ 8; "Sin resultados" on a no-match query.
    - The spec asserts result-click → target card has `.highlight` AND `page.url()` hash is unchanged (SC#2 / D-03).
    - The spec asserts a day's `.day-route-btn` is visible, label matches `/Ver ruta del día \(\d+ paradas\)/`, and `href` matches the `https://www.google.com/maps/dir/?api=1&travelmode=walking` prefix (never fetched).
    - The console-error gate tolerates only `EXPECTED_HYDRATION_MSG` and fails on any other error.
    - `pnpm test:golden -- search-route` (or the full `pnpm test:golden`) runs `search-route.spec.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:golden -- tests/parity/search-route.spec.ts</automated>
  </verify>
  <done>`tests/parity/search-route.spec.ts` exists, is self-contained (build+serve under `/guiaRoma/`, port 5740), and passes — covering dropdown ≥2/max-8/"Sin resultados", result→`.highlight`+hash-unchanged (SC#2), and route-button visibility/label/href (FEAT-09).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human paridad sign-off — search + route vs the live index.html</name>
  <files>n/a — human verification of the built site, no file changes</files>
  <what-built>
    FEAT-03 (client search) and FEAT-09 ("ruta del día") are now wired and rendered on the built SSG site:
    - The `#search` box in `#inicio` (the masthead) filters monuments via MiniSearch over typed data — dropdown opens at ≥2 chars, max 8 results, "Sin resultados" when none, click navigates to the ficha (smooth scroll + highlight, no hash change).
    - Each day's `.day-stats` band shows a "Ver ruta del día (N paradas)" button linking to a Google Maps walking route built from that day's `day.cards` in order (Saturday includes the Vatican guided + Auditorium concert — 8 stops).
    All automated gates are green: `pnpm test:unit` (dayRoute + searchIndex), `pnpm test:golden` (incl. the new self-contained `search-route.spec.ts`).
  </what-built>
  <how-to-verify>
    1. Build + serve the site under the subpath (per project memory — serving at root shows a blank page):
       - `pnpm generate`
       - serve `.output/public` under a `guiaRoma/` subdir and open `http://localhost:<port>/guiaRoma/` (e.g. copy `.output/public` into `…/guiaRoma/` and `pnpm dlx serve -l <port> <parent>`), OR open the page the running `search-route.spec.ts` serves.
    2. Open the LIVE original `index.html` side by side (the parity reference).
    3. SEARCH — compare both: type a monument name (e.g. "Pantheon"), a partial/prefix (e.g. "Cara" → Caravaggio-tagged card), a badge word ("Sorrentino"), and a nonsense string. Confirm: dropdown opens at 2 chars, ≤8 rows, each row shows the name + its day, "Sin resultados" appears for the nonsense query, clicking a result scrolls+highlights the ficha and the URL bar does NOT gain a `#hash`. The new build should find AT LEAST what the original finds (it may find a bit more — that's the sanctioned SC#1 improvement).
    4. ROUTE — for several days (incl. Saturday): confirm the "Ver ruta del día (N paradas)" button appears with the same N as the original, opens Google Maps with the day's stops in the same order. Spot-check Saturday includes the Vatican + Auditorium.
    5. Repeat the visual spot-checks in BOTH light and dark theme, and at BOTH mobile and desktop widths (the button goes full-width under 600px; the dropdown/search styling should match the original in both themes).
  </how-to-verify>
  <resume-signal>Type "approved" if search + route match the live index.html (at least parity for search, exact for route), or describe the specific discrepancies to fix.</resume-signal>
  <action>
    Pause for the human paridad sign-off (the F3/F4/F5 close pattern). Automated tests confirm presence/behavior; this gate confirms the SUBJECTIVE parity of the search + route surfaces against the live guide. Build and serve per <how-to-verify>, walk the SEARCH and ROUTE steps in light and dark theme at mobile and desktop widths, and compare side-by-side with the original `index.html`. Do not modify code in this task; if a divergence is reported, route it back to Task 1 (the spec) and/or Plan 06-03/06-04 (the wiring) for correction, then re-run `pnpm test:unit && pnpm test:golden` before re-requesting sign-off.
  </action>
  <verify>
    <human-check>Human confirms that, on the built `/guiaRoma/` site, client search finds AT LEAST what the live `index.html` finds (dropdown opens at ≥2 chars, ≤8 rows showing name + day, "Sin resultados" on no match, result click scroll-highlights the ficha with no hash change) and the "Ver ruta del día (N paradas)" button matches the original per day (same N, same ordered stops, Saturday includes Vatican + Auditorium) — verified in light/dark × mobile/desktop. Console shows no error other than the known color-mode hydration message.</human-check>
  </verify>
  <done>A human has signed off that FEAT-03 (≥ parity) and FEAT-09 (exact parity) match the live guide (Core Value) in all four theme/width combinations; any reported divergence has been corrected and re-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → built artifact | The spec builds and serves the real SSG output; it asserts behavior on the rendered DOM. No user-data boundary is introduced by the test itself. |
| app → external (Google Maps) | The spec asserts the route `href` by STRUCTURE only and never fetches it (no external request, no SSRF surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering / Info disclosure | search-result render (XSS regression) | mitigate | The spec's console-error gate + the `{{ }}`-only render (enforced in 06-04) guard against a `v-html` regression; the human checkpoint visually confirms result rendering matches the original. |
| T-06-10 | Tampering | route `href` (open redirect) | mitigate | The spec asserts the `href` starts with the fixed `https://www.google.com/maps/dir/?api=1&travelmode=walking` origin and never fetches it; reinforced by the 06-01 unit assertion. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Playwright + `serve` already used by modes/navigation specs). |
</threat_model>

<verification>
- `pnpm test:golden` green including the new `search-route.spec.ts` (Task 1 acceptance) — the phase gate from 06-VALIDATION §Sampling Rate.
- Full phase suite before sign-off: `pnpm test:unit && pnpm test:data && pnpm test:golden` all green.
- Human paridad sign-off approved (Task 2) — the binding gate for the user-visible search + route surfaces.
</verification>

<success_criteria>
- A self-contained Playwright spec proves, on the built `/guiaRoma/` site: dropdown ≥2/max-8/"Sin resultados", result→`navigateToCard` (`.highlight`, hash unchanged, SC#2), and route-button visibility/label/href (FEAT-09).
- Human confirms search (≥ parity) + route (exact) match the live `index.html` in light/dark × mobile/desktop.
- FEAT-03 and FEAT-09 complete; `pnpm test:unit` + `pnpm test:data` + `pnpm test:golden` all green.
</success_criteria>

<output>
Create `.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-05-SUMMARY.md` when done.
</output>
