---
phase: 06-derivados-de-datos-b-squeda-y-ruta-del-d-a
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/components/DaySection.vue
autonomous: true
requirements: [FEAT-09]
nyquist_compliant: true

must_haves:
  truths:
    - "Each day's .day-stats band renders a .day-route-btn linking to the day's Google Maps walking route, derived from day.cards order"
    - "The button is hidden when fewer than 2 stops resolve (v-if points.length >= 2)"
    - "The button label reads 'Ver ruta del día (N paradas)' / '(10 de N paradas)' via routeLabel; title is 'Abre Google Maps con el recorrido del día a pie'; target=_blank rel=noopener"
    - "Stops include ALL day.cards (Saturday keeps vaticano+auditorium) — same defensive filter as dayCards, NO type filter"
  artifacts:
    - path: "app/components/DaySection.vue"
      provides: "points computed (day.cards → monById → pointFor) + routeHref computed + the .day-route-btn appended inside the existing .day-stats band"
      contains: "day-route-btn"
  key_links:
    - from: "app/components/DaySection.vue"
      to: "app/utils/dayRoute.ts"
      via: "auto-imported pure utils"
      pattern: "buildDirUrl|capStops|pointFor|routeLabel"
    - from: "app/components/DaySection.vue"
      to: "day.cards + props.monById"
      via: "points computed (same chain as dayCards)"
      pattern: "props.day.cards"
---

<objective>
Wire the "ruta del día" button (FEAT-09 UI, SC#3) into the EXISTING `.day-stats` band of `app/components/DaySection.vue`, using the pure utils from Plan 06-01. The button derives its `href` reactively from `day.cards` at SSG prerender time — no DOM mutation, no `appendChild`, no client-only.

Purpose: Completes FEAT-09 by surfacing the ported route logic in the UI exactly where the original placed it (inside each day's stats band). DaySection already owns the `day.cards → monById` resolution chain (the `dayCards` computed) and the verbatim `.day-stats` markup, so this is a small, in-file reactive addition — the established "wire behavior into pre-existing verbatim shells, zero new CSS" pattern (F3/F4).
Output: a modified `app/components/DaySection.vue` whose every day renders the route button when ≥2 stops resolve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-RESEARCH.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-PATTERNS.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-UI-SPEC.md

<depends_on_note>
This plan depends on 06-01 (`app/utils/dayRoute.ts` providing the auto-imported `pointFor`/`capStops`/`buildDirUrl`/`routeLabel`/`MAX_ROUTE_STOPS`). 06-01 must be merged first. No `06-01-SUMMARY.md` re-read is required — the contract is the four exports named above.
</depends_on_note>

<critical_override>
NO `PlaceType` filter on the route stops. Reuse the SAME defensive `.filter((m): m is Monument => !!m)` already in `DaySection.vue:50` (it only drops slugs with no resolved monument). Saturday MUST keep `vaticano` (guided) + `auditorium` (concert) — the live route includes them (RESEARCH §Pitfall 2). The phase SC#3 prose "exclude guided/concert" is factually wrong about the original and must NOT be implemented; SC#4 (the per-day URL, guarded by 06-01's unit test) is the source of truth.
</critical_override>

<interfaces>
<!-- DaySection's existing props + the dayCards computed it already has. Extracted from DaySection.vue:43-51,74-87. -->
From app/components/DaySection.vue (current):
```typescript
const props = defineProps<{ day: Day, monById: Map<string, Monument> }>()
const dayCards = computed(() =>
  props.day.cards.map(slug => props.monById.get(slug)).filter((m): m is Monument => !!m),
)
```
Existing `.day-stats` band markup (DaySection.vue:74-87) — the button is appended INSIDE this `<div class="day-stats">`, AFTER the `v-for` of `.day-stats-item`:
```vue
<div class="day-stats">
  <div v-for="(s, i) in day.stats" :key="i" class="day-stats-item" :class="s.variant">
    <MDC :value="s.text" :tag="false" unwrap="p" />
  </div>
  <!-- route button goes here -->
</div>
```
Auto-imported from 06-01: `pointFor`, `capStops`, `buildDirUrl`, `routeLabel`, `MAX_ROUTE_STOPS`. CSS class `.day-route-btn` already exists in `app/assets/css/base.css:393-413` (incl. `::before` 🗺️ glyph, hover, and the <600px `width:100%` rule). ZERO new CSS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the points/routeHref computeds and append the .day-route-btn inside .day-stats</name>
  <files>app/components/DaySection.vue</files>
  <read_first>
    - app/components/DaySection.vue (whole file) — the component being modified: its existing `dayCards` computed (49-51), the `.day-stats` band (74-87), the props (45), the doc-comment conventions, and the "zero new CSS / no scoped `<style>`" rule stated at lines 38-41.
    - index.html:6625-6644 — the button shape source of truth: `btn.className='day-route-btn'`, `btn.href = buildDirUrl(capStops(points))`, `btn.target='_blank'`, `btn.rel='noopener'`, `btn.title='Abre Google Maps con el recorrido del día a pie'`, the `(N paradas)`/`(10 de N paradas)` label (6641-6643), and the `if (points.length < 2) return` guard (6633).
    - app/utils/dayRoute.ts (from 06-01) — confirm the exact export names and signatures (`pointFor(m)`, `capStops(points)`, `buildDirUrl(points)`, `routeLabel(total)`).
    - app/assets/css/base.css:393-413 — confirm `.day-route-btn` and its `::before`/hover/mobile rules already exist (so the task adds zero CSS).
    - .planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-UI-SPEC.md §"Ruta del día" button + §Copywriting — the locked label/title strings and the v-if≥2 / margin-left:auto contract.
  </read_first>
  <action>
    In `app/components/DaySection.vue` `<script setup>`, add two computeds AFTER the existing `dayCards`:
    `points` = `props.day.cards.map(slug => props.monById.get(slug)).filter((m): m is Monument => !!m).map(pointFor)` — the SAME resolution chain as `dayCards`, then `.map(pointFor)`. NO type filter (the critical override).
    `routeHref` = `buildDirUrl(capStops(points.value))`.
    Extend the existing doc-comment block (do not start a new convention) with a short note: the route button derives from `day.cards` order (DATA-03), reuses the defensive filter with NO type filter (RESEARCH §Pitfall 2 — Saturday keeps vaticano+auditorium), renders at SSG prerender via reactive `:href`/`v-if` (replacing the original's imperative `stats.appendChild`), and adds zero CSS.
    In the template, INSIDE the existing `<div class="day-stats">` and AFTER the `v-for` of `.day-stats-item`, append a single `<a>`:
    - `v-if="points.length >= 2"` (mirrors index.html:6633 `if (points.length < 2) return`).
    - `class="day-route-btn"`, `:href="routeHref"`, `target="_blank"`, `rel="noopener"`, `title="Abre Google Maps con el recorrido del día a pie"`.
    - text content `{{ routeLabel(points.length) }}`.
    Do NOT relocate the band, do NOT add a wrapping element, do NOT add a `<style>` block (a `data-v-*` would break the global `.day-stats`/`.day-route-btn` selectors — DaySection.vue:38-41), do NOT mutate the DOM imperatively, and do NOT use `navigateToCard` (this is a plain external link, NOT internal navigation — UI-SPEC §Interaction).
  </action>
  <acceptance_criteria>
    - `app/components/DaySection.vue` contains a `points` computed using `props.day.cards`, the `.filter((m): m is Monument => !!m)` defensive filter, and `.map(pointFor)` — with NO `type ===`/`type !==` filter anywhere (grep: file has `pointFor` and `points`, has NO `.type`).
    - The template renders `<a … class="day-route-btn" :href="routeHref" target="_blank" rel="noopener" title="Abre Google Maps con el recorrido del día a pie">{{ routeLabel(points.length) }}</a>` with `v-if="points.length >= 2"`, placed inside `.day-stats` after the stats `v-for`.
    - The file contains NO `<style` block and NO `v-html` (grep: neither present).
    - The button references `routeHref`/`routeLabel` (auto-imported from 06-01) and adds no new CSS class beyond the existing `.day-route-btn`.
    - `pnpm typecheck` exits 0; `pnpm lint` exits 0; `pnpm generate` exits 0 (the button must render at prerender without `window` errors).
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm generate</automated>
  </verify>
  <done>`app/components/DaySection.vue` renders the `.day-route-btn` inside each day's `.day-stats` band, derived from `day.cards` (no type filter), hidden when <2 stops, with the verbatim label/title/`target`/`rel`; typecheck, lint, and `pnpm generate` all clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → external (Google Maps) | The button is an external `<a target="_blank">` to a Google Maps deep-link. The `href` is computed from `buildDirUrl` over typed content coords — no user input crosses the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | route button `href` (open redirect / `javascript:`) | mitigate | `href` = `buildDirUrl(...)` (Plan 06-01) which emits only a fixed `https://www.google.com/maps/dir/?…` origin; `rel="noopener"` on `target="_blank"` is set verbatim from the original (index.html:6639) to prevent reverse-tabnabbing. |
| T-06-06 | Tampering | route stop set (silent parity drop) | mitigate | The same defensive filter as `dayCards` with NO type filter; the regression is also guarded by 06-01's Saturday=8-stops unit test. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (template/computed change only; utils auto-imported from 06-01). |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm generate` all clean (Task 1 acceptance).
- Behavioral verification of the rendered button (visibility, label, `href` structure) is covered by the Wave-3 Playwright spec in Plan 06-05 (`tests/parity/search-route.spec.ts`).
- The per-day URL correctness is already guarded by Plan 06-01's `tests/unit/dayRoute.spec.ts`.
</verification>

<success_criteria>
- FEAT-09 UI complete: every day's `.day-stats` band renders the route button when ≥2 stops resolve, with the verbatim label/title and `target=_blank rel=noopener`.
- Stops derived from `day.cards` with no type filter (Saturday keeps vaticano+auditorium).
- Zero new CSS, no scoped styles, no imperative DOM mutation; renders at SSG prerender.
- Typecheck, lint, and `pnpm generate` green.
</success_criteria>

<output>
Create `.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-03-SUMMARY.md` when done.
</output>
