---
phase: 06-derivados-de-datos-b-squeda-y-ruta-del-d-a
plan: 04
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - app/composables/useSearch.ts
  - app/components/SearchBox.vue
  - app/components/TheHero.vue
autonomous: true
requirements: [FEAT-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Typing ≥2 chars in #search opens the dropdown with up to 8 results; <2 chars closes it; no match shows the verbatim 'Sin resultados' italic row"
    - "Clicking a result clears the input, closes the dropdown, and calls navigateToCard(slug, event) (F5) — the URL hash does NOT change"
    - "Clicking outside .search-wrap closes the dropdown"
    - "Exactly one #search exists in the DOM (the TheHero shell is replaced/wired, not duplicated)"
    - "The MiniSearch index is built client-side in onMounted from monById (monuments-only); lifecycle hooks register synchronously before the await"
  artifacts:
    - path: "app/composables/useSearch.ts"
      provides: "useSearch() accessor (query/isOpen/results + onInput/onSelect) + useSearchController() (client-only index build + outside-click listener, hooks-before-await)"
      exports: ["useSearch", "useSearchController"]
    - path: "app/components/SearchBox.vue"
      provides: "The .search-wrap shell (verbatim markup) wired to useSearch; renders results via {{ }} only; invokes useSearchController once"
      contains: "search-wrap"
    - path: "app/components/TheHero.vue"
      provides: "Renders <SearchBox/> in place of the static .search-wrap placeholder (no second #search)"
  key_links:
    - from: "app/composables/useSearch.ts"
      to: "app/utils/searchIndex.ts (createSearchIndex) + useTrip().monById + useCardNavigation().navigateToCard"
      via: "auto-imports"
      pattern: "createSearchIndex|monById|navigateToCard"
    - from: "app/components/SearchBox.vue"
      to: "useSearch / useSearchController"
      via: "composable accessor + controller invoked once"
      pattern: "useSearch\\(\\)|useSearchController\\(\\)"
    - from: "app/components/TheHero.vue"
      to: "app/components/SearchBox.vue"
      via: "component replaces the placeholder .search-wrap block"
      pattern: "<SearchBox"
---

<objective>
Wire client search (FEAT-03 UI) end to end: a `useSearch` singleton composable (state + a controller that builds the MiniSearch index client-side and handles outside-click), a `SearchBox.vue` that renders the EXISTING `.search-wrap` markup with behavior and a verbatim dropdown, and `TheHero.vue` updated to render `<SearchBox/>` in place of its static placeholder. Results render via Vue `{{ }}` (never `v-html`) and selecting one calls F5's `navigateToCard`.

Purpose: This completes FEAT-03 by attaching behavior to the search shell F3 already rendered, replacing the original's DOM-scraping `innerHTML` dropdown (index.html:6447-6469) with a typed-index + auto-escaped Vue template. It reuses the canonical F4/F5 singleton-accessor + `*Controller()` split with the synchronous-hooks-before-`await` fix, and routes navigation through the F5 composable that was built for exactly this consumer (SC#2).
Output: `app/composables/useSearch.ts` (new), `app/components/SearchBox.vue` (new), and a modified `app/components/TheHero.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-RESEARCH.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-PATTERNS.md
@.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-UI-SPEC.md

<depends_on_note>
Depends on 06-02 (`app/utils/searchIndex.ts` providing auto-imported `createSearchIndex`). Also consumes existing composables UNCHANGED: `useTrip()` (F3, provides `monById: computed<Map<slug, Monument>>`) and `useCardNavigation()` (F5, provides `navigateToCard(id, event?)`). No F5 change is needed because the index is monuments-only (D-02), so every result slug resolves in `monById`. No prior SUMMARY re-read required.
</depends_on_note>

<locked_render_safety>
LOCKED (security V5, T-V5): search results MUST render via Vue `{{ }}` interpolation only (auto-escaped). NEVER `v-html`. The original used `searchResults.innerHTML = matches.map(...)` (index.html:6454) — F6 explicitly improves on this by using Vue templates. `name`/`day` come from MiniSearch `storeFields`. A `v-html` anywhere in SearchBox is a hard failure.
</locked_render_safety>

<pitfall_6_shell_already_exists>
CONTEXT calls the search box "NUEVO (no hay shell de F3)" — INACCURATE. F3 already rendered the full `.search-wrap` with `#search` + `#search-results` VERBATIM in `TheHero.vue:73-85` (a no-handler placeholder so masthead spacing matched the golden). Do NOT create a SECOND `#search`. Replace that placeholder block in TheHero with `<SearchBox/>` (the cleaner option, RESEARCH §Open Q3). The `#inicio` location is already correct (D-04) — do not relocate.
</pitfall_6_shell_already_exists>

<interfaces>
<!-- The exact verbatim shell to preserve + the F5 controller pattern to mirror. -->
Existing placeholder in app/components/TheHero.vue:73-85 (to be replaced by <SearchBox/>):
```vue
<div class="search-wrap">
  <input id="search" type="search" class="search-input"
         placeholder="Buscar lugar, día, anécdota…" autocomplete="off">
  <div id="search-results" class="search-results" />
</div>
```
useCardNavigation accessor + controller signatures (the analog to mirror — useCardNavigation.ts:51-86,122-178):
```typescript
export function useCardNavigation(): { navStack, activeSection, canGoBack, navigateToCard(id, event?), goBack }
export async function useCardNavigationController(): Promise<void>   // hooks SYNC before `await useTrip('roma')`, monByIdRef shallowRef holder
export async function useTrip(slug): Promise<{ monById: ComputedRef<Map<string, Monument>>, /* ... */ }>
```
createSearchIndex (from 06-02): `createSearchIndex(monuments: Monument[]): MiniSearch`. MiniSearch `.search(q)` returns objects carrying `slug`/`name`/`day` (storeFields).
CSS classes all present in base.css: `.search-wrap`(236) `.search-input`(240) `.search-results`(255) `.search-results.show`(265) `.search-result`(266) `.search-result-meta`(275). ZERO new CSS, NO scoped `<style>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/composables/useSearch.ts — singleton accessor + client-only controller (hooks before await)</name>
  <files>app/composables/useSearch.ts</files>
  <read_first>
    - app/composables/useCardNavigation.ts (whole file) — THE canonical analog: the accessor/`*Controller()` split, the `useState` namespaced-key convention, and CRITICALLY the synchronous-hooks-before-`await` fix (lines 97-105, 122-178) with the `monByIdRef` shallowRef holder captured before `await useTrip('roma')` and a `watch(monById, …)` resync. This plan's controller has the identical hazard.
    - app/composables/useTripModes.ts:45-105 — secondary analog for the accessor/controller split + `useState` defaults = prerendered values.
    - app/composables/useTrip.ts:37-82 — confirm `useTrip('roma')` returns `monById` as `computed<Map<slug, Monument>>` (monuments only) and that `useAsyncData` dedups by key (so this controller's `useTrip('roma')` is the SAME data as TripView's).
    - index.html:6447-6469 — the behavior to port: `q = value.trim().toLowerCase()` (note: MiniSearch lowercases internally, so trimming is enough), `if (q.length < 2) close; return` (6449), `.slice(0, 8)` (6450), the click handler clears input + closes + `navigateToCard` (6459-6461), the outside-click close (6467-6469).
    - app/utils/searchIndex.ts (from 06-02) — confirm `createSearchIndex` signature and the result object shape (slug/name/day).
  </read_first>
  <behavior>
    - useSearch() accessor (idempotent, callable anywhere): exposes `query = useState('search:query', () => '')`, `isOpen = useState('search:open', () => false)`, `results = useState<Array<{ slug: string, name: string, day: string }>>('search:results', () => [])`, plus `onInput(value)` and `onSelect(slug, event)`. Defaults = prerendered (empty query, closed, []).
    - onInput(value): set `query.value = value`; `const q = value.trim()`; if `q.length < 2` set `isOpen.value = false` and return; else `results.value = (index?.search(q) ?? []).slice(0, 8)` (mapped to plain `{slug,name,day}` objects) and `isOpen.value = true`.
    - onSelect(slug, event): `isOpen.value = false`; `query.value = ''`; `navigateToCard(slug, event)` (from `useCardNavigation()`).
    - useSearchController() (invoked once, in SearchBox): registers the outside-click `document` listener in onMounted / removes in onUnmounted (SAME function ref); both hooks registered SYNCHRONOUSLY before any `await`. Holds the index in a module-or-closure `shallowRef<MiniSearch | null>` captured before the await; after `const { monById } = await useTrip('roma')`, sets `indexRef.value = createSearchIndex([...monById.value.values()])` and `watch(monById, v => indexRef.value = createSearchIndex([...v.values()]))`. The outside-click handler: `if (!(e.target as HTMLElement).closest('.search-wrap')) isOpen.value = false`.
  </behavior>
  <action>
    Create `app/composables/useSearch.ts` mirroring `useCardNavigation.ts`'s structure and doc-comment doctrine. Write a header comment that: explains the accessor/controller split (effects run once, accessor is pure/idempotent — CR-01/Pitfall-4 precedent), states the index is CLIENT-ONLY ephemeral (NEVER in serialized `useState`; only `query`/`isOpen`/plain-`results` are `useState`), and explicitly calls out the synchronous-hooks-before-`await` rule (the F5 §A1 bug: a hook registered after `await` is a silent no-op and the feature dies in SSG).
    Implement `useSearch()` and `useSearchController()` per <behavior>. The index `shallowRef` must be reachable by `onInput` — keep it in module scope OR return a query function from the controller and have onInput read it; the cleanest is: `useSearch()` holds `onInput`/`onSelect` that read a module-scoped `shallowRef<MiniSearch | null> = shallowRef(null)` populated by the controller (mirror how `useCardNavigation` shares `monByIdRef` via closure). Either approach is fine PROVIDED the index is built only client-side and onInput tolerates a null index (returns [] before the index is ready). Map MiniSearch results to plain `{ slug: r.slug, name: r.name, day: r.day }` so `results` stays serializable.
    `navigateToCard` comes from `useCardNavigation()` (unchanged). `useTrip`/`useState`/`computed`/`onMounted`/`onUnmounted`/`shallowRef`/`watch` are Nuxt auto-imports; `createSearchIndex` auto-imports from `app/utils/searchIndex.ts`.
    Do NOT add keyboard navigation, debounce-beyond-the-original, enriched rows, or a focus shortcut (all deferred — UI-SPEC §Out of Scope). Mechanic fidelity = the original (D-03); a debounce is optional Claude's-Discretion but match the original feel (the original had none — instant on input).
  </action>
  <acceptance_criteria>
    - `app/composables/useSearch.ts` exports `useSearch` and `useSearchController`.
    - `useSearch()` uses `useState` keys `'search:query'`, `'search:open'`, `'search:results'` with defaults `''`/`false`/`[]`; `results` is typed `Array<{ slug, name, day }>` (grep: file contains those three `useState` keys).
    - `onInput` guards `q.length < 2` (closes + returns), slices to `8`, and sets `isOpen`; `onSelect` clears `query`, closes, and calls `navigateToCard` (grep: file contains `slice(0, 8)`, `length < 2`, `navigateToCard`).
    - `useSearchController()` registers `onMounted`/`onUnmounted` BEFORE `await useTrip('roma')`, holds the index in a `shallowRef`, and `watch`es `monById` (grep: `onMounted` appears before `await useTrip` in source order; file contains `shallowRef` and `createSearchIndex`).
    - The MiniSearch index is NOT stored in any `useState` (grep: no `useState` wrapping `createSearchIndex`/MiniSearch).
    - `pnpm typecheck` exits 0; `pnpm lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>`app/composables/useSearch.ts` exists with the accessor/controller split, client-only index build (hooks-before-await, shallowRef holder, `watch(monById)`), the ≥2/max-8/clear-on-select query logic, and `navigateToCard` delegation; index never serialized; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchBox.vue (verbatim shell + {{ }} dropdown) and render it from TheHero.vue</name>
  <files>app/components/SearchBox.vue, app/components/TheHero.vue</files>
  <read_first>
    - app/components/TheHero.vue (whole file) — the placeholder `.search-wrap` block (73-85) to replace with `<SearchBox/>`, the "zero new CSS / no scoped `<style>`" rule (20-23), and the precedent that TheHero already invokes a controller once (`useTripModesController()` at line 48) — the mirror site for invoking `useSearchController()` (it can live in SearchBox instead; pick SearchBox to keep TheHero focused).
    - index.html:6451-6456 — the VERBATIM dropdown markup: result row `<a href="#${id}" class="search-result" data-card="${id}">${title}<div class="search-result-meta">${day}</div></a>` and the empty-state `<div style="padding:.65rem 1rem;color:var(--ink-faint);font-style:italic">Sin resultados</div>` (inline style is verbatim — keep it).
    - app/composables/useSearch.ts (from Task 1) — the accessor/controller to wire.
    - app/components/DaySection.vue:38-41 / app/components/TheHero.vue:20-23 — the project-wide "no scoped `<style>`, zero new CSS" convention (a `data-v-*` would break `.search-results.show` and `.search-result:hover`).
    - .planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-UI-SPEC.md §Interaction Contract (search) + §Copywriting — the locked thresholds (≥2, max 8), the verbatim placeholder/empty-state strings, and the render-safety `{{ }}`-only rule.
  </read_first>
  <action>
    Create `app/components/SearchBox.vue` (`<script setup lang="ts">`, NO scoped style). It renders the `.search-wrap` markup VERBATIM (input `#search` with the exact placeholder `Buscar lugar, día, anécdota…`, `type="search"`, `autocomplete="off"`) and the dropdown. Wire:
    - `const { query, isOpen, results, onInput, onSelect } = useSearch()` and invoke `useSearchController()` ONCE here (it owns the controls; mirror TheHero's single `useTripModesController()`). If the controller is async, follow TheHero's pattern for invoking it (it is called at setup top-level like `useTripModesController()`; if `useSearchController` is async, ensure the hooks-before-await contract from Task 1 holds — register inside the controller, not here).
    - Input: bind `:value="query"` and `@input="onInput(($event.target as HTMLInputElement).value)"` (do NOT use `v-model` if it complicates the clear-on-select; reading `query` for the value + onInput is closest to the original).
    - Dropdown: `<div id="search-results" class="search-results" :class="{ show: isOpen }">`. Inside, `v-if="results.length"` → `<a v-for="r in results" :key="r.slug" :href="`#${r.slug}`" class="search-result" :data-card="r.slug" @click="onSelect(r.slug, $event)">{{ r.name }}<div class="search-result-meta">{{ r.day }}</div></a>`. The `v-else` branch is the verbatim empty state `<div style="padding:.65rem 1rem;color:var(--ink-faint);font-style:italic">Sin resultados</div>`.
    Render `name`/`day` via `{{ }}` ONLY — NEVER `v-html` (T-V5). Keep `data-card` on the result `<a>` (the original had it; harmless + matches the parity markup). The `@click` calls `onSelect` which does `preventDefault` via `navigateToCard`, so the hash does not change (D-03).
    Then edit `app/components/TheHero.vue`: replace the placeholder `.search-wrap` block (73-85) with a single `<SearchBox />`. Update TheHero's doc-comment line that says the search-input is "F6"/"sin manejadores" to note F6 now renders `<SearchBox/>` there. Do NOT touch the pace-wrap/light-wrap controls or anything else in TheHero. Ensure exactly ONE `#search` remains in the rendered DOM (Pitfall 6).
    Zero new CSS, no `<style>` block in SearchBox (all classes exist in base.css).
  </action>
  <acceptance_criteria>
    - `app/components/SearchBox.vue` exists, renders `.search-wrap` → `#search` input (placeholder `Buscar lugar, día, anécdota…`) + `#search-results` dropdown, and invokes `useSearch()` + `useSearchController()`.
    - The dropdown toggles `.show` via `:class="{ show: isOpen }"`, renders ≤8 `.search-result` `<a>` rows with `{{ r.name }}` + `.search-result-meta` `{{ r.day }}`, and a `v-else` `Sin resultados` row with the verbatim inline style.
    - SearchBox contains NO `v-html` and NO `<style` block (grep: neither present).
    - `app/components/TheHero.vue` renders `<SearchBox` and no longer contains the inline `<input id="search"` placeholder block (grep: `<SearchBox` present; the literal `id="search"` now lives only in SearchBox.vue, so TheHero has exactly zero `id="search"`).
    - Result rows call `onSelect(r.slug, $event)` on `@click` (grep: `onSelect` present in SearchBox).
    - `pnpm typecheck` exits 0; `pnpm lint` exits 0; `pnpm generate` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm generate</automated>
  </verify>
  <done>`SearchBox.vue` renders the verbatim search shell + a `{{ }}`-only dropdown (≥2/max-8/"Sin resultados", click→onSelect→navigateToCard) and is rendered once by `TheHero.vue` in place of the placeholder (single `#search`); zero new CSS, no `v-html`; typecheck, lint, and `pnpm generate` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → search input → dropdown render | The `#search` value is user-controlled. It reaches `MiniSearch.search()` (in-memory) and influences which stored result objects render. Result fields (`name`/`day`) are typed content, not user input, but the render path is the XSS-relevant boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V5 | Tampering / Info disclosure | SearchBox result + empty-state render | mitigate | Results render via Vue `{{ }}` interpolation only (auto-escaped); NEVER `v-html`. Enforced as an acceptance criterion + grep gate. This is the sanctioned improvement over the original's `innerHTML` (index.html:6454). |
| T-06-07 | Tampering | result `:href="#${slug}"` (open redirect) | accept | The href fragment uses internal slugs from typed data (not user input); `navigateToCard` calls `preventDefault`, so it never navigates the URL. No external/`javascript:` target possible. |
| T-06-08 | DoS | crafted query perf (ReDoS) | mitigate | MiniSearch `maxFuzzy` (default 6) + `fuzzy: 0.2` (from 06-02 config) cap fuzzy cost; ~38 docs → negligible. No regex over the raw query. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (MiniSearch already vetted in Phase 1; this plan only wires it). |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm generate` all clean (Tasks 1 + 2).
- Render-safety grep gate: SearchBox contains no `v-html`.
- Single-`#search` invariant: TheHero no longer has an inline `id="search"`; SearchBox owns it.
- Behavioral verification (≥2 opens, max 8, "Sin resultados", click → `.highlight` + hash unchanged) is the Wave-3 Playwright spec in Plan 06-05.
</verification>

<success_criteria>
- FEAT-03 UI complete and wired through F5: typing ≥2 chars opens an ≤8-result dropdown, no-match shows "Sin resultados", clicking a result clears + closes + `navigateToCard` (hash unchanged), outside-click closes.
- Exactly one `#search` (TheHero placeholder replaced by `<SearchBox/>`); index built client-side with hooks-before-await.
- Results render via `{{ }}` only (no `v-html`); zero new CSS, no scoped styles.
- Typecheck, lint, and `pnpm generate` green.
</success_criteria>

<output>
Create `.planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/06-04-SUMMARY.md` when done.
</output>
