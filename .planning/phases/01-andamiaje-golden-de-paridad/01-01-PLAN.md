---
phase: 01-andamiaje-golden-de-paridad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - playwright.config.ts
  - .gitignore
  - tests/parity/golden.spec.ts
  - tests/parity/golden.spec.ts-snapshots/
autonomous: false
requirements: [PARITY-01]
user_setup: []

must_haves:
  truths:
    - "Existen screenshots golden del index.html (las 14 vistas de D-04) en claro y oscuro, móvil y desktop, versionados en el repo"
    - "Re-ejecutar la suite Playwright sin --update pasa contra los golden recién generados (los golden son deterministas)"
    - "Los golden se capturan del index.html actual (idéntico a origin/main) antes de que exista cualquier código Nuxt que pueda divergir"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Harness del golden: webServer estático sobre la raíz, proyectos mobile/desktop, toHaveScreenshot determinista, snapshotPathTemplate sin sufijo de plataforma"
      contains: "toHaveScreenshot"
    - path: "tests/parity/golden.spec.ts"
      provides: "Captura de las 14 vistas de D-04 en light+dark"
      contains: "toHaveScreenshot"
    - path: "tests/parity/golden.spec.ts-snapshots"
      provides: "PNGs golden versionados (cota superior 56 ficheros)"
  key_links:
    - from: "tests/parity/golden.spec.ts"
      to: "index.html (servido en localhost:4173)"
      via: "page.goto('/index.html') tras webServer estático"
      pattern: "goto\\(.*index\\.html"
    - from: "tests/parity/golden.spec.ts"
      to: "localStorage['roma-theme']='dark'"
      via: "page.addInitScript para forzar el tema oscuro de forma determinista"
      pattern: "roma-theme"
---

<objective>
Capturar el **golden de paridad** del `index.html` actual — la línea base objetiva e inmutable contra la que la Fase 8 medirá la paridad 100%. Es el **primer entregable verificado de toda la migración** (constraint crítico D-05 / RESEARCH §BUILD ORDER bloque A): debe completarse y versionarse **antes** de que exista cualquier código Nuxt que pueda introducir divergencia.

Cubre PARITY-01. El `index.html` es byte-idéntico a `origin/main` (`git diff --stat origin/main -- index.html` vacío, verificado), así que el golden representa la versión viva con la "ruta del día" y las cenas.

Purpose: Sin este oráculo capturado primero, no hay forma objetiva de demostrar paridad en la Fase 8 — el golden es la red de seguridad de todo el proyecto.
Output: `playwright.config.ts`, `tests/parity/golden.spec.ts`, los PNGs golden versionados, y los scripts/deps de Playwright en `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-CONTEXT.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-VALIDATION.md

<decisions>
Decisiones que este plan implementa o resuelve:
- D-04: alcance del golden = home + 5 días + 3 fichas-tipo + 5 referencias = 14 vistas × {light,dark} × {mobile ~390px, desktop ~1280px}. NO las 37 fichas una a una.
- D-05: capturar sirviendo el `index.html` en local con Playwright, antes de divergir; PNGs versionados.
- A5 (resuelta en este plan, Tarea 2): tratamiento determinista de las imágenes hero de terceros.
- A8 (resuelta en este plan, Tarea 1): sufijo de plataforma de los snapshots.
- Localización de las fichas-tipo resuelta por PATTERNS.md: NO existe clase CSS `guided`/`concert` (las 38 fichas son `<article class="card">`, verificado: 38 matches, 0 de `card guided`/`card concert`). Representantes: `#galleria-sciarra` (card simple, línea 2450), `#vaticano` (guiada, línea 2920), `#auditorium` (concierto, línea 3381).
</decisions>

<interfaces>
<!-- Contrato de tema compartido por index.html, golden y (en Fase 3) la app Nuxt. Extraído del index.html — el executor lo usa directo, sin explorar. -->

index.html línea 2:    <html lang="es" data-theme="light">   (arranca en claro)
index.html línea 6255: document.documentElement.setAttribute('data-theme', t);
index.html línea 6256: localStorage.setItem('roma-theme', t);
index.html línea 6263: const saved = localStorage.getItem('roma-theme');

→ Para el golden oscuro: addInitScript que escribe localStorage['roma-theme']='dark' ANTES de page.goto;
  el script inline del index.html (línea 6263) lo lee y pinta dark en el primer paint. Determinista, sin clic.

Anclas/ids EXACTOS a capturar (verificados en index.html esta sesión):
  inicio        #inicio        L2283
  dia-viernes   #viernes       L2375
  dia-sabado    #sabado        L2840
  dia-domingo   #domingo       L3445
  dia-lunes     #lunes         L4002
  dia-martes    #martes        L4736
  ref-reservas      #reservas       L5260
  ref-gastronomia   #gastronomia    L5335
  ref-practica      #practica       L5825
  ref-arte          #arte           L5941
  ref-arquitectura  #arquitectura   L6104
  card-monumento  #galleria-sciarra  L2450   (article class="card")
  card-guided     #vaticano          L2920   (h3 "Vaticano · preparar la visita guiada")
  card-concert    #auditorium        L3381   (card-roman = ♪)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Tarea 1: Verificación de legitimidad de paquetes + config del harness Playwright (A8)</name>
  <files>package.json, pnpm-lock.yaml, playwright.config.ts, .gitignore</files>
  <read_first>
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 157-179 (Package Legitimacy Audit: slopcheck NO disponible → todos los paquetes tratados como [ASSUMED]; mitigación = npm view + origen oficial)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 326-359 (Pattern 2: sketch de playwright.config.ts)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 359, 565-572 (A8: sufijo -{platform} en snapshots; storage por defecto)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md líneas 201-226 (asignación de patrón de playwright.config.ts; decisiones abiertas A5/A8)
  </read_first>
  <what-built>
    Antes de instalar: este checkpoint es la **puerta de legitimidad de paquetes** exigida por el threat model (slopcheck no estaba disponible en el entorno → la convención GSD trata los paquetes como [ASSUMED] y exige un checkpoint humano bloqueante antes de instalar). Este plan instala SOLO los paquetes que el golden necesita: `@playwright/test@1.61.0` (dev) y un static server vía `pnpm dlx serve` (no se añade como dependencia). El executor presenta la lista para confirmación humana; tras aprobar, instala con pnpm (D-03), inicializa `package.json` con los scripts de golden, crea `.gitignore` (que NO debe excluir `tests/parity/`, Pitfall 2), y escribe `playwright.config.ts` con: webServer estático sobre la raíz del repo (`pnpm dlx serve -l 4173 .`, sirve el `index.html` in situ — D-05), `baseURL: http://localhost:4173`, proyectos `mobile` (`devices['iPhone 12']`, ~390px) y `desktop` (`viewport 1280×800`), `expect.toHaveScreenshot` con `animations:'disabled'`, `caret:'hide'`, `maxDiffPixelRatio:0.01`. **Decisión A8 fijada aquí:** establecer `snapshotPathTemplate: '{testFileDir}/{testFileName}-snapshots/{arg}-{projectName}{ext}'` (SIN el segmento `-{platform}`) para que los golden se comparen entre SOs en la Fase 8 sin atarse a `-linux`. El executor documenta en el SUMMARY que la captura se realizó en linux.
  </what-built>
  <how-to-verify>
    1. Revisar la lista de paquetes que el executor presenta: debe ser EXACTAMENTE `@playwright/test@1.61.0` (dev) — paquete oficial de Microsoft/Playwright, `latest` verificado el 2026-06-18 en RESEARCH §Package Legitimacy Audit. El static server se usa vía `pnpm dlx serve` (no se versiona como dep).
    2. Confirmar en https://www.npmjs.com/package/@playwright/test que es el paquete oficial (publisher Microsoft) y que 1.61.0 existe.
    3. Aprobar la instalación o señalar discrepancias.
  </how-to-verify>
  <action>
    Presentar al usuario la lista de paquetes a instalar (`@playwright/test@1.61.0` dev; static server vía `pnpm dlx serve`, sin versionar). NO usar `npx --yes`; usar pnpm (D-03). Tras aprobación humana: ejecutar `pnpm add -D @playwright/test@1.61.0` y `pnpm dlx playwright install chromium`. Inicializar `package.json` (manual, NO `nuxi init` — eso es el Plan 02) con `"packageManager": "pnpm@10.32.1"`, `"private": true`, y los scripts del golden: `"test:golden": "playwright test"`, `"test:golden:update": "playwright test --update-snapshots"`. Crear `.gitignore` con `node_modules`, `.output`, `.nuxt`, `dist`, `test-results`, `playwright-report` — y verificar que NINGÚN patrón excluya `tests/parity/` (Pitfall 2: los PNGs golden DEBEN versionarse). Escribir `playwright.config.ts` con `testDir: './tests/parity'`, `webServer: { command: 'pnpm dlx serve -l 4173 .', url: 'http://localhost:4173', reuseExistingServer: !process.env.CI }`, `use: { baseURL: 'http://localhost:4173' }`, `expect: { toHaveScreenshot: { animations: 'disabled', caret: 'hide', maxDiffPixelRatio: 0.01 } }`, `snapshotPathTemplate: '{testFileDir}/{testFileName}-snapshots/{arg}-{projectName}{ext}'` (decisión A8: sin `-{platform}`), y `projects: [{ name: 'mobile', use: { ...devices['iPhone 12'] } }, { name: 'desktop', use: { viewport: { width: 1280, height: 800 } } }]`. NO crear ningún fichero Nuxt aquí.
  </action>
  <verify>
    <automated>test -f playwright.config.ts && grep -q "toHaveScreenshot" playwright.config.ts && grep -q "iPhone 12" playwright.config.ts && grep -q "1280" playwright.config.ts && grep -q "snapshotPathTemplate" playwright.config.ts && ! grep -q "{platform}" playwright.config.ts && grep -q '"test:golden"' package.json && pnpm exec playwright --version</automated>
  </verify>
  <acceptance_criteria>
    - El usuario ha aprobado explícitamente la lista de paquetes (gate de legitimidad — no auto-aprobable).
    - `pnpm exec playwright --version` imprime 1.61.x (chromium instalado).
    - `playwright.config.ts` existe y contiene `toHaveScreenshot`, `iPhone 12`, `1280`, `snapshotPathTemplate`, y NO contiene `{platform}` (decisión A8 codificada).
    - `package.json` existe con `"packageManager": "pnpm@..."` y los scripts `test:golden`/`test:golden:update`.
    - `git check-ignore tests/parity/x.png` NO devuelve ningún patrón (los golden serán versionables — Pitfall 2).
    - NO existe `nuxt.config.ts` ni `app/` (el scaffold es el Plan 02; el golden va estrictamente primero).
  </acceptance_criteria>
  <resume-signal>Escribe "approved" para confirmar los paquetes e instalar, o describe discrepancias.</resume-signal>
  <done>Playwright instalado, package.json inicializado con scripts de golden, playwright.config.ts escrito con viewports mobile/desktop, determinismo de screenshots y snapshotPathTemplate sin plataforma (A8). Sin ningún fichero Nuxt.</done>
</task>

<task type="auto">
  <name>Tarea 2: Spec del golden (14 vistas × light/dark), heros deterministas (A5), generación y versionado de PNGs</name>
  <files>tests/parity/golden.spec.ts, tests/parity/golden.spec.ts-snapshots/</files>
  <read_first>
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 683-716 (esqueleto del golden.spec.ts; función settle)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 543-572 (determinismo: fadeIn/transitions/scroll-smooth/fuentes/heros; las 3 opciones A5; naming y cota de PNGs)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md líneas 243-264 (tabla de anclas/ids EXACTOS verificados; hallazgo crítico: no hay clase guided/concert)
    - index.html líneas 2-2 y 6255-6263 (contrato de tema: data-theme + localStorage['roma-theme'])
    - index.html líneas 2121-2125 (keyframes fadeIn + .card animation — fuente de flakiness que animations:'disabled' neutraliza)
  </read_first>
  <action>
    Escribir `tests/parity/golden.spec.ts` que capture las **14 vistas de D-04** en `light` y `dark`. Lista de vistas (nombre de snapshot → selector), EXACTA de PATTERNS.md líneas 243-264: `inicio`→`#inicio`, `dia-viernes`→`#viernes`, `dia-sabado`→`#sabado`, `dia-domingo`→`#domingo`, `dia-lunes`→`#lunes`, `dia-martes`→`#martes`, `ref-reservas`→`#reservas`, `ref-gastronomia`→`#gastronomia`, `ref-practica`→`#practica`, `ref-arte`→`#arte`, `ref-arquitectura`→`#arquitectura`, `card-monumento`→`#galleria-sciarra`, `card-guided`→`#vaticano`, `card-concert`→`#auditorium`. NO buscar clases `guided`/`concert` (no existen — las 38 fichas son `class="card"`); usar estos `id` literales. Capturar **por elemento** (`expect(page.locator(sel)).toHaveScreenshot(\`${name}-${theme}.png\`)`), no `fullPage` (más estable; A6). Para el tema oscuro: `await page.addInitScript(() => localStorage.setItem('roma-theme','dark'))` ANTES de `page.goto('/index.html')` (reutiliza el contrato del index.html, determinista). Función `settle(page)`: `await page.waitForLoadState('networkidle')` + `await page.evaluate(() => document.fonts?.ready)`. **Decisión A5 (heros remotas) — FIJAR la opción 2 (bloquear→fallback SVG):** registrar `await page.route('**/*.{jpg,jpeg,png,webp,avif,gif}', r => r.abort())` para forzar SIEMPRE el estado de fallback SVG (`onerror`→SVG por motif) — es el estado **determinista y además el "offline"** que el proyecto valora (BUILD-02), eliminando la dependencia de red de terceros (Wikimedia/turismoroma) que haría el golden no reproducible. NO usar la opción 1 (esperar carga real, frágil) ni la 3 (mask, que ignora la región). Documentar esta elección A5 en el SUMMARY y reflejarla en VALIDATION.md. Generar los PNGs con `pnpm test:golden:update` (`playwright test --update-snapshots`), revisar visualmente que las 14 vistas × 2 temas × 2 viewports se crearon (cota superior 56 PNGs), y dejarlos en `tests/parity/golden.spec.ts-snapshots/` para versionar. **Prueba de determinismo (crítica):** ejecutar `pnpm test:golden` (SIN `--update`) una segunda vez y confirmar que pasa en verde contra los PNGs recién creados — si falla, hay flakiness no resuelta (revisar animaciones/fuentes/heros). NO usar flags de watch-mode.
  </action>
  <verify>
    <automated>pnpm exec playwright test 2>&1 | tail -5; echo "EXIT=$?"; test "$(ls tests/parity/golden.spec.ts-snapshots/ 2>/dev/null | grep -c -- '-mobile.png\|-desktop.png')" -ge 44</automated>
  </verify>
  <acceptance_criteria>
    - `tests/parity/golden.spec.ts` existe y referencia los 14 selectores exactos (`grep -c '#inicio\|#viernes\|#vaticano\|#auditorium\|#galleria-sciarra' tests/parity/golden.spec.ts` ≥ 5).
    - El spec fuerza el tema oscuro vía `localStorage['roma-theme']='dark'` (`grep -q "roma-theme" tests/parity/golden.spec.ts`).
    - El spec bloquea las imágenes remotas para forzar el fallback SVG determinista (decisión A5): `grep -q "page.route" tests/parity/golden.spec.ts` y `grep -q "abort" tests/parity/golden.spec.ts`.
    - Existen ≥ 44 PNGs golden (cota inferior conservadora: 14 vistas × 2 temas × 2 viewports = 56; ≥44 cubre el caso de que alguna vista no exista exactamente) en `tests/parity/golden.spec.ts-snapshots/`.
    - `pnpm exec playwright test` (segunda ejecución, sin `--update`) sale 0 → **golden determinista** (V8 de RESEARCH).
    - `git add tests/parity && git status --porcelain tests/parity | grep -c '.png'` ≥ 44 (los PNGs son versionables; Pitfall 2).
  </acceptance_criteria>
  <done>El golden de las 14 vistas de D-04 (light+dark, mobile+desktop) está capturado, las heros remotas neutralizadas a fallback SVG determinista (A5), los PNGs versionados, y una segunda ejecución sin --update pasa en verde (determinismo confirmado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm → árbol de dependencias | Se instala `@playwright/test` desde npm; superficie de cadena de suministro |
| red de terceros (Wikimedia/turismoroma) → harness del golden | El `index.html` referencia imágenes hero remotas; entrada no controlada que puede contaminar el determinismo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `pnpm add @playwright/test` (cadena de suministro) | mitigate | slopcheck no disponible → todos los paquetes [ASSUMED]; checkpoint humano bloqueante (Tarea 1) confirmando que es el paquete oficial de Microsoft (`npmjs.com/package/@playwright/test`) y versión `latest` verificada; `pnpm-lock.yaml` commiteado |
| T-01-NET | Information Disclosure / no-determinismo | Harness del golden alcanzando imágenes de terceros por red | mitigate | Decisión A5 = bloquear todas las peticiones de imagen (`page.route(...).abort()`) → el golden no depende de red de terceros; estado determinista de fallback SVG, alineado con el objetivo offline (BUILD-02) |
| T-01-GI | Tampering | `.gitignore` del scaffold excluyendo los golden PNGs (Pitfall 2) | mitigate | `.gitignore` redactado para NO cubrir `tests/parity/`; verificación `git check-ignore` en acceptance criteria; sin esto, D-05 se incumpliría silenciosamente |

Sin auth, sin input de usuario, sin secretos, sin endpoints en este plan. La superficie real es la cadena de suministro (mitigada por el gate de legitimidad) y el no-determinismo por red (mitigado por A5).
</threat_model>

<verification>
- `pnpm exec playwright --version` → 1.61.x con chromium instalado.
- `pnpm exec playwright test` (sin `--update`) sale 0 contra los golden generados → determinismo (V8).
- `git ls-files tests/parity | grep -c '.png'` ≥ 44 tras commit → golden versionado (D-05).
- Verificación de orden: NO existe `nuxt.config.ts` ni `app/` al cerrar este plan (el golden es estrictamente el primer entregable, antes de divergir).
</verification>

<success_criteria>
ROADMAP §Phase 1 criterio 5 cubierto: hay screenshots golden del `index.html` original (home + 5 días + 3 fichas-tipo + 5 referencias) en claro/oscuro y móvil/desktop, deterministas y versionados, capturados antes de divergir.
</success_criteria>

<output>
Crear `.planning/phases/01-andamiaje-golden-de-paridad/01-01-SUMMARY.md` al terminar. Documentar explícitamente: la decisión A5 (heros bloqueadas → fallback SVG), la decisión A8 (snapshotPathTemplate sin plataforma; captura en linux), el nº exacto de PNGs generados, y la confirmación de la segunda ejecución verde.
</output>
