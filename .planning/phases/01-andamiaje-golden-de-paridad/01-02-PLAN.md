---
phase: 01-andamiaje-golden-de-paridad
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - package.json
  - pnpm-lock.yaml
  - nuxt.config.ts
  - tsconfig.json
  - eslint.config.mjs
  - content.config.ts
  - app/app.vue
  - app/assets/css/tokens.css
  - app/assets/css/base.css
  - app/assets/css/leaflet.css
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05, BUILD-02]
user_setup: []

must_haves:
  truths:
    - "nuxt dev arranca y nuxt generate compila a estático sin errores"
    - "El comando de lint (@nuxt/eslint + stylistic) pasa limpio y el typecheck (TS estricto) pasa limpio"
    - "El CSS editorial del index.html (~2.200 líneas) vive como CSS global en app/assets/css/ (tokens+base+leaflet) y se carga una sola vez vía nuxt.config.ts, sin reescribirse a ningún framework"
    - "Las 3 familias de fuentes se sirven self-hosteadas (@nuxt/fonts), sin <link> a Google Fonts en el flujo Nuxt"
    - "index.html, favicon.svg y apple-touch-icon.svg de la raíz permanecen intactos (D-02)"
  artifacts:
    - path: "nuxt.config.ts"
      provides: "Config raíz: módulos, css:[] en orden tokens->base->leaflet, colorMode (roma-theme/data-theme), fonts self-host, TS estricto"
      contains: "baseURL"
    - path: "app/assets/css/tokens.css"
      provides: ":root + [data-theme=dark] verbatim del index.html (sistema de design tokens)"
      contains: ":root {"
      min_lines: 25
    - path: "app/assets/css/base.css"
      provides: "reset/tipografía/componentes editoriales verbatim"
      min_lines: 1000
    - path: "app/assets/css/leaflet.css"
      provides: "CSS de Leaflet 1.9.4 inline verbatim + filtro dark de tiles"
      min_lines: 800
    - path: "app/app.vue"
      provides: "placeholder mínimo para que generate produzca HTML real (no SPA shell)"
    - path: "content.config.ts"
      provides: "stub vacío (módulo @nuxt/content registrado; esquema real es Fase 2)"
  key_links:
    - from: "nuxt.config.ts"
      to: "app/assets/css/{tokens,base,leaflet}.css"
      via: "css:[] en orden tokens->base->leaflet"
      pattern: "css:\\s*\\["
    - from: "nuxt.config.ts"
      to: "@nuxtjs/color-mode (roma-theme / data-theme)"
      via: "colorMode con dataValue:'theme', storageKey:'roma-theme', fallback:'light'"
      pattern: "roma-theme"
    - from: "nuxt.config.ts"
      to: "@nuxt/fonts (3 familias self-host)"
      via: "fonts.families con pesos/itálicas exactos de index.html línea 13"
      pattern: "Cormorant Garamond"
---

<objective>
Montar el **scaffold Nuxt 4 en la raíz del repo** (D-01, `srcDir=app/`), con TypeScript estricto, pnpm, `@nuxt/eslint` (flat config) + formateo `stylistic`, los módulos del stack registrados, las 3 fuentes self-hosteadas, y el **CSS editorial del `index.html` extraído verbatim** a `app/assets/css/` (tokens + base + leaflet) cargado una sola vez. Al terminar, `nuxt dev` arranca, `nuxt generate` compila, y lint + typecheck pasan limpio.

Cubre PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05 y la parte de fuentes de BUILD-02. **No** monta el subpath final, el backend dormido ni la verificación del build local — eso es el Plan 03.

Purpose: El esqueleto compilable sobre el que se construye toda la app, con el CSS verbatim como garantía de paridad por construcción (CLAUDE.md decisión #5).
Output: `package.json`, `nuxt.config.ts`, `tsconfig.json`, `eslint.config.mjs`, `content.config.ts`, `app/app.vue`, y los 3 ficheros CSS verbatim.

**Restricción innegociable (D-02):** `index.html`, `favicon.svg` y `apple-touch-icon.svg` de la RAÍZ no se tocan, no se mueven, no se sobrescriben. El scaffold debe preservarlos (Pitfall 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-CONTEXT.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-01-SUMMARY.md

<decisions>
- D-01: proyecto en la raíz, srcDir=app/, con nuxt.config.ts/content//public//server//shared//package.json en raíz.
- D-02: index.html/favicon.svg/apple-touch-icon.svg de la raíz INTACTOS.
- D-03: pnpm (pnpm-lock.yaml); usar pnpm, nunca `npx --yes`.
- Discreción (resuelta): CSS partido en 3 ficheros tokens.css/base.css/leaflet.css, cargado en orden tokens->base->leaflet.
- Discreción (resuelta): TS via typescript.typeCheck:false + comando pnpm typecheck separado (A3).
- Discreción (resuelta): formateo vía @nuxt/eslint config.stylistic:true (una sola herramienta; NO mezclar con Prettier).
- A1 (resuelta): instalar el stack completo ahora (no perezoso) para fijar la config.
</decisions>

<interfaces>
<!-- Sketch de nuxt.config.ts — bloques load-bearing (decisiones bloqueadas). El executor los usa directo; ver RESEARCH 260-322. NO inventar valores. -->

modules: ['@nuxt/content', '@nuxtjs/color-mode', '@nuxt/fonts', '@nuxt/eslint']
app: { baseURL: '/guiaRoma/' }            // el subpath se afina en el Plan 03; declararlo ya es correcto
css: ['~/assets/css/tokens.css', '~/assets/css/base.css', '~/assets/css/leaflet.css']   // ORDEN crítico
colorMode: { preference: 'system', fallback: 'light', dataValue: 'theme', storageKey: 'roma-theme', classSuffix: '' }
typescript: { strict: true, typeCheck: false }
eslint: { config: { stylistic: true } }
fonts: { families: [
  { name: 'Cormorant Garamond', provider: 'google', weights: [400,500,600,700], styles: ['normal','italic'], subsets: ['latin','latin-ext'] },
  { name: 'Lora', provider: 'google', weights: [400,500,600], styles: ['normal','italic'], subsets: ['latin','latin-ext'] },
  { name: 'JetBrains Mono', provider: 'google', weights: [400,500], styles: ['normal'], subsets: ['latin','latin-ext'] },
] }

<!-- Mapa de líneas del index.html para la extracción CSS verbatim (verificado esta sesión): -->
Leaflet CSS inline:  index.html 14-864   (<style id="leaflet-inline-css"> abre L14, cierra L864)  -> leaflet.css (copiar 15-863)
:root tokens:        index.html 866-881   -> tokens.css
[data-theme=dark]:   index.html 883-898   -> tokens.css
CSS editorial resto: index.html 900-2209  (<style> editorial abre L865, cierra L2210)             -> base.css
filtro dark tiles:   index.html 1208-1209 (vive en el bloque EDITORIAL, no en el de Leaflet)       -> base.css o leaflet.css (indiferente; que se cargue)
fuentes Google link: index.html 11-13     -> NO portar al flujo Nuxt (las inyecta @nuxt/fonts)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Tarea 1: Verificación de paquetes + scaffold Nuxt 4 en raíz (Pitfall 1) + tooling + config</name>
  <files>package.json, pnpm-lock.yaml, nuxt.config.ts, tsconfig.json, eslint.config.mjs, content.config.ts, app/app.vue</files>
  <read_first>
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 135-153 (Installation pnpm; nota crítica sobre nuxi init . en raíz NO vacía)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 157-179 (Package Legitimacy Audit: los 12 paquetes, [ASSUMED] -> checkpoint)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 258-322 (Pattern 1: sketch completo de nuxt.config.ts)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 580-616 (Tooling: TS estricto + eslint flat config + stylistic; scripts package.json)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 639-644 (Pitfall 1: nuxi init en raíz no vacía — vía A temporal o vía B manual)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md líneas 128-197 (asignaciones de nuxt.config.ts y package.json; versiones exactas)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md líneas 268-311 (app/app.vue mínimo; content.config.ts vacío; tsconfig; eslint autogenerado)
  </read_first>
  <what-built>
    Checkpoint de legitimidad de paquetes (segundo y último de la fase) para el stack Nuxt que este plan instala — distinto del de Plan 01 (que cubrió solo Playwright). slopcheck no estaba disponible -> los paquetes se tratan como [ASSUMED] y requieren confirmación humana bloqueante. Lista a instalar (todos `latest` verificados en RESEARCH §Package Legitimacy Audit el 2026-06-18, todos paquetes oficiales de la org Nuxt o librerías canónicas): nuxt@4.4.8, @nuxt/content@3.14.0, zod@4.4.3, @nuxtjs/color-mode@4.0.1, @nuxt/fonts@0.14.0, minisearch@7.2.0, leaflet@1.9.4, @nuxt/eslint@1.16.0 + eslint@10.5.0 (vía nuxi module add eslint), y dev @types/leaflet@1.9.21, prettier@3.8.4. Tras aprobar: scaffold Nuxt 4 en la RAÍZ con manejo de raíz no vacía (Pitfall 1 — nuxi init aborta/sobrescribe en raíz no vacía; usar vía A = init en temporal + mover SOLO los ficheros del scaffold, o vía B = crear el set mínimo a mano), instalar módulos, generar eslint.config.mjs (nuxi module add eslint), y escribir nuxt.config.ts completo con el sketch de interfaces. Verificar que index.html/favicon.svg/apple-touch-icon.svg/.planning/ siguen intactos.
  </what-built>
  <how-to-verify>
    1. Revisar la lista de paquetes que el executor presenta: debe coincidir EXACTAMENTE con las versiones de arriba (RESEARCH §Package Legitimacy Audit). Todos son @nuxt/*/@nuxtjs/*/nuxt (org oficial Nuxt) o canónicos (zod/leaflet/minisearch/eslint/prettier).
    2. Spot-check de 2-3 en sus páginas oficiales: https://www.npmjs.com/package/nuxt (publisher Nuxt), https://www.npmjs.com/package/@nuxt/content, https://www.npmjs.com/package/leaflet.
    3. Aprobar o señalar discrepancias.
  </how-to-verify>
  <action>
    Presentar la lista de paquetes (versiones exactas de arriba) para aprobación humana. NO `npx --yes`; usar pnpm (D-03). Tras aprobación: scaffolding del proyecto Nuxt 4 en la RAÍZ manejando que NO está vacía (Pitfall 1: index.html/favicon.svg/apple-touch-icon.svg/CLAUDE.md/.planning//.git/ ya existen, y package.json/playwright.config.ts/tests/ del Plan 01 también). Recomendado vía B (crear el set de scaffold a mano: completar package.json, tsconfig.json, app/app.vue) para no arriesgar que `nuxi init .` toque los ficheros existentes; si se usa vía A, scaffold en temporal y mover SOLO los ficheros nuevos. Instalar deps: `pnpm add nuxt@4.4.8 @nuxt/content@3.14.0 zod@4.4.3 @nuxtjs/color-mode@4.0.1 @nuxt/fonts@0.14.0 minisearch@7.2.0 leaflet@1.9.4`; `pnpm dlx nuxi module add eslint` (instala @nuxt/eslint@1.16.0+eslint@10.5.0, genera eslint.config.mjs, registra el módulo); `pnpm add -D @types/leaflet@1.9.21 prettier@3.8.4`. Fusionar en package.json los scripts Nuxt junto a los de golden ya presentes: dev=nuxi dev, build=nuxi build, generate=nuxi generate, preview=nuxi preview, typecheck=nuxi typecheck, lint=eslint ., lint:fix=eslint . --fix. Escribir nuxt.config.ts con TODO el sketch de <interfaces>: modules, app.baseURL='/guiaRoma/', css:['~/assets/css/tokens.css','~/assets/css/base.css','~/assets/css/leaflet.css'], colorMode (dataValue:'theme', storageKey:'roma-theme', fallback:'light', preference:'system', classSuffix:''), fonts.families (3 familias con los pesos/itálicas EXACTOS), typescript:{strict:true,typeCheck:false}, eslint:{config:{stylistic:true}}. NO añadir future.compatibilityVersion (4 es el default en Nuxt 4). tsconfig.json extiende ./.nuxt/tsconfig.json. app/app.vue = placeholder mínimo (<template><NuxtLayout><NuxtPage /></NuxtLayout></template> o <template><div>guiaRoma — scaffold</div></template> si aún no hay pages/). content.config.ts stub vacío/mínimo (módulo registrado; el esquema zod de las 6 colecciones es Fase 2 — NO escribirlo aquí). NO crear nitro.preset/server//public/.nojekyll aquí (Plan 03). NO ssr:false. Verificar que index.html/favicon.svg/apple-touch-icon.svg no han cambiado (git status debe mostrarlos sin modificar).
  </action>
  <verify>
    <automated>test -f nuxt.config.ts && test -f eslint.config.mjs && grep -q "roma-theme" nuxt.config.ts && grep -q "Cormorant Garamond" nuxt.config.ts && pnpm exec nuxi --version</automated>
  </verify>
  <acceptance_criteria>
    - El usuario aprobó explícitamente la lista de paquetes (gate de legitimidad — no auto-aprobable).
    - `git status --porcelain index.html favicon.svg apple-touch-icon.svg` está VACÍO (D-02: raíz intacta — Pitfall 1 evitado).
    - nuxt.config.ts contiene los 4 modules, css:[] en orden tokens->base->leaflet, colorMode con roma-theme+data-theme+fallback:'light', fonts.families con Cormorant Garamond/Lora/JetBrains Mono, typescript.strict:true, eslint.config.stylistic:true.
    - nuxt.config.ts NO contiene `ssr: false` (Anti-Pattern: rompería paridad): `grep -c 'ssr: *false' nuxt.config.ts` == 0.
    - eslint.config.mjs existe (autogenerado por nuxi module add eslint).
    - app/app.vue y content.config.ts existen; content.config.ts NO define colecciones reales (eso es Fase 2).
    - `pnpm exec nuxi --version` funciona (Nuxt instalado).
  </acceptance_criteria>
  <resume-signal>Escribe "approved" para confirmar los paquetes e instalar, o describe discrepancias.</resume-signal>
  <done>Scaffold Nuxt 4 en raíz con srcDir=app/, raíz intacta (D-02), módulos registrados, eslint flat config + stylistic, nuxt.config.ts completo con css/colorMode/fonts/TS-estricto, app.vue placeholder, content.config.ts stub.</done>
</task>

<task type="auto">
  <name>Tarea 2: Extraer el CSS editorial verbatim a tokens.css / base.css / leaflet.css y validar build+lint+typecheck</name>
  <files>app/assets/css/tokens.css, app/assets/css/base.css, app/assets/css/leaflet.css</files>
  <read_first>
    - index.html líneas 14-40 (cabecera del bloque Leaflet <style id="leaflet-inline-css">; el resto 15-863 es CSS estándar de Leaflet 1.9.4)
    - index.html líneas 865-905 (apertura del <style> editorial: :root en 866, [data-theme="dark"] en 883, reset * en 900, html{scroll-behavior:smooth;scroll-padding-top:124px} en 902)
    - index.html líneas 1208-1209 (filtro dark de tiles, en el bloque editorial)
    - index.html líneas 2121-2125 (@keyframes fadeIn + .card{animation:fadeIn .5s ease backwards} — copiar VERBATIM; la neutralización de animación es solo del golden, no del CSS)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md líneas 41-124 (asignaciones EXACTAS tokens/base/leaflet, excerpts load-bearing, selectores cruzados a preservar)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 387-418 (CSS Extraction Strategy; reglas de cascada; NO @layer, NO scoped)
  </read_first>
  <action>
    Extraer el CSS del index.html VERBATIM (sin alterar ningún valor, selector ni regla) a 3 ficheros, usando el mapa de líneas confirmado: (1) app/assets/css/leaflet.css = contenido de <style id="leaflet-inline-css">, líneas 15-863 del index.html (todo el CSS de Leaflet 1.9.4: .leaflet-pane, .leaflet-tile, .leaflet-container, controles, popups, zoom). (2) app/assets/css/tokens.css = el bloque :root { … } (líneas 866-881) + el bloque [data-theme="dark"] { … } (líneas 883-898) — las custom properties que SON el sistema de design tokens (CLAUDE.md decisión #5). (3) app/assets/css/base.css = el resto del <style> editorial, líneas 900-2209 (reset *, html/body/tipografía, y TODAS las reglas de componentes globales: .card, .timeline, .gastro-card, .artist-card, .tl-*, .day-*, .theme-btn, .facts, .sorrentino-box, .culture-box, .detail-photo, media queries, @keyframes). El filtro dark de tiles (líneas 1208-1209) está en el bloque editorial — dejarlo en base.css o moverlo a leaflet.css es indiferente, lo crítico es que se cargue exactamente una vez. Reglas innegociables (Pitfall 4 / RESEARCH 410-413): NO envolver en @layer (cambia la prioridad de cascada -> drift visual). NO usar <style scoped> (los data-v-* romperían selectores cruzados como body.modo-resumen .tl-meta, [data-theme="dark"] .leaflet-tile, [data-theme="light"] .theme-btn .moon/.sun). NO añadir SCSS/PostCSS (el CSS usa color-mix(in srgb,…) nativo). El orden de carga ya está fijado en nuxt.config.ts (Tarea 1): tokens -> base -> leaflet (base consume las variables de tokens). Verificación verbatim (V4): el contenido combinado de los 3 ficheros debe ser textualmente igual al <style id="leaflet-inline-css"> (15-863) + <style> editorial (866-2209) del index.html, salvo el split en ficheros. Si el formateo stylistic reescribiera reglas CSS, excluir app/assets/css/** del lint (vía ignores en eslint.config.mjs) para preservar el verbatim — el CSS no es código fuente a formatear. Tras extraer, pnpm lint, pnpm typecheck y pnpm generate deben pasar limpio. NO tocar index.html (D-02). NO usar watch-mode.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm generate</automated>
  </verify>
  <acceptance_criteria>
    - Existen los 3 ficheros: app/assets/css/tokens.css, app/assets/css/base.css, app/assets/css/leaflet.css.
    - tokens.css contiene `:root {` y `[data-theme="dark"]` (las custom properties verbatim).
    - base.css contiene `scroll-padding-top: 124px` (la regla html verbatim de la línea 902) y reglas de componentes (.card, .timeline).
    - leaflet.css contiene `.leaflet-container` (CSS de Leaflet verbatim).
    - NINGÚN fichero CSS contiene @layer: `cat app/assets/css/*.css | grep -v '^#' | grep -c '@layer'` == 0 (sin capas — Pitfall 4).
    - Verbatim (V4): concatenar tokens.css+base.css reproduce el <style> editorial del index.html (866-2209) salvo el split; concatenar leaflet.css reproduce el <style id="leaflet-inline-css"> (15-863). Comprobable con un diff textual ignorando reordenación de ficheros.
    - `pnpm lint` sale 0, `pnpm typecheck` sale 0, `pnpm generate` sale 0 (PLAT-01/02/03 verdes con el CSS cargado).
    - `git status --porcelain index.html` está VACÍO (D-02: el index.html no se tocó).
  </acceptance_criteria>
  <done>Los 3 ficheros CSS existen con el contenido editorial+leaflet verbatim del index.html, sin @layer ni scoped, cargados en orden tokens->base->leaflet desde nuxt.config.ts; lint+typecheck+generate pasan limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm -> árbol de dependencias | Se instala el stack Nuxt (10 paquetes) desde npm; superficie de cadena de suministro |
| scaffolder (nuxi init) -> ficheros existentes de la raíz | nuxi init en raíz no vacía podría sobrescribir index.html/favicons (D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | pnpm add del stack Nuxt (cadena de suministro) | mitigate | slopcheck no disponible -> paquetes [ASSUMED]; checkpoint humano bloqueante (Tarea 1) confirmando paquetes oficiales org Nuxt / canónicos y versiones latest verificadas; pnpm-lock.yaml commiteado |
| T-02-PI | Tampering | postinstall malicioso de un paquete del scaffold | mitigate | Todos los paquetes son oficiales/canónicos; pnpm pide confirmación de build scripts; el checkpoint de Tarea 1 es la barrera |
| T-02-D02 | Tampering | nuxi init . sobrescribiendo index.html/favicon.svg/apple-touch-icon.svg (D-02 / Pitfall 1) | mitigate | Scaffold con manejo de raíz no vacía (vía temporal o manual); acceptance criteria asserta git status --porcelain vacío para los 3 ficheros raíz |
| T-02-CDN | Information Disclosure | Cargar fuentes/Leaflet desde CDN en runtime (contra BUILD-02 offline) | mitigate | @nuxt/fonts self-hostea las 3 familias en build; NO se porta el <link> Google Fonts; Leaflet se instala para importarse de node_modules (uso en Fase 7) — la verificación de 0 requests a CDN es del Plan 03 |
| T-02-SSR | Elevation of Privilege | ssr:false o un server/api/*.ts activando superficie v2 prematura | mitigate | nuxt.config.ts SIN ssr:false (acceptance criteria lo asserta); server/ no se crea en este plan (Plan 03 lo deja dormido) |

Sin auth, sin input de usuario, sin secretos en este plan. Superficie real: cadena de suministro (gate de legitimidad) y preservar la raíz (D-02).
</threat_model>

<verification>
- pnpm lint exit 0 (PLAT-03), pnpm typecheck exit 0 (PLAT-02), pnpm generate exit 0 (PLAT-01) — con el CSS verbatim cargado.
- nuxt.config.ts lista css:[] en orden tokens->base->leaflet, colorMode con roma-theme/data-theme, fonts.families con las 3 familias incl. itálicas (BUILD-02 fuentes), typescript.strict:true.
- Estructura Nuxt 4 presente (PLAT-05): app/, nuxt.config.ts, content.config.ts en raíz (server//public/ los crea el Plan 03).
- git status --porcelain index.html favicon.svg apple-touch-icon.svg VACÍO (D-02).
- .output/public/index.html tiene contenido real (no <div id="__nuxt"></div> vacío) -> SSR-en-build ON, no SPA shell.
</verification>

<success_criteria>
ROADMAP §Phase 1 criterios cubiertos: (1) nuxt dev arranca y nuxt generate compila sin errores con TS estricto y lint limpio; (3) el CSS editorial vive como CSS global en assets/css/ (tokens+base+leaflet), cargado una vez desde nuxt.config.ts, sin reescribir a ningún framework. Parte de (2): las 3 fuentes self-hosteadas vía @nuxt/fonts (la verificación de 0×CDN y subpath es del Plan 03).
</success_criteria>

<output>
Crear `.planning/phases/01-andamiaje-golden-de-paridad/01-02-SUMMARY.md` al terminar. Documentar: vía usada para el scaffold en raíz no vacía (A o B), confirmación de que la raíz quedó intacta (D-02), y si hubo que excluir app/assets/css/** del lint para preservar el verbatim.
</output>
