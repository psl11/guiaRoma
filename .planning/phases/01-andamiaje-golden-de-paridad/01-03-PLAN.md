---
phase: 01-andamiaje-golden-de-paridad
plan: 03
type: execute
wave: 3
depends_on: ['01-02']
files_modified:
  - nuxt.config.ts
  - server/api/README.md
  - public/.nojekyll
  - public/favicon.svg
  - public/apple-touch-icon.svg
  - tests/parity/subpath.spec.ts
  - package.json
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, ARCH-03, PLAT-05]
user_setup: []

must_haves:
  truths:
    - "El sitio generado se sirve bajo /guiaRoma/ sin ningún 404 de /_nuxt/* (baseURL + nitro.preset github_pages + public/.nojekyll), verificado LOCALMENTE (D-06, sin CI)"
    - "Al servir el build no hay ninguna petición a CDNs (fonts.googleapis.com / fonts.gstatic.com / unpkg.com): fuentes y assets self-hosteados (BUILD-02)"
    - "Existe server/ con server/api/README.md y NINGÚN endpoint activo (sin *.ts/*.js); nuxt generate sigue produciendo estático con SSR-en-build ON (ARCH-03)"
    - ".output/public/.nojekyll existe tras generate; el routing es history (no hash), las anclas #id siguen siendo fragmentos"
  artifacts:
    - path: "server/api/README.md"
      provides: "Backend Nitro dormido documentado (hueco v2); ÚNICO contenido bajo server/"
      contains: "DORMIDO"
    - path: "public/.nojekyll"
      provides: "Cinturón-y-tirantes para que GitHub Pages no ignore /_nuxt/"
    - path: "tests/parity/subpath.spec.ts"
      provides: "Smoke programable: sirve el build bajo /guiaRoma/ y asserta 0×404 de /_nuxt/* y 0 requests a CDNs"
      contains: "/_nuxt/"
  key_links:
    - from: "nuxt.config.ts"
      to: "GitHub Pages subpath /guiaRoma/"
      via: "app.baseURL + nitro.preset github_pages + failOnError"
      pattern: "github_pages"
    - from: "tests/parity/subpath.spec.ts"
      to: ".output/public servido bajo /guiaRoma/"
      via: "page.on('response') asserta status<400 para /_nuxt/*; page.on('request') asserta 0 a CDNs"
      pattern: "page.on\\("
---

<objective>
Cerrar la infraestructura de la Fase 1: fijar el **subpath de producción `/guiaRoma/`** (BUILD-01/03) con `app.baseURL` + `nitro.preset: 'github_pages'` + `public/.nojekyll`, dejar el **backend Nitro presente pero dormido** (ARCH-03, `server/api/README.md`, cero endpoints), copiar los favicons a `public/` para que resuelvan bajo el subpath, y **verificar localmente** (D-06, sin CI) que el build estático se sirve bajo `/guiaRoma/` sin ningún 404 de `/_nuxt/*` y sin ninguna petición a CDNs (cerrando la verificación offline de BUILD-02).

Cubre BUILD-01, BUILD-03, ARCH-03, completa BUILD-02 (verificación 0×CDN) y completa PLAT-05 (`server/`/`public/` en raíz). **No** monta CI ni deploy real (D-06: eso es una fase posterior; el deploy vivo de `main` no se toca).

Purpose: Sin subpath correcto los assets dan 404 en masa en GitHub Pages; sin la verificación local no hay prueba objetiva de BUILD-01/03 antes de divergir más.
Output: `nuxt.config.ts` ampliado con nitro/preset, `server/api/README.md`, `public/.nojekyll`, favicons en `public/`, y `tests/parity/subpath.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-CONTEXT.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md
@.planning/phases/01-andamiaje-golden-de-paridad/01-02-SUMMARY.md

<decisions>
- D-01: server//public/ en la raíz (hermanos de app/).
- D-02: los favicon.svg/apple-touch-icon.svg de la RAÍZ son del index.html y NO se mueven; se COPIAN a public/ (A4).
- D-06: verificación del build estático en LOCAL bajo /guiaRoma/; NO montar CI/deploy; el deploy vivo de main intacto.
- Discreción (resuelta): verificación de subpath vía copiar .output/public a subcarpeta guiaRoma/ + static server + Playwright page.on('response') (A7).
- Discreción (resuelta): contenido del server/api/README.md = texto sugerido en RESEARCH 499-505.
</decisions>

<interfaces>
<!-- Bloques a AÑADIR a nuxt.config.ts (el resto ya lo escribió el Plan 02). Source: RESEARCH 276-284, 448-462. -->

nitro: {
  preset: 'github_pages',     // variante static de Pages; añade piezas de Pages
  prerender: {
    crawlLinks: true,
    routes: ['/'],            // una sola ruta en Fase 1 (placeholder)
    failOnError: true,        // un enlace roto ROMPE el build (parity guard)
  },
}
// favicons de la app Nuxt bajo el subpath: declarar en app.head.link apuntando a /favicon.svg
// (que resuelve a /guiaRoma/favicon.svg al servir public/ bajo el baseURL).

<!-- server/api/README.md — texto EXACTO (RESEARCH 499-505): -->
# server/api — Backend Nitro (DORMIDO)
Hueco reservado para el backend de v2 (auth, subida de media, API).
En la 1.0 NO hay endpoints activos: el sitio se genera 100% estático (preset github_pages).
Añadir un *.ts aquí activaría una ruta — NO hacerlo en la 1.0.

<!-- Verificación local del subpath (A7 / RESEARCH 464-480, 718-727): -->
mkdir -p /tmp/pages-preview/guiaRoma && cp -r .output/public/* /tmp/pages-preview/guiaRoma/
static server sobre /tmp/pages-preview  ->  abrir /guiaRoma/
page.on('response', r => { if (r.url().includes('/_nuxt/') && r.status()>=400) FAIL })
page.on('request',  r => { if (/fonts\.googleapis|fonts\.gstatic|unpkg\.com/.test(r.url())) FAIL })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Subpath github_pages + backend Nitro dormido + favicons en public/ + .nojekyll</name>
  <files>nuxt.config.ts, server/api/README.md, public/.nojekyll, public/favicon.svg, public/apple-touch-icon.svg</files>
  <read_first>
    - nuxt.config.ts (el fichero a modificar — leerlo entero antes de tocar; ya tiene modules/css/colorMode/fonts del Plan 02)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 276-284 (bloque nitro/prerender del sketch)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 448-485 (Base-Path Correctness; favicon bajo subpath A4)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 489-507 (Dormant Nitro Backend; texto del README)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md líneas 282-302 (server/api/README.md + .nojekyll + favicons)
    - index.html líneas 9-10 (referencias relativas a favicon.svg / apple-touch-icon.svg — para confirmar qué ficheros copiar)
  </read_first>
  <action>
    Ampliar nuxt.config.ts (NO reescribir lo del Plan 02; AÑADIR) con el bloque `nitro: { preset: 'github_pages', prerender: { crawlLinks: true, routes: ['/'], failOnError: true } }`. `app.baseURL: '/guiaRoma/'` ya está del Plan 02 — confirmar que sigue. Declarar los favicons de la app Nuxt en `app.head.link` (icon -> /favicon.svg, apple-touch-icon -> /apple-touch-icon.svg) para que resuelvan bajo el subpath. NO activar hash-mode (el routing history es el default; la app usa anclas #id como fragmentos, no rutas). NO `ssr: false`. Crear `server/api/README.md` con el texto EXACTO de <interfaces> (ÚNICO contenido bajo server/ — NINGÚN *.ts/*.js, ARCH-03: un fichero de servidor activaría una ruta). Crear `public/.nojekyll` VACÍO (cinturón-y-tirantes; el preset debería añadirlo pero hay fricción conocida nuxt#21232/#12480). COPIAR (no mover) favicon.svg y apple-touch-icon.svg de la RAÍZ a public/ (A4: los de la raíz son del index.html, D-02, permanecen intactos; la app Nuxt necesita su copia bajo /guiaRoma/). Verificar que index.html/favicon.svg/apple-touch-icon.svg de la raíz no han cambiado.
  </action>
  <verify>
    <automated>test -f server/api/README.md && test -f public/.nojekyll && test -f public/favicon.svg && grep -q "github_pages" nuxt.config.ts && grep -q "failOnError" nuxt.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - nuxt.config.ts contiene `nitro.preset: 'github_pages'` y `failOnError: true`; `app.baseURL: '/guiaRoma/'` sigue presente (del Plan 02).
    - nuxt.config.ts NO contiene `ssr: false`: `grep -c 'ssr: *false' nuxt.config.ts` == 0.
    - server/api/README.md existe y contiene "DORMIDO"; NO hay ningún `.ts`/`.js` bajo server/: `find server -name '*.ts' -o -name '*.js' | grep -c . ` == 0.
    - public/.nojekyll existe (fichero vacío).
    - public/favicon.svg y public/apple-touch-icon.svg existen (copias).
    - `git status --porcelain favicon.svg apple-touch-icon.svg index.html` está VACÍO (los de la raíz intactos — D-02).
  </acceptance_criteria>
  <done>Subpath /guiaRoma/ configurado (baseURL + github_pages + failOnError), backend Nitro dormido (server/api/README.md sin endpoints), .nojekyll y favicons en public/, raíz intacta.</done>
</task>

<task type="auto">
  <name>Tarea 2: Verificar el build estático local bajo /guiaRoma/ — 0×404 de /_nuxt/* y 0 requests a CDNs</name>
  <files>tests/parity/subpath.spec.ts</files>
  <read_first>
    - playwright.config.ts (creado en Plan 01 — leerlo SOLO como referencia de estructura; este test NO lo modifica: sirve el build él mismo de forma autocontenida, vía B)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 464-480 (verificación local del subpath: copiar a subcarpeta guiaRoma/ + servir)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 718-727 (snippet programable page.on('response') para 0×404 de /_nuxt/*)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-RESEARCH.md líneas 800-809 (Validation Architecture V5/V6: comandos exactos)
    - .planning/phases/01-andamiaje-golden-de-paridad/01-PATTERNS.md líneas 338-348 (subpath sin 404; backend dormido)
  </read_first>
  <action>
    Crear `tests/parity/subpath.spec.ts` (separado del golden.spec.ts; el golden sirve la raíz en :4173, este sirve el build en otro puerto bajo /guiaRoma/). Flujo (A7): el test (o un setup previo) ejecuta `pnpm generate`, copia `.output/public/*` a una subcarpeta `guiaRoma/` bajo un directorio temporal, sirve ese directorio padre con un static server (p. ej. `pnpm dlx serve -l 5000 <dir>`), y navega a `http://localhost:5000/guiaRoma/`. Registrar DOS listeners: (1) `page.on('response', r => { if (r.url().includes('/_nuxt/') && r.status() >= 400) bad.push(r.url()) })` y assertar `bad.length === 0` (BUILD-01/03: V5 — 0×404 de assets bajo subpath); (2) `page.on('request', r => { if (/fonts\.googleapis\.com|fonts\.gstatic\.com|unpkg\.com/.test(r.url())) cdn.push(r.url()) })` y assertar `cdn.length === 0` (BUILD-02: V6 — fuentes y Leaflet self-hosteados, cero CDN en runtime). Tras `page.waitForLoadState('networkidle')`, assertar también que la página cargó sin error de consola crítico. Verificar además que `.output/public/.nojekyll` existe (V7/BUILD-01). Servir el build de forma AUTOCONTENIDA dentro del propio spec (vía B): un `test.beforeAll`/`test.afterAll` (o un `globalSetup` local importado solo por este spec) que arranca el static server con `child_process.spawn` y lo cierra al final. NO modificar `playwright.config.ts` ni añadir un segundo `project` (vía A descartada) — así `files_modified` queda exacto y el spec es autónomo. Añadir un script `package.json` `"test:subpath": "playwright test tests/parity/subpath.spec.ts"`. Ejecutar y confirmar verde. NO usar watch-mode. NO tocar el deploy de main (D-06).
  </action>
  <verify>
    <automated>pnpm generate && test -f .output/public/.nojekyll && pnpm exec playwright test tests/parity/subpath.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm generate` sale 0 y produce `.output/public/` (estático, con SSR-en-build ON).
    - `.output/public/.nojekyll` existe (`test -f .output/public/.nojekyll`).
    - `.output/public/index.html` referencia assets bajo `/guiaRoma/_nuxt/` (no desde la raíz del dominio): `grep -c '/guiaRoma/_nuxt/' .output/public/index.html` >= 1.
    - tests/parity/subpath.spec.ts existe y registra `page.on('response')` (404 de /_nuxt/*) y `page.on('request')` (CDNs): `grep -c "page.on(" tests/parity/subpath.spec.ts` >= 2.
    - `pnpm exec playwright test tests/parity/subpath.spec.ts` sale 0 -> 0 respuestas con status>=400 para /_nuxt/* bajo /guiaRoma/ (BUILD-01/03) y 0 requests a fonts.googleapis.com/fonts.gstatic.com/unpkg.com (BUILD-02).
    - `find server -name '*.ts' -o -name '*.js' | grep -c .` == 0 y `.output/public/index.html` existe (ARCH-03: dormido + estático).
  </acceptance_criteria>
  <done>El build estático se sirve bajo /guiaRoma/ sin 404 de /_nuxt/* y sin ninguna petición a CDN, verificado en local con Playwright; .nojekyll presente; backend dormido confirmado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build estático -> GitHub Pages (subpath /guiaRoma/) | Los assets se referencian relativos al baseURL; un baseURL mal puesto -> 404 |
| navegador (runtime) -> CDNs de terceros | Si las fuentes/Leaflet se cargaran de CDN, se rompe el objetivo offline (BUILD-02) y se filtra la navegación a terceros |
| server/ -> superficie de API | Un *.ts bajo server/ activaría un endpoint (superficie v2 prematura) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-EP | Elevation of Privilege | Endpoint Nitro activo por error (server/api/*.ts) contra ARCH-03 | mitigate | server/ solo con README.md; acceptance criteria asserta 0 ficheros *.ts/*.js bajo server/; sin runtime Nitro en Pages estático |
| T-03-CDN | Information Disclosure | Cargar fuentes/Leaflet desde CDN en runtime (contra BUILD-02 offline) | mitigate | Test subpath.spec.ts asserta 0 requests a fonts.googleapis.com/fonts.gstatic.com/unpkg.com al servir el build (V6) |
| T-03-404 | Denial of Service (UX) | Assets /_nuxt/* dando 404 bajo el subpath (sitio sin estilos) | mitigate | app.baseURL + nitro.preset github_pages + public/.nojekyll + failOnError; test asserta 0×404 de /_nuxt/* bajo /guiaRoma/ (V5) |
| T-03-SSR | Tampering | ssr:false produciendo SPA shell sin HTML (rompe paridad e indexabilidad) | mitigate | Sin ssr:false; acceptance criteria asserta .output/public/index.html con contenido real |
| T-03-DEP | Tampering | Tocar el deploy vivo de main por error (D-06) | accept | D-06: este plan solo verifica en LOCAL; no monta CI ni toca Pages; riesgo residual nulo al no ejecutar comandos de deploy |

Sin auth, sin input de usuario, sin secretos. Superficie real: no activar el backend (mitigado) y mantener el offline (verificado por test).
</threat_model>

<verification>
- `pnpm generate` exit 0 con `.output/public/` y `.nojekyll` presente.
- `tests/parity/subpath.spec.ts` exit 0 -> 0×404 de /_nuxt/* bajo /guiaRoma/ (BUILD-01/03) y 0 requests a CDNs (BUILD-02).
- `.output/public/index.html` referencia /guiaRoma/_nuxt/ y tiene contenido real (no SPA shell vacío).
- 0 ficheros *.ts/*.js bajo server/ (ARCH-03).
- Raíz intacta: `git status --porcelain index.html favicon.svg apple-touch-icon.svg` VACÍO (D-02).
- D-06: ningún comando de CI/deploy ejecutado; el deploy de main no se toca.
</verification>

<success_criteria>
ROADMAP §Phase 1 criterios cubiertos: (2) el sitio generado se sirve bajo /guiaRoma/ sin 404 de /_nuxt/* (baseURL + .nojekyll), con fuentes self-hosteadas (0×CDN verificado) — Leaflet queda preparado para Fase 7; (4) existe server/ con server/api/README.md y ningún endpoint activo, y el sitio se sigue generando estático (SSR-en-build ON, sin ssr:false).
</success_criteria>

<output>
Crear `.planning/phases/01-andamiaje-golden-de-paridad/01-03-SUMMARY.md` al terminar. Documentar: cómo se sirvió el build para la verificación de subpath (servidor autocontenido en el spec vía beforeAll/child_process — vía B, sin tocar playwright.config.ts), el resultado de los asserts 0×404 y 0×CDN, y confirmación de que no se tocó CI/deploy (D-06).
</output>
