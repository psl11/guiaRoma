---
phase: 07-isla-client-only-mapa-fallback-de-imagen-y-notas
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - tests/parity/map-fallback-notes.spec.ts
autonomous: false
requirements: [FEAT-02, UI-05, FEAT-04]
user_setup: []
must_haves:
  truths:
    - "A self-contained Playwright spec builds once and serves under /guiaRoma/ (mirror of modes/navigation/search-route specs), fresh base port 5760"
    - "Map renders client-side (.leaflet-container + 39 .custom-marker incl. ★); SSG #fallback is an empty #leaflet-map"
    - "A card-popup 'Abrir ficha →' click navigates (scroll + .highlight) WITHOUT changing location.hash"
    - "Coliseo (★) and vaticano guided popups are text-only with NO anchor"
    - "Offline banner shows when tiles are aborted; hero/detail SVG fallback shows when images are aborted (caption kept); notes persist under roma-note-<slug>"
    - "Console gate tolerates ONLY the @nuxtjs/color-mode hydration message; any other console error fails the suite"
  artifacts:
    - path: "tests/parity/map-fallback-notes.spec.ts"
      provides: "All F7 behavioral parity assertions (SC#1-SC#7 of 07-UI-SPEC) in one self-contained spec"
      min_lines: 120
  key_links:
    - from: "tests/parity/map-fallback-notes.spec.ts"
      to: ".output/public served under /guiaRoma/"
      via: "ensureBuild (pnpm generate) + cpSync to guiaRoma/ subdir + pnpm dlx serve (clone of modes.spec.ts harness)"
      pattern: "ensureBuild|guiaRoma"
    - from: "spec tile/image abort"
      to: "offline banner + image fallback"
      via: "context.route('**/*.tile.openstreetmap.org/**').abort() + image src abort (A5 precedent)"
      pattern: "abort"
---

<objective>
Write the self-contained Playwright parity spec `tests/parity/map-fallback-notes.spec.ts` that proves all three F7 behaviors against the BUILT `/guiaRoma/` site (the 4th clone of the established `modes`/`navigation`/`search-route` harness), then take the human paridad sign-off that closes the phase. The spec forces `tileerror` and image `@error` via `context.route().abort()` (A5 golden precedent), asserts popup navigation via the F5 capture listener (hash unchanged), the guided text-only quirk, and notes round-trip — with the console gate tolerating only the known color-mode hydration message.

Purpose: This is the Nyquist Wave-0 behavioral verification for FEAT-02 / UI-05 / FEAT-04 that the pure-util unit specs (Plan 01) cannot cover browserless, and the phase-closing human sign-off (matching F3/F4/F5/F6). Pixel-diff total is out of scope (F8).

Output: `tests/parity/map-fallback-notes.spec.ts` (NEW) + human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-CONTEXT.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-RESEARCH.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-PATTERNS.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-UI-SPEC.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-VALIDATION.md
@.planning/phases/07-02-SUMMARY.md
@.planning/phases/07-03-SUMMARY.md

<interfaces>
<!-- Harness + assertion patterns to clone. Use directly — no exploration. -->

From tests/parity/modes.spec.ts:1-97 (the self-contained harness — copy VERBATIM, change only the port + describe text):
```ts
const EXPECTED_HYDRATION_MSG = /Hydration completed but contains mismatches/i
const OUTPUT_DIR = join(process.cwd(), '.output', 'public')
async function waitForServer(url, timeoutMs = 120_000) { … }   // copy as-is
function killGroup(proc) { … }                                  // copy as-is
function ensureBuild() {                                        // copy as-is
  if (!existsSync(join(OUTPUT_DIR, 'index.html'))) {
    const gen = spawnSync('pnpm', ['generate'], { stdio: 'inherit', shell: false })
    expect(gen.status, 'pnpm generate debe salir 0').toBe(0)
  }
  expect(existsSync(join(OUTPUT_DIR, 'index.html')), '.output/public/index.html debe existir').toBe(true)
}
// beforeAll: ensureBuild() → mkdtempSync → cpSync(OUTPUT_DIR, <tmp>/guiaRoma) → spawn('pnpm',['dlx','serve','-l',PORT,previewRoot],{detached:true}) → waitForServer
// afterAll: killGroup(server) + rmSync(previewRoot)
// Ports already used: 5700 (modes), 5720 (navigation), 5740 (search-route). USE 5760 here.
```

From tests/parity/navigation.spec.ts:186-229 (popup-nav + hash-unchanged):
```ts
// dispatchEvent('click') routes through the F5 CAPTURE listener (Playwright won't auto-scroll):
await page.locator(SELECTOR).dispatchEvent('click')
await expect(page.locator(`#${id}`)).toHaveClass(/\bhighlight\b/)
expect(new URL(page.url()).hash, 'D-03: navegar NO cambia el hash').not.toBe('#' + id)
// console gate: page.on('console', msg => { if (msg.type()==='error' && !EXPECTED_HYDRATION_MSG.test(msg.text())) consoleErrors.push(msg.text()) }); expect(consoleErrors).toHaveLength(0)
```

From tests/parity/modes.spec.ts:223-239 (localStorage preset):
```ts
const context = await browser.newContext()
await context.addInitScript(() => { try { window.localStorage.setItem('roma-note-galleria-sciarra', 'probe') } catch {} })
```

DOM contract delivered by Plans 02+03:
- Map: `.leaflet-container` (Leaflet root) + 39 `.custom-marker` (one contains `★`). SSG `#fallback` = empty `#leaflet-map`.
- Card popup anchor: `a[href^="#"]` with text `Abrir ficha →`. Guided popup: text `Visita con guía humano`, no `<a>`.
- Offline banner: `#map-offline-banner.map-offline-banner`, text `Sin conexión · solo marcadores visibles`, class `show` when offline.
- Hero fallback: `.card-hero svg` present, no `<img>` in `.card-hero`. Detail fallback: `.detail-photo svg` present + `.detail-photo-caption` present, no visible `<img>`.
- Notes: `.notes-textarea` inside `#galleria-sciarra` reads its `roma-note-<slug>` value.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → built site | The spec serves the prerendered `.output/public` it builds itself; no external trust boundary. The route-abort simulates network failure (offline/broken-image), not an attack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Spoofing / Tampering (regression) | parity spec console gate | mitigate | The console gate fails the suite on ANY console error other than the known `@nuxtjs/color-mode` hydration message — this is the guard that a future change does not silently introduce a hydration mismatch (Pitfall 6) or a runtime error in the map/fallback/notes. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No installs (Playwright + Vitest already configured; 07-RESEARCH ## Package Legitimacy Audit — N/A). |

No HIGH-severity threats. This is a test-only plan validating a static no-backend app.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: tests/parity/map-fallback-notes.spec.ts — self-contained parity spec (SC#1–SC#7)</name>
  <files>tests/parity/map-fallback-notes.spec.ts</files>
  <read_first>
    - tests/parity/modes.spec.ts lines 1-115 (THE self-contained harness to clone VERBATIM: `EXPECTED_HYDRATION_MSG`, `OUTPUT_DIR`, `waitForServer`, `killGroup`, `ensureBuild`, the `beforeAll`/`afterAll` build+copy+serve, the `STATIC_PORT` pattern, the console-gate, the `gotoHydrated` helper, and the localStorage-preset at 223-239).
    - tests/parity/navigation.spec.ts lines 186-245 (THE popup-nav assertions: `dispatchEvent('click')` to route through the F5 CAPTURE listener, `toHaveClass(/\bhighlight\b/)`, the `new URL(page.url()).hash` not-changed assertion).
    - tests/parity/search-route.spec.ts (confirm the port currently used = 5740, so 5760 is free).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-VALIDATION.md ## Wave 0 Requirements + the Per-Requirement Verification Map (the exact behaviors each assertion must cover) and the Sampling Rate (console gate tolerance).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-UI-SPEC.md ## Parity Acceptance Criteria (SC#1–SC#7 — the verbatim conditions this spec asserts) and §1f (the popup contract + vaticano quirk).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-PATTERNS.md the `tests/parity/map-fallback-notes.spec.ts` section (the harness boilerplate, the abort techniques, the popup/notes patterns).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-RESEARCH.md ## Validation Architecture > Wave 0 Gaps (the route-abort recipes + A5 precedent).
  </read_first>
  <action>
    Create `tests/parity/map-fallback-notes.spec.ts` by cloning the `modes.spec.ts` harness (lines 1-97) VERBATIM, changing ONLY: the `STATIC_PORT` base to `5760 + Number(process.env.TEST_WORKER_INDEX ?? 0)` (fresh port, avoids 5700/5720/5740), the `mkdtempSync` prefix (e.g. `guiaroma-mapfb-`), and the `describe` text. Keep `EXPECTED_HYDRATION_MSG`, `ensureBuild`, `waitForServer`, `killGroup`, the build+`cpSync('guiaRoma')`+`spawn('pnpm',['dlx','serve',…],{detached:true})`+`waitForServer` in `beforeAll`, and the `killGroup`+`rmSync` in `afterAll`. Reuse the `gotoHydrated`-style helper (wait for `networkidle`). Every test attaches the console gate (`page.on('console', …)` pushing non-`EXPECTED_HYDRATION_MSG` errors) and ends with `expect(consoleErrors).toHaveLength(0)`.
    Assert SC#1 (map client-only + generate): `ensureBuild` proves `pnpm generate` exited 0; read `.output/public/index.html` and assert it contains `<div id="leaflet-map"` with no child text (empty fallback); after `gotoHydrated`, assert `.leaflet-container` is present and `page.locator('.custom-marker')` has count `39`; assert at least one `.custom-marker` contains the text `★`.
    Assert SC#2 (popup navigates via F5): click a `card`-type marker to open its popup (use `page.locator('.custom-marker')` — pick a known card monument; you may filter by the popup content after opening), then `dispatchEvent('click')` on the popup `a[href^="#"]` whose text is `Abrir ficha →`; assert the target `.card` (by its `#slug`) gains `.highlight`; assert `new URL(page.url()).hash` is NOT `#slug` (F5 preventDefault). Use a deterministic card slug present in `monById` (e.g. one of the 38, such as `pantheon` or `doria-pamphilj`).
    Assert SC#3 (guided text-only): open the Coliseo (`★`) marker popup → assert it contains `Visita con guía humano` and contains NO `<a>` element; open the `vaticano` marker popup → same (the vaticano-has-a-ficha-but-no-link quirk honored).
    Assert SC#4 (offline banner): before navigating, `await context.route('**/*.tile.openstreetmap.org/**', r => r.abort())`; after the map mounts, poll until `#map-offline-banner` has class `show` and `isVisible()` is true; assert its text is exactly `Sin conexión · solo marcadores visibles`. (Use a context/page where tiles are aborted; A5 precedent.)
    Assert SC#5 (hero fallback): abort the hero image request for a known card (`await context.route(<that hero src or a broad image matcher>, r => r.abort())`), navigate, and assert that card's `.card-hero` contains an `<svg>` and contains no `<img>`.
    Assert SC#6 (detail fallback): with detail images aborted, assert a `.detail-photo` contains an `<svg>` carrying inline styles `width:100%`/`height:auto`/`border-radius:4px`/`display:block`, that `.detail-photo-caption` is still present with text, and no `<img>` is visible inside `.detail-photo`. (This assertion also confirms A2 — the motif reached DetailPhoto via inject.)
    Assert SC#7 (notes persistence): in a fresh context, `addInitScript(() => localStorage.setItem('roma-note-galleria-sciarra','probe'))`, load, assert the `.notes-textarea` inside `#galleria-sciarra` has value `probe`; then type into a different card's textarea, reload, assert the typed text persisted under that card's `roma-note-<slug>`. The console gate (no error beyond the color-mode message) proves no hydration warning from the onMounted read.
    Structure the tests so the tile-abort / image-abort routes are scoped per-test (separate `browser.newContext()` where needed, like modes.spec) so a route that aborts images for SC#5/SC#6 does not break the marker-count assertion in SC#1.
  </action>
  <verify>
    <automated>pnpm generate >/dev/null 2>&1 && pnpm test:golden tests/parity/map-fallback-notes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/parity/map-fallback-notes.spec.ts` exists and uses base port `5760` (`grep -c '5760' tests/parity/map-fallback-notes.spec.ts` returns ≥ 1) — distinct from 5700/5720/5740.
    - The spec contains `ensureBuild`, `EXPECTED_HYDRATION_MSG`, and `pnpm dlx serve` (or the cloned `spawn('pnpm', ['dlx', 'serve'` harness) — `grep -c "dlx" tests/parity/map-fallback-notes.spec.ts` returns ≥ 1.
    - The spec asserts a marker count of `39` and the `★` presence, the `Abrir ficha →` popup navigation with hash-unchanged, the `Visita con guía humano` text-only guided popup, the `.map-offline-banner` `show` via tile abort, `.card-hero svg` + `.detail-photo svg` via image abort, and the `roma-note-galleria-sciarra` `probe` round-trip — `grep -E "39|Abrir ficha|Visita con guía humano|map-offline-banner|card-hero|detail-photo|roma-note-galleria-sciarra" tests/parity/map-fallback-notes.spec.ts` returns multiple matches.
    - The spec contains the hash-unchanged assertion `new URL(page.url()).hash` … `.not.toBe`.
    - `pnpm generate` exits 0 (no `window is not defined`) and `pnpm test:golden tests/parity/map-fallback-notes.spec.ts` exits 0 (all SC#1–SC#7 green; console gate clean).
    - `pnpm test:unit` still exits 0 (Plan 01 utils unaffected).
  </acceptance_criteria>
  <done>The self-contained parity spec is green: 39 markers + ★, popup navigates without hash change, guided popups text-only (incl. vaticano), offline banner via tile abort, hero+detail SVG fallback via image abort (caption kept), notes round-trip under `roma-note-<slug>`, console gate tolerating only the color-mode message.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human paridad sign-off (map + image fallback + notes)</name>
  <what-built>
    Phase 7 is implemented and the self-contained parity spec (`tests/parity/map-fallback-notes.spec.ts`) is green: the Leaflet map renders client-only with 39 numbered markers, popups navigate via the F5 listener (hash unchanged), guided popups are text-only, the offline banner fires on the exact heuristic, hero/detail images fall back to motif SVGs, and per-card notes persist under `roma-note-<slug>`. `nuxt generate` is clean (no `window is not defined`). This is the same phase-closing human paridad sign-off used in F3/F4/F5/F6.
  </what-built>
  <how-to-verify>
    1. Build and serve the site under the production subpath (see MEMORY.md "Preview built site locally": serve `.output/public` under a `guiaRoma/` subdir and open `/guiaRoma/`):
       - `pnpm generate`
       - serve the build under `/guiaRoma/` (e.g. copy `.output/public` into a `guiaRoma/` folder and `pnpm dlx serve -l 5050 .`, then open `http://localhost:5050/guiaRoma/`).
    2. Scroll to "El mapa del viaje". Confirm it looks IDENTICAL to the live `index.html`:
       - The map shows OSM tiles bounded to Rome with numbered circular markers; red = paradas con ficha, dorado = guiados, verde = concierto Einaudi (the legend matches).
       - Click a red (card) marker → its popup shows the name, day, and "Abrir ficha →"; clicking the link scrolls to and highlights that ficha (the URL bar does NOT gain a `#slug`).
       - Click the Coliseo (★) marker and the Vaticano marker → each popup shows "Visita con guía humano" with NO link (Vaticano has a ficha but its map popup still has no link — this is correct/parity).
    3. Toggle dark mode → the map tiles dim (filter applied), as on the live site.
    4. (Optional offline check) Throttle to offline in devtools and reload → the "Sin conexión · solo marcadores visibles" banner appears and markers remain.
    5. Open a ficha whose hero image fails to load (or block image requests in devtools) → the hero shows the engraving-style motif SVG instead of a broken image; a detail photo similarly falls back to its SVG while keeping its caption.
    6. Type a note in a ficha's "Notas in situ", reload the page → the note is still there. Confirm there is NO "saved" indicator or any new UI (notes are silent, exactly as today).
    Compare side-by-side with the live `index.html` (open the original in another tab) in light + dark, mobile + desktop. The bar is 100% parity.
  </how-to-verify>
  <resume-signal>Type "approved" if map + popups + offline banner + image fallback + notes are identical to the live guide in light/dark × mobile/desktop, or describe any difference.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm generate` clean; `pnpm test:golden tests/parity/map-fallback-notes.spec.ts` green (SC#1–SC#7).
- `pnpm test:unit` green (regression check on Plan 01 utils).
- Human paridad sign-off APPROVED (map/popups/offline/fallback/notes identical to the live `index.html`).
- Note: the 4 pre-existing full-suite failures (golden.spec pixel-diff → F8; shell.spec dev-routing nuxi lock) are out of scope and tracked in deferred-items — do NOT attempt to fix them here.
</verification>

<success_criteria>
- FEAT-02 / UI-05 / FEAT-04 behavioral parity is proven by one green self-contained Playwright spec (the Nyquist Wave-0 behavioral dependency).
- The console gate tolerates only the known `@nuxtjs/color-mode` hydration message — no new runtime/hydration error.
- Human sign-off confirms 100% parity (the F7-specific closing gate before `/gsd:verify-work` and Phase 8's total pixel-diff).
</success_criteria>

<output>
Create `.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-04-SUMMARY.md` when done.
</output>
