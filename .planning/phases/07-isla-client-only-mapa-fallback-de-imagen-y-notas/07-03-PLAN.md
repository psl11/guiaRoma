---
phase: 07-isla-client-only-mapa-fallback-de-imagen-y-notas
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/components/MonumentCard.vue
  - app/components/DetailPhoto.global.vue
autonomous: true
requirements: [UI-05, FEAT-04]
user_setup: []
must_haves:
  truths:
    - "On hero <img> error, .card-hero shows the motif SVG (hidden only if no motif — dead branch for monuments)"
    - "On detail <img> error, .detail-photo swaps only the <img> for the motif SVG and keeps .detail-photo-caption"
    - "Per-monument notes persist under the exact key roma-note-<slug>, read in onMounted, saved on input"
    - "No hydration warning beyond the known @nuxtjs/color-mode message (notes empty at SSR, filled in onMounted)"
  artifacts:
    - path: "app/components/MonumentCard.vue"
      provides: "Hero @error → SVG (v-html toggle) + notes :value/@input localStorage persistence + provide('monumentMotif')"
      contains: "roma-note-"
    - path: "app/components/DetailPhoto.global.vue"
      provides: "Detail @error → SVG (4 inline styles, caption kept) + inject('monumentMotif')"
      contains: "monumentMotif"
  key_links:
    - from: "app/components/MonumentCard.vue + app/components/DetailPhoto.global.vue"
      to: "app/utils/svgMotifs.ts"
      via: "motifSvg(monument.motif) auto-imported"
      pattern: "motifSvg"
    - from: "app/components/MonumentCard.vue (provide)"
      to: "app/components/DetailPhoto.global.vue (inject)"
      via: "provide/inject 'monumentMotif' through the MDCRenderer subtree"
      pattern: "monumentMotif"
    - from: "app/components/MonumentCard.vue notes textarea"
      to: "localStorage"
      via: "onMounted read + debounced @input write, key roma-note-<slug>"
      pattern: "roma-note-"
---

<objective>
Wire the two F4-mounted shells: the hero `<img>` and notes `<textarea>` in `MonumentCard.vue`, and the detail `<img>` in `DetailPhoto.global.vue`. Add the image-with-fallback (`@error` → SVG by `monument.motif`, hero + detail modes, port of `loadSvgFallback`/`loadSvgFallbackDetail`) and the per-card notes persistence (`localStorage['roma-note-<slug>']`, read in `onMounted`, saved on `input`, port of `setupNotes`). The `motif` reaches `DetailPhoto.global.vue` via `provide`/`inject` from `MonumentCard`.

Purpose: UI-05 (image-with-fallback reproducing the `onerror` → SVG-by-motif behavior) and FEAT-04 (notes with identical keys/behavior). Both complete the "shell mounted in F4, behavior wired in its phase" pattern; reading `localStorage` and deciding image-failed state ONLY in browser events avoids hydration mismatches.

Output: `app/components/MonumentCard.vue` (MODIFY); `app/components/DetailPhoto.global.vue` (MODIFY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-CONTEXT.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-RESEARCH.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-PATTERNS.md
@.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-UI-SPEC.md
@.planning/phases/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + existing F4 shells. Use directly — no exploration. -->

From app/utils/svgMotifs.ts (Plan 01, auto-imported — no import statement needed):
```ts
export function motifSvg(motif: Motif | undefined): string | undefined  // SVG string or undefined
```

From shared/schemas.ts:
```ts
export type Motif = '...'   // 19-value enum
// MonumentSchema has: slug, motif (required), hero: { src, alt }
```

MonumentCard.vue current state:
- `defineProps<{ monument: Monument }>()` (line 75) — `monument` is in scope (this file's style uses `monument.*`, NOT `props.monument.*`).
- Hero shell (lines 150-156): `<div class="card-hero"><img :src="monument.hero.src" :alt="monument.hero.alt" loading="lazy"></div>` — PLAIN, no @error (D-01 frontier of F4).
- Notes shell (lines 229-237): `<div class="notes-area"><label …>Notas in situ</label><textarea :id="'note-'+monument.slug" class="notes-textarea" :data-note-key="monument.slug" placeholder="Lo que quieras recordar de aquí…" /></div>` — no persistence (D-02 frontier of F4).
- Sections rendered via `<MDCRenderer>` (lines 158-176) — DetailPhoto is nested HERE, so a `provide` on MonumentCard reaches it.

DetailPhoto.global.vue current state:
- `defineProps<{ src: string, alt: string, caption: string }>()` (line 25) — NO motif today.
- Plain `<img :src="src" :alt="alt" loading="lazy">` (lines 30-34) + `<div class="detail-photo-caption"><MDC :value="caption" unwrap="p" /></div>` (lines 35-40).

CSS already sizes the swapped SVGs:
- HERO: `.card-hero svg, .card-hero img { width:100%;height:100%;object-fit:cover }` (base.css:719) → hero SVG needs NO inline styles.
- DETAIL: `.detail-photo img` is styled (base.css:825), NOT svg → the detail SVG NEEDS the four inline styles.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes → localStorage → textarea `:value` | Notes are the user's own text, echoed back only into `:value` (never `v-html`/`innerHTML`) — no reflection into HTML. |
| static SVG constant → `v-html` | The SVG injected on image error comes from `svgMotifs.ts` (trusted static constant), never user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering / Elevation (stored XSS) | notes localStorage → textarea | accept | Notes are written back ONLY to the textarea `value` (`:value`), never `v-html`/`innerHTML`. No HTML reflection surface → safe by construction. (07-RESEARCH.md ## Security Domain — Stored XSS via notes.) |
| T-07-06 | Tampering (HTML injection) | `v-html` of motif SVG | mitigate | `v-html` is applied ONLY to the trusted static `motifSvg(monument.motif)` constant from `svgMotifs.ts`, never to a data/user field. Add `<!-- eslint-disable-next-line vue/no-v-html — TRUSTED static constant (svgMotifs.ts), never user input -->` ON THAT LINE ONLY (never on `name`/`day`/notes). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan (07-RESEARCH ## Package Legitimacy Audit — N/A). |

No HIGH-severity threats. ASVS L1: V5 (Output Encoding) satisfied — the only `v-html` is a trusted constant; user input (notes) is never injected as HTML.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: MonumentCard.vue — hero @error → SVG + notes persistence + provide('monumentMotif')</name>
  <files>app/components/MonumentCard.vue</files>
  <read_first>
    - app/components/MonumentCard.vue lines 67-80 (the `defineProps<{ monument: Monument }>()`, the `monument.*` in-scope style, the no-`<style scoped>` convention + header-comment style) AND lines 150-156 (the plain hero `<img>` shell) AND lines 158-176 (the `<MDCRenderer>` sections where DetailPhoto nests — the provide must be in this component's setup) AND lines 229-237 (the notes-textarea shell with `:data-note-key="monument.slug"`).
    - app/components/SearchBox.vue lines 62-64 (THE `:value`/`@input` binding analog — keeps the SSR default explicit; NOT `v-model`).
    - app/composables/useTripModes.ts lines 88-104 (THE "read localStorage in onMounted, never in setup" discipline + the sanctioned micro-flash precedent).
    - index.html lines 2215-2227 (THE parity source for hero: `loadSvgFallback` — if motif & SVG then `img.parentElement.innerHTML = SVG`; else `img.parentElement.style.display='none'` at 2222, the dead branch) AND lines 6471-6483 (THE parity source for notes: `setupNotes` — key `'roma-note-'+ta.dataset.noteKey` at 6474, read at 6476-6477, save on `input` at 6479-6481).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-PATTERNS.md the `app/components/MonumentCard.vue` section (the hero `failed`-flag + `v-html` shape, the `:value`/`@input` notes shape, the `provide('monumentMotif', monument.motif)` instruction).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-UI-SPEC.md §2b (Hero Mode — no inline styles on hero SVG; eslint-disable comment) and §3b (Notes Behavior Contract — exact key, onMounted read, input write, ~200ms debounce, try/catch, monuments only, empty at SSR).
  </read_first>
  <action>
    HERO `@error` → SVG (port of index.html:2215-2227). Replace the plain hero `<img>` (lines 150-156) with the `failed`-flag + `v-html` toggle: keep `.card-hero` but add `v-show="!heroHidden"`; render `<img v-if="!heroFailed" :src="monument.hero.src" :alt="monument.hero.alt" loading="lazy" @error="onHeroError">` and a sibling `<span v-else v-html="motifSvg(monument.motif)" />`. On the `v-html` line add the comment `eslint-disable-next-line vue/no-v-html` justifying it as a TRUSTED static constant (svgMotifs.ts), never user input. In setup: `const heroFailed = ref(false)`, `const heroHidden = ref(false)`, and `function onHeroError()` that sets `heroFailed.value = true` when `motifSvg(monument.motif)` returns a string, else `heroHidden.value = true` (the dead branch for monuments — motif always present — ported for fidelity; mirrors `img.parentElement.style.display='none'` at index.html:2222). NO inline styles on the hero SVG — `.card-hero svg` (base.css:719) already sizes it. `motifSvg` is auto-imported from Plan 01.
    NOTES persistence (port of index.html:6471-6483; key `roma-note-<slug>` exact). Bind the textarea (lines 231-236) with `:value="noteText"` and `@input="onNoteInput(($event.target as HTMLTextAreaElement).value)"` (NOT `v-model` — keep SSR default explicit, parallels SearchBox). In setup: `const noteText = ref('')`; a constant `NOTE_KEY` equal to the template-literal `roma-note-` followed by `monument.slug` (exact key, index.html:6474); read in `onMounted` inside try/catch — `noteText.value = localStorage.getItem(NOTE_KEY) ?? ''` (no hydration warning); a debounced writer `onNoteInput(v)` that sets `noteText.value = v`, clears a module-scoped timer, and after ~200ms writes `localStorage.setItem(NOTE_KEY, v)` inside try/catch (innocuous debounce, D-03 allows). Keep the existing label/placeholder/`:data-note-key` untouched. Notes are monuments-only (this component only).
    PROVIDE the motif for the nested DetailPhoto: add `provide('monumentMotif', monument.motif)` in MonumentCard's setup (import `provide` from `vue` if not auto-imported). There is exactly one `<DetailPhoto>` per card, nested under it via the `<MDCRenderer>` at lines 158-176, so the provide reaches the right one (Task 2 injects it). NO `<style scoped>`.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'roma-note-' app/components/MonumentCard.vue` returns ≥ 1 (the exact note-key prefix).
    - `grep -c '@error' app/components/MonumentCard.vue` returns ≥ 1 and `grep -c 'motifSvg' app/components/MonumentCard.vue` returns ≥ 1 (hero fallback wired).
    - `grep -c "provide('monumentMotif'" app/components/MonumentCard.vue` returns ≥ 1 (provide present).
    - `grep -c "onMounted" app/components/MonumentCard.vue` returns ≥ 1 (notes read in onMounted, not setup).
    - The notes textarea uses `:value`/`@input`, NOT `v-model` — `grep -c 'v-model' app/components/MonumentCard.vue` returns 0.
    - Exactly one `eslint-disable-next-line vue/no-v-html` appears (on the hero SVG line only); `grep -c 'vue/no-v-html' app/components/MonumentCard.vue` returns 1.
    - `app/components/MonumentCard.vue` has NO new `<style` block (`grep -c '<style' app/components/MonumentCard.vue` returns 0).
    - `pnpm typecheck` and `pnpm lint` exit 0.
  </acceptance_criteria>
  <done>Hero `@error` swaps in the motif SVG (or hides on the dead branch); notes read in onMounted + debounced-save under `roma-note-<slug>`; `provide('monumentMotif')` set; `:value`/`@input` (no v-model); typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: DetailPhoto.global.vue — detail @error → SVG (4 inline styles, keep caption) + inject motif</name>
  <files>app/components/DetailPhoto.global.vue</files>
  <read_first>
    - app/components/DetailPhoto.global.vue (whole file, ~42 lines — the `.global.vue` header explaining the MDC-resolution mechanism, `defineProps<{ src, alt, caption }>()` at :25, the plain `<img>` at :30-34, the `.detail-photo-caption` MDC at :35-40, the no-`<style scoped>` rule at :22-24).
    - app/components/MonumentCard.vue (Task 1 of this plan — the hero `failed`-flag + `v-html` shape to mirror, and the `provide('monumentMotif', monument.motif)` this file injects).
    - index.html lines 2229-2252 (THE parity source for detail: `loadSvgFallbackDetail` — sets the FOUR `svg.style.*` props `width:100%`/`height:auto`/`border-radius:4px`/`display:block` at 2238-2241 then `img.replaceWith(svg)`; if no svg then `img.style.display='none'` at 2244/2247; the `.detail-photo-caption` is preserved).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-PATTERNS.md the `app/components/DetailPhoto.global.vue` section (the `inject<Motif | undefined>('monumentMotif', undefined)` + the four-inline-styles-verbatim instruction + A2 confirm-via-spec note).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-UI-SPEC.md §2c (Detail Mode — exact four inline styles, caption preserved, no-motif hides only the img, provide/inject plumbing).
    - .planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-RESEARCH.md §Pitfall 5 + Assumptions A2 (provide/inject resolves through MDCRenderer; the Plan 04 spec must assert the detail SVG shows — fallback is an MDC prop if A2 fails).
  </read_first>
  <action>
    Add motif via `inject`: extend setup with `const motif = inject<Motif | undefined>('monumentMotif', undefined)` (import `inject` from `vue` if not auto-imported; import the `Motif` type from `~~/shared/schemas`). This is provided by the host `MonumentCard` (Task 1) — there is exactly one DetailPhoto per card, nested under it, so the inject resolves through the MDCRenderer subtree (A2 — the Plan 04 parity spec asserts the detail SVG shows the right motif; if A2 ever fails, the fallback is passing `motif` as an MDC prop, but inject is the chosen path).
    DETAIL `@error` → SVG (port of index.html:2229-2252). Replace the plain detail `<img>` (lines 30-34) with the `failed`-flag + `v-html` toggle, KEEPING the `.detail-photo-caption` div untouched: `<img v-if="!failed" :src="src" :alt="alt" loading="lazy" @error="onError">` and a sibling `<span v-else v-html="detailSvg" />` carrying the FOUR inline styles verbatim — apply `width:100%; height:auto; border-radius:4px; display:block` either on the wrapping `<span style="…">` or inlined into the SVG markup (these inline styles are REQUIRED because `.detail-photo img` CSS targets `img`, not the swapped-in `svg` — base.css:825). On the `v-html` line add the comment `eslint-disable-next-line vue/no-v-html` (TRUSTED static constant). In setup: `const failed = ref(false)`; a computed `detailSvg` returning `motifSvg(motif)`; `function onError() { if (motifSvg(motif)) failed.value = true; else { /* hide only the img */ failed.value = true with an empty/undefined svg → render nothing } }` — match the original's "no svg → `img.style.display='none'`" by ensuring that when `motifSvg(motif)` is undefined the `<img>` is hidden and NO SVG span renders (the caption stays). Keep the `<div class="detail-photo-caption"><MDC :value="caption" unwrap="p" /></div>` exactly as-is. NO `<style scoped>` (a `data-v-*` would break `.detail-photo img` / `[data-theme="dark"] .detail-photo img`).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'monumentMotif' app/components/DetailPhoto.global.vue` returns ≥ 1 (inject present).
    - `grep -c '@error' app/components/DetailPhoto.global.vue` returns ≥ 1 and `grep -c 'motifSvg' app/components/DetailPhoto.global.vue` returns ≥ 1.
    - The file contains the four inline styles `width:100%`, `height:auto`, `border-radius:4px`, `display:block` (for the swapped detail SVG).
    - The file still contains `detail-photo-caption` and the `<MDC :value="caption"` render (caption preserved).
    - Exactly one `eslint-disable-next-line vue/no-v-html` appears; `grep -c 'vue/no-v-html' app/components/DetailPhoto.global.vue` returns 1.
    - `app/components/DetailPhoto.global.vue` has NO new `<style` block.
    - `pnpm typecheck` and `pnpm lint` exit 0.
  </acceptance_criteria>
  <done>Detail `@error` swaps only the `<img>` for the motif SVG carrying the four inline styles, keeps the caption, injects the motif from MonumentCard, and hides only the img when no motif; typecheck + lint clean.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` + `pnpm lint` clean (both components).
- `pnpm generate` still clean (run as part of the Plan 04 parity wave) — no hydration regression introduced.
- Hero/detail `@error` wired to `motifSvg(monument.motif)`; notes persist under `roma-note-<slug>` read in onMounted; the only `v-html` per file is the trusted SVG (one eslint-disable each).
- Full behavioral parity (hero SVG on abort, detail SVG + caption on abort, notes round-trip) is asserted by the Plan 04 parity spec.
</verification>

<success_criteria>
- UI-05: hero replaces `.card-hero` content with the motif SVG on error (CSS-sized, no inline styles); detail replaces only the `<img>` with the motif SVG (four inline styles) and keeps the caption.
- FEAT-04: notes persist under the exact `roma-note-<slug>` key, read in onMounted, debounced-saved on input, monuments-only, empty at SSR (no hydration warning).
- The motif reaches DetailPhoto via `provide`/`inject`; no data/MDC-prop changes.
</success_criteria>

<output>
Create `.planning/phases/07-isla-client-only-mapa-fallback-de-imagen-y-notas/07-03-SUMMARY.md` when done.
</output>
