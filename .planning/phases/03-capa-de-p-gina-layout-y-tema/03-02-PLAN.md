---
phase: 03-capa-de-p-gina-layout-y-tema
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/composables/useTrip.ts
  - app/utils/tripIndexes.ts
  - tests/unit/tripIndexes.spec.ts
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "useTrip('roma') aggregates all 6 collections (trip, days, monuments, food, artists, reference) into typed refs"
    - "days are returned ordered by day.order ASC"
    - "id-indexed Maps (monById/foodById/artById/refById) provide O(1) lookup keyed by slug"
    - "the pure index builder is unit-tested without @nuxt/test-utils (SC#1 index shape)"
  artifacts:
    - path: "app/composables/useTrip.ts"
      provides: "Single trip-data entry point (SC#1) — aggregates 6 collections + id Maps, resolves at SSG"
      exports: ["useTrip"]
    - path: "app/utils/tripIndexes.ts"
      provides: "Pure id-Map builder extracted for unit-testability"
      exports: ["buildTripIndexes"]
    - path: "tests/unit/tripIndexes.spec.ts"
      provides: "SC#1 index-shape coverage via plain Vitest"
  key_links:
    - from: "app/composables/useTrip.ts"
      to: "queryCollection"
      via: "queryCollection('<name>').where('trip','=',slug) / .where('slug','=',slug).first()"
      pattern: "queryCollection\\("
    - from: "app/composables/useTrip.ts"
      to: "app/utils/tripIndexes.ts"
      via: "calls buildTripIndexes to construct the id Maps"
      pattern: "buildTripIndexes"
---

<objective>
Build `useTrip(slug)` — the single, typed entry point that aggregates the 6 Nuxt Content collections for one trip with id-indexed Maps, resolved at prerender (offline). This is the data root that `/`, `/trips/[slug]`, and every F4-F7 consumer reads from (SC#1, ARCH-01).

Purpose: SC#1 requires `useTrip(slug)` to aggregate `trip`/`days`/`monuments`/`food`/`artists`/`reference` with id indexes, and "adding a trip = adding content files, no code." The query layer is the place that promise is kept (filter each collection by its `trip` field; the trip itself by `slug`). RESEARCH §Pattern 1 + PATTERNS §useTrip give the verified `@nuxt/content` API; the schema field contract is confirmed in `shared/schemas.ts` (every collection carries `slug`; `day`/`monument`/`food`/`artist`/`reference` carry `trip`; `day`/`reference` carry `order`). The pure Map-building is EXTRACTED into `app/utils/tripIndexes.ts` so SC#1's index shape is unit-testable with plain Vitest (no `@nuxt/test-utils` dependency), per RESEARCH §Validation Architecture ▸ Wave 0.

Output: `app/composables/useTrip.ts`, `app/utils/tripIndexes.ts`, `tests/unit/tripIndexes.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md

<interfaces>
<!-- CONFIRMED field contract — read directly from shared/schemas.ts (resolves RESEARCH A1 / Pitfall 3): -->
<!-- Every collection's stable key is `slug` (NEVER the reserved Content `id`). -->
TripSchema      → slug (no `trip` field; the trip IS the slug)  → query: .where('slug','=',slug).first()
DaySchema       → slug, trip, order, eyebrow                    → query: .where('trip','=',slug).order('order','ASC').all()
MonumentSchema  → slug, trip                                     → query: .where('trip','=',slug).all()
FoodSchema      → slug, trip                                     → query: .where('trip','=',slug).all()
ArtistSchema    → discriminatedUnion(kind): each variant has slug + trip → .where('trip','=',slug).all()
ReferenceSchema → discriminatedUnion(slug): each variant has slug + trip + order → .where('trip','=',slug).order('order','ASC').all()

<!-- Types to import (from shared/schemas.ts; ~~ = rootDir, shared/ is at repo root): -->
import type { Trip, Day, Monument, Food, Artist, Reference } from '~~/shared/schemas'

<!-- Verified @nuxt/content v3 API (RESEARCH §Pattern 1, from node_modules client.d.ts): -->
queryCollection(name).where(field, operator, value).order(field, 'ASC'|'DESC').all() / .first()
useAsyncData(uniqueKey, fn)   // unique key REQUIRED per (collection, slug)
<!-- queryCollection, useAsyncData, computed are auto-imported by Nuxt. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure tripIndexes builder + unit test (SC#1 index shape)</name>
  <files>app/utils/tripIndexes.ts, tests/unit/tripIndexes.spec.ts</files>
  <read_first>
    - shared/schemas.ts (the 6 schemas + their exported types Trip/Day/Monument/Food/Artist/Reference, lines 280-285; confirm `slug` is the key on every entity)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Pattern 1, §Validation Architecture ▸ Wave 0 Gaps "Recommend extracting the pure indexer to avoid the dependency")
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md (§useTrip "Testability note (Wave 0): the id-index Map-building is pure")
    - tests/unit/dayLabel.spec.ts (created in Plan 01 — mirror its plain-Vitest import/test style)
    - vitest.config.ts (Plan 01 added tests/unit/** to include — confirm the unit runner picks this spec up)
  </read_first>
  <behavior>
    - buildTripIndexes returns an object with monById, foodById, artById, refById — each a Map
    - Given monuments [{slug:'galleria-sciarra',...},{slug:'vaticano',...}], monById.get('galleria-sciarra') returns that monument and monById.size === 2
    - Empty/undefined input arrays produce empty Maps (no throw) — handles the null-data hydration window
    - Keys are the entity `slug` (e.g. monById.has('vaticano') === true), never a numeric/reserved id
  </behavior>
  <action>
    Create `app/utils/tripIndexes.ts` exporting a pure function `buildTripIndexes(monuments, food, artists, reference)` that returns `{ monById, foodById, artById, refById }` where each value is a `new Map(items.map(x => [x.slug, x]))`. Accept possibly-`null`/`undefined` arrays (use `?? []`) so it is safe during the async-data hydration window. Type the parameters with the imported entity types (`Monument[] | null`, etc.) and return typed `Map<string, Monument>` etc. Keep it framework-free (no Nuxt imports) so it is unit-testable with plain Vitest.

    Create `tests/unit/tripIndexes.spec.ts` importing `buildTripIndexes` directly and asserting the 4 behaviors above with minimal fixture objects (only the `slug` field is needed on the fixtures plus an `as` cast to the entity type). Use `import { describe, it, expect } from 'vitest'` mirroring `tests/unit/dayLabel.spec.ts`.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm vitest run tests/unit/tripIndexes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/utils/tripIndexes.ts` exports `buildTripIndexes` and contains NO `import` from `#imports`, `nuxt`, or `@nuxt/content` (pure, framework-free)
    - `buildTripIndexes` builds each Map via `new Map(...map(x => [x.slug, x]))` and guards inputs with `?? []`
    - `tests/unit/tripIndexes.spec.ts` asserts: 4 Maps exist; `.get(slug)` resolves the right entity; `.size` is correct; empty input → empty Maps
    - `pnpm vitest run tests/unit/tripIndexes.spec.ts` exits 0
  </acceptance_criteria>
  <done>The id-Map builder is a pure, framework-free function with green plain-Vitest coverage of the SC#1 index shape.</done>
</task>

<task type="auto">
  <name>Task 2: useTrip composable — aggregate 6 collections, resolve at SSG (SC#1/ARCH-01)</name>
  <files>app/composables/useTrip.ts</files>
  <read_first>
    - app/utils/tripIndexes.ts (Task 1 — useTrip composes its Maps from this)
    - shared/schemas.ts (full file — confirm exact field names per collection before writing each `.where(...)`; header note lines 19-20: `id` is reserved, anchor is `slug`)
    - content.config.ts (the 6 registered collection NAMES: trip, day, monument, food, artist, reference — these are the exact strings passed to queryCollection)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Pattern 1 full code; §Pitfall 3 "useTrip field-name / trip filter mismatch"; §Architectural Responsibility Map "Aggregate trip data → Build/SSG")
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md (§useTrip "Core aggregation pattern" + "Confirmed field contract")
    - .planning/STATE.md (Fase 1 note: better-sqlite3 build-time connector → deployed site is 100% static; useTrip must resolve at prerender, not runtime)
  </read_first>
  <action>
    Create `app/composables/useTrip.ts` exporting an `async function useTrip(slug: string)`. Inside, run six `useAsyncData` calls in `Promise.all`, each with a UNIQUE key per (collection, slug): `trip-${slug}`, `days-${slug}`, `mon-${slug}`, `food-${slug}`, `art-${slug}`, `ref-${slug}`. The query bodies (verified API):
    - trip: `queryCollection('trip').where('slug', '=', slug).first()`
    - day: `queryCollection('day').where('trip', '=', slug).order('order', 'ASC').all()`
    - monument: `queryCollection('monument').where('trip', '=', slug).all()`
    - food: `queryCollection('food').where('trip', '=', slug).all()`
    - artist: `queryCollection('artist').where('trip', '=', slug).all()`
    - reference: `queryCollection('reference').where('trip', '=', slug).order('order', 'ASC').all()`

    Use `slug` as the entity key everywhere; NEVER `.where('id', ...)`. Build the id Maps as `computed(() => buildTripIndexes(monuments.data.value, food.data.value, artists.data.value, reference.data.value))` (import `buildTripIndexes` from `~/utils/tripIndexes` or rely on auto-import) and expose them as `monById`/`foodById`/`artById`/`refById` (either four computed refs or destructured from one computed — pick one and keep it consistent for F4). Return `{ trip: trip.data, days: days.data, monuments: monuments.data, food: food.data, artists: artists.data, reference: reference.data, monById, foodById, artById, refById }`. Type the return against the imported entity types from `~~/shared/schemas`. Add a top-of-file comment stating the chosen architectural convention for F4 (A3): "TripView calls useTrip and owns the chrome+sections; pages render `<TripView :slug>`" so F4 follows the same data-flow.

    Do NOT use `fetch('/content/...')` or `useState` as a data store (RESEARCH §Don't Hand-Roll); `queryCollection` + `useAsyncData` is the typed, prerendered, offline path.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/composables/useTrip.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/composables/useTrip.ts` exports `async function useTrip(slug: string)`
    - Contains all six `queryCollection(...)` calls with collection names exactly `'trip'`,`'day'`,`'monument'`,`'food'`,`'artist'`,`'reference'`
    - Trip is queried with `.where('slug', '=', slug).first()`; the other five with `.where('trip', '=', slug)`; `day` and `reference` additionally `.order('order', 'ASC')`
    - Contains NO `.where('id'` substring (the reserved field is never queried)
    - Each `useAsyncData` key is unique and templated with `${slug}` (six distinct keys)
    - Builds id Maps via `buildTripIndexes` (from Task 1) inside `computed(...)`
    - Return object exposes keys: trip, days, monuments, food, artists, reference, monById, foodById, artById, refById
    - `pnpm typecheck` exits 0 (strict TS) and `pnpm lint app/composables/useTrip.ts` is clean
  </acceptance_criteria>
  <done>useTrip aggregates the 6 collections (slug-keyed, trip-filtered, ordered days/reference) with id Maps, typed strictly, resolving at prerender; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content (repo YAML) → build query | `useTrip` reads repo-authored, zod-validated (Fase 2) content via `queryCollection` at prerender. No external/runtime input. |
| URL `slug` → `queryCollection('trip').where('slug','=',slug)` | The slug param (used by `/trips/[slug]` in Plan 05) reaches a parameterized `.where` — Content's query builder parameterizes values (no string injection). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering/Information Disclosure | `useTrip(slug)` `.where('slug','=',slug)` | accept | `.where` is parameterized (RESEARCH §Security Domain); slug only selects a collection key, never builds a query string. Unknown slug → empty/`null` (Plan 05 turns that into a 404). No data beyond the public static site is reachable. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan (RESEARCH §Package Legitimacy Audit). `@nuxt/content` is already installed and exercised by Fase 2's green tests. No `[ASSUMED]`/`[SUS]` package. |

**Conclusion:** No high-severity threats; no blocking mitigations required. Static SSG, parameterized content queries over trusted repo data, no server/auth/secrets.
</threat_model>

<verification>
- `pnpm vitest run tests/unit/tripIndexes.spec.ts` — index shape green (SC#1).
- `pnpm typecheck` — strict TS over the composable + utils.
- `pnpm lint app/composables/useTrip.ts app/utils/tripIndexes.ts` — clean.
- Behavioral aggregation against real content is asserted end-to-end in Plan 05 (the `/` build renders #inicio from `useTrip('roma')`); a runtime `queryCollection` unit test is intentionally avoided to skip the optional `@nuxt/test-utils` dependency (RESEARCH Wave 0).
</verification>

<success_criteria>
- `useTrip(slug)` aggregates all 6 collections with slug-keyed id Maps and order-sorted days/reference, typed strictly, resolving at SSG (SC#1, ARCH-01).
- The pure index builder is unit-tested green without adding a test dependency.
- The data-flow convention (TripView calls useTrip) is documented in-file for F4 to follow.
</success_criteria>

<output>
Create `.planning/phases/03-capa-de-p-gina-layout-y-tema/03-02-SUMMARY.md` when done.
</output>
