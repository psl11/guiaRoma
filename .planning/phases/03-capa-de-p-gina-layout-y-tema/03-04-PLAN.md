---
phase: 03-capa-de-p-gina-layout-y-tema
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - app/components/TripView.vue
  - app/components/TheHero.vue
autonomous: true
requirements: [ARCH-01, ARCH-02, UI-01]
must_haves:
  truths:
    - "TripView calls useTrip(props.slug) and owns the full document tree: Topbar (with days) then main with all 12 section anchors then BackButton then flourish then footer"
    - "All 12 page-section anchors exist in order with their slug-based id (inicio, mapa, viernes..martes, reservas, gastronomia, practica, arte, arquitectura)"
    - "#inicio is rendered FULL: masthead (decoration, h1 with em, meta, quote) plus layout-only placeholders plus info-grid from trip.infoCards plus how-to from trip.howTo"
    - "Prose fields (trip.title, infoCards value, howTo) render through MDC"
    - "The 11 non-inicio sections are empty placeholders carrying only their id (no fixed height)"
  artifacts:
    - path: "app/components/TripView.vue"
      provides: "Page owner: chrome plus 12-anchor section scaffold (D-05, ARCH-01/02)"
      contains: "useTrip"
    - path: "app/components/TheHero.vue"
      provides: "Full #inicio block from trip data via MDC (D-06)"
      contains: "info-grid"
  key_links:
    - from: "app/components/TripView.vue"
      to: "app/composables/useTrip.ts"
      via: "const trip,days = await useTrip(props.slug)"
      pattern: "useTrip"
    - from: "app/components/TripView.vue"
      to: "Topbar/BackButton/TheHero"
      via: "auto-imported child components"
      pattern: "Topbar|BackButton|TheHero"
    - from: "app/components/TheHero.vue"
      to: "MDC (@nuxt/content)"
      via: "MDC value bindings for title/infoCards/howTo"
      pattern: "MDC"
---

<objective>
Build TripView — the component that owns the whole page (chrome + all 12 section anchors) — and TheHero, the full #inicio block rendered from trip.yml data. This satisfies ARCH-01 (page renders a trip from useTrip), ARCH-02 (one reusable TripView for / and /trips/[slug]), D-05 (TripView owns the section scaffold), D-06 (full #inicio in F3), and UI-01 (the shell is wired and componentized).

Purpose: D-05 makes TripView the owner of the page structure: it mounts the shell (Topbar with the trip's days, BackButton, flourish, footer) and ALL 12 section anchors in order so nav targets, the future scrollspy, and layout parity work; it fills only #inicio with real content and leaves the other 11 as minimal empty placeholders for F4/F7 to plug into. D-06 puts the entire #inicio (masthead + trip.infoCards grid + trip.howTo) in F3 so the first screen has real, verifiable content. TripView calls useTrip(props.slug) (the convention chosen in Plan 02) so both routes are 1-liners. Prose fields render through MDC (_Roma_ becomes em Roma); the two h4 labels are STATIC template text (they live in index.html, not trip.yml). These components are mounted into a route by Plan 05; here they are verified by source assertions + typecheck/lint (rendered visual parity + the MDC unwrap cuadre are proven against the golden in Plan 05).

Output: app/components/TripView.vue, app/components/TheHero.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-UI-SPEC.md

<interfaces>
useTrip return (Plan 02) — TripView destructures from it:
trip, days, monuments, food, artists, reference, monById, foodById, artById, refById.
trip and days are refs (trip.value / days.value). TripView passes days to Topbar (typed Day[] prop).

Shell components available (Plan 03, auto-imported): Topbar (prop days Day[]), NavPills, ThemeToggle, BackButton.

TripView document tree (mirror index.html body 2255-6240) — render in this sibling order:
  Topbar with days  ->  main (wraps the 12 sections)  ->  BackButton  ->  div.flourish "·  ·  ·  ✦  ·  ·  ·"  ->  footer

Footer (verbatim index.html:6235-6239): footer > div.container > p with three lines via br:
  Itinerario preparado para [em]Pay[/em] y dos colegas / Roma · 19—23 giugno 2026 / "Roma no se cuenta, se camina."

12 section anchors in order (id = slug): inicio (FULL), mapa, viernes, sabado, domingo, lunes, martes, reservas, gastronomia, practica, arte, arquitectura. The 11 non-inicio are empty section elements carrying only their id (no content, no fixed height — keep them real empty sections so the section padding 3rem 0 and section+section border-top rules apply; base.css:93-94).

#inicio (TheHero) content — trip.yml fields (TripSchema):
  trip.decoration "·  ROMA AETERNA  ·"  -> div.hero-decoration (plain text)
  trip.title "Cinque giorni a _Roma_"   -> h1 with MDC (renders em Roma)
  trip.meta "19 — 23 giugno 2026 · Hotel Royal Court" -> div.hero-meta (plain text)
  trip.quote + trip.quoteAttr           -> div.hero-quote {quote} then span.hero-quote-attr {quoteAttr}
  trip.infoCards (4 cards: label + value Md) -> div.info-grid of div.info-card (info-card-label {label}, info-card-value MDC {value})
  trip.howTo (2 paragraphs Md)          -> one p per item via MDC

STATIC template text in #inicio (NOT data — verbatim from index.html):
  h4 "Datos del viaje" (index.html:2334) before the info-grid
  h4 "Cómo usar esta guía" (index.html:2354) before the how-to paragraphs

Layout-only DOM placeholders inside #inicio (NO id beyond layout, NO behavior in F3 — index.html:2295-2332):
  div.search-wrap, div.pace-wrap (plus its inner pace controls), two div.light-wrap.
  These exist so the masthead->info-grid spacing/border rhythm matches the golden. Their handlers are F4/F6. Reproduce the markup as in index.html but wire NO handlers.

MDC unwrap caveat (RESEARCH Open Q 1 / PITFALLS §7): MDC wraps inline content in a p (ProseP). For trip.title (inside h1) and infoCards value (inside .info-card-value) you likely need the unwrap behavior so no extra p breaks vertical rhythm; for howTo the p is wanted. This is a CSS/markup cuadre verified against the golden in Plan 05 — flag it, do not over-engineer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TheHero — full #inicio from trip data via MDC (D-06)</name>
  <files>app/components/TheHero.vue</files>
  <read_first>
    - index.html (lines 2283-2358 — the full verbatim #inicio: hero block, search-wrap, pace-wrap with two light-wrap toggles, h4 Datos del viaje, info-grid of 4 info-cards, h4 Cómo usar esta guía, 2 paragraphs)
    - content/trips/roma/trip.yml (the data: decoration/title/meta/quote/quoteAttr/infoCards/howTo — confirm exact strings and that each infoCards value is a multiline Markdown block whose leading bold becomes a block strong)
    - shared/schemas.ts (TripSchema lines 259-277 — field names/types for decoration/title/meta/quote/quoteAttr/infoCards/howTo)
    - app/assets/css/base.css (the .hero/.hero-decoration/.hero-meta/.hero-quote rules, .info-grid/.info-card/.info-card-label/.info-card-value rules including the info-card-value strong display block rule, and the h4 rule — to reproduce the exact class markup)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md (§TheHero.vue VERBATIM markup, data-bound via MDC; the verbatim data mapping list)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-UI-SPEC.md (Component Inventory #inicio block; Copywriting Contract #inicio content table)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Code Examples #inicio content (D-06); §Open Questions 1 MDC unwrap)
  </read_first>
  <action>
    Create app/components/TheHero.vue rendering the full #inicio. It receives the trip object as a prop typed Trip from ~~/shared/schemas — define defineProps with a single trip prop of type Trip. Template root: section with id inicio wrapping div.container. Inside, in this order, verbatim per index.html:2283-2358:
    1. div.hero containing: div.hero-decoration with trip.decoration (plain text); h1 wrapping an MDC bound to trip.title (renders em Roma); div.hero-meta with trip.meta; div.hero-quote with trip.quote immediately followed by span.hero-quote-attr with trip.quoteAttr.
    2. The layout-only DOM placeholders verbatim from index.html:2295-2332 — div.search-wrap (with its search-input and search-results children), div.pace-wrap (with pace-label, pace-options of three pace-btn, pace-help, and two light-wrap blocks each with their toggle button and help span). Reproduce the markup as-is but bind NO event handlers (these are F4/F6 surfaces; their behavior is out of F3 scope). Keep them so the masthead-to-info-grid rhythm matches the golden.
    3. A STATIC h4 with text Datos del viaje (template text, not data), then div.info-grid containing a v-for over trip.infoCards keyed by label, each emitting div.info-card with div.info-card-label of c.label and div.info-card-value wrapping an MDC bound to c.value.
    4. A STATIC h4 with text Cómo usar esta guía, then a v-for over trip.howTo keyed by index emitting a p wrapping an MDC bound to the paragraph string.

    Use the MDC component (auto-imported from @nuxt/content) for trip.title, each infoCards value, and each howTo entry. For the inline cases (trip.title inside h1, infoCards value inside .info-card-value) apply MDC unwrap of the wrapping paragraph so no extra p breaks vertical rhythm; leave the p for howTo. Add a brief comment noting the unwrap is a golden cuadre verified in Plan 05. Add NO style block.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/TheHero.vue && grep -q inicio app/components/TheHero.vue && grep -q info-grid app/components/TheHero.vue && grep -cq style app/components/TheHero.vue; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - TheHero.vue root is a section whose id is inicio, wrapping div.container
    - Contains div.hero with hero-decoration (trip.decoration), an h1 with an MDC bound to trip.title, hero-meta (trip.meta), and hero-quote with hero-quote-attr (trip.quote / trip.quoteAttr)
    - Contains the layout-only placeholders search-wrap, pace-wrap, and two light-wrap with NO @click / no event handlers bound
    - Contains the STATIC text Datos del viaje and Cómo usar esta guía as h4 elements (template literals, not data fields)
    - Contains div.info-grid with a v-for over trip.infoCards emitting info-card / info-card-label / info-card-value, and a v-for over trip.howTo emitting one p per item; all three prose surfaces use MDC
    - Contains NO style block (grep for the opening style tag finds nothing)
    - pnpm typecheck exits 0; pnpm lint of the file is clean
  </acceptance_criteria>
  <done>TheHero renders the full verbatim #inicio (masthead + layout placeholders + info-grid + how-to) from trip data via MDC, with the two static h4 labels; zero new CSS; typecheck + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: TripView — chrome owner + 12-anchor section scaffold (D-05, ARCH-01/02)</name>
  <files>app/components/TripView.vue</files>
  <read_first>
    - index.html (lines 2255-2281 for the body/header/main opening; 2283 #inicio; 2360 #mapa; the day section ids; 6228-6240 for main close + back-btn + flourish + footer — to mirror the sibling order and the 12 section ids)
    - app/composables/useTrip.ts (Plan 02 — the return shape TripView destructures; the in-file comment stating TripView calls useTrip and owns chrome+sections)
    - app/components/Topbar.vue and app/components/BackButton.vue (Plan 03 — the chrome TripView mounts; Topbar needs days)
    - app/components/TheHero.vue (Task 1 — TripView passes trip to it for #inicio)
    - shared/schemas.ts (Trip and Day types)
    - app/assets/css/base.css (lines 93-94 section padding + section+section border-top; 987-998 footer; 978 flourish — confirm placeholders must stay real empty sections)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md (§TripView.vue owns the section scaffold; §layouts/default.vue Core pattern for the main/flourish/footer markup; §useTrip "Who calls useTrip is Claude's Discretion — RESEARCH leans TripView calls it")
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-UI-SPEC.md (Section anchor scaffold table — the 12 anchors and F3 state; "placeholders must not suppress section rules")
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Anti-Patterns "Giving placeholders a fixed height that diverts scroll")
  </read_first>
  <action>
    Create app/components/TripView.vue. Script (script setup lang ts): define defineProps with a single slug prop of type string; then const result = await useTrip(props.slug) and destructure trip and days (and keep the id maps available for F4 even if unused now — a destructure with an eslint-allowed unused or omit them; prefer destructuring only trip and days to keep lint clean). Import Trip/Day types from ~~/shared/schemas as needed. Add an in-file comment: this component owns the chrome + the 12-anchor scaffold (D-05); F4/F7 fill the placeholder sections.

    Template — mirror the index.html body sibling order exactly:
    1. Topbar receiving days bound to the days ref value (pass days so its NavPills derives the 5 day pills). If days is a ref, bind days (Vue unwraps refs in template) — ensure the Day[] type lines up with Topbar's prop.
    2. main wrapping the 12 sections IN ORDER:
       - TheHero receiving trip bound to the trip ref value (this renders the FULL #inicio with id inicio).
       - section with id mapa (empty placeholder — F7 Leaflet island).
       - five empty sections with ids viernes, sabado, domingo, lunes, martes (F4 timeline+cards).
       - empty sections with ids reservas, gastronomia, practica, arte, arquitectura (F4 reference/food/artists).
       Each non-inicio section is a real empty section element carrying ONLY its id — no inner content, no inline style, no fixed height (a tall placeholder shifts every anchor offset and breaks the future scrollspy).
    3. BackButton (the resting-invisible shell from Plan 03).
    4. div.flourish with the literal text ·  ·  ·  ✦  ·  ·  · (verbatim index.html:6234).
    5. footer > div.container > p with the three verbatim lines separated by br: Itinerario preparado para em Pay /em y dos colegas, then Roma · 19—23 giugno 2026, then the quoted "Roma no se cuenta, se camina." (verbatim index.html:6236-6238).

    Add NO style block. Do NOT add a NuxtLink to any /trips/* route (would pull it into the prerender via crawlLinks — D-01). The #inicio anchor is owned by TheHero (id inicio); do not also wrap TheHero in another id inicio section (avoid a duplicate id).
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/TripView.vue && grep -q useTrip app/components/TripView.vue && grep -q arquitectura app/components/TripView.vue && grep -cq style app/components/TripView.vue; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - TripView.vue declares defineProps with a single slug prop of type string and calls await useTrip(props.slug)
    - Template renders, in sibling order: Topbar (with days bound), a main element, BackButton, div.flourish, footer
    - The main element contains TheHero (with trip bound) plus 11 empty section elements whose ids are exactly mapa, viernes, sabado, domingo, lunes, martes, reservas, gastronomia, practica, arte, arquitectura (12 anchors total counting #inicio inside TheHero)
    - No section other than #inicio has inner content or an inline style/height
    - footer contains the three verbatim lines (Itinerario preparado para Pay y dos colegas / Roma · 19—23 giugno 2026 / Roma no se cuenta, se camina.) and div.flourish contains the dotted flourish glyphs
    - Contains NO style block and NO NuxtLink to a /trips/ route
    - pnpm typecheck exits 0; pnpm lint of the file is clean
  </acceptance_criteria>
  <done>TripView owns the document tree (chrome + 12 ordered slug-id anchors), fills #inicio via TheHero, leaves 11 empty placeholders, and reuses verbatim chrome/footer; zero new CSS; no stray /trips link; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content (trip.yml) → MDC render | TheHero renders trip-authored Markdown-inline prose (title/infoCards/howTo) via MDC. Content is repo-versioned and zod-validated (Fase 2), not user input. |
| useTrip data → TripView template | TripView consumes the aggregated trip/days from useTrip (build-time, trusted). No runtime/external input enters this component. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering/Elevation (XSS via MDC) | TheHero MDC bindings (title/infoCards/howTo) | accept | MDC renders only repo-versioned, zod-validated prose (RESEARCH §Security Domain; PITFALLS §7). No user-supplied content reaches MDC in 1.0. Re-evaluate only if v2 introduces third-party content. |
| T-03-06 | Information Disclosure | TripView placeholder sections | accept | The 11 placeholders carry only an id and no data; no sensitive content is exposed. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan (RESEARCH §Package Legitimacy Audit). MDC ships with the already-installed @nuxt/content. No [ASSUMED]/[SUS] package. |

**Conclusion:** No high-severity threats; no blocking mitigations required. Static SSG rendering trusted repo content via MDC; no user input, no server, no secrets.
</threat_model>

<verification>
- pnpm typecheck — strict TS over TripView + TheHero.
- pnpm lint app/components/TripView.vue app/components/TheHero.vue — clean.
- grep assertions: TripView contains useTrip and all section ids through arquitectura; TheHero contains id inicio + info-grid; neither contains a style block.
- Rendered visual parity of #inicio + the 12-anchor scroll behavior + the MDC unwrap cuadre against the golden are asserted in Plan 05 (Playwright on the built /).
</verification>

<success_criteria>
- TripView owns the page: chrome (Topbar with days, BackButton, flourish, footer) + all 12 slug-id section anchors in order (D-05), reusable for / and /trips/[slug] (ARCH-02), driven by useTrip (ARCH-01).
- #inicio is fully rendered from trip.yml via MDC (D-06) with the two static h4 labels and the layout-only placeholders preserved for golden rhythm.
- The 11 non-inicio sections are real empty placeholders carrying only their id (no height drift), ready for F4/F7.
- Zero new CSS; no stray /trips/* link that would break the D-01 prerender discipline.
</success_criteria>

<output>
Create `.planning/phases/03-capa-de-p-gina-layout-y-tema/03-04-SUMMARY.md` when done.
</output>
