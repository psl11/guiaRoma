---
phase: 03-capa-de-p-gina-layout-y-tema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/utils/dayLabel.ts
  - tests/unit/dayLabel.spec.ts
  - vitest.config.ts
  - package.json
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "dayLabel('venerdì · 19 giugno') returns exactly 'Venerdì' (grave accent preserved)"
    - "All 5 day eyebrows map 1:1 to Venerdì/Sabato/Domenica/Lunedì/Martedì"
    - "pnpm test:unit runs the new tests/unit suite green and stays disjoint from tests:data"
  artifacts:
    - path: "app/utils/dayLabel.ts"
      provides: "Pure eyebrow→label transform (D-04), auto-imported as dayLabel"
      exports: ["dayLabel"]
    - path: "tests/unit/dayLabel.spec.ts"
      provides: "Unit coverage of all 5 day labels + accent safety (SC#2/D-04)"
    - path: "vitest.config.ts"
      provides: "tests/unit/** added to the include set without disturbing tests/data/**"
  key_links:
    - from: "tests/unit/dayLabel.spec.ts"
      to: "app/utils/dayLabel.ts"
      via: "direct import of dayLabel"
      pattern: "from .*dayLabel"
---

<objective>
Close the Wave 0 test-tooling gap and ship the pure `dayLabel` helper that the NavPills day pills (D-04) will consume — entirely with plain Vitest, no new dependency.

Purpose: D-04 mandates the Italian day-pill label is DERIVED from `day.eyebrow` (`venerdì · 19 giugno` → `Venerdì`), not stored. The transform must be a pure, locale-safe helper unit-tested against all 5 days so a one-character drift (which would fail SC#2 parity) is caught immediately. RESEARCH §Validation Architecture ▸ Wave 0 requires extending `vitest.config.ts` to add `tests/unit/**` (keeping `tests/data/**` disjoint) and a `test:unit` script. This plan is the foundation Plan 02 (useTrip unit test) and Plan 03 (NavPills) build on.

Output: `app/utils/dayLabel.ts`, `tests/unit/dayLabel.spec.ts`, extended `vitest.config.ts`, new `test:unit` package script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md

<interfaces>
<!-- Day eyebrow values (verified from content/trips/roma/days/*.yml). The helper transforms these. -->
viernes.yml  → eyebrow: "venerdì · 19 giugno"   → label "Venerdì"
sabado.yml   → eyebrow: "sabato · 20 giugno"    → label "Sabato"
domingo.yml  → eyebrow: "domenica · 21 giugno"  → label "Domenica"
lunes.yml    → eyebrow: "lunedì · 22 giugno"    → label "Lunedì"
martes.yml   → eyebrow: "martedì · 23 giugno"   → label "Martedì"

<!-- Existing vitest.config.ts (Fase 2) — to EXTEND, not rewrite: -->
test: { include: ['tests/data/**/*.spec.ts'] }
<!-- The data runner (tests/data) MUST stay green and logically separate from the new unit runner. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure dayLabel helper + unit test (D-04)</name>
  <files>app/utils/dayLabel.ts, tests/unit/dayLabel.spec.ts</files>
  <read_first>
    - app/assets/css/base.css (lines 77-90 — .nav-pill typography that renders the label; confirms it is plain text)
    - content/trips/roma/days/viernes.yml, sabado.yml, domingo.yml, lunes.yml, martes.yml (the `eyebrow` field — the helper's input; all 5 verified)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Code Examples "Pure day-label helper"; §Pitfall 5 "eyebrow → label not locale-safe")
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md (§`app/utils/dayLabel.ts`)
    - shared/schemas.ts (DaySchema.eyebrow, line 129 — the field type is `z.string()`)
    - tests/data/schema.spec.ts (existing Vitest spec — mirror its import/test style and TS conventions for the new unit spec)
  </read_first>
  <behavior>
    - dayLabel('venerdì · 19 giugno') === 'Venerdì'
    - dayLabel('sabato · 20 giugno') === 'Sabato'
    - dayLabel('domenica · 21 giugno') === 'Domenica'
    - dayLabel('lunedì · 22 giugno') === 'Lunedì'
    - dayLabel('martedì · 23 giugno') === 'Martedì'
    - Grave accent is preserved (output contains 'ì' for Venerdì/Lunedì/Martedì; NOT 'i' and NOT all-caps)
    - Only the first character is uppercased; the rest of the first word is untouched
  </behavior>
  <action>
    Create `app/utils/dayLabel.ts` exporting a pure named function `dayLabel(eyebrow: string): string`. Implementation per D-04: take the substring before the `·` separator (`eyebrow.split('·')[0]`), `.trim()` it, then uppercase ONLY the first character locale-safely with `.charAt(0).toLocaleUpperCase('it')` concatenated to `.slice(1)`. This preserves the grave accent `ì` (a naive `.toUpperCase()` on the whole string would yield `VENERDÌ`; dropping the accent would yield `Venerdi` — both are SC#2 parity failures). Do NOT add a `navLabel` field to `shared/schemas.ts` and do NOT touch the 5 day YAML files (D-04: derive, never store). The function lives in `app/utils/` so Nuxt auto-imports it as `dayLabel` for Plan 03's NavPills.

    Create `tests/unit/dayLabel.spec.ts` that imports `dayLabel` directly (plain Vitest, no Nuxt runtime, no `@nuxt/test-utils`) and asserts all 7 behaviors above with `describe`/`it`/`expect`. Mirror the TS style and `import { describe, it, expect } from 'vitest'` convention used in `tests/data/schema.spec.ts`.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm vitest run tests/unit/dayLabel.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/utils/dayLabel.ts` exports a function named `dayLabel` with signature `(eyebrow: string): string`
    - `app/utils/dayLabel.ts` contains `split('·')` and `toLocaleUpperCase('it')` and does NOT contain a bare `.toUpperCase()` applied to the whole string
    - `tests/unit/dayLabel.spec.ts` imports `dayLabel` and asserts each of the 5 mappings (Venerdì, Sabato, Domenica, Lunedì, Martedì) as separate expectations
    - `pnpm vitest run tests/unit/dayLabel.spec.ts` exits 0 with all assertions passing
    - No change to `shared/schemas.ts` and no change to any file under `content/trips/roma/days/` (verify with `git status` showing only the new helper + new spec)
  </acceptance_criteria>
  <done>dayLabel exists as a pure helper, all 5 labels (accent-preserved) are asserted green by plain Vitest.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Vitest to a disjoint tests/unit runner + test:unit script</name>
  <files>vitest.config.ts, package.json</files>
  <read_first>
    - vitest.config.ts (current `include: ['tests/data/**/*.spec.ts']` — the data gate; comment explains data runner must stay isolated)
    - package.json (the `scripts` block — `test:data` is `vitest run tests/data`; mirror that style for the new script)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Validation Architecture ▸ Wave 0 Gaps: "Extend vitest.config.ts include to add tests/unit/**", "keep tests/data/**", "Consider Vitest projects/multiple includes so the data gate and unit tests stay logically separate")
  </read_first>
  <action>
    Modify `vitest.config.ts` so the `include` array covers BOTH `tests/data/**/*.spec.ts` AND `tests/unit/**/*.spec.ts`. The data gate (`tests/data`) must keep running exactly as before — only ADD the unit glob; do not remove or rename the data glob. Preserve the existing explanatory comment block. Add a `test:unit` script to `package.json` defined as `vitest run tests/unit` (parallel in style to the existing `test:data` = `vitest run tests/data`), so the unit suite has its own dedicated command. Do not modify any other script. Do not introduce watch-mode flags (no bare `vitest` without `run`).
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm test:unit && pnpm test:data</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` `include` array contains both `'tests/data/**/*.spec.ts'` and `'tests/unit/**/*.spec.ts'`
    - `package.json` scripts contains `"test:unit": "vitest run tests/unit"`
    - `pnpm test:unit` exits 0 and runs at least the `tests/unit/dayLabel.spec.ts` suite
    - `pnpm test:data` still exits 0 (Fase 2 data gate unaffected)
    - No `vitest` watch invocation (the new script uses `run`)
  </acceptance_criteria>
  <done>A dedicated, disjoint `tests/unit` runner exists via `pnpm test:unit`; the Fase 2 data gate is untouched and still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This plan creates a pure string helper + test config. No I/O, no network, no user input, no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `dayLabel` input | accept | Input is repo-authored `eyebrow` strings from validated YAML (Fase 2 zod gate); no external input reaches the helper. Pure function, no sink. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package install occurs in this plan (RESEARCH §Package Legitimacy Audit: F3 installs nothing). `test:unit` reuses the already-vetted `vitest@4.1.9`. No `[ASSUMED]`/`[SUS]` package introduced. |

**Conclusion:** No high-severity threats; no blocking mitigations required. This is build-time test tooling + a pure helper on trusted repo data.
</threat_model>

<verification>
- `pnpm vitest run tests/unit/dayLabel.spec.ts` — all 5 labels + accent safety green.
- `pnpm test:unit && pnpm test:data` — both runners green, disjoint.
- `pnpm lint && pnpm typecheck` — the new `.ts` files lint/typecheck clean (strict TS).
</verification>

<success_criteria>
- `dayLabel` is a pure, auto-importable helper deriving all 5 Italian day labels 1:1 with the grave accent preserved (D-04, supports SC#2).
- A dedicated `tests/unit` Vitest runner exists (`pnpm test:unit`) without disturbing the Fase 2 `tests/data` gate.
- No new dependency added; `shared/schemas.ts` and the 5 day YAML files are untouched.
</success_criteria>

<output>
Create `.planning/phases/03-capa-de-p-gina-layout-y-tema/03-01-SUMMARY.md` when done.
</output>
