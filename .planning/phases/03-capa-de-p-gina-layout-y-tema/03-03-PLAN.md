---
phase: 03-capa-de-p-gina-layout-y-tema
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/components/Topbar.vue
  - app/components/ThemeToggle.vue
  - app/components/NavPills.vue
  - app/components/BackButton.vue
autonomous: true
requirements: [UI-01, ARCH-01, FEAT-01]
must_haves:
  truths:
    - "Topbar reproduces index.html:2257-2263 markup verbatim (header.topbar > topbar-inner > brand + ThemeToggle, then NavPills)"
    - "ThemeToggle is 2-state, inverts the resolved colorMode value, writes a concrete preference (never 'system'), icon CSS-only (no v-if)"
    - "NavPills renders 7 structural pills literally + 5 day pills derived from props.days ordered by order, labelled via dayLabel"
    - "BackButton is a mounted-but-hidden visual shell (no .show, no @click) — invisible at rest"
    - "No shell component contains a style block (verbatim global CSS only)"
  artifacts:
    - path: "app/components/Topbar.vue"
      provides: "Sticky header chrome (UI-01)"
      contains: "topbar"
    - path: "app/components/ThemeToggle.vue"
      provides: "2-state theme toggle, CSS-only icon (D-08/D-10/SC#4/FEAT-01)"
      contains: "theme-btn"
    - path: "app/components/NavPills.vue"
      provides: "Hybrid nav: structural + data-derived day pills (D-03/D-04/ARCH-01)"
      contains: "nav-pills"
    - path: "app/components/BackButton.vue"
      provides: "Resting-invisible back button shell (D-07)"
      contains: "back-btn"
  key_links:
    - from: "app/components/Topbar.vue"
      to: "ThemeToggle + NavPills"
      via: "auto-imported child components in template"
      pattern: "ThemeToggle|NavPills"
    - from: "app/components/NavPills.vue"
      to: "app/utils/dayLabel.ts"
      via: "dayLabel(d.eyebrow) in v-for"
      pattern: "dayLabel"
    - from: "app/components/ThemeToggle.vue"
      to: "useColorMode"
      via: "colorMode.preference toggles resolved value"
      pattern: "useColorMode"
---

<objective>
Componentize the fixed layout chrome — Topbar, ThemeToggle, NavPills, BackButton — as verbatim reproductions of the live index.html markup/classes, writing ZERO new CSS. This is the parity-by-construction core of UI-01 (Topbar/NavPills identical), the hybrid data-derived nav (D-03/D-04, ARCH-01), the 2-state CSS-only theme toggle (D-08/D-10/SC#4, FEAT-01), and the resting-invisible BackButton shell (D-07).

Purpose: The editorial CSS (app/assets/css/base.css, loaded globally in Fase 1) already defines every shell class with cross-component descendant selectors (.topbar-inner .theme-btn, [data-theme] .theme-btn .moon/.sun). Components must reproduce the exact markup with those classes and add NO scoped style — scoping injects data-v-* attributes that silently break those selectors (RESEARCH Pitfall 2). Topbar contains ThemeToggle + NavPills; NavPills consumes dayLabel (Plan 01) and a days prop; ThemeToggle consumes the already-configured @nuxtjs/color-mode. These components are wired into the page tree by Plan 04 (TripView) / Plan 05; here they are built against their contracts and verified by exact-markup/source assertions + typecheck/lint. Their rendered parity + theme behavior are proven in Plan 05.

Output: app/components/Topbar.vue, ThemeToggle.vue, NavPills.vue, BackButton.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md
@.planning/phases/03-capa-de-p-gina-layout-y-tema/03-UI-SPEC.md

<interfaces>
Shell markup to reproduce VERBATIM (index.html:2257-2278) — classes are LOCKED by base.css.

Topbar (2257-2263): header.topbar wrapping div.topbar-inner (which holds div.brand "Roma [span.brand-dot ✦] giugno MMXXVI" then the theme button) and, as a sibling of topbar-inner still inside header, nav.nav-pills with id nav-pills.

ThemeToggle button (2260-2262): button.theme-btn aria-label Cambiar tema, onclick toggleTheme, containing two ADJACENT no-whitespace spans: span.moon ☾ then span.sun ☀. onclick becomes click handler.

NavPills (2264-2277): 12 a.nav-pill anchors in this exact order — Inicio(#inicio), Mapa(#mapa), then 5 day pills Venerdì(#viernes) Sabato(#sabado) Domenica(#domingo) Lunedì(#lunes) Martedì(#martes), then Reservas(#reservas) Gastronomía(#gastronomia) Pratica(#practica) Arte(#arte) Arquitectura(#arquitectura). In F3 the 5 day pills are DERIVED from days; the rest are literal.

BackButton (6230-6232): button.back-btn with id back-btn, aria-label Volver, containing span.back-btn-arrow ← then text Volver. onclick goBack is DROPPED in F3 (no click handler).

CSS that MANDATES the structure (base.css — DO NOT add scoped style):
- .topbar-inner .theme-btn { grid-column: 3; justify-self: end; } (descendant selector → theme-btn must be inside topbar-inner)
- [data-theme=light] .theme-btn .moon display block / .sun display none + the dark inverse (base.css:58-61)
- .nav-pill.active exists at base.css:90 but is wired in F5 — NOT in F3
- .back-btn opacity 0, pointer-events none, transform translateY(120%) (resting = invisible)
- .back-btn.show opacity 1 (.show added by F5 only)

color-mode API (verified, RESEARCH §Pattern 3):
const colorMode = useColorMode()  returns preference (writable), value (readonly RESOLVED), unknown, forced
Set colorMode.preference to the inverse of the resolved colorMode.value ('dark' becomes 'light', else 'dark') — D-08: invert resolved, NEVER 'system'.

Day type + helper (from Plans 01/02 / shared):
import type Day from ~~/shared/schemas  (Day.slug, Day.eyebrow, Day.order)
dayLabel(eyebrow)  auto-imported from app/utils/dayLabel.ts (Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Topbar + ThemeToggle (UI-01, D-08, D-10, SC#4)</name>
  <files>app/components/ThemeToggle.vue, app/components/Topbar.vue</files>
  <read_first>
    - index.html (lines 2257-2263 — verbatim Topbar + theme-btn markup, including the two adjacent spans with NO whitespace between them)
    - app/assets/css/base.css (lines 24-61 — .topbar sticky/blur/border, .topbar-inner 3-col grid, the ".topbar-inner .theme-btn" descendant rule, .theme-btn circle, and the [data-theme] .theme-btn .moon/.sun icon rules — the LOCKED contract that forbids scoping)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md (§Topbar.vue VERBATIM; §ThemeToggle.vue VERBATIM markup + CSS-only icon)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-UI-SPEC.md (Component Inventory Topbar; ThemeToggle; Theme/FOUC Contract row "Icon: CSS-only, no v-if")
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Pattern 3; §Anti-Patterns "v-if for the icon"; §Don't Hand-Roll "Reactive theme state → useColorMode")
    - nuxt.config.ts (lines 26-32 — colorMode already configured; ThemeToggle CONSUMES this, must not reconfigure)
    - app/app.vue (the only existing SFC — mirror its script setup lang ts convention)
  </read_first>
  <action>
    Create app/components/ThemeToggle.vue first (Topbar references it). Template: a button.theme-btn with aria-label Cambiar tema and a click handler bound to a toggle function, containing exactly the two ADJACENT no-whitespace spans span.moon (glyph ☾) immediately followed by span.sun (glyph ☀) — verbatim from index.html:2261. aria-label is exactly the string Cambiar tema. Script (script setup lang ts): const colorMode = useColorMode() (auto-imported) and a toggle function that sets colorMode.preference to the inverse of the resolved colorMode.value (when value is 'dark' set 'light', otherwise set 'dark'). This reads the RESOLVED colorMode.value, writes a CONCRETE preference, mirroring toggleTheme 1:1; it must NEVER write the string 'system' (D-08). Do NOT add any v-if/v-show keyed on the theme for the icon — both spans always render and base.css:58-61 shows exactly one per [data-theme] (D-10/SC#4). Do NOT read localStorage/window/matchMedia in the script. Add NO style block.

    Create app/components/Topbar.vue reproducing index.html:2257-2263 verbatim: outer header.topbar; inside it div.topbar-inner containing div.brand with text "Roma ", a span.brand-dot (glyph ✦), then " giugno MMXXVI", followed by the ThemeToggle component; and — as a sibling of .topbar-inner, still inside header — a NavPills component receiving days bound to the days prop (NavPills is created in Task 2; auto-imported, so referencing it here is fine). Topbar accepts a days prop typed as an array of Day via defineProps, with Day imported from ~~/shared/schemas, and passes it to NavPills. The brand-dot ✦ is a literal glyph. Keep header.topbar as the OUTER element so the sticky/border/backdrop-filter apply. Add NO style block.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/Topbar.vue app/components/ThemeToggle.vue && grep -L 'style>' app/components/Topbar.vue app/components/ThemeToggle.vue</automated>
  </verify>
  <acceptance_criteria>
    - ThemeToggle.vue contains class theme-btn, aria-label Cambiar tema, a click handler bound to toggle, and the adjacent spans span.moon ☾ immediately followed by span.sun ☀ with NO whitespace/newline between the two spans
    - ThemeToggle script contains useColorMode() and inverts the resolved colorMode.value to set colorMode.preference; it contains NO v-if/v-show and NO literal string 'system'
    - Topbar.vue contains class topbar, class topbar-inner, class brand, class brand-dot, a ThemeToggle reference inside .topbar-inner, and a NavPills reference with days bound, placed as a sibling of .topbar-inner inside header
    - Topbar declares defineProps with an array-of-Day days prop importing Day from ~~/shared/schemas
    - Neither file contains a style block (grep -L 'style>' lists both files as having no match)
    - pnpm typecheck exits 0; pnpm lint of both files is clean
  </acceptance_criteria>
  <done>Topbar + ThemeToggle reproduce the verbatim shell; the toggle is 2-state/resolved-invert/never-'system'; the icon is CSS-only; zero new CSS; typecheck + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: NavPills (hybrid, D-03/D-04) + BackButton shell (D-07)</name>
  <files>app/components/NavPills.vue, app/components/BackButton.vue</files>
  <read_first>
    - index.html (lines 2264-2277 — the 12 nav-pill anchors in LOCKED order; and 6230-6232 — the back-btn markup with its dropped onclick)
    - app/assets/css/base.css (lines 63-90 — .nav-pills flex/scroll/border, the 680px centering media query, .nav-pill typography, .nav-pill:hover, .nav-pill.active; and 1001-1034 — .back-btn fixed/resting-invisible + .back-btn.show)
    - app/utils/dayLabel.ts (Plan 01 — the auto-imported transform NavPills calls; returns Venerdì/Sabato/Domenica/Lunedì/Martedì)
    - shared/schemas.ts (DaySchema — slug, eyebrow, order fields; the Day type NavPills imports)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-PATTERNS.md (§NavPills.vue VERBATIM shell + derived day pills; §BackButton.vue VERBATIM resting-invisible)
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-UI-SPEC.md (Component Inventory NavPills order table + BackButton "spec resting (invisible) appearance only")
    - .planning/phases/03-capa-de-p-gina-layout-y-tema/03-RESEARCH.md (§Pattern 2 "Order is LOCKED, No .active, no scrollspy in F3"; §Anti-Patterns about scoped CSS)
  </read_first>
  <action>
    Create app/components/NavPills.vue as the hybrid pill bar (D-03). Script (script setup lang ts): defineProps with an array-of-Day days prop importing Day from ~~/shared/schemas. Template: a single nav.nav-pills with id nav-pills (keep the id — F5 scrollspy targets it) containing, IN THIS EXACT ORDER: literal a.nav-pill linking to #inicio with text Inicio; literal a.nav-pill linking to #mapa with text Mapa; then a v-for over days (already ordered ASC by useTrip) emitting a.nav-pill whose href is the string built from a leading hash plus d.slug, whose key is d.slug, and whose text is dayLabel of d.eyebrow; then literal pills a.nav-pill linking to #reservas Reservas, #gastronomia Gastronomía, #practica Pratica (Italian spelling verbatim), #arte Arte, #arquitectura Arquitectura. The day href uses the Spanish slug anchors (#viernes through #martes). Do NOT add active class logic and do NOT add scrollspy — F3 pills are plain anchors (that is FEAT-05/F5). dayLabel is auto-imported (Plan 01). Add NO style block.

    Create app/components/BackButton.vue as a VISUAL SHELL ONLY (D-07). Template: button.back-btn with id back-btn and aria-label Volver, containing span.back-btn-arrow (glyph ←) followed by the text Volver (verbatim index.html:6230-6232). DROP the onclick goBack — there is NO click handler and NO behavior in F3 (the back-stack + show toggle are wired in F5). Do NOT add the show class — at rest the CSS gives opacity 0, pointer-events none, translateY(120%), so it is invisible (golden unaffected). Keep id back-btn. Add NO style block.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/NavPills.vue app/components/BackButton.vue && grep -q dayLabel app/components/NavPills.vue && grep -L 'style>' app/components/NavPills.vue app/components/BackButton.vue</automated>
  </verify>
  <acceptance_criteria>
    - NavPills.vue contains nav.nav-pills with id nav-pills and the exact 7 structural anchors (Inicio #inicio, Mapa #mapa, Reservas #reservas, Gastronomía #gastronomia, Pratica #practica, Arte #arte, Arquitectura #arquitectura)
    - NavPills contains a v-for over the days prop that calls dayLabel and binds href to a hash + d.slug; the day v-for sits between the Mapa and Reservas anchors
    - NavPills contains NO occurrence of the substring active and NO scroll listener
    - BackButton.vue contains class back-btn, id back-btn, aria-label Volver, span.back-btn-arrow ←, and the text Volver; it contains NO click handler and NO show class
    - Neither file contains a style block (grep -L 'style>' lists both files); NavPills grep for dayLabel matches
    - pnpm typecheck exits 0; pnpm lint of both files is clean
  </acceptance_criteria>
  <done>NavPills renders the LOCKED hybrid order (7 structural + 5 derived day pills via dayLabel) and BackButton is a resting-invisible shell with no behavior; zero new CSS; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ localStorage (theme) | ThemeToggle writes colorMode.preference; the color-mode module persists it to localStorage['roma-theme']. Value space is constrained to 'light'/'dark'. |
| repo data → NavPills props | NavPills renders day labels derived from repo-authored, zod-validated `eyebrow` strings. No external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | localStorage['roma-theme'] | accept | ThemeToggle only ever sets a concrete `'light'`/`'dark'` preference (D-08); the value never reaches a DOM/HTML sink beyond the `[data-theme]` attribute the module controls. A tampered storage value is normalized by the module to a known mode; no injection path. |
| T-03-04 | Tampering/Elevation | NavPills href construction | accept | Day hrefs are in-page fragments built from `'#' + d.slug`; `slug` is a validated repo identifier (Fase 2 zod). No user-controlled URL, no `javascript:` sink. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan (RESEARCH §Package Legitimacy Audit: F3 installs nothing). `@nuxtjs/color-mode` is already installed/configured. No `[ASSUMED]`/`[SUS]` package. |

**Conclusion:** No high-severity threats; no blocking mitigations required. Presentational components over trusted repo data; theme state is a constrained 2-value preference with no injection sink.
</threat_model>

<verification>
- pnpm typecheck — strict TS over all 4 components.
- pnpm lint app/components/Topbar.vue app/components/ThemeToggle.vue app/components/NavPills.vue app/components/BackButton.vue — clean.
- grep -L 'style>' across the 4 components — none contains a style block (parity-by-construction: zero new CSS).
- Rendered parity (shell matches golden), the live theme toggle/icon behavior, and the day-pill labels-in-DOM are asserted end-to-end in Plan 05 (Playwright on the built `/`).
</verification>

<success_criteria>
- Topbar/NavPills/BackButton/ThemeToggle reproduce the verbatim index.html shell markup with existing classes and zero new CSS (UI-01).
- NavPills is the hybrid bar: 7 structural pills + 5 day pills derived from `days` (D-03) and labelled by `dayLabel` (D-04), in the LOCKED order, no `.active`/scrollspy (F5 boundary respected).
- ThemeToggle is 2-state, inverts the resolved theme, writes a concrete preference (never `'system'`), and the icon is CSS-only with no `v-if` (D-08/D-10/SC#4).
- BackButton is a mounted-but-hidden shell with no behavior (D-07), invisible at rest so the golden is unaffected.
</success_criteria>

<output>
Create `.planning/phases/03-capa-de-p-gina-layout-y-tema/03-03-SUMMARY.md` when done.
</output>
