---
phase: 08-verificaci-n-de-paridad
plan: 05
type: execute
wave: 4
depends_on: ["08-03", "08-04"]
files_modified:
  - tests/README.md
autonomous: true
requirements: [PARITY-02]

must_haves:
  truths:
    - "D-04: both gate exclusions (golden.spec.ts at file level; the dev-routing test via env-flag + title grep-invert) are documented with reasons in tests/README.md"
    - "D-04 (Pitfall 4): tests/README.md records the EXPECTED gate test count plus how to recompute it"
    - "D-01: tests/README.md records the frozen-baseline invariant — test:golden:update FORBIDDEN, the 56 PNGs read-only, golden.spec.ts is the on-demand F1 capture tool"
  artifacts:
    - path: "tests/README.md"
      provides: "Gate command + 3 layers; both exclusions with reasons (D-04); D-01 frozen-baseline invariant; recorded expected gate test count + recompute instructions"
      min_lines: 25
  key_links:
    - from: "tests/README.md"
      to: "the recorded gate test count"
      via: "run the gate parity suite once, read the Playwright summary, record the figure (Pitfall 4)"
      pattern: "golden.spec.ts"
---

<objective>
Document the SC#4 gate so it is self-explaining and tamper-evident (D-04 docs + Pitfall 4). Plan 03 added the gate config + scripts; Plan 04 added the dev-routing env-flag. This plan writes tests/README.md: the gate command and its three layers, the TWO exclusions with reasons, the D-01 frozen-baseline invariant, and the EXPECTED gate test count with recompute instructions. A future accidental over-exclusion then shows up as a recorded-count mismatch. First real gate run + diff classification (D-02) is Plan 06. No new dependencies.
Output: new tests/README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-verificaci-n-de-paridad/08-CONTEXT.md
@.planning/phases/08-verificaci-n-de-paridad/08-RESEARCH.md

<interfaces>
- package.json (after Plan 03): verify = pnpm generate, then test:unit, test:data, test:parity; test:parity runs the gate config with a grep-invert on the dev-routing title.
- Gate config (Plan 03): testIgnore golden.spec.ts at file level; snapshotPathTemplate pinned to tests/parity/golden.spec.ts-snapshots/.
- shell.spec.ts (after Plan 04): dev-routing test + its pnpm dev spawn gated behind RUN_DEV_ROUTING.
- Provenance for reason text: .planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/deferred-items.md (5 pre-existing full-suite failures: 4 golden pixel flakes + 1 dev-routing).
- Frozen baseline (on disk): tests/parity/golden.spec.ts-snapshots/ = 56 read-only PNGs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/README.md — gate command, exclusions, frozen-baseline invariant, recorded test count</name>
  <read_first>
    - .planning/phases/08-verificaci-n-de-paridad/08-RESEARCH.md (Pitfall 4 record-the-count; the two exclusions + reasons; the D-01 invariant)
    - package.json (the verify + test:parity scripts from Plan 03)
    - tests/parity/shell.spec.ts (confirm Plan 04 RUN_DEV_ROUTING gating, to describe exclusion #2)
    - .planning/phases/06-derivados-de-datos-b-squeda-y-ruta-del-d-a/deferred-items.md (provenance for reason text)
  </read_first>
  <action>
    Create tests/README.md with four parts. (1) The gate command pnpm verify and its three layers: test:unit (pure logic), test:data (data invariants), test:parity (gate-scoped Playwright with a front-of-gate clean build via pnpm generate). (2) The TWO gate exclusions with reasons and the D-04 reference: golden.spec.ts excluded at file level (re-renders the OLD index.html, redundant once tests/parity/visual-diff.spec.ts exists, flaky under parallel load; still runnable on-demand to regenerate the F1 baseline); the dev-routing shell.spec.ts test excluded via RUN_DEV_ROUTING plus the test:parity title grep-invert (spawns nuxi dev, fragile to a stale lock; ARCH-02 covered by the static build plus shell.spec's static assertions). (3) The D-01 frozen-baseline invariant: test:golden:update is FORBIDDEN in F8, the 56 PNGs in tests/parity/golden.spec.ts-snapshots/ are read-only, golden.spec.ts is the on-demand F1 capture tool, the visual-diff never passes the update flag. (4) The EXPECTED number of gate tests + HOW to recompute it (run the gate parity suite, read the Playwright summary line) — run it once during this task and record the concrete figure (Pitfall 4).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests/README.md exists, at least 25 lines, and covers the gate command + three layers, BOTH exclusions with reasons and the D-04 reference, the D-01 frozen-baseline invariant, and a CONCRETE recorded expected gate test count with recompute instructions.
    - tests/README.md mentions golden.spec.ts, RUN_DEV_ROUTING, D-04, and D-01.
    - pnpm typecheck stays clean (docs-only change introduces no type errors).
  </acceptance_criteria>
  <done>The gate is documented end to end (command, exclusions, frozen-baseline invariant, recorded count); a future over-exclusion is visible as a count mismatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> maintainer trust | tests/README.md is the human-readable contract that the gate's exclusions are intentional and the baseline is read-only; no code/runtime boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Repudiation | Undocumented gate exclusions | mitigate | tests/README.md records both exclusions with reasons (D-04) and the expected test count (Pitfall 4), so a silent coverage drop is detectable and attributable. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: F8 installs zero packages). |
</threat_model>

<verification>
- pnpm typecheck clean.
- tests/README.md contains golden.spec.ts, RUN_DEV_ROUTING, D-04, D-01, and a concrete recorded gate test count.
</verification>

<success_criteria>
- The gate is fully documented (command, three layers, both exclusions with reasons, frozen-baseline invariant, recorded count + recompute steps).
</success_criteria>

<output>
Create `.planning/phases/08-verificaci-n-de-paridad/08-05-SUMMARY.md` when done.
</output>
