---
phase: 08-verificaci-n-de-paridad
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/parity/visual-diff.spec.ts
autonomous: true
requirements: [PARITY-02]

must_haves:
  truths:
    - "D-01: a NEW self-contained spec builds+serves the Nuxt site under /guiaRoma/ and screenshots the 14 golden VIEWS to compare against the 56 FROZEN PNGs as a read-only baseline (golden.spec.ts stays byte-unchanged as the F1 capture tool)"
    - "D-01: the spec calls toHaveScreenshot with a name-theme filename and NEVER passes --update-snapshots; a beforeAll guard-assert proves the frozen baseline exists so a misconfiguration fails loudly instead of auto-baselining (Pitfall 2)"
    - "D-06: #mapa is NOT among the spec's VIEWS (no golden baseline exists for the OSM-tile map)"
  artifacts:
    - path: "tests/parity/visual-diff.spec.ts"
      provides: "Nuxt-vs-golden visual-diff over the 14 VIEWS x light/dark; reuses golden.spec.ts VIEWS+settle()+A5 verbatim; self-contained build+serve on base port 5780; beforeAll guard-asserts the frozen baseline"
      min_lines: 80
      contains: "toHaveScreenshot"
  key_links:
    - from: "tests/parity/visual-diff.spec.ts"
      to: "the served Nuxt build under /guiaRoma/"
      via: "page.goto(STATIC_URL) against pnpm dlx serve on port 5780 (NOT the base config webServer that serves index.html)"
      pattern: "guiaRoma"
    - from: "tests/parity/visual-diff.spec.ts"
      to: "tests/parity/golden.spec.ts-snapshots/inicio-light-desktop.png"
      via: "beforeAll existsSync guard-assert (the frozen F1 baseline must exist)"
      pattern: "golden.spec.ts-snapshots"
---

<objective>
Create the single net-new spec of F8 — the Nuxt-vs-golden visual-diff the phase is named for. It does NOT exist yet: golden.spec.ts only re-captures the OLD index.html (the F1 baseline/capture tool). This spec is a 5th clone of the established self-contained build+serve pattern (map-fallback-notes.spec.ts) that grafts in golden.spec.ts's VIEWS + settle() + A5 image-block VERBATIM, swaps the goto target to the served /guiaRoma/ Nuxt build, and screenshots each of the 14 views per theme to compare against the 56 frozen PNGs.

Purpose: this is the objective proof of SC#1 (pixel parity). The snapshot-directory mechanic and the gate command live in Plan 03; the first real run + diff classification (D-02) is Plan 04. This plan delivers a structurally-correct, lint-clean spec.
Output: tests/parity/visual-diff.spec.ts (NEW). No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verificaci-n-de-paridad/08-CONTEXT.md
@.planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md

<interfaces>
Verbatim-reuse anchors (PATTERNS.md sub-sections A-G are copy-ready):
- golden.spec.ts (DO NOT MODIFY; copy FROM it): VIEWS lines 17-32 (the EXACT 14 name+selector pairs the 56 PNGs were captured from; #mapa is NOT among them, D-06); settle() lines 44-69 (eager-load every img, networkidle, await each img.complete, document.fonts.ready, double rAF); A5 image-block lines 91-93 (page.route star-star, abort when resourceType is image else continue, registered BEFORE goto); dark via addInitScript localStorage roma-theme=dark; the golden's page.goto('/index.html') line 101 is the ONE thing the new spec changes.
- map-fallback-notes.spec.ts (clone the scaffold): imports, OUTPUT_DIR, EXPECTED_HYDRATION_MSG/ABORTED_REQUEST_MSG, waitForServer/killGroup/ensureBuild lines 55-91, beforeAll/afterAll lines 112-143 (build, cp .output/public to previewRoot/guiaRoma/, spawn detached pnpm dlx serve, waitForServer, teardown), trackConsoleErrors lines 100-110. Base ports taken: modes=5700, navigation=5720, search-route=5740, map-fallback-notes=5760 -> the new spec MUST use 5780.
- playwright.config.ts (read for context): toHaveScreenshot maxDiffPixelRatio 0.01 is inherited; the gate config (Plan 03) supplies the snapshotPathTemplate that points this spec at the frozen dir. This spec just calls toHaveScreenshot with a name-theme filename.
- Frozen baseline (on disk): tests/parity/golden.spec.ts-snapshots/ = exactly 56 PNGs named view-theme-project.png (e.g. inicio-light-desktop.png).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: visual-diff.spec.ts — Nuxt-vs-golden visual-diff over the 14 views (SC#1/D-01)</name>
  <read_first>
    - tests/parity/golden.spec.ts (copy VIEWS lines 17-32, settle() lines 44-69, A5 lines 91-93 VERBATIM; goto('/index.html') line 101 is the ONE change)
    - tests/parity/map-fallback-notes.spec.ts (clone the scaffold: imports, OUTPUT_DIR, ensureBuild/waitForServer/killGroup, beforeAll/afterAll, trackConsoleErrors; base port 5760 -> use 5780)
    - .planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md (section "tests/parity/visual-diff.spec.ts — NEW", sub-sections A through G plus the guard-assert)
    - playwright.config.ts (so you know maxDiffPixelRatio:0.01 is inherited and the gate config will pin the snapshot dir)
  </read_first>
  <behavior>
    - beforeAll: ensureBuild() (pnpm generate if .output/public/index.html missing); guard-assert existsSync of tests/parity/golden.spec.ts-snapshots/inicio-light-desktop.png toBe true (message names D-01); mkdtemp previewRoot; cp .output/public into previewRoot/guiaRoma/; spawn detached serve on 5780; waitForServer on the /guiaRoma/ URL.
    - For each theme in light then dark: register A5 image-abort BEFORE goto; if dark, addInitScript localStorage roma-theme=dark; goto the served /guiaRoma/ build (NOT /index.html); settle(page); for each name+sel of VIEWS, locator(sel).scrollIntoViewIfNeeded() then expect(locator).toHaveScreenshot with the name-theme.png filename.
    - Console gate tolerateAborts=true (A5 aborts images so net::ERR_FAILED is expected); each test ends with consoleErrors toHaveLength 0.
  </behavior>
  <action>
    Create tests/parity/visual-diff.spec.ts. Clone the self-contained scaffold from map-fallback-notes.spec.ts (imports, OUTPUT_DIR, EXPECTED_HYDRATION_MSG, ABORTED_REQUEST_MSG, waitForServer/killGroup/ensureBuild, beforeAll/afterAll, trackConsoleErrors), changing ONLY the mkdtempSync prefix to guiaroma-vdiff- and the base port to 5780 (const STATIC_PORT = 5780 + Number(process.env.TEST_WORKER_INDEX ?? 0)); STATIC_URL points at the served /guiaRoma/ origin. Graft in VIEWS, settle() (add type Page to the @playwright/test import or reuse PWPage), and the A5 image-block — all verbatim from golden.spec.ts. Add the Pitfall-2 guard-assert in beforeAll AFTER ensureBuild(): expect existsSync of join(process.cwd, tests/parity/golden.spec.ts-snapshots, inicio-light-desktop.png) toBe true with a message about the frozen F1 baseline (56 PNGs) and NO rebaselinar (D-01). Capture loop: for theme light then dark as const, a test that registers A5 plus (dark) addInitScript BEFORE page.goto(STATIC_URL), then settle(page), then the per-element VIEWS loop with toHaveScreenshot of the name-theme filename, ending with the console-errors assertion. Do NOT add #mapa to VIEWS (D-06). Do NOT modify golden.spec.ts. Do NOT call --update-snapshots (D-01). Do NOT pre-apply any mask/stylePath/maxDiffPixels override (D-02: classification is Plan 04). The snapshot directory resolution is supplied by the gate config in Plan 03 — this spec just calls toHaveScreenshot with the name-theme filename.
  </action>
  <verify>
    <automated>pnpm lint tests/parity/visual-diff.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/parity/visual-diff.spec.ts exists with at least 80 lines and contains: toHaveScreenshot, the base port 5780, the VIEWS and settle identifiers, the A5 resourceType image-abort, the guard-assert string golden.spec.ts-snapshots, and a /guiaRoma/ URL.
    - grep confirms the file does NOT contain the token #mapa (D-06) and does NOT contain update-snapshots (D-01).
    - golden.spec.ts is byte-unchanged: git diff --quiet tests/parity/golden.spec.ts exits 0.
    - pnpm lint of the file is clean.
    - Not expected to PASS the visual-diff yet (it needs Plan 03's snapshot template to resolve the frozen dir; the first real run + classification is Plan 04). This task's gate is structural correctness + lint, not a green visual-diff.
  </acceptance_criteria>
  <done>The net-new visual-diff spec exists, structurally correct, reusing the golden determinism harness verbatim against the served Nuxt build; golden.spec.ts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> served static build | Playwright drives a hydrated browser against a locally-served nuxt generate output; A5 aborts all images/tiles so there is no network egress; no untrusted input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Frozen golden baseline (56 PNGs) | mitigate | The spec NEVER passes --update-snapshots; mismatches write actual/diff PNGs to ephemeral test-results/ only. The beforeAll existsSync guard-assert fails loudly if the baseline is missing, preventing silent auto-baselining of Nuxt-against-itself (Pitfall 2). golden.spec.ts is left byte-unchanged. |
| T-08-02 | Tampering | Test temp dirs (mkdtempSync previewRoot) | accept | The cloned scaffold writes only to OS temp via mkdtempSync and tears down with rmSync in afterAll (ASVS V12 marginal); no write outside repo temp dirs. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: F8 installs zero packages). serve is fetched transiently by the cloned scaffold via pnpm dlx serve, already in use F1-F7. |
</threat_model>

<verification>
- pnpm lint tests/parity/visual-diff.spec.ts clean.
- git diff --quiet tests/parity/golden.spec.ts exits 0 (capture tool untouched).
- File contains 5780, VIEWS, settle, toHaveScreenshot, the guard-assert; contains neither #mapa nor update-snapshots.
</verification>

<success_criteria>
- The Nuxt-vs-golden visual-diff spec exists and is lint-clean, reusing the golden determinism harness (VIEWS/settle/A5) verbatim against the served Nuxt build under /guiaRoma/ on base port 5780.
- The frozen baseline is treated as read-only by construction (guard-assert + no update flag); golden.spec.ts is unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-verificaci-n-de-paridad/08-02-SUMMARY.md` when done.
</output>
