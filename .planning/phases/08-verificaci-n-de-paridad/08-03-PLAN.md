---
phase: 08-verificaci-n-de-paridad
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - playwright.gate.config.ts
  - package.json
autonomous: true
requirements: [PARITY-02]

must_haves:
  truths:
    - "D-01: the visual-diff spec resolves snapshots to tests/parity/golden.spec.ts-snapshots/ via a pinned snapshotPathTemplate in the gate config (decoupled from testFileName), reading the FROZEN baseline and never creating a new -snapshots dir"
    - "D-03: a single gate command (pnpm verify) chains a clean build + test:unit + test:data + the gate-scoped parity suite; green = the 1.0 precondition"
    - "D-04 (exclusion #1): golden.spec.ts is excluded from the gate at the FILE level via testIgnore (it re-renders the OLD index.html and is flaky under parallel load)"
  artifacts:
    - path: "playwright.gate.config.ts"
      provides: "Gate-scoped config: extends the base config, testIgnore golden.spec.ts, snapshotPathTemplate pinned to the frozen golden dir"
      contains: "golden.spec.ts-snapshots"
    - path: "package.json"
      provides: "verify (D-03, clean build at front) + test:parity (gate-scoped, dev-routing grep-inverted) scripts; all existing scripts retained"
      contains: "verify"
  key_links:
    - from: "package.json verify script"
      to: "the gate config"
      via: "test:parity runs playwright test against the gate config; verify chains generate + unit + data + parity"
      pattern: "test:parity"
    - from: "the gate config snapshotPathTemplate"
      to: "tests/parity/golden.spec.ts-snapshots/"
      via: "Plan 02 visual-diff toHaveScreenshot resolves through this template to the frozen PNG"
      pattern: "golden.spec.ts-snapshots"
---

<objective>
Stand up the gate-scoped Playwright config and the single gate command (D-01 mechanic + D-03 + D-04 exclusion #1).

One task, two coupled pieces: the gate config extends the base config, pins snapshotPathTemplate to the frozen golden dir so Plan 02's visual-diff spec reads the 56 PNGs (Pattern 3, the one net-new mechanic), and testIgnores golden.spec.ts at the file level; the package.json scripts add verify (clean build, then unit, data, gate-scoped parity) and test:parity (the gate config, with the dev-routing test grep-inverted as a belt to the env flag added in Plan 04).

Purpose: this is the skeleton of the SC#4 gate. The dev-routing env-flag, the exclusion docs, and the recorded test count are Plan 04; the first real gate run + diff classification (D-02) is Plan 05. No new dependencies.
Output: playwright.gate.config.ts (NEW) + modified package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verificaci-n-de-paridad/08-CONTEXT.md
@.planning/phases/08-verificaci-n-de-paridad/08-RESEARCH.md
@.planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md

<interfaces>
- The base playwright config (extend, do not edit): its snapshotPathTemplate at line 35 is the A8 default the gate OVERRIDES; the mobile + desktop projects supply the projectName token.
- package.json scripts (6-19): existing test:golden, test:golden:update (FORBIDDEN to invoke in F8), test:subpath, test:data, test:unit, generate. ADD test:parity + verify; remove none.
- Frozen-baseline template (Pattern 3, verified vs docs): tests/parity/golden.spec.ts-snapshots/ with the arg, projectName and ext tokens — arg is name-theme, projectName is mobile or desktop, yielding the existing frozen PNG. The array-path form is FORBIDDEN.
- Dev-routing test title (the grep-invert belt targets it; the env flag is added in Plan 04): "/trips/roma reutiliza el MISMO TripView y un slug desconocido 404 (ARCH-02)".
- Plan 02 created tests/parity/visual-diff.spec.ts (port 5780); the gate template lets its toHaveScreenshot resolve to the frozen dir.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gate config + package.json gate scripts</name>
  <read_first>
    - playwright.config.ts (the base you extend via import base from this file; line 35 A8 template is OVERRIDDEN; mobile/desktop projects supply projectName)
    - package.json (scripts 6-19; ADD, remove nothing)
    - .planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md (sections "playwright.gate.config.ts — NEW", "package.json — MODIFY", "Shared Pattern: Frozen-baseline snapshotPathTemplate")
    - .planning/phases/08-verificaci-n-de-paridad/08-RESEARCH.md (Code Examples "The single gate command" + Open-Q#1 clean build at front)
  </read_first>
  <action>
    Create the gate config file playwright.gate.config.ts: import defineConfig from @playwright/test and the base config from the project root config; export defineConfig spreading base and adding testIgnore set to the glob matching golden.spec.ts and snapshotPathTemplate set to tests/parity/golden.spec.ts-snapshots/ with the arg, projectName and ext tokens (the frozen-baseline pin from Pattern 3). Add a top-of-file comment documenting the D-04 exclusion #1 (golden.spec.ts excluded at file level: it re-renders the OLD index.html, redundant once Plan 02's spec exists, flaky under parallel load) and the D-01 frozen-baseline invariant (the template pins the visual-diff baseline to the FROZEN golden dir; arg is name-theme, projectName is mobile or desktop; never --update-snapshots). In package.json scripts (keep ALL existing entries), ADD test:parity running playwright test against the gate config file plus a grep-invert on the stable title substring "reutiliza el MISMO TripView" (the belt to the env flag added in Plan 04; both target the SAME dev-routing test; never line numbers), and verify running pnpm generate then pnpm test:unit then pnpm test:data then pnpm test:parity (D-03 with a clean build at the front per Open-Q#1, so the diff reflects current code). Do NOT remove test:golden / test:golden:update / test:subpath. Do NOT edit the base config file.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - playwright.gate.config.ts exists, extends the base config, sets testIgnore to the golden.spec.ts glob, and sets snapshotPathTemplate to tests/parity/golden.spec.ts-snapshots/ with the arg/projectName/ext tokens.
    - package.json has a verify script that starts with pnpm generate and chains test:unit + test:data + test:parity, and a test:parity script that runs playwright test against the gate config with a grep-invert on the stable dev-routing title substring.
    - No existing script removed (test:unit, test:data, test:golden, test:golden:update, test:subpath, generate all present).
    - The base playwright config is byte-unchanged (git diff --quiet on it exits 0).
    - Running the gate listing (pnpm test:parity with the Playwright --list flag) succeeds and does NOT list golden.spec.ts's tests (file-level testIgnore proven). Note: the dev-routing test is still listed here because its env-flag skip is added in Plan 04; the grep-invert belt removes it at run time — Plan 04 records the final count.
  </acceptance_criteria>
  <done>The gate config reads the frozen baseline and excludes golden.spec.ts at file level; pnpm verify is defined with a front-of-gate clean build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate command -> test runners | pnpm verify orchestrates three local runners (Vitest unit, Vitest data, Playwright parity) against a local static build; no network, no backend (Nitro dormant). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | Frozen golden baseline | mitigate | The gate config pins the snapshot dir to the frozen golden path and the gate never passes --update-snapshots; test:golden:update is retained for on-demand F1 use but is never invoked by verify/test:parity. Baseline stays read-only. |
| T-08-04 | Repudiation | Silent over-exclusion of gate coverage | mitigate | golden.spec.ts is excluded at the FILE level (stable across renames), not by a brittle title/line; the dev-routing belt uses a stable title substring; Plan 04 records the expected test count so a future drop is visible (Pitfall 4). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: F8 installs zero packages). |
</threat_model>

<verification>
- pnpm typecheck clean.
- git diff --quiet on the base playwright config exits 0 (base untouched).
- pnpm test:parity with the Playwright --list flag runs and omits golden.spec.ts's tests.
</verification>

<success_criteria>
- A gate-scoped config exists that pins the visual-diff baseline to the frozen golden dir and excludes golden.spec.ts at the file level.
- pnpm verify chains a clean build + the three test layers; all pre-existing scripts are retained; the base config is unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-verificaci-n-de-paridad/08-03-SUMMARY.md` when done.
</output>
