---
phase: 08-verificaci-n-de-paridad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/parity/map-fallback-notes.spec.ts
  - tests/parity/search-route.spec.ts
autonomous: true
requirements: [PARITY-02]

must_haves:
  truths:
    - "D-05: the back-stack 'Volver restores scroll' is proven end-to-end from the MAP popup entry point (popup→ficha→Volver→scrollY back to originY)"
    - "D-05: the back-stack 'Volver restores scroll' is proven end-to-end from the SEARCH result entry point (result→ficha→Volver→scrollY back to originY)"
    - "D-05: the two existing specs are EXTENDED, not rewritten — every pre-existing test in both files still passes"
  artifacts:
    - path: "tests/parity/map-fallback-notes.spec.ts"
      provides: "Map-popup back-stack back-half assertion (originY capture + #back-btn show/click + scroll restored + stack emptied)"
      contains: "originY"
    - path: "tests/parity/search-route.spec.ts"
      provides: "Search-result back-stack back-half assertion (originY capture + #back-btn show/click + scroll restored + stack emptied)"
      contains: "originY"
  key_links:
    - from: "tests/parity/map-fallback-notes.spec.ts"
      to: "#back-btn"
      via: "page.click('#back-btn', { force: true }) after popup→ficha navigation"
      pattern: "back-btn"
    - from: "tests/parity/search-route.spec.ts"
      to: "#back-btn"
      via: "page.click('#back-btn', { force: true }) after result→ficha navigation"
      pattern: "back-btn"
---

<objective>
Close the ONE enumerated SC#2 behavior gap (D-05): the "Volver restaura el scroll" back-stack is proven end-to-end today ONLY from the timeline/internal-link entry point (`navigation.spec.ts` SC#1). It is NOT proven from the two other entry points — the map popup and the search result — even though both already prove the front-half (popup→ficha→`.highlight`, result→ficha→`.highlight`). This plan adds the back-half (→Volver→scroll-restored→stack-emptied) to each, mirroring the canonical assertion in `navigation.spec.ts:217-241`.

This is **audit + minimal gap-fill, NOT a rewrite** (D-05). Both specs are extended in place; every existing test must keep passing.

Purpose: After this plan, every SC#2 item maps to a passing test, so the F8 gate (Plan 02/03) can assert full behavioral coverage. The two added assertions are the only `❌ Wave-0 gap-fill` rows in 08-VALIDATION.md's SC#2 map.
Output: Two extended parity specs (`map-fallback-notes.spec.ts`, `search-route.spec.ts`), each with a back-stack back-half.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verificaci-n-de-paridad/08-CONTEXT.md
@.planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md

<interfaces>
<!-- The canonical back-stack assertion the two gap-fills must mirror. Read it; do not re-derive it. -->
<!-- From tests/parity/navigation.spec.ts (SC#1, lines 217-241) — the proven pattern: -->
<!--   const originY = await page.evaluate(() => window.scrollY)          // capture BEFORE navigating -->
<!--   … navigate to ficha + assert .highlight …                          // (existing front-half) -->
<!--   await expect(page.locator('#back-btn')).toHaveClass(/\bshow\b/)     // canGoBack → .show -->
<!--   await page.click('#back-btn', { force: true })                       // Volver -->
<!--   await expect.poll(() => page.evaluate(() => window.scrollY)).toBe(originY)  // scroll restored -->
<!--   await expect(page.locator('#back-btn')).not.toHaveClass(/\bshow\b/) // stack emptied -->
<!-- Why force:true on the back-btn click: it is a fixed-position chrome control; force avoids actionability churn. -->
<!-- Why dispatchEvent (map) vs .click() (search) for the SOURCE: dispatchEvent does not auto-scroll the source, -->
<!--   so the scrollY the controller pushes onto the stack stays exactly originY (the map case captures mid-page). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Back-stack back-half from the MAP popup (extend map-fallback-notes.spec.ts)</name>
  <read_first>
    - tests/parity/map-fallback-notes.spec.ts (the full file — the SC#2 popup→ficha test at lines ~188-225 is the front-half you extend; the scaffold/console-gate at lines 1-143 is shared and MUST NOT change)
    - tests/parity/navigation.spec.ts (lines 198-245 — the canonical SC#1 back-stack assertion you mirror: originY capture, #back-btn show, force-click, expect.poll scrollY toBe(originY), #back-btn not show)
    - .planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md (section "tests/parity/map-fallback-notes.spec.ts — MODIFY" — the exact front-half excerpt and the back-half to ADD)
  </read_first>
  <behavior>
    - The existing front-half stays: the `I`-roman `.custom-marker` is `dispatchEvent('click')`ed, the `Abrir ficha →` popup link is read for its `#slug`, dispatched, and `#${slug}` gains `.highlight` with the URL hash unchanged (D-03).
    - NEW back-half: capture `originY = window.scrollY` BEFORE the popup→ficha navigation (the map is near page top, so originY is stable); after `.highlight`, assert `#back-btn` has class `show`; `page.click('#back-btn', { force: true })`; assert via `expect.poll` that `window.scrollY` returns to `originY`; assert `#back-btn` no longer has class `show` (stack emptied).
    - Console gate unchanged: the test keeps `trackConsoleErrors(page, true)` (A5/tile aborts emit net::ERR_FAILED) and still ends with `expect(consoleErrors, …).toHaveLength(0)`.
  </behavior>
  <action>
    Extend the existing map-popup SC#2 test in `tests/parity/map-fallback-notes.spec.ts` (the one that already navigates popup→ficha and asserts `.highlight`). Add the back-half mirroring `navigation.spec.ts:217-241`: capture `const originY = await page.evaluate(() => window.scrollY)` BEFORE the `popupLink.dispatchEvent('click')`; after the existing `.highlight` assertion, add `await expect(page.locator('#back-btn')).toHaveClass(/\bshow\b/)`, then `await page.click('#back-btn', { force: true })`, then `await expect.poll(() => page.evaluate(() => window.scrollY)).toBe(originY)`, then `await expect(page.locator('#back-btn')).not.toHaveClass(/\bshow\b/)`. Use the source marker/popup interaction via `dispatchEvent('click')` (NOT `.click()`/`force`) exactly as the existing front-half does — `.click()` would auto-scroll the source and corrupt the stacked scrollY (see Memory: F5 capture-phase listener + the physically-overlapping markers). Do NOT touch the shared scaffold (`ensureBuild`/`beforeAll`/`afterAll`/`trackConsoleErrors`, lines 1-143), do NOT change the base port 5760, do NOT rewrite any other test. Either extend the existing test in place (preferred — same popup interaction already set up) or add a sibling test that re-derives the same popup→ficha setup; if a sibling, keep the `describe` and console gate identical.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/parity/map-fallback-notes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test tests/parity/map-fallback-notes.spec.ts` exits 0 with ALL tests green (the pre-existing 12 + the back-stack assertion; count does not drop).
    - `grep -c 'originY' tests/parity/map-fallback-notes.spec.ts` returns ≥ 1 AND the file contains `page.click('#back-btn', { force: true })` AND `expect.poll(() => page.evaluate(() => window.scrollY)).toBe(originY)`.
    - The shared scaffold is byte-unchanged: `grep -c '5760' tests/parity/map-fallback-notes.spec.ts` still returns 2 (STATIC_PORT + comment) and `ensureBuild`/`trackConsoleErrors` are still present.
  </acceptance_criteria>
  <done>The map-popup entry point proves popup→ficha→Volver→scroll-restored→stack-emptied; no existing test regressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Back-stack back-half from the SEARCH result (extend search-route.spec.ts)</name>
  <read_first>
    - tests/parity/search-route.spec.ts (the full file — the result→ficha test at lines ~196-228 is the front-half you extend; the scaffold/console-gate is shared and MUST NOT change; base port 5740)
    - tests/parity/navigation.spec.ts (lines 198-245 — same canonical back-stack assertion to mirror)
    - .planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md (section "tests/parity/search-route.spec.ts — MODIFY" — the exact front-half excerpt and the back-half to ADD; note `#search` lives in the masthead so originY is naturally near 0 and `.click()` on the result is fine here)
  </read_first>
  <behavior>
    - The existing front-half stays: search is filled (≥2 chars), the first `.search-result` is read for its `data-card` slug, `.click()`ed, and `#${slug}` gains `.highlight` with the URL hash unchanged (D-03).
    - NEW back-half: capture `originY = window.scrollY` BEFORE the result click; after `.highlight`, assert `#back-btn` has class `show`; `page.click('#back-btn', { force: true })`; assert via `expect.poll` that `window.scrollY` returns to `originY`; assert `#back-btn` no longer has class `show`.
    - Console gate unchanged: still tolerates ONLY the color-mode hydration message; test ends with `expect(consoleErrors, …).toHaveLength(0)`.
  </behavior>
  <action>
    Extend the existing result→navigation test in `tests/parity/search-route.spec.ts` (the one that fills the search, clicks the first `.search-result`, and asserts `.highlight`). Add the back-half mirroring `navigation.spec.ts:217-241`: capture `const originY = await page.evaluate(() => window.scrollY)` BEFORE `firstResult.click()`; after the existing `.highlight` assertion, add `await expect(page.locator('#back-btn')).toHaveClass(/\bshow\b/)`, `await page.click('#back-btn', { force: true })`, `await expect.poll(() => page.evaluate(() => window.scrollY)).toBe(originY)`, `await expect(page.locator('#back-btn')).not.toHaveClass(/\bshow\b/)`. The result `.click()` is fine here (the `#search` source is in the masthead `#inicio`, in-viewport, unlike the map/timeline cases). Do NOT touch the shared scaffold, do NOT change base port 5740, do NOT rewrite any other test. Either extend the existing test in place (preferred) or add a sibling re-deriving the same result→ficha setup with an identical console gate.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/parity/search-route.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test tests/parity/search-route.spec.ts` exits 0 with ALL tests green (the pre-existing 10 + the back-stack assertion; count does not drop).
    - `grep -c 'originY' tests/parity/search-route.spec.ts` returns ≥ 1 AND the file contains `page.click('#back-btn', { force: true })` AND `expect.poll(() => page.evaluate(() => window.scrollY)).toBe(originY)`.
    - The shared scaffold is unchanged: `grep -c '5740' tests/parity/search-route.spec.ts` still returns the original count and `ensureBuild` is still present.
  </acceptance_criteria>
  <done>The search-result entry point proves result→ficha→Volver→scroll-restored→stack-emptied; no existing test regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → served static build | Playwright drives a hydrated browser against a locally-served `nuxt generate` output; no untrusted input crosses, no network egress (A5 aborts all images/tiles). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Test temp dirs (`mkdtempSync` previewRoot) | accept | Existing scaffold writes only to OS temp via `mkdtempSync` and tears down with `rmSync` in `afterAll`; this plan does not change the scaffold. No write outside repo temp dirs (ASVS V12 marginal). |
| T-08-02 | Tampering | Frozen golden baseline (56 PNGs) | accept | This plan touches behavior specs only — no `toHaveScreenshot`, no `--update-snapshots`, no read/write of `golden.spec.ts-snapshots/`. Baseline untouched by construction. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit: F8 installs zero packages). `serve` is fetched transiently by the unchanged existing scaffold via `pnpm dlx serve`, already in use F1-F7. |
</threat_model>

<verification>
- `pnpm exec playwright test tests/parity/map-fallback-notes.spec.ts` green (count ≥ prior 12).
- `pnpm exec playwright test tests/parity/search-route.spec.ts` green (count ≥ prior 10).
- `pnpm typecheck` and `pnpm lint` clean (no new type/lint errors introduced by the added assertions).
</verification>

<success_criteria>
- Both back-stack entry points (map popup, search result) prove Volver restores scroll end-to-end, mirroring the canonical timeline assertion.
- Zero existing tests removed or rewritten; both files remain self-contained with their original ports and console gates.
</success_criteria>

<output>
Create `.planning/phases/08-verificaci-n-de-paridad/08-01-SUMMARY.md` when done.
</output>
