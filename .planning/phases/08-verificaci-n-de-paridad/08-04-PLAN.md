---
phase: 08-verificaci-n-de-paridad
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - tests/parity/shell.spec.ts
autonomous: true
requirements: [PARITY-02]

must_haves:
  truths:
    - "D-04 (exclusion #2): the dev-routing shell.spec.ts test is gated behind RUN_DEV_ROUTING so the gate never spawns nuxi dev; the skip is at describe level so the spawning beforeAll is skipped when the flag is unset"
    - "shell.spec.ts static assertions (the non-dev-routing parts) still run in the gate unchanged"
    - "D-04 belt-and-suspenders: the env-flag (here) plus Plan 03's test:parity title grep-invert both target the same dev-routing test; the env-flag is what guarantees nuxi dev is never launched"
  artifacts:
    - path: "tests/parity/shell.spec.ts"
      provides: "Dev-routing test + its pnpm dev spawn gated behind RUN_DEV_ROUTING (describe-level skip); static assertions untouched"
      contains: "RUN_DEV_ROUTING"
  key_links:
    - from: "tests/parity/shell.spec.ts dev-routing describe"
      to: "process.env.RUN_DEV_ROUTING"
      via: "describe-level test.skip(!process.env.RUN_DEV_ROUTING, ...) runs before the spawning beforeAll"
      pattern: "RUN_DEV_ROUTING"
---

<objective>
Gate the dev-routing shell.spec.ts test behind an env flag so the gate never spawns nuxi dev (D-04 exclusion #2). Plan 03 already excludes golden.spec.ts at the file level and adds a test:parity grep-invert belt on the dev-routing title; this plan adds the suspenders — the env-flag that actually prevents the nuxi dev SPAWN (the stale-lock fragility that put this test on the deferred list).

Purpose: completes the deterministic side of the SC#4 gate (no dev server ever launched). The exclusion docs + recorded test count are Plan 05; the first real gate run + diff classification (D-02) is Plan 06. No new dependencies.
Output: modified tests/parity/shell.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verificaci-n-de-paridad/08-CONTEXT.md
@.planning/phases/08-verificaci-n-de-paridad/08-RESEARCH.md
@.planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md

<interfaces>
- shell.spec.ts dev-routing block: the test title at line 224 is "/trips/roma reutiliza el MISMO TripView y un slug desconocido 404 (ARCH-02)"; its enclosing describe-block beforeAll spawns pnpm dev at lines 210-217 (env NUXT_APP_BASE_URL=/guiaRoma/). Playwright evaluates a describe-level test.skip BEFORE that block's beforeAll, so a describe-level skip prevents the spawn.
- Plan 03 already added the gate config and the test:parity grep-invert belt on this test's title.
- Provenance of the dev-routing flake: .planning/phases/05-navegaci-n-transversal/deferred-items.md (nuxi dev stale lock).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the dev-routing test behind RUN_DEV_ROUTING (D-04 exclusion #2)</name>
  <read_first>
    - tests/parity/shell.spec.ts (lines ~205-245 — the dev-routing describe whose beforeAll spawns pnpm dev; confirm the skip must be at describe level so the spawn is skipped, not just the assertion)
    - .planning/phases/08-verificaci-n-de-paridad/08-PATTERNS.md (the D-04 exclusion #2 note: env-flag skip is preferred because it never spawns nuxi dev)
    - .planning/phases/08-verificaci-n-de-paridad/08-RESEARCH.md (Pitfall 4 + Runtime State Inventory "Stale nuxi dev lock")
  </read_first>
  <behavior>
    - When process.env.RUN_DEV_ROUTING is unset (the gate default): the dev-routing test does NOT run AND pnpm dev is NOT spawned.
    - When RUN_DEV_ROUTING=1: the dev-routing test runs exactly as before (on-demand ARCH-02 dev-routing verification).
    - All of shell.spec.ts's STATIC (non-dev) assertions keep running in the gate unchanged.
  </behavior>
  <action>
    In tests/parity/shell.spec.ts, gate the dev-routing path behind process.env.RUN_DEV_ROUTING. Because that describe's beforeAll spawns pnpm dev, the skip must prevent the SPAWN: put test.skip with the condition !process.env.RUN_DEV_ROUTING and a Spanish reason ("dev-routing excluido del gate (D-04): lanza nuxi dev, fragil al lock rancio; ARCH-02 cubierto por el build estatico") at the TOP of the dev-routing describe body (Playwright evaluates a describe-level test.skip before that block's beforeAll, so the pnpm dev spawn is skipped). If the spawning beforeAll is not inside an isolatable describe, ALSO add an early-return guard in the spawn body when !process.env.RUN_DEV_ROUTING so the spawn is a no-op — do both, so the test is skipped AND nuxi dev is never launched. Keep ALL of shell.spec.ts's STATIC assertions running in the gate unchanged. Do NOT remove the dev-routing test; do NOT touch its static siblings.
  </action>
  <verify>
    <automated>pnpm lint tests/parity/shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/parity/shell.spec.ts contains RUN_DEV_ROUTING and a describe-level test.skip (or equivalent) gating the dev-routing path so its pnpm dev spawn is skipped when the flag is unset.
    - Running the gate parity listing (the Playwright list flag against the gate config) WITHOUT RUN_DEV_ROUTING does not include the dev-routing test, and no nuxi dev process is spawned during a gate run.
    - pnpm lint tests/parity/shell.spec.ts is clean; the static (non-dev) shell tests still appear in the listing.
  </acceptance_criteria>
  <done>The dev-routing test and its nuxi dev spawn are gated behind RUN_DEV_ROUTING; the gate never launches a dev server; static shell coverage is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate run -> OS dev-server lifecycle | Without the env flag the gate must never spawn nuxi dev; the env-flag skip removes that OS-state dependency (stale lock) from the deterministic gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Denial of Service | Gate determinism (stale nuxi dev lock) | mitigate | The describe-level RUN_DEV_ROUTING skip prevents the pnpm dev spawn, so a stale dev-server lock can no longer hang/fail the gate; ARCH-02 stays covered by the static build + shell.spec's static assertions. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: F8 installs zero packages). |
</threat_model>

<verification>
- pnpm lint tests/parity/shell.spec.ts clean.
- A gate parity listing without RUN_DEV_ROUTING omits the dev-routing test; the static shell tests remain.
</verification>

<success_criteria>
- The dev-routing test and its nuxi dev spawn are skipped in the gate by default (RUN_DEV_ROUTING), runnable on-demand; static shell coverage unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-verificaci-n-de-paridad/08-04-SUMMARY.md` when done.
</output>
