---
phase: 08-verificaci-n-de-paridad
plan: 07
type: execute
wave: 6
depends_on: ["08-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-verificaci-n-de-paridad/parity-signoff.md
autonomous: false
requirements: [PARITY-02]

must_haves:
  truths:
    - "D-07 prerequisite (Pitfall 5): the PENDING F7 human paridad sign-off (07-04 Task 2) is closed FIRST — FEAT-02/UI-05/FEAT-04 cannot stay unmarked while F8 declares global parity"
    - "D-07: F8 closes on a green suite PLUS one human global parity sign-off (a human reviews the visual-diff results + green pnpm verify and approves the 1.0 as parity-good)"
    - "D-06: the sign-off explicitly documents the single deliberate pixel-parity exception — #mapa is behavior-only (no golden baseline; OSM tiles non-deterministic), covered by map-fallback-notes.spec.ts inside the gate"
    - "D-08: F8 STOPS at green + sign-off — the merge of release/nuxt-4 into main and deploy/CI are OUT OF SCOPE (a separate ship act; main stays intact)"
  artifacts:
    - path: ".planning/phases/08-verificaci-n-de-paridad/parity-signoff.md"
      provides: "The recorded F8 global parity sign-off: green pnpm verify confirmation, the #mapa behavior-only exception (D-06), F7 sign-off closed, and the explicit D-08 scope boundary (no merge/deploy)"
      min_lines: 15
    - path: ".planning/REQUIREMENTS.md"
      provides: "FEAT-02/UI-05/FEAT-04 marked complete (after F7 sign-off) and PARITY-02 marked complete (after F8 sign-off)"
      contains: "PARITY-02"
  key_links:
    - from: "F7 sign-off closed"
      to: "FEAT-02/UI-05/FEAT-04 marked complete"
      via: "the F7 Task-2 human approval (prerequisite) unblocks marking those requirements"
      pattern: "FEAT-02"
    - from: "F8 global sign-off"
      to: "PARITY-02 marked complete"
      via: "green pnpm verify + human approval declares 1.0 parity-good (D-07)"
      pattern: "PARITY-02"
---

<objective>
Close Phase 8 (D-07): take the pending F7 human paridad sign-off as a PREREQUISITE (Pitfall 5 — F8 cannot certify global parity while a constituent phase is unsigned), then take the F8 global parity sign-off. Plan 06's green pnpm verify DEMONSTRATES parity; the human gives the OK that declares the 1.0 parity-good.

Per D-08, F8 STOPS here: the merge of release/nuxt-4 into main and the deploy/CI (GitHub Pages) are OUT OF SCOPE — a separate ship act; main stays intact (honors F1's D-06). Per D-06, the sign-off explicitly documents the single deliberate pixel-parity exception: #mapa is behavior-only.

Purpose: Phase 8 closes on green suite + one human sign-off (SC#4). Two human checkpoints, no autonomous code work. No new dependencies.
Output: parity-signoff.md + REQUIREMENTS.md requirement-status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-verificaci-n-de-paridad/08-CONTEXT.md

<interfaces>
- F7 sign-off status: 07-04 Task 2 (checkpoint:human-verify, the map+fallback+notes paridad sign-off) is PENDING per STATE.md and 07-04-SUMMARY.md; FEAT-02/UI-05/FEAT-04 are behaviorally green (map-fallback-notes.spec 12/12) but NOT marked complete in REQUIREMENTS.md until that human approval.
- Plan 06 produced a green pnpm verify + diff-classification.md (the objective evidence the human reviews).
- D-06 exception: #mapa has no golden baseline (OSM tiles non-deterministic); it is verified by behavior (map-fallback-notes.spec.ts) inside the gate — the one documented pixel-parity exception.
- D-08 boundary: merge release/nuxt-4 -> main and deploy/CI are OUT OF SCOPE (deferred to a dedicated ship step; main intact).
- REQUIREMENTS.md Traceability table: FEAT-02/UI-05/FEAT-04 rows (Phase 7) and the PARITY-02 row (Phase 8) get their Status updated after the respective sign-offs.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Close the pending F7 human paridad sign-off (prerequisite, D-07 / Pitfall 5)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    This is a human-verification checkpoint, not autonomous code. Present the F7 evidence to the user and request the pending F7 paridad sign-off (07-04 Task 2). First run pnpm generate and serve the built site under /guiaRoma/ (per MEMORY: serve the build under a guiaRoma/ subdir and open /guiaRoma/). Ask the user to compare, in light AND dark x mobile AND desktop, against the live index.html: the Leaflet map with numbered markers per type; popups "Abrir ficha ->" navigating to the ficha (scroll+highlight, hash unchanged); the guided markers (Coliseo, Vaticano) text-only; the offline banner on tile failure; hero/detail images degrading to the motif SVG; a typed note persisting across reload. On the user's "approved", mark FEAT-02, UI-05, and FEAT-04 complete in .planning/REQUIREMENTS.md (checkbox + Traceability Status = Complete). Do NOT self-approve; wait for the human signal.
  </action>
  <what-built>
    Phase 7 (Leaflet map island, image fallback, per-card notes) is implemented and tests/parity/map-fallback-notes.spec.ts is 12/12 green (SC#1-SC#7 x mobile+desktop). This is the same phase-closing human paridad sign-off used in F3-F6; it was left PENDING as 07-04 Task 2 and must close before F8 declares global parity.
  </what-built>
  <how-to-verify>
    1. pnpm generate, then serve the built site under /guiaRoma/ and open it.
    2. In light AND dark, mobile AND desktop, vs the live index.html: numbered markers per type; popups navigate (scroll+highlight, hash unchanged); guided markers text-only; offline banner on tile failure; hero/detail fall back to motif SVG; a typed note persists across reload.
    3. Confirm identical to the live guide.
  </how-to-verify>
  <verify>
    <human-check>The user confirms map + popups + offline banner + image fallback + notes are identical to the live guide in light/dark x mobile/desktop.</human-check>
  </verify>
  <resume-signal>Type "approved" if identical, or describe any difference.</resume-signal>
  <done>F7 paridad sign-off approved by the user; FEAT-02, UI-05, FEAT-04 marked complete in REQUIREMENTS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: F8 global parity sign-off (D-07) + record scope boundary (D-08)</name>
  <files>.planning/phases/08-verificaci-n-de-paridad/parity-signoff.md, .planning/REQUIREMENTS.md</files>
  <action>
    This is a human-verification checkpoint, not autonomous code. Present the F8 gate evidence and request the global parity sign-off that declares the 1.0 parity-good. Confirm pnpm verify is green (re-run if desired) and walk the user through diff-classification.md (every visual diff is pass / real-fixed / artifact-justified-with-evidence), the single deliberate pixel-parity exception (#mapa behavior-only via map-fallback-notes.spec.ts, D-06), that the F7 sign-off (Task 1) is closed, and the D-08 boundary (F8 STOPS at green + sign-off; merge release/nuxt-4 -> main and deploy/CI are OUT OF SCOPE; main intact). On the user's "approved", record the approval in .planning/phases/08-verificaci-n-de-paridad/parity-signoff.md (green pnpm verify confirmed; the #mapa exception per D-06; F7 sign-off closed; the D-08 no-merge/no-deploy boundary) and mark PARITY-02 complete in .planning/REQUIREMENTS.md (checkbox + Traceability Status = Complete). Do NOT self-approve; do NOT merge or deploy (D-08).
  </action>
  <what-built>
    The F8 verification gate is complete and green: pnpm verify passes end to end (Plan 06) — clean build, test:unit, test:data (counts 38/26/13/5/2/1, unique ids, cross-refs resolve, motif per monument), and the gate-scoped parity suite incl. the NEW Nuxt-vs-golden visual-diff (14 views x light/dark x mobile/desktop vs the 56 frozen PNGs within 0.01) and the behavior specs (theme no-flash, pace matrix, search, day-route URL, notes, scrollspy +130, back-stack from timeline + map + search). diff-classification.md records every diff's verdict; the two non-Nuxt failures are excluded with documented reasons and a recorded count.
  </what-built>
  <how-to-verify>
    1. Confirm pnpm verify is green.
    2. Review diff-classification.md: every visual diff is pass / real-fixed / artifact-justified-with-evidence (paridad = ley).
    3. Confirm the #mapa behavior-only exception (D-06).
    4. Confirm the F7 sign-off (Task 1) is closed.
    5. Acknowledge the D-08 boundary (no merge/deploy in F8; main intact).
  </how-to-verify>
  <verify>
    <human-check>The user reviews the green pnpm verify + diff-classification.md and approves the 1.0 as parity-good, acknowledging the #mapa exception and the D-08 boundary.</human-check>
  </verify>
  <resume-signal>Type "approved" to declare the 1.0 parity-good (SC#4), or describe any blocking parity concern.</resume-signal>
  <done>F8 global parity sign-off recorded in parity-signoff.md; PARITY-02 marked complete; no merge/deploy performed (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence -> human attestation | The human sign-off converts a green automated gate into the recorded 1.0 parity attestation; the only input is the reviewer's approval. No code/runtime boundary; Nitro dormant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Repudiation | Parity attestation provenance | mitigate | The sign-off is recorded in parity-signoff.md with the green pnpm verify confirmation, the diff-classification reference, the documented #mapa exception (D-06), and the D-08 boundary — an auditable record that the 1.0 was declared parity-good on stated evidence. |
| T-08-10 | Elevation of Privilege | Out-of-scope ship actions (merge/deploy) | mitigate | D-08 is recorded as an explicit boundary in the sign-off: F8 does NOT merge release/nuxt-4 into main or set up deploy/CI; main stays intact (honors F1 D-06). The ship act is a separate, later step. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: F8 installs zero packages). |
</threat_model>

<verification>
- F7 Task 2 sign-off closed and FEAT-02/UI-05/FEAT-04 marked complete in REQUIREMENTS.md.
- parity-signoff.md records the green pnpm verify, the #mapa behavior-only exception (D-06), and the D-08 boundary; PARITY-02 marked complete.
</verification>

<success_criteria>
- The pending F7 human paridad sign-off is closed (prerequisite); FEAT-02/UI-05/FEAT-04 complete.
- The F8 global parity sign-off declares the 1.0 parity-good on a green pnpm verify, with the #mapa exception (D-06) and the D-08 no-merge/no-deploy boundary recorded; PARITY-02 complete.
</success_criteria>

<output>
Create `.planning/phases/08-verificaci-n-de-paridad/08-07-SUMMARY.md` when done.
</output>
