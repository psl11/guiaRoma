---
phase: 04-render-de-contenido-modos-de-ritmo
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/components/MonumentCard.vue
autonomous: true
requirements: [UI-02]
user_setup: []
must_haves:
  truths:
    - "MonumentCard reproduce index.html:2450-2510 verbatim, data-bound desde un Monument (card-header, card-hero img plano, card-section prosa, facts, maps-link, sorrentino/culture box, notes-area shell)"
    - "La prosa de sections[].body se renderiza con <MDC> SIN unwrap (párrafos <p> reales); la PRIMERA sección lleva dropcap, las demás .no-dropcap (Pitfall 2)"
    - ":detail-photo{...} dentro de la prosa resuelve a un .detail-photo > img real (vía DetailPhoto.global.vue del Plan 01), no a texto sin renderizar"
    - "Las listas 'En qué fijarse' conservan la clase/look .detail-list (✦ + bordes) (Pitfall 1, resuelto en Plan 01)"
    - "El hero es <img> PLANO (src/alt/loading) SIN @error→SVG (frontera D-01/F7); las notas son un textarea shell con data-note-key SIN persistencia (frontera D-02/F7)"
    - "El href de maps-link se reconstruye con encodeURIComponent(monument.mapsQuery); el componente NO lleva CSS ni <style scoped>"
  artifacts:
    - path: "app/components/MonumentCard.vue"
      provides: "La .card de monumento completa data-driven (UI-02)"
      contains: "card-section"
  key_links:
    - from: "app/components/MonumentCard.vue"
      to: "DetailPhoto.global.vue (vía <MDC>)"
      via: ":detail-photo{...} en sections[].body resuelto por resolveComponent"
      pattern: "MDC"
    - from: "app/components/MonumentCard.vue"
      to: "shared/schemas.ts Monument"
      via: "defineProps<{ monument: Monument }>()"
      pattern: "Monument"
---

<objective>
Construir `MonumentCard.vue` — la `.card` de monumento completa, transcripción 1:1 del markup de `index.html:2450-2510` data-bound desde un `Monument` tipado. Es el corazón de UI-02 (ficha de atracción idéntica a hoy) y donde se resuelve la sutileza de paridad del **dropcap** (Pitfall 2: prosa `<MDC>` SIN unwrap + `.no-dropcap` en secciones 2..n). Consume los componentes globales del Plan 01 (`DetailPhoto` para `:detail-photo{...}`, los overrides `ProseUl`/`ProseLi` para `.detail-list`). Respeta las dos fronteras del roadmap: hero `<img>` PLANO (el fallback `@error`→SVG es F7) y notas `<textarea>` shell SIN persistencia (FEAT-04 es F7).

Purpose: `MonumentCard` es el componente más rico de la fase y el que `DaySection` (Plan 05, vía TripView) instancia para cada id de `day.cards[]`. Construirlo en su propio plan mantiene el diff enfocado (precedente F3: un componente/familia por plan) y aísla el riesgo de paridad del dropcap y de la resolución MDC de `:detail-photo`. El render de prosa por secciones con `<MDC>` SIN unwrap es lo que distingue a `MonumentCard` de `TheHero` (que usa `unwrap="p"` en los casos inline): el dropcap (`.card-section p:first-of-type::first-letter`) NECESITA `<p>` reales. La verificación end-to-end del DOM real (detail-photo resuelto, detail-list con clase, dropcap, facts, maps-link) vive en el Plan 05 (`render-cards.spec.ts`); aquí el componente se construye contra su contrato con typecheck/lint + gate de no-CSS.

Output: nuevo `app/components/MonumentCard.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md
@content/trips/roma/monuments/galleria-sciarra.yml

<interfaces>
Tipo de prop (shared/schemas.ts:46-65 MonumentSchema → type Monument):
  slug, roman, name, italian, day, coords, type, motif,
  badge?, artists?: Link[] (→ #art-*), arch?: Link[] (→ #arq-*),
  hero: { src, alt }, sections: { heading, body }[], facts: { label, value }[],
  mapsQuery, sorrentino?: { label, text }, culture?: { title, text }[]
  Link = { ref, label (Md), note?: (Md) }   (shared/schemas.ts:27)

Markup VERBATIM (index.html:2450-2510, ficha galleria-sciarra) — clases LOCKED por base.css:
  article.card :id="slug"
    div.card-header > span.card-roman {{roman}} + div.card-title(h3 {{name}} + div.card-italian {{italian}}) + span.card-badge {{badge}}?
    div.card-artists.card-arch?  → "Arquitectura: " + a.art-link[href="#"+ref] {{label}}  (index.html:2521; de monument.artists/arch; enlaces planos, intercepción es F5)
    div.card-hero > img :src :alt loading="lazy"   ← PLANO en F4 (index.html:2459 tiene onerror; F4 lo OMITE, F7 lo reañade)
    div.card-section ×n  → h4 {{heading}} + <MDC :value="body" />   ← SIN unwrap (dropcap + :detail-photo + listas)
       · 1ª sección: dropcap (sin clase extra); secciones 2..n: class="card-section no-dropcap"
    div.facts > div.facts-row ×n (span.label {{f.label}} + span.value {{f.value}})
    a.maps-link[target=_blank rel=noopener] "Ver en Google Maps"  ← href = `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(monument.mapsQuery)}`
    div.sorrentino-box? > span.label {{sorrentino.label}} + <MDC :value="sorrentino.text" unwrap="p" />   (index.html:2501-2504; texto directo, sin <p>)
    div.culture-box? (monument.culture[] = {title,text}; ref-item/ref-title; prosa <MDC>)
    div.notes-area > label[:for] "Notas in situ" + textarea.notes-textarea[:id][:data-note-key=slug] placeholder "Lo que quieras recordar de aquí…"   ← shell SIN persistencia (F7)

Matriz unwrap (UI-SPEC ▸ Contrato del render de prosa):
  sections[].body → SIN unwrap (multi-párrafo; dropcap necesita <p>)
  sorrentino.text → unwrap="p" (texto directo tras el label, sin <p>)
  DetailPhoto caption → la gestiona DetailPhoto.global.vue (Plan 01)

Análogo de componente: app/components/TheHero.vue (props Trip tipados + <MDC> + markup verbatim + CERO CSS + cabecera que cita el rango index.html). La diferencia clave: TheHero usa unwrap="p"; MonumentCard NO en la prosa por secciones.
Patrón LOCKED F3: un componente por concern, markup+clases verbatim, CERO CSS, sin <style scoped> (data-v-* rompería .card-section p:first-of-type::first-letter, .detail-list li::before).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MonumentCard — .card verbatim con prosa MDC (dropcap), hero plano y notas shell (UI-02)</name>
  <files>app/components/MonumentCard.vue</files>
  <read_first>
    - index.html (líneas 2450-2510 — la ficha galleria-sciarra COMPLETA, el markup a transcribir 1:1: card-header con card-roman/card-title/card-italian/card-badge, card-hero img, las card-section con dropcap en la 1ª y no-dropcap en 2460/2465/2471/2476, facts, maps-link 2498, sorrentino-box 2501-2504, notes-area 2506-2509; línea 2521 — el patrón card-artists.card-arch con a.art-link)
    - content/trips/roma/monuments/galleria-sciarra.yml (el dato real: confirmar que sections[3].body empieza con :detail-photo{...} seguido de lista Markdown nativa; ver la forma de mapsQuery, sorrentino, facts, artists/arch)
    - shared/schemas.ts (líneas 46-65 MonumentSchema → type Monument; línea 27 Link) — el tipo exacto de la prop y sus campos opcionales
    - app/components/TheHero.vue (el análogo: cabecera que cita el rango index.html + por qué CERO CSS; el uso de <MDC>; defineProps<{}> con tipo de ~~/shared/schemas)
    - app/assets/css/base.css (líneas 772-797 .card-section + dropcap p:first-of-type::first-letter + .no-dropcap; 799-818 .detail-list; 820-844 .detail-photo; y las reglas .card/.card-header/.card-roman/.card-title/.card-italian/.card-badge/.facts-row/.maps-link/.sorrentino-box/.notes-area) — el contrato que forbids scoping
    - app/components/DetailPhoto.global.vue (Plan 01 — el componente que <MDC> resuelve para :detail-photo; confirmar que existe y es global)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pattern 2 MonumentCard; §Pattern 3 matriz unwrap; §Pitfall 1 detail-list; §Pitfall 2 dropcap; §Pattern 2 nota maps-link/A4)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§MonumentCard.vue — cabecera, patrón núcleo prosa, hero plano, maps-link, sorrentino, notes-area, card-artists)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario ▸ MonumentCard; matriz unwrap; Gotchas Pitfall 2 + maps-link href)
  </read_first>
  <action>
    Crear app/components/MonumentCard.vue (script setup lang ts; cabecera estilo TheHero que cita index.html:2450-2510, explica CERO CSS/sin scoped y las dos fronteras D-01 hero plano / D-02 notas shell). `import type { Monument } from '~~/shared/schemas'` + `defineProps<{ monument: Monument }>()`. Template = transcripción 1:1 del markup:
    - `article.card :id="monument.slug"`.
    - `div.card-header`: `span.card-roman {{ monument.roman }}` + `div.card-title` (`h3 {{ monument.name }}` + `div.card-italian {{ monument.italian }}`) + `span.card-badge {{ monument.badge }}` con v-if.
    - card-artists/card-arch (opcional, index.html:2521): si monument.arch/artists, `div.card-artists.card-arch` con el prefijo de texto + `a.art-link :href="'#'+ link.ref"` por cada Link (enlaces planos #id; la intercepción es F5). Reproducir la combinación exacta del original (verificar el prefijo "Arquitectura:"/"Artistas:" contra el dato/index.html).
    - `div.card-hero` > `img :src="monument.hero.src" :alt="monument.hero.alt" loading="lazy"` — PLANO, SIN onerror/@error (frontera D-01).
    - `div.card-section` por cada `monument.sections` con `v-for (s,i)`, `:class="{ 'no-dropcap': i !== 0 }"`, conteniendo `h4 {{ s.heading }}` + `<MDC :value="s.body" />` SIN unwrap (Pitfall 2: párrafos reales para el dropcap + :detail-photo resuelto por DetailPhoto.global + listas con .detail-list por ProseUl).
    - `div.facts` > `div.facts-row` por cada `monument.facts` (`span.label {{ f.label }}` + `span.value {{ f.value }}`).
    - `a.maps-link[target="_blank" rel="noopener"]` con texto estático "Ver en Google Maps" y `:href` = la URL `https://www.google.com/maps/search/?api=1&query=` + `encodeURIComponent(monument.mapsQuery)`.
    - `div.sorrentino-box` con v-if (`span.label {{ monument.sorrentino.label }}` + `<MDC :value="monument.sorrentino.text" unwrap="p" />`).
    - culture-box con v-if (monument.culture[] = {title,text}; reproducir el markup ref-item/ref-title del original — leer la clase exacta `.culture-box` en index.html antes de escribir).
    - `div.notes-area`: `label :for="'note-'+monument.slug"` "Notas in situ" + `textarea.notes-textarea :id="'note-'+monument.slug" :data-note-key="monument.slug"` placeholder "Lo que quieras recordar de aquí…" — SHELL sin v-model ni persistencia (frontera D-02).
    CERO CSS, sin bloque <style>. Verificar el escaping de maps-link en una ficha con apóstrofo/acento (p.ej. Sant'Eustachio) contra el href del original (A4) y anotar el resultado en el SUMMARY.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/MonumentCard.vue && grep -q "no-dropcap" app/components/MonumentCard.vue && grep -q "data-note-key" app/components/MonumentCard.vue && grep -q "encodeURIComponent" app/components/MonumentCard.vue && ! grep -qE "onerror|@error|<style" app/components/MonumentCard.vue</automated>
  </verify>
  <acceptance_criteria>
    - MonumentCard.vue contiene article.card con :id de slug, card-header (card-roman + card-title h3 + card-italian + card-badge condicional), card-hero > img plano (sin onerror/@error), card-section v-for con :class no-dropcap para i!==0 y <MDC :value="s.body" /> SIN prop unwrap, facts/facts-row, a.maps-link con texto "Ver en Google Maps" y href con encodeURIComponent(monument.mapsQuery), sorrentino-box condicional con <MDC unwrap="p">, y notes-area con textarea data-note-key=slug SIN v-model
    - El <MDC> de sections[].body NO lleva unwrap (necesario para el dropcap); el de sorrentino.text SÍ lleva unwrap="p"
    - defineProps tipa monument como Monument importado de ~~/shared/schemas
    - El fichero NO contiene <style>, onerror ni @error (grep negativo); contiene no-dropcap, data-note-key, encodeURIComponent (greps positivos)
    - El escaping del href de maps-link en una ficha con apóstrofo se verificó contra el original y el resultado quedó anotado en el SUMMARY
    - pnpm typecheck exit 0; pnpm lint del fichero limpio
  </acceptance_criteria>
  <done>MonumentCard reproduce la .card verbatim (header/hero plano/prosa MDC con dropcap/facts/maps-link/sorrentino/notes shell), respeta las fronteras D-01/D-02, usa unwrap solo donde toca, y no escribe CSS; typecheck + lint verdes. La verificación DOM real (detail-photo resuelto, detail-list, dropcap) la cierra el Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo data (Monument) → MonumentCard | El componente renderiza datos zod-validados versionados en el repo (prosa, hero, facts). Sin input de usuario. |
| `<MDC>` sobre monument.sections[].body → DOM | La prosa Markdown es contenido versionado y confiable; `:detail-photo` resuelve a DetailPhoto (Plan 01), no a v-html sobre input externo. |
| maps-link href + textarea de notas | El href es un fragmento Google Maps construido desde mapsQuery (dato validado); el textarea es un shell sin lectura/escritura (persistencia es F7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | maps-link href (target=_blank) | accept | href construido con encodeURIComponent sobre mapsQuery (dato validado); se conserva `rel="noopener"` VERBATIM (anti-tabnabbing) como en el original. Sin URL controlada por usuario. |
| T-04-04 | Tampering/Elevation | `<MDC>` sobre sections[].body | accept | MDC renderiza SOLO prosa versionada en el repo (RESEARCH §Security Domain); :detail-photo pasa src/alt como atributos de `<img>` (no v-html). Reevaluar solo si v2 admite contenido de terceros. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalación en este plan (RESEARCH §Package Legitimacy Audit: F4 no instala nada). Sin paquete [ASSUMED]/[SUS]. |

**Conclusión:** Sin amenazas de severidad alta. Componente presentacional sobre datos confiables; href con noopener verbatim; notas sin sink (shell).
</threat_model>

<verification>
- pnpm typecheck — TS estricto sobre MonumentCard (prop Monument).
- pnpm lint app/components/MonumentCard.vue — limpio.
- grep gates: no-dropcap presente (Pitfall 2), data-note-key presente (shell D-02), encodeURIComponent presente (maps-link), y AUSENCIA de <style>/onerror/@error (CERO CSS + frontera D-01).
- El render real (detail-photo → .detail-photo > img, .detail-list con ✦, dropcap en la 1ª sección, facts, maps-link, sorrentino, notes shell) se asevera en el Plan 05 (tests/parity/render-cards.spec.ts, autocontenido) — no se rebaselina el golden (D-08).
</verification>

<success_criteria>
- MonumentCard reproduce el markup de index.html:2450-2510 1:1 con las clases existentes y CERO CSS (UI-02).
- La prosa de sections[].body va por `<MDC>` SIN unwrap con dropcap solo en la 1ª sección (`.no-dropcap` en el resto) (SC#1/Pitfall 2); `:detail-photo` resuelve a `.detail-photo > img` (Plan 01).
- El hero es `<img>` plano (frontera D-01/F7) y las notas son textarea shell con data-note-key sin persistencia (frontera D-02/F7).
- El href de maps-link se reconstruye con encodeURIComponent y conserva rel="noopener"; sin `<style scoped>`.
</success_criteria>

<output>
Create `.planning/phases/04-render-de-contenido-modos-de-ritmo/04-02-SUMMARY.md` when done.
</output>
