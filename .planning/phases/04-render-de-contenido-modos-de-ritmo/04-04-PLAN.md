---
phase: 04-render-de-contenido-modos-de-ritmo
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/components/GastroCard.vue
  - app/components/GastroSection.vue
  - app/components/ArtistCard.vue
  - app/components/ReservasSection.vue
  - app/components/PracticaSection.vue
  - app/utils/foodGroups.ts
  - tests/unit/foodGroups.spec.ts
autonomous: true
requirements: [UI-04]
user_setup: []
must_haves:
  truths:
    - "GastroSection agrupa food por food.group en el ORDEN CANÓNICO del index.html (Pasta→Quinto quarto→Ghetto→Pizza→Gelato→Café→Bar), NO el orden alfabético de queryCollection.all() (Pitfall 6)"
    - "GastroCard reproduce la .gastro-card verbatim (badge-* desde badgeKind, name, address, gastro-card-desc, gastro-plato, footer con gastro-maps-link e itinerary-tag)"
    - "Un componente por familia (D-10): GastroCard, ArtistCard (UN SOLO componente que unifica artist/arquitectura/glossary por kind: art-*/arq-* con sections+seenIn, y arq-glosario con terms[]), ReservasSection, PracticaSection"
    - "ReservasSection reproduce reservas-box (confirmadas mesas+visitas + reservas-table con badges urgent/done/rec e is-done)"
    - "PracticaSection reproduce eyebrow+intro+sections(MDC)+media por category; ningún componente lleva <style scoped>"
    - "Las listas de prosa de ArtistCard/PracticaSection respetan la decisión de Pitfall 1 del Plan 01 (las .artist-section usan <ul> SIN .detail-list en el original)"
  artifacts:
    - path: "app/utils/foodGroups.ts"
      provides: "agrupado puro de food por group en orden canónico (Pitfall 6)"
      contains: "export function"
    - path: "tests/unit/foodGroups.spec.ts"
      provides: "el agrupado produce el orden canónico de grupos"
    - path: "app/components/GastroSection.vue"
      provides: "#gastronomia: intro + grupos ordenados + GastroCard (UI-04)"
      contains: "gastro-section-title"
    - path: "app/components/GastroCard.vue"
      provides: ".gastro-card data-driven (UI-04)"
      contains: "gastro-card"
    - path: "app/components/ArtistCard.vue"
      provides: "artist/arquitectura/glossary unificado por kind (UI-04)"
      contains: "artist-card"
    - path: "app/components/ReservasSection.vue"
      provides: "#reservas: confirmadas + tabla (UI-04)"
      contains: "reservas-table"
    - path: "app/components/PracticaSection.vue"
      provides: "#practica: prosa + media (UI-04)"
      contains: "section-eyebrow"
  key_links:
    - from: "app/components/GastroSection.vue"
      to: "app/utils/foodGroups.ts"
      via: "agrupado en orden canónico"
      pattern: "groupFood"
    - from: "app/components/ArtistCard.vue"
      to: "shared/schemas.ts Artist (discriminatedUnion kind)"
      via: "v-if row.kind === 'glossary' estrecha la rama"
      pattern: "kind"
---

<objective>
Construir las **secciones de referencia** (UI-04): `GastroCard` + su contenedor `GastroSection` (#gastronomia, agrupado por `food.group`), `ArtistCard` (un solo componente que unifica `artist`/`arquitectura`/`glossary` por `kind`, para #arte y #arquitectura), `ReservasSection` (#reservas) y `PracticaSection` (#practica), todas data-driven desde `useTrip`. Aquí se resuelve la sutileza de paridad de Pitfall 6: el agrupado de gastronomía debe salir en el **orden canónico del index.html**, NO en el orden alfabético que devuelve `queryCollection('food').all()` — se extrae un util puro `foodGroups` con su test unitario. Consume los componentes globales del Plan 01 (`ProseUl`/`ProseLi` para las listas de prosa que deban ser `.detail-list`, respetando la decisión tomada allí).

Purpose: Las 5 secciones de referencia las enchufa `TripView` (Plan 05) en las anclas #reservas/#gastronomia/#practica/#arte/#arquitectura. Construirlas en un plan dedicado mantiene el diff enfocado (precedente F3: F2 migró estos datos en su Plan 07; F4 los renderiza). Dos riesgos concretos viven aquí: (1) **Pitfall 6** — el agrupado por `Map` preserva el orden de `.all()` (alfabético por filename: g-armando, g-baffetto, g-bar-musa…), pero el index.html ordena los grupos Pasta→Quinto quarto→Ghetto→Pizza→Gelato→Café→Bar; como los ficheros NO tienen campo `order`, el orden canónico de grupos DEBE codificarse explícitamente (verificado: `g-bar-musa`/`g-bar-pace` saldrían terceros en `.all()` pero son el ÚLTIMO grupo en el original). (2) **Pitfall 1 / D1 de F3** — `ArtistCard` y `PracticaSection` tienen prosa con listas; en el index.html las `.artist-section` usan `<ul>` SIN `.detail-list` (ver index.html ~5959+), mientras la prosa de monumento/práctica usa `.detail-list`; el executor debe respetar la decisión que el Plan 01 dejó documentada para no aplicar ✦ donde no toca. Además, `artist`/`reference` son `discriminatedUnion` cuyos item-types de Content v3 son vacíos (STATE: D1 diferido a F4): `useTrip` ya castea a los tipos zod, así que las props se tipan contra `Artist`/`Reference` de shared/schemas. La verificación E2E del DOM (reservas-table+badges, gastro-cards agrupadas en orden, artist-cards, arq-glosario) vive en el Plan 05 (`render-reference.spec.ts`).

Output: nuevos `app/components/GastroCard.vue`, `GastroSection.vue`, `ArtistCard.vue`, `ReservasSection.vue`, `PracticaSection.vue`, `app/utils/foodGroups.ts`, `tests/unit/foodGroups.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md

<interfaces>
Tipos de prop (shared/schemas.ts):
  Food (153-167): { slug, trip, group, groupIntro?, badge, badgeKind: 'trattoria'|'deli'|'quinto'|'ghetto'|'pizza'|'gelato'|'caffe'|'pasticceria', name, address, desc, plato?, footer, itineraryTag?, mapsQuery }
  Artist (175-209, discriminatedUnion kind):
    artist:       { kind:'artist', slug, trip, avatar, name, dates, epithet, sections: Section[], seenIn: Link[] }
    arquitectura: { kind:'arquitectura', slug, trip, avatar, name, dates, epithet, sections: Section[], seenIn: Link[], archLink?: Link[] }
    glossary:     { kind:'glossary', slug:'arq-glosario', trip, avatar, name, dates, epithet, terms: { term, def }[] }
  Reference (248, discriminatedUnion slug):
    ReservasSchema (212-232): { slug:'reservas', trip, order, title, eyebrow, intro, confirmed: { group:'mesas'|'visitas', when, text }[], table: { ref?, name, badge?, badgeKind?: 'urgent'|'done'|'rec', isDone, desc }[] }
    PracticaSchema (233-245): { slug:'practica', trip, order, title, eyebrow, intro, sections: Section[], media: { category:'libros'|'peliculas'|'series'|'playlist', items: Md[] }[] }
  TripSchema.sections (272-276): { gastronomia?, arte?, arquitectura? } cada uno { eyebrow, intro }  (eyebrow+intro de SECCIÓN; de trip.yml, pasado por TripView)
  Link (27): { ref, label (Md), note? (Md) }

Pitfall 6 — ORDEN CANÓNICO de grupos (index.html:5342/5500/5540/5563/5636/5709/5782, verificado):
  1. 'Pasta clásica · trattorias históricas'
  2. 'Quinto quarto · cocina de Testaccio'
  3. 'Cocina giudaico-romana · Ghetto'
  4. 'Pizza'
  5. 'Gelato'
  6. 'Café · desayuno · pastelería'
  7. 'Bar · aperitivo · salotto romano'
  queryCollection('food').all() devuelve ALFABÉTICO por filename (g-armando, g-baffetto, g-bar-musa, g-bar-pace, g-checchino…) → un Map por primera aparición daría el orden EQUIVOCADO. Codificar el orden canónico (array constante) y ordenar por él; dentro de cada grupo, conservar el orden de aparición. groupIntro existe en g-checchino (quinto quarto) y g-giggetto (ghetto).

Markup VERBATIM (index.html — leer cada rango antes de transcribir):
  #reservas 5260-5333: section-eyebrow + h2.section-title + p.gastro-intro + reservas-box(reservas-confirmadas h4 + ul×2 por group mesas/visitas con li(rc-when {{when}} + text) · reservas-box-header · reservas-table con tr(.is-done?) > td(a[#ref]? + reservas-badge.badge-urgent/.badge-done/.badge-rec) + td(desc))
  #gastronomia 5335-5377+ (card 5346-5360): section-eyebrow + h2 + gastro-intro(trip.sections.gastronomia) + POR GRUPO: p.gastro-section-title + (groupIntro? como p.gastro-intro) + div.gastro-grid > .gastro-card; card = header(gastro-card-badge.badge-* + name + address) + p.gastro-card-desc + .gastro-plato? + footer(span {{footer}} + a.gastro-maps-link; .gastro-itinerary-tag?)
  #arte ~5941: section-eyebrow + intro(trip.sections.arte) + ArtistCard(kind='artist') ×n  (.artist-card art-*: artist-avatar {{avatar}} + name + artist-dates + artist-epithet + artist-section ×n con <ul> SIN .detail-list + artist-trip ✦ seenIn)
  #arquitectura ~6104: section-eyebrow + intro(trip.sections.arquitectura) + ArtistCard(kind='arquitectura') ×n + arq-glosario ~6202 (arch-term ×10: <b>{{term}}</b><span>{{def}}</span>)
  #practica 5825: section-eyebrow + h2 + intro + sections(<MDC>) + media por category (libros/peliculas/series/playlist), cada item Markdown-inline

unwrap (UI-SPEC matriz): confirmed[].text y table[].desc → unwrap="p" (dentro de li/td); gastro-card-desc/gastro-plato → unwrap="p" (clase en el contenedor propio); media items → inline (unwrap="p"); intro de sección → inline (unwrap="p"); la prosa por secciones de artist/practica (sections[].body) → SIN unwrap (mismo criterio que MonumentCard, dropcap).

CRÍTICO Pitfall 1 (leer la decisión del Plan 01 en su SUMMARY ANTES de implementar ArtistCard/PracticaSection): las listas de .artist-section del original usan <ul> SIN clase, NO .detail-list. Si el Plan 01 creó ProseUl.global.vue que pone .detail-list a TODA lista MDC, las listas de artista DIVERGIRÍAN. Respetar la decisión del Plan 01; si hay conflicto real (la prosa de artista va por <MDC> sections[].body y el override global la convierte en detail-list), DETENERSE y anotarlo en el SUMMARY (no tocar datos de F2).

Análogos: GastroCard→MonumentCard (gemelo, Plan 02); GastroSection→NavPills (v-for)+util agrupado; ArtistCard→ThemeToggle (ramas en script)+MonumentCard (prosa por secciones); ReservasSection/PracticaSection→TheHero (sección data-bound + <MDC>). Util puro→app/utils/tripIndexes.ts (función pura testeable). Patrón LOCKED F3: un componente por concern, markup+clases verbatim, CERO CSS, sin <style scoped>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agrupado de gastronomía en orden canónico (util puro + test) + GastroCard + GastroSection (UI-04, Pitfall 6)</name>
  <files>app/utils/foodGroups.ts, tests/unit/foodGroups.spec.ts, app/components/GastroCard.vue, app/components/GastroSection.vue</files>
  <read_first>
    - index.html (líneas 5335-5377 — la sección #gastronomia, su h2/eyebrow/intro y el primer gastro-section-title + gastro-grid; 5346-5360 — una gastro-card completa con badge/name/address/desc/plato/footer/maps-link/itinerary-tag; las 7 líneas de gastro-section-title 5342/5500/5540/5563/5636/5709/5782 — el ORDEN canónico)
    - shared/schemas.ts (líneas 153-167 FoodSchema → type Food; 272-276 TripSchema.sections.gastronomia) — los tipos de prop
    - content/trips/roma/food/g-bar-musa.yml y g-armando.yml (confirmar que NO hay campo order; group es el único signal; ver desc/plato/footer/itineraryTag/badgeKind)
    - app/utils/tripIndexes.ts (el análogo de util puro: función framework-free, testeable; guard ?? [])
    - tests/unit/dayLabel.spec.ts (el análogo de test unitario puro)
    - app/components/MonumentCard.vue (el gemelo F4 para el estilo de card; Plan 02 — leer cómo quedó)
    - app/components/NavPills.vue (análogo de v-for para el contenedor)
    - app/assets/css/base.css (reglas .gastro-card*/.gastro-card-badge.badge-*/.gastro-card-desc/.gastro-plato/.gastro-maps-link/.gastro-itinerary-tag/.gastro-section-title/.gastro-grid/.gastro-intro/.section-eyebrow/.section-title) — clases LOCKED
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pattern 5 agrupado; §Pitfall 6/A3 orden de grupos)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§GastroCard + contenedor #gastronomia)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario ▸ GastroCard; matriz unwrap)
  </read_first>
  <behavior>
    - groupFood agrupa una lista de Food por food.group y devuelve los grupos en el ORDEN CANÓNICO (Pasta clásica → Quinto quarto → Ghetto → Pizza → Gelato → Café → Bar), NO el orden de entrada alfabético
    - Dentro de cada grupo, los items conservan el orden en que aparecen en la lista de entrada
    - groupIntro del grupo se toma del primer item del grupo que lo tenga (quinto quarto, ghetto)
  </behavior>
  <action>
    Crear app/utils/foodGroups.ts como función PURA auto-importada (estilo tripIndexes.ts: cabecera que explica que se extrae para test Vitest plano y que codifica el ORDEN CANÓNICO porque queryCollection('food').all() es alfabético por filename y los ficheros no tienen campo order, Pitfall 6). Exportar `groupFood(items: Food[])` que: agrupe por `group`, y devuelva un array de `{ group, groupIntro, items }` ORDENADO según un array constante CANÓNICO de los 7 textos de grupo exactos (los del bloque <interfaces>), conservando el orden de aparición dentro de cada grupo y tomando `groupIntro` del primer item del grupo que lo tenga. Si aparece un `group` no presente en el array canónico, colocarlo al final de forma determinista (cubrirlo en el test o anotarlo en el SUMMARY). Guard `?? []`. Sin dependencia Nuxt.

    Crear tests/unit/foodGroups.spec.ts (análogo dayLabel.spec.ts): con una lista de Food de prueba en orden alfabético-por-filename (incluir al menos un g-bar-* que en .all() saldría temprano pero canónicamente va último, y dos de Pasta), aseverar que groupFood devuelve los grupos en el orden CANÓNICO (no el de entrada), que los items dentro de un grupo conservan su orden, y que groupIntro se propaga del primer item que lo tenga. Vitest puro.

    Crear app/components/GastroCard.vue (gemelo de MonumentCard; cabecera citando index.html:5346-5360 + CERO CSS). `import type { Food } from '~~/shared/schemas'` + `defineProps<{ food: Food }>()`. Template: `div.gastro-card` con header (`span.gastro-card-badge :class="'badge-'+food.badgeKind"` con texto food.badge; name; address), `p.gastro-card-desc` con `<MDC :value="food.desc" unwrap="p" />`, `div.gastro-plato` con v-if + `<MDC :value="food.plato" unwrap="p" />`, footer (span con food.footer + `a.gastro-maps-link[target="_blank" rel="noopener"]` href `https://www.google.com/maps/search/?api=1&query=`+encodeURIComponent(food.mapsQuery); `span.gastro-itinerary-tag {{ food.itineraryTag }}` con v-if). Reproducir el markup exacto. Sin <style>.

    Crear app/components/GastroSection.vue (contenedor #gastronomia; análogo NavPills + el util). `defineProps<{ food: Food[], section?: { eyebrow: string, intro: string } }>()` (section = trip.sections.gastronomia, pasada por TripView). Template: `p.section-eyebrow {{ section.eyebrow }}` (v-if) + `h2.section-title` con el texto exacto de la sección (verificar contra index.html — puede ser texto estático de plantilla como las h4 de TheHero; reproducir verbatim) + `p.gastro-intro` con `<MDC :value="section.intro" unwrap="p" />` (v-if) + por cada grupo de `groupFood(food)`: `p.gastro-section-title {{ g.group }}` + (`p.gastro-intro` con `<MDC :value="g.groupIntro" unwrap="p" />` si g.groupIntro) + `div.gastro-grid` con `GastroCard v-for :food="item"` por g.items. Sin <style>.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm test:unit && pnpm typecheck && pnpm lint app/utils/foodGroups.ts tests/unit/foodGroups.spec.ts app/components/GastroCard.vue app/components/GastroSection.vue && grep -q "gastro-card-badge" app/components/GastroCard.vue && grep -q "gastro-section-title" app/components/GastroSection.vue && ! grep -lE "<style" app/components/GastroCard.vue app/components/GastroSection.vue</automated>
  </verify>
  <acceptance_criteria>
    - app/utils/foodGroups.ts exporta groupFood que ordena los grupos por un array constante canónico (los 7 textos exactos), conserva el orden de items dentro de grupo y propaga groupIntro
    - tests/unit/foodGroups.spec.ts asevera con entrada alfabética que la salida sale en orden canónico (g-bar-* último, no tercero) y pasa en Vitest puro
    - GastroCard.vue: div.gastro-card con gastro-card-badge (clase badge-+badgeKind), name, address, gastro-card-desc (<MDC unwrap="p">), gastro-plato condicional, footer con gastro-maps-link (encodeURIComponent + rel=noopener) e itinerary-tag condicional
    - GastroSection.vue: section-eyebrow + h2.section-title + gastro-intro de sección + por cada grupo p.gastro-section-title + gastro-grid de GastroCard; usa groupFood
    - Ni GastroCard ni GastroSection contienen <style> (grep negativo); greps positivos gastro-card-badge / gastro-section-title
    - pnpm test:unit verde (incl. foodGroups); pnpm typecheck exit 0; pnpm lint de los 4 ficheros limpio
  </acceptance_criteria>
  <done>El agrupado de gastronomía sale en orden canónico (Pitfall 6 resuelto y testeado); GastroCard/GastroSection reproducen el markup verbatim; CERO CSS; typecheck + lint + unit verdes.</done>
</task>

<task type="auto">
  <name>Task 2: ArtistCard unificado (artist/arquitectura/glossary) + ReservasSection + PracticaSection (UI-04, Pitfall 1)</name>
  <files>app/components/ArtistCard.vue, app/components/ReservasSection.vue, app/components/PracticaSection.vue</files>
  <read_first>
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-01-SUMMARY.md (la DECISIÓN de Pitfall 1: si ProseUl/ProseLi globales existen y aplican .detail-list a TODA lista MDC — clave para no romper las listas de .artist-section/practica)
    - index.html (líneas 5260-5333 — #reservas completo: reservas-confirmadas mesas/visitas + reservas-table con badges e is-done; ~5941-6094 — varias .artist-card art-*/arq-* con artist-avatar/name/artist-dates/artist-epithet/artist-section(<ul> SIN clase)/artist-trip seenIn; ~6202 — arq-glosario con arch-term ×10 <b>+<span>; 5825 — #practica con sections + media)
    - shared/schemas.ts (líneas 175-209 ArtistSchema discriminatedUnion (las 3 ramas + terms/seenIn/archLink); 212-245 ReservasSchema/PracticaSchema; 27 Link) — los tipos exactos
    - app/components/ThemeToggle.vue (análogo de lógica de ramas en script — aquí la ramificación por kind va en template con v-if que estrecha la unión)
    - app/components/MonumentCard.vue (Plan 02 — el patrón de prosa por secciones con <MDC> SIN unwrap, para las sections de artist; y dónde se renderiza .detail-list)
    - app/components/TheHero.vue (análogo de sección data-bound + <MDC> + v-for de párrafos, para Reservas/Practica)
    - app/assets/css/base.css (reglas .artist-card/.artist-avatar/.artist-dates/.artist-epithet/.artist-section/.artist-trip/.arq-glosario/.arch-term; .reservas-box/.reservas-confirmadas/.rc-when/.reservas-box-header/.reservas-table/.reservas-badge.badge-urgent/.badge-done/.badge-rec/tr.is-done; .practica reglas + media) — clases LOCKED
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pattern 5 ArtistCard unificado/ReservasSection/PracticaSection; §Pitfall 1)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§ArtistCard.vue; §ReservasSection.vue; §PracticaSection.vue)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario ▸ ArtistCard/ReservasSection/PracticaSection; matriz unwrap)
  </read_first>
  <action>
    Crear app/components/ArtistCard.vue — UN SOLO componente que ramifica por `kind` (NO tres). `import type { Artist } from '~~/shared/schemas'` + `defineProps<{ artist: Artist }>()` (o `row`). Template con ramas estrechadas:
    - `v-if="artist.kind === 'glossary'"`: `div.artist-card.arq-glosario` (verificar la clase raíz exacta del glosario contra index.html ~6202) con `div.arch-term` por cada `artist.terms` (`<b>{{ term.term }}</b>` + `<span>` con `<MDC :value="term.def" unwrap="p" />`), más el avatar/name/header del glosario verbatim.
    - `v-else` (artist y arquitectura comparten estructura): `div.artist-card` con la clase de variante (art-*/arq-* — verificar cómo se expresa) > `div.artist-avatar {{ artist.avatar }}` + name + `div.artist-dates {{ artist.dates }}` + `div.artist-epithet {{ artist.epithet }}` + `div.artist-section` por cada `artist.sections` (h4 + `<MDC :value="s.body" />` SIN unwrap, mismo criterio que MonumentCard; RESPETAR la decisión de Pitfall 1 del Plan 01 sobre las listas) + `div.artist-trip` ✦ con `seenIn` (Links ✦ a #monumento, enlaces planos). Reproducir el markup verbatim. Sin <style>. IMPORTANTE: si el override global ProseUl del Plan 01 convierte las listas de .artist-section en .detail-list (que el original NO tiene), DETENERSE y anotar el conflicto en el SUMMARY (no tocar datos F2).

    Crear app/components/ReservasSection.vue (análogo TheHero). `import type { Reference } from '~~/shared/schemas'` + recibir la entrada de reservas por prop (p.ej. `defineProps<{ reservas: Reference }>()`; el slug discrimina). Template: `p.section-eyebrow {{ reservas.eyebrow }}` + `h2.section-title {{ reservas.title }}` (o el texto exacto) + `p.gastro-intro` con `<MDC :value="reservas.intro" unwrap="p" />` + `div.reservas-box`: `div.reservas-confirmadas` con `h4` + dos `ul` (una por group 'mesas' y otra 'visitas') con `li` (`span.rc-when {{ c.when }}` + `<MDC :value="c.text" unwrap="p" />`); `div.reservas-box-header`; `table.reservas-table` con `tr :class="{ 'is-done': r.isDone }"` por cada `reservas.table`, `td` con (`a :href="'#'+r.ref"` si r.ref, si no `{{ r.name }}`) + `span.reservas-badge :class="'badge-'+r.badgeKind"` con v-if (r.badgeKind/r.badge) + `td` con `<MDC :value="r.desc" unwrap="p" />`. Reproducir verbatim index.html:5260-5333. Sin <style>.

    Crear app/components/PracticaSection.vue (análogo TheHero). Recibir la entrada de practica por prop (`defineProps<{ practica: Reference }>()`). Template: `p.section-eyebrow {{ practica.eyebrow }}` + `h2.section-title {{ practica.title }}` + intro con `<MDC unwrap="p">` + `sections` por v-for (h4 + `<MDC :value="s.body" />` SIN unwrap, respetando la decisión de Pitfall 1 si llevan listas) + el bloque de media por `category` (libros/peliculas/series/playlist) con cada item Markdown-inline (`<MDC unwrap="p">`). Reproducir verbatim index.html:5825 (leer el markup exacto del bloque de media y los títulos de categoría). Sin <style>.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/ArtistCard.vue app/components/ReservasSection.vue app/components/PracticaSection.vue && grep -q "arch-term" app/components/ArtistCard.vue && grep -q "reservas-table" app/components/ReservasSection.vue && grep -q "section-eyebrow" app/components/PracticaSection.vue && ! grep -lE "<style" app/components/ArtistCard.vue app/components/ReservasSection.vue app/components/PracticaSection.vue</automated>
  </verify>
  <acceptance_criteria>
    - ArtistCard.vue es UN solo componente con v-if artist.kind === 'glossary' (arq-glosario con arch-term <b>+<span>) y v-else para artist/arquitectura (artist-avatar/name/dates/epithet + artist-section con <MDC> SIN unwrap + artist-trip seenIn); la prosa de artist NO recibe .detail-list salvo que la decisión del Plan 01 lo justifique (si conflicto, anotado en SUMMARY)
    - ReservasSection.vue: reservas-box con reservas-confirmadas (ul mesas + ul visitas, rc-when + <MDC unwrap="p">) y reservas-table con tr.is-done condicional, reservas-badge badge-urgent/done/rec condicional, desc vía <MDC unwrap="p">
    - PracticaSection.vue: section-eyebrow + h2 + intro + sections(<MDC> SIN unwrap) + media por category (libros/peliculas/series/playlist) con items inline
    - Ninguno de los tres contiene <style> (grep negativo); greps positivos arch-term / reservas-table / section-eyebrow
    - pnpm typecheck exit 0 (con el cast a tipos zod de useTrip para las uniones); pnpm lint de los tres limpio
  </acceptance_criteria>
  <done>ArtistCard unifica las 3 ramas por kind, ReservasSection y PracticaSection reproducen su markup verbatim, las listas de prosa respetan la decisión de Pitfall 1; CERO CSS; typecheck + lint verdes. La verificación E2E del DOM la cierra el Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo data (Food/Artist/Reference) → componentes de sección | Datos zod-validados versionados (uniones discriminadas). Sin input de usuario. |
| `<MDC>` sobre desc/intro/sections/text → DOM | Prosa Markdown versionada y confiable; sin v-html sobre input externo. |
| gastro-maps-link / reservas-table / seenIn href | Enlaces a fragmentos #id (validados) o Google Maps construidos desde mapsQuery; intercepción es F5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | gastro-maps-link href (target=_blank) | accept | href con encodeURIComponent sobre mapsQuery (dato validado); rel="noopener" VERBATIM (anti-tabnabbing). Sin URL controlada por usuario. |
| T-04-08 | Tampering/Elevation | `<MDC>` sobre desc/intro/sections de food/artist/reference | accept | MDC renderiza SOLO prosa versionada en el repo (RESEARCH §Security Domain); inline con unwrap="p" o párrafos, sin v-html. Reevaluar solo si v2 admite contenido de terceros. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalación en este plan (RESEARCH §Package Legitimacy Audit: F4 no instala nada). Sin paquete [ASSUMED]/[SUS]. |

**Conclusión:** Sin amenazas de severidad alta. Componentes presentacionales sobre datos confiables; enlaces con noopener verbatim; MDC sobre prosa confiable versionada.
</threat_model>

<verification>
- pnpm test:unit — foodGroups (orden canónico) + los 9 de isVisible + los de F3, verdes.
- pnpm typecheck — TS estricto sobre los 5 componentes + el util (con el cast de useTrip para las uniones discriminadas).
- pnpm lint sobre los 7 ficheros — limpio.
- grep gates: groupFood en GastroSection; arch-term en ArtistCard; reservas-table en ReservasSection; section-eyebrow en PracticaSection; AUSENCIA de <style> en los 5 componentes.
- El render E2E (reservas-table+badges+is-done, gastro-cards agrupadas EN ORDEN canónico, artist-cards con artist-section, arq-glosario con 10 arch-term, practica media) se asevera en el Plan 05 (tests/parity/render-reference.spec.ts, autocontenido) — sin rebaselinar el golden (D-08).
</verification>

<success_criteria>
- El agrupado de gastronomía sale en el orden canónico del index.html (Pitfall 6), verificado por test unitario, y GastroCard/GastroSection reproducen el markup verbatim (UI-04).
- ArtistCard es un solo componente que unifica artist/arquitectura/glossary por kind (arq-glosario con terms) (UI-04).
- ReservasSection (confirmadas + tabla con badges/is-done) y PracticaSection (prosa + media) reproducen su markup verbatim (UI-04).
- Las listas de prosa de artist/practica respetan la decisión de Pitfall 1 del Plan 01; sin `<style scoped>` en ningún componente.
</success_criteria>

<output>
Create `.planning/phases/04-render-de-contenido-modos-de-ritmo/04-04-SUMMARY.md` when done.
</output>
