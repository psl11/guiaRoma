---
phase: 04-render-de-contenido-modos-de-ritmo
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/components/Timeline.vue
  - app/components/TimelineStop.vue
  - app/components/TimelineTransport.vue
  - app/components/TimelineMeta.vue
  - app/components/TimelineFood.vue
  - app/components/TimelineReservation.vue
autonomous: true
requirements: [UI-03, FEAT-06]
user_setup: []
must_haves:
  truths:
    - "Timeline itera day.timeline[] en el ORDEN del dato y despacha a UN COMPONENTE POR KIND (stop/transport/meta/food/reservation) vía <component :is> — mapea 1:1 el discriminatedUnion (D-09)"
    - "Solo TimelineStop y TimelineTransport consultan isVisible y aplican :class={'tl-hidden': !isVisible(row.pace)}; TimelineMeta/Food/Reservation NUNCA filtran por ritmo (Pitfall 4)"
    - "Cada componente reproduce su markup+clases verbatim del index.html (tl-item con disabled/reserved-event, tl-transport con taxi/walk/train/metro/metro-b, tl-meta ok/warn/plain, tl-food, tl-resv-meta)"
    - "Se conserva :data-pace='row.pace' en tl-item/tl-transport por paridad de atributo"
    - "Los fragmentos inline (tl-note, desc/meta de transporte, tl-meta-item, tl-food-desc, footnotes) van con <MDC unwrap='p'>; ningún componente lleva <style scoped>"
  artifacts:
    - path: "app/components/Timeline.vue"
      provides: "Dispatcher del timeline por kind (UI-03)"
      contains: "component"
    - path: "app/components/TimelineStop.vue"
      provides: ".tl-item, se filtra por ritmo (UI-03/FEAT-06)"
      contains: "tl-item"
    - path: "app/components/TimelineTransport.vue"
      provides: ".tl-transport (variantes), se filtra por ritmo"
      contains: "tl-transport"
    - path: "app/components/TimelineMeta.vue"
      provides: ".tl-meta, NO se filtra por ritmo"
      contains: "tl-meta"
    - path: "app/components/TimelineFood.vue"
      provides: ".tl-food, NO se filtra por ritmo"
      contains: "tl-food"
    - path: "app/components/TimelineReservation.vue"
      provides: ".tl-resv-meta, NO se filtra por ritmo"
      contains: "tl-resv-meta"
  key_links:
    - from: "app/components/Timeline.vue"
      to: "TimelineStop/Transport/Meta/Food/Reservation"
      via: "<component :is=COMPONENT_BY_KIND[row.kind]>"
      pattern: "component"
    - from: "app/components/TimelineStop.vue"
      to: "useTripModes().isVisible"
      via: ":class binding tl-hidden"
      pattern: "isVisible"
---

<objective>
Construir la familia `Timeline` — el dispatcher `Timeline.vue` (itera `day.timeline[]` y despacha por `kind` con `<component :is>`) más los 5 componentes hoja, uno por cada rama del `discriminatedUnion('kind')` del esquema (D-09): `TimelineStop` / `TimelineTransport` / `TimelineMeta` / `TimelineFood` / `TimelineReservation`. Es UI-03 (timeline idéntico a hoy) y donde se aplica el filtrado por ritmo de FEAT-06 con la regla EXACTA de Pitfall 4: **solo `.tl-item` y `.tl-transport` se ocultan por ritmo** (vía `:class="{ 'tl-hidden': !isVisible(row.pace) }"` consumiendo `useTripModes` del Plan 01); `.tl-meta`/`.tl-food`/`.tl-resv-meta` NUNCA se filtran (aunque el esquema lleve `pace` en `food`).

Purpose: El `Timeline` lo instancia `DaySection` (Plan 05, vía TripView) por cada día. Construir la familia completa en un plan mantiene el diff enfocado (precedente F3) y concentra el riesgo de Pitfall 4 (filtrado en las filas equivocadas) en los componentes que lo poseen: el filtrado vive en `TimelineStop`/`TimelineTransport`, NUNCA en el dispatcher ni en los otros tres. El dispatch por `kind` mapea 1:1 el `discriminatedUnion('kind')` de `shared/schemas.ts` (5 kinds), así que cada componente recibe su rama estrechada como `row` y transcribe su markup verbatim. La verificación E2E (los 5 kinds presentes con su markup + orden + el filtrado por ritmo) vive en el Plan 05 (`render-timeline.spec.ts` + `modes.spec.ts`); aquí cada componente se construye contra su contrato con typecheck/lint + gate de no-CSS.

Output: nuevos `app/components/Timeline.vue`, `TimelineStop.vue`, `TimelineTransport.vue`, `TimelineMeta.vue`, `TimelineFood.vue`, `TimelineReservation.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md

<interfaces>
Tipos de prop — las 5 ramas de TimelineRow (shared/schemas.ts:85-123, discriminatedUnion('kind')):
  stop (86-96):        { kind:'stop', pace, time, title, ref?, disabled, reservedEvent, tag?, note? }
  transport (97-104):  { kind:'transport', pace, variant?: 'taxi'|'walk'|'train', header, modes: TransportMode[], footnote? }
  meta (105-111):      { kind:'meta', items: { level:'ok'|'warn'|'plain', text }[] }   (NO lleva pace)
  food (112-118):      { kind:'food', pace, header, entries: FoodEntry[], footnote? }
  reservation (119-122):{ kind:'reservation', text }
  TransportMode (68-74): { icon, recommended, desc, tag?, meta? }
  FoodEntry (75-83):   { ref?, href?, name, reserved, badge?, time?, desc }
  Day['timeline'] = TimelineRow[]   (el tipo de la prop del dispatcher)

Dispatch (RESEARCH §Pattern 4 + PATTERNS §Timeline.vue) — análogo NavPills.vue (v-for sobre array tipado):
  defineProps<{ rows: Day['timeline'] }>()
  const COMPONENT_BY_KIND = { stop:'TimelineStop', transport:'TimelineTransport', meta:'TimelineMeta', food:'TimelineFood', reservation:'TimelineReservation' } as const
  template: div.timeline > <component :is="COMPONENT_BY_KIND[row.kind]" v-for="(row,i) in rows" :key="i" :row="row" />
  <component :is="'TimelineStop'"> resuelve por string para auto-imports (A5).

Filtrado por ritmo (Pitfall 4 — CRÍTICO):
  SOLO TimelineStop y TimelineTransport: const { isVisible } = useTripModes() (auto-importado)
    elemento raíz :class="{ 'tl-hidden': !isVisible(row.pace), ... }" + :data-pace="row.pace"
  TimelineMeta / TimelineFood / TimelineReservation: NO importan useTripModes, NO consultan isVisible.
  (Mapea index.html:6521 querySelectorAll('.tl-item[data-pace], .tl-transport[data-pace]') — solo esas dos clases.)

Markup VERBATIM (index.html — leer cada rango antes de transcribir):
  Timeline contenedor: 2403-2446 (div.timeline)
  TimelineStop: 2404 (+ .disabled 2406, .fixed-event, .reserved-event 2432) → tl-time + (a.tl-title[href=#ref] | span.tl-title.disabled) + tl-tag? + tl-note?
  TimelineTransport: 2405 (taxi), 2411 (walk), train (1×), metro/metro-b → header + tl-transport-modes(.tl-transport-mode con .recommended?, icon, desc+tag, meta) + tl-transport-footnote?; variante de clase taxi/walk/train/metro/metro-b
  TimelineMeta: 2407/2410/2413 → tl-meta-item con .ok/.warn/(plain); 1-2 items
  TimelineFood: 2420-2426 / 2434-2444 → tl-food-header + tl-food-list(tl-food-item con .reserved?, a.tl-food-name, tl-resv-badge/tl-food-time, tl-food-desc) + tl-food-foot
  TimelineReservation: 2433 → banda verde div.tl-resv-meta "✅ <strong>…</strong> — …" (+ tl-resv-tel mono si hay tel)

unwrap (UI-SPEC matriz): tl-note, tl-food-desc, tl-meta-item, footnotes, transport desc/meta → <MDC unwrap="p"> (fragmentos inline).
Patrón LOCKED F3: un componente por concern, markup+clases verbatim, CERO CSS, sin <style scoped> (data-v-* rompería .tl-item[data-pace], body.modo-resumen .tl-transport, etc.).
eslint: el nombre 'Timeline' (una palabra) ya está en la allowlist de eslint.config.mjs (Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeline dispatcher + filas que SE FILTRAN por ritmo (TimelineStop, TimelineTransport) (UI-03/FEAT-06)</name>
  <files>app/components/Timeline.vue, app/components/TimelineStop.vue, app/components/TimelineTransport.vue</files>
  <read_first>
    - index.html (líneas 2403-2446 — el contenedor .timeline y el orden de filas de un día; 2404/2406 tl-item + .disabled; 2432 tl-item.reserved-event; un .fixed-event (buscar el primero); 2405/2408 tl-transport.taxi; 2411 tl-transport.walk; las apariciones de .train/.metro/.metro-b para sus variantes de clase)
    - shared/schemas.ts (líneas 85-104 — las ramas stop y transport del discriminatedUnion + TransportMode 68-74; el tipo Day['timeline'] para la prop del dispatcher)
    - app/components/NavPills.vue (el análogo de v-for sobre array tipado + markup verbatim por item)
    - app/composables/useTripModes.ts (Plan 01 — la función isVisible que stop/transport consumen; confirmar la firma isVisible(row.pace))
    - app/assets/css/leaflet.css (línea 731 .tl-hidden display:none !important; y las reglas .tl-transport.taxi/.walk/.train/.metro/.metro-b con sus colores de borde) — el contrato a reproducir
    - app/assets/css/base.css (las reglas .timeline, .tl-item, .tl-time, .tl-title(.disabled), .tl-tag, .tl-note, .tl-transport*, .tl-transport-mode(.recommended)) — clases LOCKED, sin scoping
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pattern 4 dispatch + filtrado; §Pitfall 4 matriz/filas; nota :data-pace por paridad)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§Timeline.vue; §TimelineStop.vue; §TimelineTransport.vue)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario ▸ Timeline/TimelineStop/TimelineTransport; matriz unwrap; Gotchas Pitfall 4)
  </read_first>
  <action>
    Crear app/components/Timeline.vue (cabecera estilo F3 citando index.html:2403-2446 + CERO CSS). `import type { Day } from '~~/shared/schemas'` + `defineProps<{ rows: Day['timeline'] }>()`. Definir `const COMPONENT_BY_KIND = { stop:'TimelineStop', transport:'TimelineTransport', meta:'TimelineMeta', food:'TimelineFood', reservation:'TimelineReservation' } as const`. Template: `div.timeline` con `<component :is="COMPONENT_BY_KIND[row.kind]" v-for="(row,i) in rows" :key="i" :row="row" />`. El dispatcher NO importa useTripModes y NO reordena (orden = dato). Sin <style>.

    Crear app/components/TimelineStop.vue. Prop `row` tipada como la rama kind:'stop' de TimelineRow (estrechar el tipo desde Day['timeline'] o tipar con un Extract). `const { isVisible } = useTripModes()` (auto-importado). Template: elemento raíz `div.tl-item` con `:class="{ 'tl-hidden': !isVisible(row.pace), 'reserved-event': row.reservedEvent, disabled: row.disabled }"` y `:data-pace="row.pace"` (paridad de atributo). Dentro: `span.tl-time {{ row.time }}`; el título = si row.ref → `a.tl-title :href="'#'+row.ref" {{ row.title }}` (enlace plano, intercepción F5), si no → `span.tl-title :class="{ disabled: row.disabled }" {{ row.title }}`; `span.tl-tag {{ row.tag }}` con v-if; `div.tl-note` con v-if + `<MDC :value="row.note" unwrap="p" />`. Reproducir el markup exacto de index.html:2404/2406/2432 (incluida la variante .fixed-event si el dato la lleva — verificar el campo/clase contra el original). Sin <style>.

    Crear app/components/TimelineTransport.vue. Prop `row` = rama kind:'transport'. `const { isVisible } = useTripModes()`. Template: elemento raíz `div.tl-transport` con la clase de variante (`row.variant` → taxi/walk/train; y metro/metro-b si el dato/markup lo usa — verificar cómo se expresa la variante metro contra index.html y el enum del esquema), `:class="{ 'tl-hidden': !isVisible(row.pace) }"` (combinar con la clase de variante), `:data-pace="row.pace"`. Dentro: header `{{ row.header }}`; `div.tl-transport-modes` con `div.tl-transport-mode` por cada `row.modes` (`:class="{ recommended: mode.recommended }"`, icon, desc vía `<MDC :value="mode.desc" unwrap="p" />`, tag con v-if, meta vía `<MDC unwrap="p">` con v-if); footnote con v-if + `<MDC unwrap="p">`. Reproducir el markup de index.html:2405/2411. Sin <style>.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/Timeline.vue app/components/TimelineStop.vue app/components/TimelineTransport.vue && grep -q "COMPONENT_BY_KIND" app/components/Timeline.vue && grep -q "isVisible" app/components/TimelineStop.vue && grep -q "isVisible" app/components/TimelineTransport.vue && grep -q "data-pace" app/components/TimelineStop.vue && ! grep -lE "<style" app/components/Timeline.vue app/components/TimelineStop.vue app/components/TimelineTransport.vue</automated>
  </verify>
  <acceptance_criteria>
    - Timeline.vue contiene div.timeline + <component :is> con COMPONENT_BY_KIND mapeando los 5 kinds; itera rows con v-for sin reordenar; NO importa useTripModes
    - TimelineStop.vue: div.tl-item con :data-pace y :class que incluye 'tl-hidden': !isVisible(row.pace); título condicional a.tl-title (con #ref) vs span.tl-title.disabled; tl-tag/tl-note condicionales con <MDC unwrap="p"> en note
    - TimelineTransport.vue: div.tl-transport con la clase de variante (taxi/walk/train/metro/metro-b) + 'tl-hidden': !isVisible(row.pace) + :data-pace; tl-transport-mode por modes con recommended condicional y <MDC unwrap="p"> en desc/meta
    - Tanto TimelineStop como TimelineTransport consultan isVisible (greps positivos); Timeline.vue NO
    - Ninguno de los 3 ficheros contiene <style> (grep -lE negativo)
    - pnpm typecheck exit 0; pnpm lint de los 3 limpio (incl. que el nombre Timeline pasa por la allowlist del Plan 01)
  </acceptance_criteria>
  <done>Timeline despacha por kind sin reordenar; TimelineStop/TimelineTransport reproducen su markup verbatim, conservan :data-pace y SE FILTRAN por ritmo vía isVisible; CERO CSS; typecheck + lint verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Filas que NO se filtran por ritmo — TimelineMeta, TimelineFood, TimelineReservation (UI-03, Pitfall 4)</name>
  <files>app/components/TimelineMeta.vue, app/components/TimelineFood.vue, app/components/TimelineReservation.vue</files>
  <read_first>
    - index.html (líneas 2407/2410/2413 — tl-meta con tl-meta-item ok/warn/plain; 2420-2426 y 2434-2444 — tl-food completo: tl-food-header, tl-food-list, tl-food-item con .reserved, a.tl-food-name, tl-resv-badge/tl-food-time, tl-food-desc, tl-food-foot; 2433 — tl-resv-meta banda verde con ✅ + strong + tl-resv-tel)
    - shared/schemas.ts (líneas 105-122 — las ramas meta, food y reservation; FoodEntry 75-83) — los tipos exactos de las props
    - app/components/NavPills.vue (análogo de markup verbatim por item)
    - app/assets/css/base.css y leaflet.css (las reglas .tl-meta/.tl-meta-item(.ok/.warn), .tl-food*/.tl-food-item(.reserved)/.tl-food-name/.tl-resv-badge/.tl-food-time/.tl-food-desc/.tl-food-foot, .tl-resv-meta/.tl-resv-tel) — clases LOCKED; los colores literales semánticos (#5a7a3a ok, #c47a2a warn, #2e7d4f banda verde) se reproducen vía esas clases
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pitfall 4 — estos tres NUNCA consultan isVisible; matriz unwrap para los fragmentos inline)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§TimelineMeta/TimelineFood/TimelineReservation — la tabla con markup/tipo/ritmo)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario ▸ TimelineMeta/Food/Reservation; matriz unwrap; Gotchas Pitfall 4)
  </read_first>
  <action>
    Crear los tres componentes (cabecera estilo F3 citando su rango de index.html + CERO CSS + la nota explícita "NO se filtra por ritmo, Pitfall 4"). NINGUNO importa useTripModes ni consulta isVisible.

    app/components/TimelineMeta.vue: prop `row` = rama kind:'meta'. Template: `div.tl-meta` con `div.tl-meta-item` (o el elemento exacto del original) por cada `row.items`, aplicando `:class` con la clase del `item.level` ('ok'/'warn'; 'plain' sin clase extra) y el texto vía `<MDC :value="item.text" unwrap="p" />`. Reproducir index.html:2407/2410/2413 (1-2 items). Sin <style>.

    app/components/TimelineFood.vue: prop `row` = rama kind:'food' (con `pace` en el esquema, pero NO se usa para filtrar). Template: `div.tl-food` con `div.tl-food-header {{ row.header }}` + `div.tl-food-list` con `div.tl-food-item` por cada `row.entries` (`:class="{ reserved: entry.reserved }"`; el nombre = `a.tl-food-name :href` usando entry.ref→'#'+ref o entry.href (enlace plano/externo; conservar target/rel del original si lo hay); `span.tl-resv-badge {{ entry.badge }}` o `span.tl-food-time {{ entry.time }}` según corresponda con v-if; `div.tl-food-desc` con `<MDC :value="entry.desc" unwrap="p" />`) + `div.tl-food-foot` con v-if/footnote vía `<MDC unwrap="p">`. Reproducir index.html:2420-2426/2434-2444. Sin <style>.

    app/components/TimelineReservation.vue: prop `row` = rama kind:'reservation' ({text}). Template: `div.tl-resv-meta` reproduciendo la banda verde de index.html:2433 — el ✅ es literal en el markup; el texto (con `<strong>`/em/tl-resv-tel) va vía `<MDC :value="row.text" unwrap="p" />` (verificar contra el original que el ✅ literal + el MDC del resto reproducen exactamente la banda, incluido el tl-resv-tel mono si aplica). Sin <style>.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/TimelineMeta.vue app/components/TimelineFood.vue app/components/TimelineReservation.vue && grep -q "tl-meta" app/components/TimelineMeta.vue && grep -q "tl-food" app/components/TimelineFood.vue && grep -q "tl-resv-meta" app/components/TimelineReservation.vue && ! grep -lE "isVisible|useTripModes|<style" app/components/TimelineMeta.vue app/components/TimelineFood.vue app/components/TimelineReservation.vue</automated>
  </verify>
  <acceptance_criteria>
    - TimelineMeta.vue: div.tl-meta con tl-meta-item por items y la clase del level (ok/warn/plain), texto vía <MDC unwrap="p">
    - TimelineFood.vue: div.tl-food con tl-food-header + tl-food-list (tl-food-item con reserved condicional, a.tl-food-name, tl-resv-badge/tl-food-time condicionales, tl-food-desc vía <MDC unwrap="p">) + tl-food-foot condicional
    - TimelineReservation.vue: div.tl-resv-meta con el ✅ literal + <MDC unwrap="p"> sobre row.text
    - NINGUNO de los tres contiene isVisible, useTripModes ni <style> (grep -lE negativo) — confirma Pitfall 4
    - pnpm typecheck exit 0; pnpm lint de los tres limpio
  </acceptance_criteria>
  <done>TimelineMeta/Food/Reservation reproducen su markup verbatim y NO consultan isVisible (nunca se filtran por ritmo, Pitfall 4); CERO CSS; typecheck + lint verdes. La verificación E2E de los 5 kinds + filtrado la cierra el Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo data (Day['timeline']) → componentes Timeline | Las filas son datos zod-validados versionados (discriminatedUnion). Sin input de usuario. |
| `<MDC>` sobre note/desc/footnote → DOM | Fragmentos Markdown versionados y confiables, renderizados inline con unwrap="p"; sin v-html sobre input externo. |
| tl-food-name / tl-title href | Enlaces a fragmentos #id (datos validados) o URLs externas del dato; la intercepción de navegación es F5. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | tl-food-name href (posible externo, target=_blank) | accept | href proviene de entry.ref (#id validado) o entry.href (dato validado del repo); conservar rel="noopener" VERBATIM donde el original lo tenga (anti-tabnabbing). Sin URL controlada por usuario. |
| T-04-06 | Tampering/Elevation | `<MDC>` sobre row.note/desc/text | accept | MDC renderiza SOLO prosa versionada en el repo (RESEARCH §Security Domain); inline con unwrap="p", sin v-html. Reevaluar solo si v2 admite contenido de terceros. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalación en este plan (RESEARCH §Package Legitimacy Audit: F4 no instala nada). Sin paquete [ASSUMED]/[SUS]. |

**Conclusión:** Sin amenazas de severidad alta. Componentes presentacionales sobre datos confiables; enlaces con noopener verbatim; MDC inline sobre prosa confiable.
</threat_model>

<verification>
- pnpm typecheck — TS estricto sobre el dispatcher + las 5 ramas estrechadas.
- pnpm lint sobre los 6 componentes — limpio (el nombre Timeline pasa por la allowlist del Plan 01).
- grep gates: COMPONENT_BY_KIND en Timeline; isVisible en stop+transport y AUSENTE en meta/food/reservation (Pitfall 4); :data-pace en stop; AUSENCIA de <style> en los 6.
- El render E2E (los 5 kinds presentes con su markup, orden = dato, reserved-event/disabled/variantes de transporte) y el filtrado por ritmo (pace=slow oculta tl-item/tl-transport medium+slow-only pero NUNCA tl-food/tl-meta) se aseveran en el Plan 05 (tests/parity/render-timeline.spec.ts + modes.spec.ts).
</verification>

<success_criteria>
- Timeline despacha `day.timeline[]` por `kind` con `<component :is>` (mapea el discriminatedUnion 1:1), en el orden del dato (UI-03).
- Cada uno de los 5 componentes reproduce su markup+clases verbatim del index.html con CERO CSS (UI-03).
- Solo TimelineStop y TimelineTransport se filtran por ritmo (`tl-hidden` vía isVisible); TimelineMeta/Food/Reservation NUNCA (Pitfall 4, FEAT-06); se conserva `:data-pace` por paridad.
- Los fragmentos inline van con `<MDC unwrap="p">`; sin `<style scoped>`.
</success_criteria>

<output>
Create `.planning/phases/04-render-de-contenido-modos-de-ritmo/04-03-SUMMARY.md` when done.
</output>
