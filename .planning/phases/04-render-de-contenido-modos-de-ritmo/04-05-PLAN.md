---
phase: 04-render-de-contenido-modos-de-ritmo
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - app/components/TheHero.vue
  - app/components/DaySection.vue
  - app/components/TripView.vue
  - tests/parity/render-cards.spec.ts
  - tests/parity/render-timeline.spec.ts
  - tests/parity/render-reference.spec.ts
  - tests/parity/modes.spec.ts
autonomous: false
requirements: [UI-02, UI-03, UI-04, FEAT-06, FEAT-07, FEAT-08]
user_setup: []
must_haves:
  truths:
    - "TheHero CONSUME el composable para cablear sus controles ya montados a useTripModes (pace-btn :class .active + @click; light/resumen :aria-pressed + @click) SIN reestructurar el DOM del #inicio; el 1er pace-btn pierde el .active literal y queda solo el binding (D-05)"
    - "DaySection resuelve day.cards[]→monById→MonumentCard en el ORDEN del dato (nunca reordenar) y monta light-banner/eyebrow/day-header/day-stats/dia-ligera/Timeline/cards-list"
    - "TripView rellena las 11 secciones vacías (5 DaySection + reservas/gastronomia/practica/arte/arquitectura) pasando datos por props (un solo useTrip); #mapa sigue vacío (F7)"
    - "render-cards/timeline/reference aseveran el DOM del / construido (detail-photo resuelto, detail-list, dropcap, los 5 kinds del timeline, reservas-table+badges, gastro agrupado en orden, arq-glosario)"
    - "modes (E2E del comportamiento de los 3 modos) asevera la matriz de ritmo EXACTA, caminar-menos (fuerza slow, no revierte, dia-ligera), resumen (set de ocultos), persistencia (roma-pace/light/resumen) y el micro-flash; tolera SOLO el error de hidratación de color-mode (D-07)"
    - "Un humano confirma que fichas/timeline/secciones se ven idénticas al index.html con imágenes reales cargando (D-06); no se rebaselina el golden (D-08)"
  artifacts:
    - path: "app/components/DaySection.vue"
      provides: "contenedor de día: header/stats/dia-ligera/Timeline/cards-list (UI-03)"
      contains: "cards-list"
    - path: "app/components/TripView.vue"
      provides: "las 11 secciones enchufadas desde datos (UI-02/03/04)"
      contains: "DaySection"
    - path: "app/components/TheHero.vue"
      provides: "controles del #inicio cableados a useTripModes (FEAT-06/07/08)"
      contains: "useTripModes"
    - path: "tests/parity/render-cards.spec.ts"
      provides: "DOM de MonumentCard sobre el / construido (SC#1)"
    - path: "tests/parity/render-timeline.spec.ts"
      provides: "DOM del timeline (5 kinds + orden) (SC#2)"
    - path: "tests/parity/render-reference.spec.ts"
      provides: "DOM de las secciones de referencia (SC#3)"
    - path: "tests/parity/modes.spec.ts"
      provides: "E2E de los 3 modos + persistencia + micro-flash (SC#4)"
  key_links:
    - from: "app/components/DaySection.vue"
      to: "app/components/MonumentCard.vue (vía monById)"
      via: "day.cards.map(slug => monById.get(slug)) en orden"
      pattern: "MonumentCard"
    - from: "app/components/TripView.vue"
      to: "DaySection + Reservas/Gastro/Practica/ArtistCard sections"
      via: "11 secciones con datos por props (un solo useTrip)"
      pattern: "DaySection"
    - from: "app/components/TheHero.vue"
      to: "app/composables/useTripModes.ts"
      via: "binding :class/@click/:aria-pressed sin tocar el DOM"
      pattern: "useTripModes"
    - from: "tests/parity/modes.spec.ts"
      to: "pace-btn / body.light-mode / .tl-hidden"
      via: "click controles → asserts de matriz + persistencia + flash"
      pattern: "tl-hidden"
---

<objective>
Cerrar la Fase 4: (1) **cablear** los controles del #inicio que `TheHero` ya monta a `useTripModes` (FEAT-06/07/08) sin reestructurar el DOM; (2) construir `DaySection` (contenedor de día) y **enchufar** las 11 secciones vacías de `TripView` con el render data-driven de los Planes 02-04; (3) **PROBAR** la fase con los 4 specs Playwright autocontenidos (DOM de fichas/timeline/referencia + comportamiento de los 3 modos) y un **sign-off humano** de paridad visual con imágenes reales cargando. Esta es la unión que hace visibles UI-02/03/04 y la que demuestra SC#1-SC#4.

Purpose: Tras los Planes 01-04 existen el composable, los componentes globales MDC, MonumentCard, la familia Timeline y las secciones de referencia, pero nada los renderiza. Aquí `TheHero` consume `useTripModes` (cambiando `class="pace-btn active"`→`class="pace-btn" :class="{active}"` en el 1er botón, añadiendo `@click`/`:aria-pressed` reactivos — D-05, sin tocar la estructura del #inicio que la paridad de F3 fijó); `DaySection` resuelve `day.cards[]` contra `monById` en el orden del dato y monta el día completo; `TripView` amplía su destructuring de `useTrip` (monById/food/artists/reference) y rellena las 11 anclas pasando datos por props (un solo `useTrip`, mismo patrón que `Topbar :days`), dejando `#mapa` vacío (F7). RESEARCH/VALIDATION nombran los specs de mayor valor: el DOM que verifica que `:detail-photo` resuelve a un `.detail-photo > img` real (no a texto sin renderizar) y la matriz E2E de ritmo/light/resumen. Los specs son AUTOCONTENIDOS (mirror de `shell.spec.ts`/`theme.spec.ts`: build+serve propio sobre `.output/public` bajo /guiaRoma/), NO usan el webServer de `playwright.config.ts` (que sirve el index.html VIVO del golden), y NO tocan `golden.spec.ts` ni rebaselinan (D-08). El checkpoint humano es la barra de paridad intermedia con imágenes reales (D-06), porque el fallback de imagen (F7) y el pixel-diff total (F8) van después.

Output: modificados `app/components/TheHero.vue` + `app/components/TripView.vue`; nuevos `app/components/DaySection.vue`, `tests/parity/render-cards.spec.ts`, `render-timeline.spec.ts`, `render-reference.spec.ts`, `modes.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-VALIDATION.md

<interfaces>
TheHero (MODIFICAR — D-05; markup F3 en app/components/TheHero.vue, NO reestructurar el #inicio):
  Añadir al <script setup>: const { pace, light, resumen } = useTripModes()
  pace-btn 1º (hoy class="pace-btn active", líneas 67-69): → class="pace-btn" :class="{ active: pace === 'optimistic' }" @click="pace = 'optimistic'"  (quitar el active literal o Vue lo mergea y queda siempre active)
  pace-btn 2º (neutral): :class="{ active: pace === 'neutral' }" @click="pace = 'neutral'"
  pace-btn 3º (slow): :class="{ active: pace === 'slow' }" @click="pace = 'slow'"
  #light-toggle (líneas 93-97, hoy aria-pressed="false"): :aria-pressed="light" @click="light = !light"   (NO revertir pace al desactivar — lo hace el watch del composable)
  #resumen-toggle (líneas 105-108): :aria-pressed="resumen" @click="resumen = !resumen"
  NO añadir aria-pressed a los pace-btn (el original usa .active). NO tocar el search-input (F6).

DaySection (NUEVO — análogo Topbar/TripView):
  import type { Day, Monument } from '~~/shared/schemas'
  defineProps<{ day: Day, monById: Map<string, Monument> }>()
  const dayCards = computed(() => props.day.cards.map(slug => props.monById.get(slug)).filter((m): m is Monument => !!m))  // ORDEN = dato, NUNCA reordenar
  markup (index.html:2375-2448): .light-banner (texto verbatim 2377, CSS-hidden salvo body.light-mode) + .section-eyebrow {{day.eyebrow}} + .day-header(day-number {{day.roman}} + h2 {{day.title}} + .day-subtitle {{day.subtitle}}) + .day-stats(.day-stats-item por day.stats[] con la clase de variant walk/train/taxi/metro/ticket + <MDC unwrap="p"> en el text) + .dia-ligera (v-if day.light; 2393-2401, CSS-hidden; head {{day.light.title}} + ul con li.lg-{kind} por items con <MDC unwrap="p">) + <Timeline :rows="day.timeline" /> + .cards-list(<MonumentCard v-for :monument>)

TripView (MODIFICAR — markup F3 en app/components/TripView.vue; hoy const { trip, days } = await useTrip(props.slug) y 11 <section id> vacías):
  Ampliar: const { trip, days, monById, food, artists, reference } = await useTrip(props.slug)  (+ artById/refById si se usan)
  Rellenar las 11 secciones pasando datos por props (un solo useTrip; mismo patrón que Topbar :days). Bocetos:
    <section id="viernes"><DaySection :day="days.find(d => d.slug === 'viernes')!" :mon-by-id="monById" /></section>  (idem sabado/domingo/lunes/martes)
    <section id="reservas"><ReservasSection :reservas="refById.get('reservas')!" /></section>
    <section id="gastronomia"><GastroSection :food="food" :section="trip?.sections?.gastronomia" /></section>
    <section id="practica"><PracticaSection :practica="refById.get('practica')!" /></section>
    <section id="arte">…eyebrow/intro de trip.sections.arte + ArtistCard(kind='artist') por los artists de kind artist…</section>
    <section id="arquitectura">…eyebrow/intro de trip.sections.arquitectura + ArtistCard(kind='arquitectura') por los de kind arquitectura + ArtistCard del arq-glosario…</section>
    <section id="mapa" />  ← SIGUE VACÍO (F7)
  Mantener el orden de hermanos VERBATIM. NINGÚN <NuxtLink> a /trips/* (heredado F3). Filtrar artists por kind (artists.filter(a => a.kind === 'artist'/'arquitectura'); el glosario es a.kind === 'glossary'). Verificar dónde van eyebrow/intro de #arte/#arquitectura (markup del original: section-eyebrow + intro fuera de las cards) y reproducirlo.

Specs autocontenidos (mirror EXACTO de tests/parity/shell.spec.ts — leer su cabecera, helpers waitForServer/killGroup, el patrón beforeAll generate → copiar .output/public a <tmp>/guiaRoma/ → serve, afterAll teardown):
  TOLERAR SOLO el error de hidratación de color-mode: const EXPECTED_HYDRATION_MSG = /Hydration completed but contains mismatches/i ; fallar ante cualquier otro error de consola (page.on('console')).
  NO usar el webServer por defecto. NO modificar golden.spec.ts ni sus snapshots (D-08). Preferir aserciones DOM/texto a screenshots.
  Ejemplos de aserción (RESEARCH §Validation Architecture ▸ "Cómo se prueban los criterios"):
    render-cards: expect('#galleria-sciarra .detail-photo img').toBeVisible(); expect('#galleria-sciarra .detail-list li').toHaveCount(N); dropcap (1ª card-section sin no-dropcap, resto con); facts-row; a.maps-link[href*=google.com/maps]; .notes-area textarea[data-note-key]
    render-timeline: en #viernes los 5 kinds presentes (.tl-item, .tl-transport, .tl-meta, .tl-food, .tl-resv-meta), .tl-item.reserved-event presente, orden = data; conservar :data-pace
    render-reference: #reservas .reservas-table tr + .reservas-badge.badge-*; tr.is-done; #gastronomia los 7 gastro-section-title en ORDEN canónico + .gastro-card; #arte .artist-card; #arquitectura .arq-glosario .arch-term ×10
    modes: click pace-btn[data-pace=slow] → .tl-item[data-pace=slow-only] y [data-pace=medium] con .tl-hidden (display:none); .tl-item[data-pace=all] visible; .tl-food/.tl-meta SIEMPRE visibles. click #light-toggle → body.light-mode + pace pasa a slow + .dia-ligera visible + aria-pressed=true; click otra vez → body sin light-mode pero pace SIGUE slow. #resumen-toggle → body.modo-resumen + set de ocultos (day-stats/day-subtitle/dia-ligera/tl-meta/tl-transport/cards-list) + tl-item/tl-food/tl-resv-meta visibles. Persistencia: addInitScript(localStorage.setItem('roma-pace','slow')) → recarga → estado slow. Micro-flash: con roma-pace=slow preseteado, observar la transición default→slow 1 frame tras montar (mirror de theme.spec.ts:117-161).

Patrón de test de interacción/persistencia/flash: tests/parity/theme.spec.ts (click sobre control + assert de clase/atributo; context.addInitScript para preseteo; observar transición). Patrón unit: tests/unit/dayLabel.spec.ts (ya no aplica aquí; los unit son de los Planes 01/04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cablear TheHero a useTripModes + DaySection + enchufar las 11 secciones de TripView (FEAT-06/07/08, UI-02/03/04)</name>
  <files>app/components/TheHero.vue, app/components/DaySection.vue, app/components/TripView.vue</files>
  <read_first>
    - app/components/TheHero.vue (estado actual COMPLETO — los pace-btn/light-toggle/resumen-toggle ya montados sin handlers; el 1er pace-btn con class="pace-btn active"; NO reestructurar)
    - app/components/TripView.vue (estado actual — const { trip, days } = await useTrip; las 11 <section id> vacías; el orden de hermanos a conservar)
    - app/composables/useTripModes.ts (Plan 01 — la API { pace, light, resumen, isVisible } que TheHero consume)
    - app/composables/useTrip.ts (qué expone: trip/days/monuments/food/artists/reference + monById/foodById/artById/refById — para ampliar el destructuring de TripView)
    - app/components/MonumentCard.vue / Timeline.vue / GastroSection.vue / ReservasSection.vue / PracticaSection.vue / ArtistCard.vue (Planes 02-04 — las props que DaySection/TripView les pasan; leer las firmas reales)
    - app/components/Topbar.vue (el patrón de pasar :days por prop desde TripView — el mismo flujo de datos a replicar para monById/food/etc.)
    - index.html (líneas 2375-2448 — la sección de día completa: light-banner 2377, section-eyebrow, day-header, day-stats 2387-2391, dia-ligera 2393-2401, timeline, cards-list 2448; las anclas #arte ~5941 / #arquitectura ~6104 para el eyebrow/intro de sección + dónde va el glosario)
    - shared/schemas.ts (DaySchema 124-145 — eyebrow/roman/title/subtitle/stats/light/timeline/cards; TripSchema.sections 272-276)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pattern 7 cablear TheHero; §Pattern 5 DaySection + nota de flujo de datos por props)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§DaySection.vue; §TheHero.vue MODIFICAR; §TripView.vue MODIFICAR)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario ▸ DaySection; Contrato de interacción — los 3 modos; matriz unwrap)
  </read_first>
  <action>
    Modificar app/components/TheHero.vue (D-05 — NO reestructurar el DOM del #inicio): añadir al <script setup> `const { pace, light, resumen } = useTripModes()`. Cablear los controles YA montados: el 1er pace-btn cambia de `class="pace-btn active"` a `class="pace-btn" :class="{ active: pace === 'optimistic' }" @click="pace = 'optimistic'"` (quitar el active literal); el 2º `:class="{ active: pace === 'neutral' }" @click="pace = 'neutral'"`; el 3º `:class="{ active: pace === 'slow' }" @click="pace = 'slow'"`. `#light-toggle`: `:aria-pressed="light" @click="light = !light"` (NO revertir pace al desactivar — lo hace el watch del composable). `#resumen-toggle`: `:aria-pressed="resumen" @click="resumen = !resumen"`. NO añadir aria-pressed a los pace-btn, NO tocar el search-input (F6), NO cambiar la estructura/orden/clases base. Sin <style> nuevo.

    Crear app/components/DaySection.vue (análogo Topbar/TripView; cabecera citando index.html:2375-2448 + CERO CSS). `import type { Day, Monument } from '~~/shared/schemas'` + `defineProps<{ day: Day, monById: Map<string, Monument> }>()`. `const dayCards = computed(() => props.day.cards.map(slug => props.monById.get(slug)).filter((m): m is Monument => !!m))` (ORDEN = dato, NUNCA reordenar — Pitfall 6). Template (transcripción 1:1): `.light-banner` (texto verbatim de 2377, CSS-hidden salvo body.light-mode) + `p.section-eyebrow {{ day.eyebrow }}` + `.day-header` (day-number {{ day.roman }} + `h2 {{ day.title }}` + `.day-subtitle {{ day.subtitle }}`) + `.day-stats` con `.day-stats-item` por `day.stats[]` (clase de variant walk/train/taxi/metro/ticket + `<MDC :value="s.text" unwrap="p" />`) + `.dia-ligera` con v-if day.light (head {{ day.light.title }} + ul con `li :class="'lg-'+item.kind"` por day.light.items con `<MDC :value="item.text" unwrap="p" />`) + `<Timeline :rows="day.timeline" />` + `div.cards-list` con `<MonumentCard v-for="m in dayCards" :key="m.slug" :monument="m" />`. Reproducir el markup exacto. Sin <style>.

    Modificar app/components/TripView.vue: ampliar el destructuring a `const { trip, days, monById, food, artists, reference, refById } = await useTrip(props.slug)` (añadir solo lo que se use; artById/foodById si hacen falta). Rellenar las 11 secciones VACÍAS conservando el orden de hermanos VERBATIM: `#viernes`..`#martes` → `<DaySection :day="days.find(d => d.slug === '<slug>')!" :mon-by-id="monById" />` (uno por día); `#reservas` → `<ReservasSection :reservas="refById.get('reservas')!" />`; `#gastronomia` → `<GastroSection :food="food" :section="trip?.sections?.gastronomia" />`; `#practica` → `<PracticaSection :practica="refById.get('practica')!" />`; `#arte` → el eyebrow/intro de trip.sections.arte (markup del original) + `<ArtistCard v-for :artist>` filtrando `artists.filter(a => a.kind === 'artist')`; `#arquitectura` → el eyebrow/intro de trip.sections.arquitectura + `<ArtistCard v-for :artist>` filtrando `a.kind === 'arquitectura'` + `<ArtistCard>` del glosario (`a.kind === 'glossary'`). `#mapa` SIGUE VACÍO (F7). NINGÚN `<NuxtLink>` a /trips/*. Verificar el markup exacto del eyebrow/intro de #arte/#arquitectura contra index.html y reproducirlo. Sin <style> nuevo.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/TheHero.vue app/components/DaySection.vue app/components/TripView.vue && grep -q "useTripModes" app/components/TheHero.vue && ! grep -q 'class="pace-btn active"' app/components/TheHero.vue && grep -q "DaySection" app/components/TripView.vue && grep -q "cards-list" app/components/DaySection.vue && ! grep -lE "<style" app/components/TheHero.vue app/components/DaySection.vue app/components/TripView.vue</automated>
  </verify>
  <acceptance_criteria>
    - TheHero importa useTripModes; el 1er pace-btn ya NO tiene class="pace-btn active" literal (grep negativo) sino :class con active reactivo; los 3 pace-btn tienen @click que asigna pace; light/resumen-toggle tienen :aria-pressed y @click; el search-input y la estructura del #inicio quedan intactos
    - DaySection: defineProps Day + monById; dayCards mapea day.cards→monById en orden con filter de no-nulos; monta light-banner/section-eyebrow/day-header/day-stats/dia-ligera(v-if)/Timeline/cards-list con MonumentCard
    - TripView amplía el destructuring de useTrip y rellena las 11 secciones (5 DaySection + reservas/gastronomia/practica/arte/arquitectura) por props; #mapa queda vacío; sin NuxtLink a /trips/*; orden de hermanos conservado
    - Ningún fichero contiene <style> (grep negativo); greps positivos useTripModes / DaySection / cards-list
    - pnpm typecheck exit 0; pnpm lint de los tres limpio
  </acceptance_criteria>
  <done>TheHero cablea los 3 modos sin tocar el DOM; DaySection monta el día y resuelve cards en orden; TripView enchufa las 11 secciones desde datos; CERO CSS; typecheck + lint verdes. El render ya es visible para los specs de las Tasks siguientes.</done>
</task>

<task type="auto">
  <name>Task 2: Specs DOM autocontenidos de render — cards + timeline + reference (SC#1/SC#2/SC#3)</name>
  <files>tests/parity/render-cards.spec.ts, tests/parity/render-timeline.spec.ts, tests/parity/render-reference.spec.ts</files>
  <read_first>
    - tests/parity/shell.spec.ts (el patrón AUTOCONTENIDO a copiar 1:1: imports node:child_process/fs/os/path, helpers waitForServer/killGroup, beforeAll que hace pnpm generate → copia .output/public a <tmp>/guiaRoma/ → spawn serve, afterAll teardown; la tolerancia EXPECTED_HYDRATION_MSG; OUTPUT_DIR)
    - tests/parity/subpath.spec.ts (la otra referencia de build+serve por si shell.spec usó una variante)
    - playwright.config.ts (testDir, proyectos mobile/desktop chromium, ajustes de determinismo) — para que los specs nuevos hereden mobile+desktop
    - app/components/MonumentCard.vue / Timeline.vue + las 5 TimelineKind / GastroSection.vue / ReservasSection.vue / ArtistCard.vue (Planes 02-04 — las clases/estructura que los specs aseveran)
    - content/trips/roma/monuments/galleria-sciarra.yml (el dato para contar .detail-list li y confirmar el detail-photo de la sección "En qué fijarse")
    - index.html (líneas 2450-2510 ficha galleria-sciarra; 2403-2446 timeline de viernes; 5260-5333 reservas; 5335+ gastronomia; ~6202 arq-glosario) — los counts/estructura de referencia
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Validation Architecture ▸ Test Map filas SC#1/SC#2/SC#3; §"Cómo se prueban los criterios" ▸ Render)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-VALIDATION.md (Per-Task Verification Map + Phase Requirements → Test Map; Wave 0 Requirements — los 4 specs)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario por componente — las clases exactas a aseverar)
  </read_first>
  <action>
    Crear los tres specs como Playwright AUTOCONTENIDOS sobre el / construido (mirror EXACTO de tests/parity/shell.spec.ts: beforeAll que ejecuta `pnpm generate` una vez y sirve `.output/public` bajo /guiaRoma/ con un static server propio; afterAll que mata el server y limpia; tolerar SOLO el error de hidratación de color-mode vía EXPECTED_HYDRATION_MSG y fallar ante cualquier otro). Compartir la orquestación build+serve entre los tres (mismo patrón; cada fichero puede tener su propio beforeAll/afterAll como shell.spec, o reutilizar — seguir lo que hizo shell.spec). NO usar el webServer por defecto. NO modificar golden.spec.ts. Preferir aserciones DOM/texto a screenshots (no rebaselinar, D-08).

    render-cards.spec.ts (SC#1/UI-02): sobre el / construido, aseverar en una ficha representativa (#galleria-sciarra): `.detail-photo img` visible (PRUEBA que :detail-photo resolvió a un componente real, no a `<p>:detail-photo{...}</p>` sin renderizar — la aserción de mayor valor); `.detail-list li` con el count del dato (✦ + bordes presentes — Pitfall 1); el dropcap (la 1ª `.card-section` SIN la clase no-dropcap y las siguientes CON no-dropcap — Pitfall 2); `.facts-row` presente; `a.maps-link` con href que contiene google.com/maps/search; `.notes-area textarea[data-note-key]` presente (shell). Aseverar también que la prosa NO añadió `<p>` extra donde el original no lo tenía (p.ej. dentro de .sorrentino-box no hay `<p>`).

    render-timeline.spec.ts (SC#2/UI-03): en #viernes (que tiene los 5 kinds) aseverar que existen `.tl-item`, `.tl-transport`, `.tl-meta`, `.tl-food`, `.tl-resv-meta` con su markup; `.tl-item.reserved-event` presente; que `:data-pace` se conserva en tl-item/tl-transport; y que el orden de las filas coincide con el del dato (p.ej. comprobar la secuencia de las primeras filas).

    render-reference.spec.ts (SC#3/UI-04): #reservas → `.reservas-table tr` + `.reservas-badge.badge-urgent`/`.badge-done`/`.badge-rec` presentes + algún `tr.is-done`; #gastronomia → los 7 `.gastro-section-title` en el ORDEN canónico (Pasta→Quinto quarto→Ghetto→Pizza→Gelato→Café→Bar — Pitfall 6) + `.gastro-card` por grupo; #arte → `.artist-card` presentes con `.artist-section`; #arquitectura → `.arq-glosario .arch-term` con count 10.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm playwright test tests/parity/render-cards.spec.ts tests/parity/render-timeline.spec.ts tests/parity/render-reference.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Los tres specs construyen el output Nuxt en beforeAll y lo sirven ellos mismos (no dependen del webServer del index.html vivo), con teardown en afterAll, y toleran SOLO el error de hidratación de color-mode
    - render-cards asevera #galleria-sciarra .detail-photo img visible, .detail-list li con el count del dato, dropcap (1ª card-section sin no-dropcap / resto con), facts-row, a.maps-link href google maps, notes-area textarea[data-note-key]
    - render-timeline asevera en #viernes los 5 kinds (.tl-item/.tl-transport/.tl-meta/.tl-food/.tl-resv-meta), .tl-item.reserved-event, :data-pace conservado, y el orden = dato
    - render-reference asevera reservas-table+badges+is-done, los 7 gastro-section-title en orden canónico + gastro-card, artist-card en #arte, y 10 arch-term en #arquitectura
    - Ninguno modifica golden.spec.ts ni añade snapshots nuevos; pnpm playwright test de los tres pasa en mobile + desktop
  </acceptance_criteria>
  <done>El DOM del / construido prueba que detail-photo resuelve, detail-list/dropcap están bien, los 5 kinds del timeline se renderizan, y las secciones de referencia salen desde datos con el orden de gastro canónico — todo verde en Playwright sin rebaselinar el golden.</done>
</task>

<task type="auto">
  <name>Task 3: Spec E2E de los 3 modos — matriz de ritmo + caminar-menos + resumen + persistencia + micro-flash (SC#4)</name>
  <files>tests/parity/modes.spec.ts</files>
  <read_first>
    - tests/parity/theme.spec.ts (el patrón de interacción + persistencia + micro-flash a reusar: click sobre control + assert de clase/atributo; context.addInitScript(localStorage.setItem(...)) para preseteo; observar la transición default→estado para confirmar el flash — líneas ~117-161)
    - tests/parity/shell.spec.ts (la orquestación build+serve autocontenida + la tolerancia EXPECTED_HYDRATION_MSG; o el bloque que usa pnpm dev si se prefiere para la interacción)
    - app/composables/useTripModes.ts (Plan 01 — la lógica exacta: matriz isVisible, watch light→slow sin revertir, claves roma-pace/light/resumen, restauración en onMounted)
    - app/components/TheHero.vue (Task 1 — los controles cableados: pace-btn[data-pace], #light-toggle, #resumen-toggle con :aria-pressed)
    - app/assets/css/leaflet.css (línea 731 .tl-hidden; 760-798 body.light-mode/body.modo-resumen y el SET exacto que resumen oculta: day-stats/day-subtitle/dia-ligera/tl-meta/tl-transport/cards-list; 812-833 dia-ligera) — el contrato a aseverar
    - index.html (líneas 6505-6577 — la lógica original de los 3 modos, para aseverar el MISMO comportamiento)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Validation Architecture ▸ Test Map filas SC#4 (FEAT-06/07/08 + persistencia + micro-flash); §"Cómo se prueban los criterios" ▸ Modos; §Pitfall 3/4/5)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Contrato de interacción — los 3 modos: matriz EXACTA, caminar-menos fuerza slow/no revierte, resumen set de ocultos, micro-flash)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-VALIDATION.md (Phase Requirements → Test Map filas SC#4)
  </read_first>
  <action>
    Crear tests/parity/modes.spec.ts como Playwright sobre el / (autocontenido como shell.spec, o vía pnpm dev como el bloque de routing de shell.spec — elegir el que permita interacción real; tolerar SOLO el error de hidratación de color-mode). NO modificar golden.spec.ts. Aseverar:
    1. Matriz de ritmo (FEAT-06): por defecto (optimista) todas las `.tl-item`/`.tl-transport` visibles. Click en `pace-btn[data-pace="slow"]` → las `.tl-item[data-pace="slow-only"]` y `.tl-item[data-pace="medium"]` (y sus equivalentes tl-transport) tienen `.tl-hidden` (display:none); las `.tl-item[data-pace="all"]` siguen visibles; las `.tl-food`/`.tl-meta`/`.tl-resv-meta` SIEMPRE visibles (Pitfall 4). Click en `pace-btn[data-pace="neutral"]` → solo `slow-only` oculto, `medium` visible.
    2. Caminar menos (FEAT-07): click en `#light-toggle` → `body.light-mode`, el pace pasa a slow (las medium se ocultan), `.dia-ligera` visible, `#light-toggle[aria-pressed="true"]`. Click otra vez → `body` SIN light-mode pero el pace SIGUE slow (NO revierte — Pitfall 5).
    3. Resumen (FEAT-08): click en `#resumen-toggle` → `body.modo-resumen`; el SET exacto oculto (`.day-stats`, `.day-subtitle`, `.dia-ligera`, `.tl-meta`, `.tl-transport`, `.cards-list`) con display:none; `.tl-item`/`.tl-food`/`.tl-resv-meta` siguen visibles; `#resumen-toggle[aria-pressed="true"]`.
    4. Persistencia (SC#4): `context.addInitScript` que pone `localStorage.setItem('roma-pace','slow')` (y light/resumen en otra prueba) → recargar el / → el estado se restaura (pace slow aplicado tras montar). Repetir para roma-light='1' (debe forzar slow + body.light-mode) y roma-resumen='1'.
    5. Micro-flash (SC#4, INTENCIONAL): con `roma-pace=slow` preseteado, confirmar que el primer paint es el DEFAULT (optimista) y que cambia a slow 1 frame tras montar (observar la transición, mirror de theme.spec.ts; NO debe haber script inline que aplique el estado en SSR).
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm playwright test tests/parity/modes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - El spec construye/sirve el / él mismo (o usa pnpm dev) y tolera SOLO el error de hidratación de color-mode; no modifica golden.spec.ts
    - Aserta la matriz EXACTA: slow oculta data-pace medium+slow-only en tl-item/tl-transport y mantiene all; neutral oculta solo slow-only; tl-food/tl-meta/tl-resv-meta NUNCA se ocultan por ritmo
    - Aserta caminar-menos: body.light-mode + fuerza slow + .dia-ligera visible + aria-pressed=true; al desactivar body pierde light-mode pero el pace SIGUE slow (no revierte)
    - Aserta resumen: body.modo-resumen oculta el set exacto (day-stats/day-subtitle/dia-ligera/tl-meta/tl-transport/cards-list) y mantiene tl-item/tl-food/tl-resv-meta; aria-pressed=true
    - Aserta persistencia (roma-pace/roma-light/roma-resumen restaurados tras recarga) y el micro-flash (primer paint default → estado guardado 1 frame después)
    - pnpm playwright test tests/parity/modes.spec.ts pasa en mobile + desktop
  </acceptance_criteria>
  <done>El comportamiento de los 3 modos (matriz exacta, caminar-menos sin revertir, resumen set correcto), la persistencia con las claves literales y el micro-flash intencional quedan probados en verde (SC#4).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Sign-off humano de paridad de render con imágenes reales (D-06)</name>
  <files>(ninguno — verificación humana del render cableado de las Tasks 1-3)</files>
  <action>
    Gate de verificación humana bloqueante (no se escribe código aquí). El agente construye y sirve el sitio (pnpm generate + servir .output/public bajo /guiaRoma/ con imágenes permitidas cargando), confirma que los specs de las Tasks 2-3 están en verde, y presenta el render en marcha al usuario para el sign-off de paridad visual descrito abajo. No avanzar más allá de este gate hasta que el usuario responda con la señal de reanudación. NO rebaselinar el golden (D-08).
  </action>
  <verify>
    <human-check>El humano confirma que las fichas, el timeline y las secciones de referencia se ven idénticas al index.html con imágenes reales cargando (un día representativo + cada sección de referencia, claro+oscuro).</human-check>
  </verify>
  <what-built>
    El render data-driven completo: las 5 secciones de día (DaySection con fichas/timeline) y las 5 de referencia (reservas/gastronomía/práctica/arte/arquitectura) rellenadas desde los datos, y los 3 modos cableados sobre los controles del #inicio. Las Tasks 2-3 ya aseveran el DOM/estructura y el comportamiento de los modos automáticamente; este checkpoint es la confirmación visual humana de paridad con imágenes reales (D-06) — la barra intermedia, antes del fallback de imagen (F7) y el pixel-diff total (F8).
  </what-built>
  <how-to-verify>
    1. Confirmar que los specs automáticos están en verde: cd /home/vcompanyb/guiaRoma y ejecutar pnpm playwright test tests/parity/render-cards.spec.ts tests/parity/render-timeline.spec.ts tests/parity/render-reference.spec.ts tests/parity/modes.spec.ts (mobile + desktop) + pnpm test:unit + pnpm test:data + pnpm typecheck + pnpm lint.
    2. Construir y servir: pnpm generate, luego servir .output/public bajo el subpath (p.ej. pnpm dlx serve -l 4173 .output/public) y abrir http://localhost:4173/guiaRoma/ (con red abierta para que las imágenes de Wikimedia carguen).
    3. Comparar contra el index.html actual (o el golden de F1, vistas de día/ficha/secciones): una ficha de monumento (hero cargado, nombre/italiano, prosa con dropcap en la 1ª sección, detail-photo embebida, "En qué fijarse" con bullets ✦, facts, "Ver en Google Maps", caja de notas), el timeline de un día (las filas en orden, transporte/comidas/reservas), y las 5 secciones de referencia (reservas con tabla y badges, gastronomía con los 7 grupos EN ORDEN, práctica, arte, arquitectura con el glosario) — en claro y oscuro.
    4. Probar los 3 modos en vivo: cambiar el ritmo (Optimista/Neutra/Pesimista) y ver ocultarse las paradas correctas; activar "Caminar menos" (fuerza pesimista, aparece la "Versión ligera"); activar "Modo resumen" (vista índice). Recargar con un modo activo y comprobar que se mantiene.
    5. Confirmar que NO se ha rebaselinado el golden (git status no muestra cambios en tests/parity/golden.spec.ts-snapshots).
  </how-to-verify>
  <resume-signal>Escribe "approved" si las fichas, el timeline y las secciones de referencia se ven idénticas al index.html con imágenes reales (claro+oscuro) y los 3 modos se comportan como hoy; si no, describe las diferencias (qué elemento, qué sección, qué tema) para corregirlas.</resume-signal>
  <acceptance_criteria>
    - El humano ha cargado el / construido con imágenes reales y confirmado que fichas/timeline/secciones de referencia se ven idénticas al index.html en claro + oscuro
    - El humano ha confirmado que los 3 modos (ritmo/caminar-menos/resumen) se comportan como hoy y que el estado persiste al recargar
    - El golden NO se ha rebaselinado (D-08)
    - Señal de reanudación recibida: approved (o una lista concreta de diffs a corregir y re-verificar)
  </acceptance_criteria>
  <done>Un humano ha firmado que el render data-driven es visualmente idéntico al index.html con imágenes reales (claro+oscuro) y los 3 modos funcionan como hoy — la barra de paridad intermedia de F4 (D-06) se cumple, sin rebaselinar el golden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage (roma-pace/roma-light/roma-resumen) → estado de modos | TheHero/useTripModes leen/escriben estas claves; valores constreñidos; solo conducen clases CSS verbatim. |
| repo data (Day/Monument/Food/Artist/Reference) → DaySection/TripView | Datos zod-validados versionados; sin input de usuario. |
| SSG output → public | Sitio estático; solo / se prerenderiza; sin servidor/auth/formularios funcionales. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | localStorage roma-pace/light/resumen (restaurado por useTripModes) | accept | useTripModes valida roma-pace contra los 3 literales y trata light/resumen como booleano; un valor manipulado solo activa/desactiva clases CSS conocidas. Sin sink de inyección. |
| T-04-10 | Information Disclosure | alcance de prerender | mitigate | Heredado de F3 (D-01): nitro.prerender.routes sigue siendo un solo / y #mapa queda vacío; F4 no añade rutas ni enlaces a /trips/*. TripView no introduce NuxtLink a rutas. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalación en este plan (RESEARCH §Package Legitimacy Audit: F4 no instala nada). Playwright/Nuxt/Content ya instalados y verificados. Sin paquete [ASSUMED]/[SUS]. |

**Conclusión:** Sin amenazas de severidad alta; sin mitigaciones bloqueantes. Estado de modos constreñido sin sink; render sobre datos confiables; alcance de prerender intacto (heredado F3).
</threat_model>

<verification>
- pnpm typecheck && pnpm lint — TS estricto + lint sobre TheHero/DaySection/TripView (los specs viven en tests/parity, ignorados por la config de Nuxt — patrón F3).
- pnpm playwright test tests/parity/render-cards.spec.ts tests/parity/render-timeline.spec.ts tests/parity/render-reference.spec.ts — DOM del / construido (SC#1/SC#2/SC#3), autocontenidos.
- pnpm playwright test tests/parity/modes.spec.ts — matriz de ritmo + caminar-menos + resumen + persistencia + micro-flash (SC#4).
- pnpm test:unit && pnpm test:data — los unit de F4 (isVisible/foodGroups) + las puertas de datos de F2 + los unit de F3, sin regresión.
- Sign-off humano de paridad de render con imágenes reales (Task 4 checkpoint) — la barra intermedia D-06; el golden NO se rebaselina (D-08).
</verification>

<success_criteria>
- TheHero cablea los 3 modos a useTripModes sin reestructurar el #inicio (D-05); DaySection y TripView enchufan el render data-driven en las 11 secciones (UI-02/03/04).
- La ficha de monumento se ve idéntica con la prosa MDC sin `<p>` extra y el dropcap en la 1ª sección (SC#1); el timeline despacha por kind (SC#2); las secciones de referencia salen desde datos con el orden de gastro canónico (SC#3) — todo probado en Playwright.
- Los 3 modos se comportan como hoy (matriz exacta, caminar-menos fuerza slow sin revertir, resumen oculta el set correcto), persisten con las claves literales y preservan el micro-flash (SC#4).
- Un humano confirma la paridad visual del render con imágenes reales cargando (D-06), sin rebaselinar el golden (D-08).
</success_criteria>

<output>
Create `.planning/phases/04-render-de-contenido-modos-de-ritmo/04-05-SUMMARY.md` when done.
</output>
