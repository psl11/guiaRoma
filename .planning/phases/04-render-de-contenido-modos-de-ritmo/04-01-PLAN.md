---
phase: 04-render-de-contenido-modos-de-ritmo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/utils/pace.ts
  - tests/unit/pace.spec.ts
  - app/composables/useTripModes.ts
  - app/components/DetailPhoto.global.vue
  - app/components/ProseUl.global.vue
  - app/components/ProseLi.global.vue
  - eslint.config.mjs
autonomous: true
requirements: [FEAT-06, FEAT-07, FEAT-08, UI-02]
user_setup: []
must_haves:
  truths:
    - "isVisible(itemPace, pace) reproduce la matriz EXACTA: optimistic→todo visible; neutral→oculta slow-only; slow→oculta slow-only Y medium (solo 'all' visible)"
    - "useTripModes expone pace/light/resumen (useState) + isVisible; default = optimistic/false/false = HTML prerenderizado (cero mismatch) — composable reactivo único, NO funciones globales ni manipulación imperativa del DOM, estado compartido TheHero↔timeline (D-03)"
    - "Activar light fuerza pace='slow' (watch sin else); desactivar NO revierte el ritmo (Pitfall 5)"
    - "El estado se restaura de localStorage (roma-pace/roma-light/roma-resumen) en onMounted, NO en SSR (micro-flash intencional de 1 frame, SC#4; cero mismatch de hidratación); light se restaura DESPUÉS de pace (D-04)"
    - "body.light-mode / body.modo-resumen se aplican vía useHead({bodyAttrs}), nunca classList/querySelectorAll"
    - "DetailPhoto.global.vue es resolvible globalmente → <MDC> resuelve :detail-photo{...} a un .detail-photo > img real"
    - "ProseUl/ProseLi.global.vue reproducen <ul class=\"detail-list\"> SOLO si toda lista de prosa debe verse como detail-list (verificado por grep antes de implementar)"
  artifacts:
    - path: "app/utils/pace.ts"
      provides: "isVisible(itemPace, pace) pura (FEAT-06)"
      contains: "export function isVisible"
    - path: "tests/unit/pace.spec.ts"
      provides: "los 9 casos de la matriz (3 paces × 3 itemPaces)"
    - path: "app/composables/useTripModes.ts"
      provides: "estado reactivo pace/light/resumen + persistencia + isVisible (FEAT-06/07/08)"
      contains: "useTripModes"
    - path: "app/components/DetailPhoto.global.vue"
      provides: "componente MDC inline GLOBAL (:detail-photo) — img plano + caption (D-01)"
      contains: "detail-photo"
  key_links:
    - from: "app/composables/useTripModes.ts"
      to: "app/utils/pace.ts"
      via: "isVisible delega en la función pura"
      pattern: "isVisible"
    - from: "app/composables/useTripModes.ts"
      to: "localStorage roma-pace/roma-light/roma-resumen"
      via: "lectura/escritura en onMounted"
      pattern: "roma-pace"
    - from: "app/components/DetailPhoto.global.vue"
      to: "<MDC> resolveComponent('DetailPhoto')"
      via: "sufijo .global.vue registra el componente globalmente"
      pattern: "detail-photo"
---

<objective>
Construir los CIMIENTOS data-driven y de modos de la Fase 4, sin tocar todavía el render de fichas/timeline/referencia: (1) la matriz de ritmo pura `isVisible` + su test unitario, (2) el composable reactivo `useTripModes` (pace/light/resumen + persistencia + las clases de `<body>`), y (3) los componentes MDC **globales** que `<MDC>` necesita resolver (`DetailPhoto` para `:detail-photo{...}`, y los overrides `ProseUl`/`ProseLi` para conservar la clase `.detail-list` de las listas de prosa). Más la entrada de allowlist de eslint para `Timeline` (Plan 03 la consumirá). Cubre la lógica de FEAT-06/07/08 y la mecánica MDC de UI-02; el render visible llega en los Planes 02-05.

Purpose: Los Planes 02-04 (MonumentCard, Timeline, secciones de referencia) consumen estas tres piezas — `MonumentCard` necesita `DetailPhoto`/`ProseUl` resueltos por `<MDC>`, el `Timeline` necesita `useTripModes().isVisible`, y el cableado de `TheHero` (Plan 05) necesita `useTripModes`. Construirlas primero, con su test unitario y las dos sutilezas de paridad de mayor riesgo resueltas aquí (matriz EXACTA de Pitfall 4/5 en el composable; clase `.detail-list` de Pitfall 1 en los overrides Prose), evita el anti-patrón "scavenger hunt" en los planes consumidores: reciben contratos ya construidos. `DetailPhoto.global.vue` es el PRIMER componente `.global.vue` del repo — el sufijo `.global.vue` es el mecanismo (RESEARCH §Pattern 1, verificado desde `@nuxtjs/mdc@0.22.0`: `<MDC>` llama `resolveComponent('DetailPhoto')` contra el registro GLOBAL, y en Content v3 los auto-imports normales NO son globales).

Output: nuevos `app/utils/pace.ts`, `tests/unit/pace.spec.ts`, `app/composables/useTripModes.ts`, `app/components/DetailPhoto.global.vue`, `app/components/ProseUl.global.vue`, `app/components/ProseLi.global.vue`; modificado `eslint.config.mjs` (allowlist `Timeline`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md
@.planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md

<interfaces>
Matriz EXACTA (portada 1:1 de index.html:6521-6534 setPace) — NO "corregir":
  isVisible(itemPace, pace):
    pace==='optimistic' → true (todo visible)
    pace==='neutral'    → itemPace !== 'slow-only'
    pace==='slow'       → itemPace === 'all'   (oculta 'medium' Y 'slow-only')
  Tipos: Pace = 'optimistic'|'neutral'|'slow'; ItemPace = 'all'|'medium'|'slow-only'.

Análogo de util puro: app/utils/dayLabel.ts (pura, framework-free, auto-importada como <nombre>,
testeable en Vitest plano sin runtime Nuxt). Mismo estilo de cabecera (comentario que explica por qué se extrae).
Análogo de test unitario: tests/unit/dayLabel.spec.ts (import directo de ../../app/utils/<x>, un it por caso).

useTripModes (forma exacta, RESEARCH §Pattern 6 + PATTERNS):
  const pace = useState<'optimistic'|'neutral'|'slow'>('pace', () => 'optimistic')  // DEFAULT = prerenderizado
  const light = useState('light', () => false)
  const resumen = useState('resumen', () => false)
  watch(light, on => { if (on) pace.value = 'slow' })        // SIN else (Pitfall 5)
  isVisible(itemPace) → delega en isVisible(itemPace, pace.value) de utils/pace.ts
  useHead({ bodyAttrs: { class: computed(() => [light?'light-mode':'', resumen?'modo-resumen':''].filter(Boolean).join(' ')) } })
  onMounted(() => {  // micro-flash intencional (SC#4)
    restaurar roma-pace → pace (validar el valor ∈ los 3); roma-light==='1' → light=true; roma-resumen==='1' → resumen=true
    // restaurar light DESPUÉS de pace (al poner light=true el watch fuerza pace='slow' — orden de index.html:6650-6652)
    watch(pace, v => localStorage.setItem('roma-pace', v))           // persistencia DENTRO de onMounted
    watch(light, v => localStorage.setItem('roma-light', v?'1':'0'))
    watch(resumen, v => localStorage.setItem('roma-resumen', v?'1':'0'))
  })
  return { pace, light, resumen, isVisible }
Claves localStorage LITERALES: roma-pace ('optimistic'|'neutral'|'slow'), roma-light ('1'|'0'), roma-resumen ('1'|'0').
useState/useHead/onMounted/watch/computed son auto-importados por Nuxt.

DetailPhoto.global.vue (markup index.html:2479-2482; props = los {key=value} del MDC):
  defineProps<{ src: string, alt: string, caption: string }>()
  template: div.detail-photo > (img :src :alt loading="lazy" — PLANO, sin @error, D-01) + div.detail-photo-caption con la caption
  La caption ADMITE Markdown-inline → renderizar con <MDC :value="caption" unwrap="p" /> (seguro para cualquiera).

ProseUl/ProseLi.global.vue (Pitfall 1 — markup index.html:2483, CSS base.css:799-818 .detail-list):
  ProseUl → <ul class="detail-list"><slot/></ul> ; ProseLi → <li><slot/></li>
  El nombre ProseUl/ProseLi es la convención de override de Prose components de MDC (findMappedTag los resuelve).

eslint.config.mjs: Timeline (Plan 03) es nombre de UNA palabra → vue/multi-word-component-names lo rechaza.
F3 ya resolvió el mismo caso con Topbar (eslint.config.mjs bloque files:['app/components/Topbar.vue']).
Añadir un bloque GEMELO para app/components/Timeline.vue. El resto de componentes F4 son multi-palabra.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Matriz de ritmo pura isVisible + test unitario (FEAT-06)</name>
  <files>app/utils/pace.ts, tests/unit/pace.spec.ts</files>
  <read_first>
    - index.html (líneas 6505-6535 — setPace: la matriz EXACTA a portar 1:1; confirmar que neutral oculta solo slow-only y slow oculta slow-only Y medium)
    - app/utils/dayLabel.ts (el análogo EXACTO de util puro: cabecera explicativa, framework-free, auto-import, exporta tipos+función)
    - tests/unit/dayLabel.spec.ts (el análogo de test: import directo de ../../app/utils/, un it por caso, sin runtime Nuxt)
    - shared/schemas.ts (líneas 36 Pace enum 'all'|'medium'|'slow-only'; confirmar los literales del itemPace)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Code Examples ▸ matriz isVisible; §Pitfall 4 — la matriz es contraintuitiva, no corregir)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§utils/pace.ts — análogo dayLabel.ts)
  </read_first>
  <behavior>
    - isVisible('all','optimistic') === true; isVisible('medium','optimistic') === true; isVisible('slow-only','optimistic') === true
    - isVisible('all','neutral') === true; isVisible('medium','neutral') === true; isVisible('slow-only','neutral') === false
    - isVisible('all','slow') === true; isVisible('medium','slow') === false; isVisible('slow-only','slow') === false
  </behavior>
  <action>
    Crear app/utils/pace.ts como función PURA auto-importada (mismo estilo que app/utils/dayLabel.ts: cabecera de comentario que explica que se extrae para test Vitest plano sin runtime Nuxt y cita index.html:6521-6534). Exportar los tipos `Pace` ('optimistic'|'neutral'|'slow') e `ItemPace` ('all'|'medium'|'slow-only') y la función `isVisible(itemPace: ItemPace, pace: Pace): boolean` con la matriz EXACTA: si pace es 'optimistic' devolver true; si 'neutral' devolver itemPace !== 'slow-only'; si no (slow) devolver itemPace === 'all'. NO añadir ninguna otra rama ni "corregir" la contraintuición (Pitfall 4). Sin CSS, sin dependencia Nuxt.

    Crear tests/unit/pace.spec.ts (análogo de tests/unit/dayLabel.spec.ts): importar isVisible de ../../app/utils/pace y cubrir los 9 casos (3 paces × 3 itemPaces) con un it por caso, aseverando los booleanos del bloque <behavior>. Vitest puro (vitest run), sin watch, sin dependencia nueva.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm test:unit && pnpm typecheck && pnpm lint app/utils/pace.ts tests/unit/pace.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/utils/pace.ts exporta `isVisible`, `Pace`, `ItemPace`; isVisible NO contiene más de las tres ramas (optimistic/neutral/slow)
    - Los 9 asertos del bloque <behavior> pasan: isVisible('slow-only','neutral')===false, isVisible('medium','slow')===false, isVisible('slow-only','slow')===false, isVisible('all','slow')===true, y el resto
    - tests/unit/pace.spec.ts importa de ../../app/utils/pace y NO usa runtime Nuxt ni mountSuspended
    - pnpm test:unit verde (incluye los 9 casos nuevos + los 7 de F3); pnpm typecheck exit 0; pnpm lint de ambos ficheros limpio
  </acceptance_criteria>
  <done>isVisible reproduce la matriz exacta (3 ramas, contraintuición preservada) y los 9 casos pasan en Vitest puro; typecheck + lint verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Composable useTripModes — estado reactivo + persistencia + clases de body (FEAT-06/07/08)</name>
  <files>app/composables/useTripModes.ts</files>
  <read_first>
    - index.html (líneas 6505-6577 — setPace/setLightMode/toggleLightMode/setResumen/toggleResumen VERBATIM; y 6648-6660 init() — el ORDEN de restore: restorePace ANTES de restoreLightMode)
    - app/composables/useTrip.ts (convención de composable del repo: cabecera que explica el contrato, exporta función)
    - app/components/ThemeToggle.vue (análogo de init cliente + claves localStorage literales roma-*; evita FOUC/mismatch leyendo en cliente)
    - app/utils/pace.ts (Task 1 — isVisible que el composable consume; importarla/usarla como auto-import)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pattern 6 — forma exacta useState + useHead bodyAttrs + onMounted; las 4 sutilezas CRÍTICAS de paridad; §Pitfall 3/5; §Anti-Patterns — NO classList/querySelectorAll, NO localStorage en setup síncrono, NO script inline)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (▸ Contrato de interacción — los 3 modos: matriz de ritmo, caminar-menos fuerza slow sin revertir, resumen por CSS, micro-flash)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§useTripModes.ts — boceto + claves literales)
  </read_first>
  <action>
    Crear app/composables/useTripModes.ts exportando `useTripModes()` (estilo de cabecera como useTrip.ts: explicar el contrato + por qué onMounted preserva el micro-flash). Estado con `useState` (SSR-singleton, compartido por clave entre TheHero y Timeline): `pace` clave 'pace' default 'optimistic', `light` clave 'light' default false, `resumen` clave 'resumen' default false — estos defaults SON el HTML prerenderizado (cero mismatch, Pitfall 3). Acoplamiento light→slow: `watch(light, on => { if (on) pace.value = 'slow' })` SIN rama else (al desactivar NO revierte — Pitfall 5; mapea index.html:6552 `if (on) setPace('slow')`). `isVisible(itemPace)` que delega en la función pura `isVisible(itemPace, pace.value)` de utils/pace.ts. Clases de `<body>` vía `useHead({ bodyAttrs: { class: computed(...) } })` que une 'light-mode' si light y 'modo-resumen' si resumen (NUNCA classList/querySelectorAll — anti-patrón CLAUDE.md). Persistencia + restauración SOLO en `onMounted`: leer roma-pace (validar ∈ los 3 valores antes de asignar), roma-light==='1'→light=true, roma-resumen==='1'→resumen=true, restaurando light DESPUÉS de pace (el watch fuerza slow — orden index.html:6650-6652); y registrar los tres `watch(...→localStorage.setItem(...))` DENTRO de onMounted (no en setup síncrono, para no escribir en prerender). Devolver { pace, light, resumen, isVisible }. CERO CSS, sin script inline para modos.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/composables/useTripModes.ts && grep -q "if (on) pace" app/composables/useTripModes.ts && grep -q "roma-pace" app/composables/useTripModes.ts && grep -q "bodyAttrs" app/composables/useTripModes.ts && ! grep -q "querySelectorAll\|classList" app/composables/useTripModes.ts</automated>
  </verify>
  <acceptance_criteria>
    - useTripModes usa useState para pace ('optimistic')/light (false)/resumen (false) con esos defaults exactos; isVisible delega en utils/pace.ts (no reimplementa la matriz)
    - El watch del acoplamiento es `if (on) pace.value = 'slow'` SIN else (Pitfall 5); no hay ninguna asignación que revierta pace al desactivar light
    - Las clases de body se aplican vía useHead({ bodyAttrs }) con 'light-mode'/'modo-resumen'; el fichero NO contiene querySelectorAll ni classList (grep negativo)
    - La lectura de localStorage (roma-pace/roma-light/roma-resumen) y los watch de persistencia están DENTRO de onMounted; light se restaura después de pace
    - pnpm typecheck exit 0; pnpm lint limpio; los 4 greps pasan (incl. el negativo de classList/querySelectorAll)
  </acceptance_criteria>
  <done>useTripModes mantiene pace/light/resumen en useState, acopla light→slow sin revertir, aplica las clases de body vía useHead, restaura/persiste en onMounted con las claves literales, y delega isVisible en la función pura; typecheck + lint verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Componentes MDC globales (DetailPhoto + ProseUl/ProseLi) + allowlist eslint de Timeline (UI-02 / Pitfall 1)</name>
  <files>app/components/DetailPhoto.global.vue, app/components/ProseUl.global.vue, app/components/ProseLi.global.vue, eslint.config.mjs</files>
  <read_first>
    - index.html (líneas 2479-2482 — markup del .detail-photo (img + .detail-photo-caption); línea 2483 + alrededores — <ul class="detail-list"> en la prosa de ficha; líneas 5959-6094 — las listas de .artist-section que usan <ul> SIN clase, NO .detail-list)
    - app/assets/css/base.css (líneas 799-818 .detail-list ✦ + bordes; 820-844 .detail-photo) — el contrato visual a reproducir
    - app/components/TheHero.vue (líneas 1-27 + el uso de <MDC unwrap="p"> — el estilo de cabecera y el patrón MDC inline a copiar)
    - eslint.config.mjs (el bloque files:['app/components/Topbar.vue'] con vue/multi-word-component-names off — el GEMELO a replicar para Timeline)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-RESEARCH.md (§Pattern 1 — DetailPhoto GLOBAL vía sufijo .global.vue, mecánica verificada; §Pitfall 1 — opción 1 ProseUl/ProseLi; §Open Q 1 — grep obligatorio de las listas; §Open Q 3 — caption texto plano vs MDC)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-PATTERNS.md (§DetailPhoto.global.vue; §ProseUl/ProseLi.global.vue; §Notas de configuración — eslint Timeline)
    - .planning/phases/04-render-de-contenido-modos-de-ritmo/04-UI-SPEC.md (Inventario ▸ DetailPhoto y la fila override ProseUl/ProseLi; matriz unwrap ▸ caption)
  </read_first>
  <action>
    IMPORTANTE — Pitfall 1 / decisión de implementación clave: ANTES de elegir el alcance de ProseUl/ProseLi, ejecutar el grep que confirma qué listas de prosa deben verse como `.detail-list`. El index.html usa `<ul class="detail-list">` SOLO en la prosa de fichas/práctica (sections[].body de monumentos y practica), pero las `.artist-section` de artistas usan `<ul>` SIN clase (`.artist-section ul`, ver index.html ~5959+) — y la `.dia-ligera`/reservas-confirmadas también usan `<ul>` propios fuera de `<MDC>`. Un override GLOBAL incondicional de ProseUl que ponga `detail-list` a TODA lista de `<MDC>` aplicaría ✦+bordes también a las listas de prosa de artista → DIVERGENCIA. Resolver así: como las listas de artista van renderizadas por `<MDC>` igual que las de monumento, NO se pueden distinguir por contexto desde un override global plano. Verificar con el grep si las listas de artista están dentro de `sections[].body` (renderizadas por `<MDC>`, mismo árbol) o si el ArtistCard del Plan 04 las renderiza aparte; documentar el hallazgo en el SUMMARY y elegir entre: (a) override global que ponga `.detail-list` (válido SOLO si todas las listas de prosa MDC deben verse así), o (b) si artista difiere, NO crear ProseUl/ProseLi globales y resolver `.detail-list` de monumento con un componente de bloque `::detail-list` por dato — pero eso tocaría datos de F2; en ese caso DETENERSE y anotar el conflicto en el SUMMARY para decisión, sin tocar los YAML. La opción por defecto recomendada (RESEARCH Pitfall 1 op.1) es (a), pero SOLO tras confirmar por grep que ninguna lista de prosa MDC debe verse distinta.

    Crear app/components/DetailPhoto.global.vue (PRIMER .global.vue del repo — el sufijo es el mecanismo de registro global que <MDC> necesita; RESEARCH §Pattern 1 verificado). Cabecera estilo TheHero (cita index.html:2479-2482, explica CERO CSS + por qué global + que es la frontera D-01: img plano, F7 añade @error→SVG). `defineProps<{ src: string, alt: string, caption: string }>()`. Template: `div.detail-photo` con `img :src :alt loading="lazy"` (PLANO, sin onerror/@error) y `div.detail-photo-caption` que renderiza la caption con `<MDC :value="caption" unwrap="p" />` (seguro para captions con o sin markup). Sin <style>.

    Crear (si la decisión es (a)) app/components/ProseUl.global.vue → `<ul class="detail-list"><slot/></ul>` y app/components/ProseLi.global.vue → `<li><slot/></li>`, ambos con cabecera que cita index.html:2483 + base.css:799-818 y explica que son overrides de Prose para conservar `.detail-list`. Sin <style>. (Si la decisión es (b), NO crear estos dos y documentar el conflicto; el plan de MonumentCard se replanificará.)

    Modificar eslint.config.mjs: añadir un bloque GEMELO al de Topbar para `app/components/Timeline.vue` con `'vue/multi-word-component-names': 'off'` (Plan 03 crea Timeline, nombre de una palabra). Comentario breve explicando que es por el contrato de auto-import (igual que Topbar).
  </action>
  <verify>
    <automated>cd /home/vcompanyb/guiaRoma && pnpm typecheck && pnpm lint app/components/DetailPhoto.global.vue eslint.config.mjs && grep -q "detail-photo" app/components/DetailPhoto.global.vue && ! grep -q "onerror\|@error" app/components/DetailPhoto.global.vue && grep -q "app/components/Timeline.vue" eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - El grep de Pitfall 1 se ejecutó y su hallazgo (qué listas de prosa son detail-list vs cuáles no, en especial las .artist-section) está documentado en el SUMMARY; la decisión (a) ó (b) está justificada
    - app/components/DetailPhoto.global.vue: clase .detail-photo, img con :src/:alt/loading="lazy" SIN onerror ni @error (frontera D-01), .detail-photo-caption con la caption; sin bloque <style>
    - Si decisión (a): ProseUl.global.vue emite <ul class="detail-list"> y ProseLi.global.vue emite <li>; ambos sin <style>. Si decisión (b): no se crean y el conflicto queda anotado
    - eslint.config.mjs contiene un bloque files con 'app/components/Timeline.vue' y 'vue/multi-word-component-names': 'off'
    - pnpm typecheck exit 0; pnpm lint limpio; greps: detail-photo presente, onerror/@error ausente, Timeline.vue en eslint config
  </acceptance_criteria>
  <done>DetailPhoto.global.vue resuelve :detail-photo como img plano + caption; la decisión de Pitfall 1 (ProseUl/ProseLi o conflicto anotado) está tomada y justificada por grep; eslint admite el nombre Timeline; typecheck + lint verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage (roma-pace/roma-light/roma-resumen) → estado de modos | useTripModes lee/escribe estas claves; el espacio de valores está constreñido (3 paces, '1'/'0'). El valor solo conduce clases CSS verbatim, nunca un sink HTML. |
| `<MDC>` sobre prosa versionada → DetailPhoto/Prose overrides | Los componentes globales renderizan props/slots provenientes de prosa Markdown VERSIONADA en el repo (confianza), nunca input de usuario. |
| SSG output → public | Sitio estático, sin servidor, sin auth, sin formularios funcionales en esta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | localStorage roma-pace/roma-light/roma-resumen | accept | useTripModes valida roma-pace contra los 3 literales antes de asignar y trata roma-light/roma-resumen como booleano ('1'); un valor manipulado solo activa/desactiva clases CSS conocidas (.tl-hidden, body.light-mode/modo-resumen). Sin sink de inyección. |
| T-04-02 | Tampering/Elevation | `<MDC>` → DetailPhoto/ProseUl/ProseLi | accept | MDC renderiza SOLO prosa versionada en el repo (RESEARCH §Security Domain); DetailPhoto pasa src/alt como atributos de `<img>` (no v-html), la caption va por `<MDC>` sobre el mismo contenido confiable. Reevaluar solo si v2 admite contenido de terceros. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Cero instalación en este plan (RESEARCH §Package Legitimacy Audit: F4 no instala nada). `<MDC>` viene de @nuxtjs/mdc (transitiva oficial de @nuxt/content, ya en node_modules). Sin paquete [ASSUMED]/[SUS]. |

**Conclusión:** Sin amenazas de severidad alta; sin mitigaciones bloqueantes. Estado de modos = preferencias constreñidas sin sink; MDC sobre prosa confiable versionada.
</threat_model>

<verification>
- pnpm test:unit — los 9 casos de la matriz isVisible (+ los 7 de F3) verdes; feedback < 30s.
- pnpm typecheck — TS estricto sobre util + composable + los 3 componentes globales.
- pnpm lint — limpio sobre los ficheros nuevos + eslint.config.mjs.
- grep gates: useTripModes sin classList/querySelectorAll; DetailPhoto sin onerror/@error; eslint admite Timeline; claves roma-* literales presentes.
- El render visible que consume estas piezas (DetailPhoto resuelto por MDC, isVisible filtrando el timeline, las clases de body) se asevera end-to-end en el Plan 05 (Playwright sobre el / construido).
</verification>

<success_criteria>
- isVisible reproduce la matriz EXACTA (Pitfall 4) y los 9 casos pasan en Vitest puro (FEAT-06).
- useTripModes mantiene pace/light/resumen en useState con el default prerenderizado, acopla light→slow sin revertir (Pitfall 5), aplica body.light-mode/modo-resumen vía useHead, y restaura/persiste en onMounted con las claves literales (micro-flash SC#4; FEAT-06/07/08).
- DetailPhoto.global.vue es resolvible por `<MDC>` como `.detail-photo > img` plano + caption (UI-02, frontera D-01); la decisión de Pitfall 1 (ProseUl/ProseLi globales o conflicto anotado) está tomada y justificada por grep.
- eslint admite el nombre de una palabra Timeline (que el Plan 03 crea).
</success_criteria>

<output>
Create `.planning/phases/04-render-de-contenido-modos-de-ritmo/04-01-SUMMARY.md` when done.
</output>
