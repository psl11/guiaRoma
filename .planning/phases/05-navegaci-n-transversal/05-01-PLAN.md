---
phase: 05-navegaci-n-transversal
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/utils/cardNav.ts
  - tests/unit/cardNavigation.spec.ts
autonomous: true
requirements: [FEAT-05]

must_haves:
  truths:
    - "computeActiveSection picks the LAST <section> whose offsetTop <= scrollY+130 (parity with index.html:6492-6496)"
    - "A section between scrollY+124 and scrollY+130 activates (proves +130 is load-bearing, not 124)"
    - "navStack push/pop is LIFO; canGoBack is true exactly when stack length > 0"
    - "isFichaTarget(id, monById) is true only when monById.has(id) (ficha-vs-section discriminator, D-02)"
  artifacts:
    - path: "app/utils/cardNav.ts"
      provides: "Pure DOM-free scrollspy selector + navStack helpers + ficha predicate, auto-imported by Nuxt"
      contains: "export function computeActiveSection"
    - path: "tests/unit/cardNavigation.spec.ts"
      provides: "Vitest unit coverage of the pure logic (SC#1 stack, SC#2 last-wins + +130 boundary, ficha predicate)"
      contains: "computeActiveSection"
  key_links:
    - from: "tests/unit/cardNavigation.spec.ts"
      to: "app/utils/cardNav.ts"
      via: "direct relative import (no Nuxt runtime, no @nuxt/test-utils)"
      pattern: "from '\\.\\./\\.\\./app/utils/cardNav'"
---

<objective>
Extract the DOM-free pure logic of card navigation into `app/utils/cardNav.ts` and cover it with a plain Vitest unit test. This is the Wave 0 unit scaffold for FEAT-05: the scrollspy last-wins selector (SC#2), the navStack push/pop + canGoBack derivation (SC#1), and the ficha-vs-section discriminator (D-02/SC#3 gate).

Purpose: The scrollspy algorithm and the back-stack are the two pieces with clear `expect(fn(input)).toBe(output)` contracts. Extracting them (mirroring `app/utils/pace.ts` → `tests/unit/pace.spec.ts`) gives fast Vitest coverage without a browser, and lets the composable in Plan 02 delegate to them (exactly as `useTripModes.isVisible` delegates to `pace.isVisible`). The `+130` boundary test is what proves the offset is load-bearing (Pitfall 3).
Output: `app/utils/cardNav.ts` (auto-imported pure functions) + `tests/unit/cardNavigation.spec.ts` (green under `pnpm test:unit`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-navegaci-n-transversal/05-CONTEXT.md
@.planning/phases/05-navegaci-n-transversal/05-RESEARCH.md
@.planning/phases/05-navegaci-n-transversal/05-PATTERNS.md
@.planning/phases/05-navegaci-n-transversal/05-VALIDATION.md

<interfaces>
<!-- Exact analog to copy. Read these in full; the executor mirrors their structure. -->

app/utils/pace.ts — the precedent pure-util: JSDoc cites the index.html line range, exports types, single pure function with the parity matrix, no I/O / no state / no Nuxt-Vue dependency. Auto-imported by Nuxt as `isVisible`.

tests/unit/pace.spec.ts — the precedent unit test: `import { isVisible } from '../../app/utils/pace'`, plain Vitest `describe`/`it`/`expect`, one `it` per case, NO `@nuxt/test-utils`.

Source of truth (index.html, verbatim — port the algorithm 1:1):
  - Scrollspy: lines 6488-6501 — `const scrollY = window.scrollY + 130; let current=''; sections.forEach(s => { if (scrollY >= s.offsetTop) current = s.id })`. LAST matching section wins. The `+130 > scroll-padding-top:124px` comment is lines 6489-6491 (load-bearing).
  - Stack: lines 6382-6409 — `const navStack = []`; `navigateToCard` does `navStack.push(window.scrollY)`; `goBack` does `navStack.pop()`; `updateBackBtn` shows the button when `navStack.length > 0`.
  - Discriminator: the original used `querySelectorAll('.card')` (index.html:6420-6429) — D-01 REPLACES that DOM-scan with a `Map.has(id)` check against `useTrip().monById`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing unit spec for the pure card-navigation logic</name>
  <files>tests/unit/cardNavigation.spec.ts</files>
  <read_first>
    - tests/unit/pace.spec.ts (copy this exact shape: relative import of the util, plain Vitest, one `it` per case, no Nuxt runtime)
    - app/utils/pace.ts (the import target precedent — confirms the auto-import + direct-import path `../../app/utils/...`)
    - .planning/phases/05-navegaci-n-transversal/05-RESEARCH.md §Code Examples (the verbatim `computeActiveSection` + navStack port) and §Validation Architecture (Wave 0 coverage list)
    - .planning/phases/05-navegaci-n-transversal/05-VALIDATION.md (Per-Task Verification Map rows: assign this task to the three `unit` rows for SC#1/SC#2/SC#2-predicate)
    - index.html lines 6382-6409 and 6488-6501 (the parity source of truth)
  </read_first>
  <behavior>
    - computeActiveSection: with sections `[{id:'inicio',offsetTop:0},{id:'mapa',offsetTop:800},{id:'viernes',offsetTop:1600}]`, `computeActiveSection(0, …)` returns 'inicio'; `computeActiveSection(900, …)` returns 'mapa' (900+130=1030 >= 800, < 1600); `computeActiveSection(1500, …)` returns 'viernes' (1500+130=1630 >= 1600).
    - computeActiveSection last-wins: when two sections both satisfy `y >= offsetTop`, the LAST in array order wins (iterate-and-overwrite, never break early).
    - computeActiveSection +130 boundary (load-bearing): a section at `offsetTop = 1000` with `scrollY = 875` must NOT activate via 124 but MUST activate via 130 — assert `computeActiveSection(875, [{id:'a',offsetTop:0},{id:'target',offsetTop:1000}])` returns 'target' (875+130=1005 >= 1000), and that a scrollY where `scrollY+124 < 1000 <= scrollY+130` (i.e. scrollY=874: 874+124=998 < 1000, 874+130=1004 >= 1000) still returns 'target'. This proves the offset is 130, not 124.
    - computeActiveSection empty/before-first: `computeActiveSection(0, [{id:'x',offsetTop:50}])` returns '' (nothing satisfied yet, default empty string = no active pill).
    - navStack helpers: `pushScroll([], 200)` returns `[200]`; `pushScroll([200], 450)` returns `[200,450]`; `popScroll([200,450])` returns `{ top: 450, rest: [200] }` (LIFO); `popScroll([])` returns `{ top: undefined, rest: [] }`.
    - canGoBack derivation: a stack of length 0 → false; length >= 1 → true.
    - isFichaTarget(id, monById): with `monById = new Map([['g-fortunata', {}], ['vaticano', {}]])`, `isFichaTarget('g-fortunata', monById)` is true, `isFichaTarget('reservas', monById)` is false (section → not intercepted, D-02).
  </behavior>
  <action>
    Create `tests/unit/cardNavigation.spec.ts` as a plain Vitest spec mirroring `tests/unit/pace.spec.ts` exactly. Import the as-yet-unwritten functions directly: `import { computeActiveSection, pushScroll, popScroll, isFichaTarget } from '../../app/utils/cardNav'`. Do NOT import any Nuxt runtime and do NOT add `@nuxt/test-utils` (RESEARCH §Environment Availability forbids it). Write `describe` blocks per concern with the cases enumerated in `<behavior>`: SC#2 scrollspy (basic last-wins, the +130 vs 124 boundary, empty-before-first), SC#1 stack (pushScroll append, popScroll LIFO returning `{top, rest}`, empty-pop guard, canGoBack as `stack.length > 0`), and the ficha-vs-section predicate against a mock `Map`. Use a top-of-file JSDoc citing `index.html:6382-6409` (stack) and `index.html:6488-6501` (scrollspy) and FEAT-05 SC#1/SC#2/SC#3, in the style of `pace.spec.ts`. Run it now and confirm it FAILS (module `cardNav` does not exist yet) — that is the RED state. Do not write `app/utils/cardNav.ts` in this task.
  </action>
  <verify>
    <automated>pnpm test:unit 2>&1 | grep -Eiq "cardNavigation|Cannot find module|cardNav" && echo "RED confirmed (spec runs and fails on missing cardNav)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/cardNavigation.spec.ts exists and is picked up by `vitest.config.ts` (`include: ['tests/unit/**']`).
    - The spec imports from `'../../app/utils/cardNav'` (source assertion: file contains `from '../../app/utils/cardNav'`).
    - The spec does NOT reference `@nuxt/test-utils` or `mountSuspended` (source assertion: `grep -L "@nuxt/test-utils" tests/unit/cardNavigation.spec.ts` lists the file).
    - The spec contains an explicit `+130` boundary case asserting a section at offsetTop 1000 activates at scrollY 874 (source assertion: file contains both `1000` and a comment or value tying `874`/`875` to the 130-vs-124 distinction).
    - `pnpm test:unit` currently FAILS because `app/utils/cardNav.ts` does not exist (RED).
  </acceptance_criteria>
  <done>Spec file exists, imports the pure util by relative path, enumerates SC#1/SC#2/SC#3 cases including the +130 boundary, and fails because the util is not yet implemented.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement app/utils/cardNav.ts (verbatim port of the pure logic)</name>
  <files>app/utils/cardNav.ts</files>
  <read_first>
    - app/utils/pace.ts (mirror its file shape: JSDoc citing index.html lines, exported types, pure functions, no I/O/state/Nuxt)
    - tests/unit/cardNavigation.spec.ts (the contract just written in Task 1 — implement exactly to satisfy it)
    - index.html lines 6382-6409 (navStack/navigateToCard/goBack/updateBackBtn) and 6488-6501 (updateActivePill, the `+130` comment)
    - .planning/phases/05-navegaci-n-transversal/05-PATTERNS.md §"app/utils/cardNav.ts (NEW — pure-logic extraction)" (the verbatim computeActiveSection and the delegation precedent useTripModes.ts:50-51)
  </read_first>
  <action>
    Create `app/utils/cardNav.ts` exporting the pure, DOM-free functions the spec imports. Implement `computeActiveSection(scrollY: number, sections: { id: string, offsetTop: number }[]): string` as the verbatim port of index.html:6492-6496: compute `const y = scrollY + 130` (the literal `+130` — never 124; cite the load-bearing comment), iterate ALL sections with a `for` loop assigning `current = s.id` whenever `y >= s.offsetTop` (last-wins, no early break), default `current = ''`. Implement `pushScroll(stack: number[], scrollY: number): number[]` returning a new array `[...stack, scrollY]` (immutable helper for the unit test; the composable will mutate its `useState` ref directly). Implement `popScroll(stack: number[]): { top: number | undefined, rest: number[] }` returning the last element and the remaining array (LIFO; `{ top: undefined, rest: [] }` for an empty stack). Implement `isFichaTarget(id: string, monById: Map<string, unknown>): boolean` returning `monById.has(id)` (the D-02 ficha-vs-section discriminator that replaces the original `querySelectorAll('.card')` DOM-scan). Export a `Section` type `{ id: string; offsetTop: number }` for reuse by the controller. Add a top-of-file JSDoc in the style of `pace.ts` citing `index.html:6488-6501` and `index.html:6382-6409`, noting these are auto-imported by Nuxt and tested in plain Vitest. Do NOT reference `window`, `document`, Vue, or Nuxt anywhere in this file. Run the spec and confirm GREEN.
  </action>
  <verify>
    <automated>pnpm test:unit 2>&1 | tail -5 && pnpm test:unit >/dev/null 2>&1 && echo "GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit` exits 0 (all unit specs green, including cardNavigation.spec.ts and the pre-existing pace/dayLabel specs).
    - `app/utils/cardNav.ts` contains `export function computeActiveSection` and the literal token `+ 130` (source assertion: `grep -E "\\+ ?130" app/utils/cardNav.ts` matches).
    - `app/utils/cardNav.ts` contains NO `window`, `document`, `useState`, or `import ... from 'vue'`/`'nuxt'` (source assertion: `grep -E "window|document|useState|from 'vue'|from 'nuxt'" app/utils/cardNav.ts` returns nothing).
    - `pnpm typecheck` exits 0 (the new util compiles under TS strict).
    - `pnpm lint` exits 0 (the new util passes `@nuxt/eslint`; no eslint config change needed — it is a util, not a single-word component).
  </acceptance_criteria>
  <done>`app/utils/cardNav.ts` implements computeActiveSection (verbatim +130 last-wins), pushScroll/popScroll, and isFichaTarget as pure functions; `pnpm test:unit`, `pnpm typecheck`, and `pnpm lint` all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure functions over in-memory values. No DOM, no network, no user input, no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `isFichaTarget(id, monById)` — the ficha-vs-section gate | mitigate | The predicate is the security control itself (RESEARCH §Security Domain V5): only ids present in `monById` (built from zod-validated F2 content, not runtime user input) are treated as fichas. An arbitrary id falls through to a native section jump. This plan implements the predicate as a pure `Map.has(id)`; `getElementById` (Plan 02) is not an injection sink. Severity: LOW. |

Static SSG; no server, no auth, no persisted user input. This plan adds only pure functions — no executable threat surface beyond documenting that the ficha discriminator is the bounded gate consumed in Plan 02.
</threat_model>

<verification>
- `pnpm test:unit` green (cardNavigation.spec.ts + existing specs).
- `pnpm typecheck` and `pnpm lint` clean.
- `app/utils/cardNav.ts` is DOM-free and Nuxt-free (pure logic only).
</verification>

<success_criteria>
- SC#2 (scrollspy selector) and SC#1 (navStack/canGoBack) and the D-02 ficha predicate are covered by fast Vitest unit tests, including the `+130` vs `124` boundary case that proves the offset is load-bearing.
- The pure util is ready for Plan 02's controller to delegate to (no DOM logic duplicated).
</success_criteria>

<output>
Create `.planning/phases/05-navegaci-n-transversal/05-01-SUMMARY.md` when done.
</output>
