---
phase: 05-navegaci-n-transversal
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - tests/parity/navigation.spec.ts
  - app/composables/useCardNavigation.ts
autonomous: false
requirements: [FEAT-05]

must_haves:
  truths:
    - "Clicking a prose a[href^='#g-...'] (a ficha) intercepts the click: target .card gains .highlight, scroll position moves — NOT a native instant jump"
    - "Clicking 'Volver' restores the prior scroll position and the .back-btn.show class disappears (stack emptied)"
    - "Scrolling to a section offset marks the correct .nav-pill.active using the scrollY+130 switch point (not IntersectionObserver timing)"
    - "Clicking a section pill (#reservas) is NOT intercepted: native anchor jump, URL hash changes, no .highlight (proves only fichas are intercepted, D-02/D-03)"
    - "Pitfall 1 is resolved empirically: bubble-phase vs capture-phase is decided by which one reproduces parity in the real browser; the chosen mechanism is committed in useCardNavigation.ts"
  artifacts:
    - path: "tests/parity/navigation.spec.ts"
      provides: "Self-contained Playwright behavior spec (build+serve under /guiaRoma/) covering SC#1/SC#2/SC#3"
      contains: "navigateToCard"
  key_links:
    - from: "tests/parity/navigation.spec.ts"
      to: ".output/public (pnpm generate)"
      via: "self-contained beforeAll: generate once, serve under /guiaRoma/ (mirror of modes.spec.ts)"
      pattern: "guiaRoma"
    - from: "tests/parity/navigation.spec.ts"
      to: "app/composables/useCardNavigation.ts"
      via: "asserts the wired behavior (.highlight, .active, .show, interception) in a real browser"
      pattern: "highlight"
---

<objective>
Prove FEAT-05's three success criteria in a real browser with a self-contained Playwright spec (mirroring `tests/parity/modes.spec.ts`), and use it to empirically resolve Pitfall 1 — whether the delegated click listener must run in bubble phase (default) or capture phase to beat NuxtLink's own `onClick` on prose links. If capture is required for parity, apply the one-line fix to `useCardNavigation.ts`. Close the phase with the full suite green and a human golden parity sign-off (consistent with the F3/F4 close).

Purpose: SC#1/SC#2/SC#3 are DOM/scroll behaviors (smooth scroll, `.highlight`, reactive `.active`/`.show`, prose-interception-vs-native-jump) that jsdom cannot validate realistically — they need a real browser. This is also the only place to settle research assumption A1 (bubble vs capture): the spec is the empirical decider. The human checkpoint confirms the navigation feels identical to the live `index.html` (the parity bar).
Output: `tests/parity/navigation.spec.ts` (green under `pnpm test:golden`) + a possible capture-phase tweak to the controller + recorded sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-navegaci-n-transversal/05-CONTEXT.md
@.planning/phases/05-navegaci-n-transversal/05-RESEARCH.md
@.planning/phases/05-navegaci-n-transversal/05-PATTERNS.md
@.planning/phases/05-navegaci-n-transversal/05-VALIDATION.md

<interfaces>
<!-- The exact harness to copy + the behaviors to assert. -->

HARNESS to mirror VERBATIM (read in full) — tests/parity/modes.spec.ts:
  - `EXPECTED_HYDRATION_MSG = /Hydration completed but contains mismatches/i` — tolerate ONLY the color-mode hydration message; fail on any other console error.
  - `OUTPUT_DIR = join(process.cwd(), '.output', 'public')`.
  - helpers `waitForServer(url, timeoutMs)`, `killGroup(proc)`, `ensureBuild()` (runs `pnpm generate` once if `.output/public/index.html` is missing).
  - `beforeAll`: `ensureBuild()`, `mkdtempSync` a tmp root, `mkdirSync` a `guiaRoma/` subdir, `cpSync(OUTPUT_DIR, subDir)`, `spawn('pnpm', ['dlx','serve','-l',PORT,previewRoot], { detached:true })`, `waitForServer`. STATIC_URL = `http://localhost:${PORT}/guiaRoma/`. PORT = `5700 + Number(process.env.TEST_WORKER_INDEX ?? 0)` (pick a different base, e.g. 5720, to avoid clashing with modes.spec's 5700).
  - `afterAll`: `killGroup(server)` + `rmSync(previewRoot)`.
  - `gotoHydrated(page)`: `page.goto(STATIC_URL)` + `waitForLoadState('networkidle')` + assert a known hydrated reactive state before interacting.

DOM targets (from F3/F4, confirmed):
  - Ficha scroll target: `<article :id="monument.slug" class="card">` (MonumentCard.vue) — e.g. `#g-fortunata`, `#vaticano`.
  - Prose ficha link: an `<a href="#g-...">` rendered by MDC inside `.card-section` prose (a ficha cross-ref).
  - Timeline ficha link: `<a :href="'#'+row.ref" class="tl-title">` (TimelineStop.vue).
  - Section pill: `<a href="#reservas" class="nav-pill">` in `#nav-pills` (a SECTION, not a ficha → native jump).
  - Back button: `<button id="back-btn" class="back-btn">` — reveals via `.back-btn.show` (base.css:1025).
  - Active pill: `.nav-pill.active` (base.css:90).
  - Highlight: `.card.highlight` (base.css:694), present ~2500ms after navigateToCard.

SC → assertion map (RESEARCH §Phase Requirements → Test Map; VALIDATION §Per-Task Verification Map):
  - SC#1 (behavior): prose `#g-...` click → that `.card` gains `.highlight` AND `window.scrollY` changed from its pre-click value; then click "Volver" → `window.scrollY` returns toward the pre-click value AND `.back-btn` no longer has `.show`.
  - SC#2 (behavior): `page.evaluate(() => window.scrollTo(0, Y))` to a known section offset → the correct `.nav-pill.active`; assert the switch matches `scrollY+130` semantics (e.g. just-before vs just-after a section's offsetTop flips the active pill), NOT IntersectionObserver timing.
  - SC#3 (behavior): prose ficha `#g-...` click → intercepted (highlight + stack, hash unchanged); `#reservas` section pill click → native jump (URL hash becomes `#reservas`, NO `.highlight`).

Pitfall 1 (A1) — the empirical decision:
  - Default: controller registers `document.addEventListener('click', onDelegatedClick)` in bubble phase (Plan 05-02 default).
  - If the spec shows a prose-link click jumps WITHOUT smooth scroll / WITHOUT `.highlight` / "Volver" does not appear → NuxtLink's onClick is winning; switch the controller to capture phase: `addEventListener('click', onDelegatedClick, true)` and add `e.stopPropagation()` after `e.preventDefault()` to cut NuxtLink off (RESEARCH Pitfall 1 / PATTERNS Critical Pitfall).

Test runner: `pnpm test:golden` (package.json:14 → `playwright test`). `tests/parity/**` is eslint-ignored (eslint.config.mjs:16) — format as in modes.spec.ts; do NOT add `@nuxt/test-utils`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-contained Playwright spec covering SC#1/SC#2/SC#3 + resolve Pitfall 1 empirically</name>
  <files>tests/parity/navigation.spec.ts, app/composables/useCardNavigation.ts</files>
  <read_first>
    - tests/parity/modes.spec.ts (the EXACT harness to mirror: waitForServer/killGroup/ensureBuild, the beforeAll generate+copy+serve, afterAll cleanup, gotoHydrated, the EXPECTED_HYDRATION_MSG tolerance — copy verbatim, change only the port base and the assertions)
    - app/composables/useCardNavigation.ts (the unit under test from Plan 05-02 — the delegated listener whose phase this task may flip)
    - .planning/phases/05-navegaci-n-transversal/05-RESEARCH.md §Common Pitfalls Pitfall 1 (bubble vs capture, the A1 decision) and §Validation Architecture
    - .planning/phases/05-navegaci-n-transversal/05-PATTERNS.md §"tests/parity/navigation.spec.ts (NEW)" and §"Critical Pitfall"
    - .planning/phases/05-navegaci-n-transversal/05-VALIDATION.md (assign this task to the behavior rows for SC#1/SC#2/SC#3 in the Per-Task Verification Map)
    - app/assets/css/base.css lines 90, 694, 1025 (the classes the spec asserts: `.nav-pill.active`, `.card.highlight`, `.back-btn.show`)
    - app/components/MonumentCard.vue (confirm `<article :id="monument.slug" class="card">` and a real `.card-section` prose ficha link to use as the SC#1/SC#3 fixture) and app/components/NavPills.vue (the `#reservas` section pill for the SC#3 native-jump case)
  </read_first>
  <action>
    Create `tests/parity/navigation.spec.ts` by copying the harness scaffolding from `tests/parity/modes.spec.ts` VERBATIM: the `EXPECTED_HYDRATION_MSG` regex, `OUTPUT_DIR`, `waitForServer`/`killGroup`/`ensureBuild`, the `beforeAll` (generate once + copy `.output/public` into a `guiaRoma/` tmp subdir + `pnpm dlx serve` detached), the `afterAll` cleanup, and a `gotoHydrated(page)` helper. Use a distinct port base (e.g. `5720 + TEST_WORKER_INDEX`) so it does not collide with modes.spec's 5700. In `gotoHydrated`, after `goto` + `networkidle`, assert a known hydrated state before interacting (e.g. wait until `#back-btn` exists and a known initial `.nav-pill.active` is set by the initial `updateActivePill()`). Attach the console-error listener that pushes any error NOT matching `EXPECTED_HYDRATION_MSG`, and assert the array is empty at the end of each test.

    Write three tests:
    - SC#1 "navigate-to-ficha highlights + back restores scroll": locate a prose ficha link inside a `.card-section` (an `<a href="#g-...">`), record `window.scrollY` (page.evaluate), click it, then assert the target `.card` (by `#<id>`) has class `highlight` (Playwright `toHaveClass(/\bhighlight\b/)`) and `window.scrollY` differs from the recorded value. Then click `#back-btn` and assert `window.scrollY` returns toward the recorded origin and `#back-btn` no longer has `.show` (the stack is empty → `canGoBack` false). Use `expect.poll`/`toHaveClass` for the smooth-scroll settle (interaction+settle pattern from modes.spec).
    - SC#2 "scrollspy marks the correct pill at scrollY+130 (not IntersectionObserver)": `page.evaluate` the `offsetTop` of two adjacent `<section>`s; `window.scrollTo(0, target.offsetTop - 130 + 1)` should activate the target pill (the `+130` switch point), while `window.scrollTo(0, target.offsetTop - 130 - 5)` should still show the previous pill — asserting the switch happens at `+130`, not at the element top (which is what IntersectionObserver would key on). Assert the matching `.nav-pill[href="#<id>"]` has `.active` and the others do not.
    - SC#3 "only fichas are intercepted; section pills jump natively": click a prose `#g-...` ficha link → assert `.highlight` present AND `page.url()` hash did NOT change to `#g-...` (D-03 — interception preventDefaults the hash). Then click the `#reservas` section pill → assert `page.url()` ends with `#reservas` (native anchor jump, D-02) AND the `#reservas` `<section>` did NOT receive `.highlight` (only `.card`s highlight; sections are not fichas).

    EMPIRICAL Pitfall 1 resolution: run `pnpm test:golden -- tests/parity/navigation.spec.ts`. If SC#1 or SC#3 FAILS in a way consistent with NuxtLink winning the click (no `.highlight`, no scroll change, or "Volver" absent), edit `app/composables/useCardNavigation.ts`: change the click registration to capture phase `document.addEventListener('click', onDelegatedClick, true)` and add `e.stopPropagation()` immediately after `e.preventDefault()` in `onDelegatedClick`, update the matching `removeEventListener('click', onDelegatedClick, true)`, and update the header JSDoc to record that capture phase was empirically required (A1 resolved). Re-run until green. If bubble phase already passes, leave the controller as-is and note in the SUMMARY that bubble phase reproduced parity (A1 resolved: capture not needed). Touch NOTHING else in the composable.
  </action>
  <verify>
    <automated>pnpm test:golden 2>&1 | tail -15 && pnpm test:golden >/dev/null 2>&1 && echo "GOLDEN GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/parity/navigation.spec.ts` exists and mirrors the modes.spec harness (source assertion: contains `EXPECTED_HYDRATION_MSG`, `pnpm`+`generate` build path, and a `/guiaRoma/` static URL).
    - The spec contains three behavior tests covering SC#1 (`.card.highlight` + scroll restore), SC#2 (`.nav-pill.active` at `scrollY+130`), SC#3 (prose interception with unchanged hash + section pill native jump to `#reservas`) — source assertion: `highlight`, `nav-pill`, `#reservas`, and a `+ 130`/`- 130` token all present.
    - The spec uses a port base distinct from 5700 (source assertion: a `57[0-9][0-9]` literal other than 5700, e.g. 5720).
    - The spec tolerates ONLY the color-mode hydration error and fails on any other console error (source assertion: the `EXPECTED_HYDRATION_MSG` filter + an empty-errors assertion are present).
    - `pnpm test:golden` exits 0 (the new navigation spec AND the pre-existing golden/shell/theme/modes specs all pass).
    - If the controller was switched to capture phase, `app/composables/useCardNavigation.ts` contains `addEventListener('click', onDelegatedClick, true)` and `e.stopPropagation()`; if bubble phase passed, neither is present and the SUMMARY records that. (Exactly one of the two states is true — A1 resolved either way.)
    - The spec does NOT import `@nuxt/test-utils` (source assertion).
  </acceptance_criteria>
  <done>A self-contained Playwright spec proves SC#1 (highlight + scroll restore + `.show` toggling), SC#2 (`.nav-pill.active` switching at `scrollY+130`, not IntersectionObserver), and SC#3 (prose ficha interception with no hash change vs section pill native jump) in a real browser; Pitfall 1 (A1) is resolved empirically with the chosen click-phase committed; `pnpm test:golden` passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human golden parity sign-off (navigation identical to live index.html)</name>
  <what-built>
    FEAT-05 navegación transversal is wired and machine-verified: `useCardNavigation` singleton (navigate-to-ficha with smooth scroll + 2500ms `.highlight`, back-stack that restores scroll), the `scrollY+130` scrollspy driving `.nav-pill.active`, and the native delegated listener intercepting prose/timeline ficha links (D-01..D-05). Unit tests (`pnpm test:unit`) and the self-contained Playwright behavior spec (`pnpm test:golden`) are green; `pnpm typecheck` and `pnpm lint` are clean. This checkpoint confirms the navigation FEELS identical to the live `index.html` (the parity bar) — the same human golden sign-off F3/F4 used, since automated assertions verify presence/behavior but not subjective parity of motion.
  </what-built>
  <how-to-verify>
    1. Build + serve the static site under the production subpath:
       `pnpm generate` then `pnpm dlx serve -l 5050 .` and open `http://localhost:5050/guiaRoma/` (or run `pnpm preview` and use that URL). Confirm there are no console errors other than the known color-mode hydration message.
    2. SC#3 + SC#1 (ficha interception): scroll into a day, find a prose link inside a `.card-section` that points to another ficha (e.g. a `#g-...` gastro cross-ref) or a timeline `tl-title` link. Click it. EXPECT: the page glides (smooth scroll, not an instant jump) to that ficha, the ficha shows the gold `.highlight` border for ~2.5s, the URL hash does NOT change, and the "Volver" button appears bottom-right.
    3. SC#1 (back stack): click "Volver". EXPECT: the page glides back to where you were before the click, and "Volver" disappears.
    4. SC#3 (sections jump natively): click a SECTION pill in the top nav (e.g. "Reservas"). EXPECT: a native anchor jump, the URL hash becomes `#reservas`, and NO highlight border — proving only fichas are intercepted.
    5. SC#2 (scrollspy): scroll slowly through the page. EXPECT: the active nav pill switches to the section you are entering at the SAME point the live `index.html` switches it (the `+130` offset); compare side-by-side against the live guide (open `index.html` directly) if unsure.
    6. Repeat steps 2-5 in BOTH light and dark theme and at mobile + desktop widths — confirm the highlight/active/show styling matches the live guide in all four.
    Cross-reference the live behavior at the deployed/`index.html` original to confirm 100% parity (Core Value).
  </how-to-verify>
  <resume-signal>Type "approved" if the navigation matches the live guide in all four theme/width combinations, or describe any divergence (jump instead of smooth scroll, missing/late highlight, wrong active-pill switch point, intercepted section link, hash changing on ficha nav).</resume-signal>
  <action>
    Pause for the human golden parity sign-off (the F3/F4 close pattern). Automated tests confirm presence/behavior; this gate confirms the SUBJECTIVE parity of motion against the live guide. Build and serve per <how-to-verify>, walk the SC#1/SC#2/SC#3 steps in light and dark theme at mobile and desktop widths, and compare side-by-side with the original `index.html`. Do not modify code in this task; if a divergence is reported, route it back to Task 1 (spec + controller) for correction.
  </action>
  <verify>
    <human-check>Human confirms the navigation (smooth scroll-to-ficha, ~2.5s gold highlight, back-stack scroll restore, scrollspy active-pill switch point at +130, section pills jumping natively while ficha links are intercepted with no hash change) is identical to the live `index.html` in light/dark x mobile/desktop. Console shows no error other than the known color-mode hydration message.</human-check>
  </verify>
  <done>A human has signed off that FEAT-05 navigation is 100% behaviorally identical to the live guide (Core Value) in all four theme/width combinations; any reported divergence has been corrected and re-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (inherited from 05-02) | This plan adds a test spec + at most a one-line click-phase change. The behavioral surface (delegated listener reading `href`) is unchanged from Plan 05-02; see its threat register. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | delegated click → `getElementById` (capture-phase variant) | mitigate | Switching to capture phase does not change the trust boundary: the listener still gates on `isFichaTarget(id, monById)` before acting, still `preventDefault`s, and `getElementById` is still not an injection sink. Capture phase only changes ordering vs NuxtLink's handler (RESEARCH Pitfall 1). Severity: LOW. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs NOTHING (RESEARCH §Package Legitimacy Audit: zero new packages; do NOT add `@nuxt/test-utils`). Uses only Playwright already audited in F1. |

Static SSG; no server, no auth, no persisted user input. No new threat surface beyond the bounded delegated listener already documented in Plan 05-02. Expected severity LOW.
</threat_model>

<verification>
- `pnpm test:unit && pnpm test:golden` both green (full suite: unit logic + behavior, plus the pre-existing golden/shell/theme/modes specs).
- `pnpm typecheck` and `pnpm lint` clean.
- Pitfall 1 (A1) resolved empirically — the committed click-phase reproduces parity.
- Human golden sign-off recorded (navigation identical to live `index.html` in light/dark × mobile/desktop).
</verification>

<success_criteria>
- SC#1, SC#2, SC#3 all proven in a real browser by `tests/parity/navigation.spec.ts`.
- The bubble-vs-capture question (A1) is settled by the spec, with the winning mechanism committed to `useCardNavigation.ts`.
- Human confirms 100% parity of navigation behavior against the live guide (Core Value) — FEAT-05 complete.
</success_criteria>

<output>
Create `.planning/phases/05-navegaci-n-transversal/05-03-SUMMARY.md` when done. Record in it which click phase (bubble or capture) resolved Pitfall 1 / A1.
</output>
