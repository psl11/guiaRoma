---
phase: 05-navegaci-n-transversal
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/composables/useCardNavigation.ts
  - app/components/NavPills.vue
  - app/components/BackButton.vue
  - app/components/TripView.vue
autonomous: true
requirements: [FEAT-05]

must_haves:
  truths:
    - "useCardNavigation() returns navStack, activeSection, canGoBack, navigateToCard, goBack as a useState singleton (callable from any component, no side effects)"
    - "useCardNavigationController() registers ONE native document click listener + ONE passive scroll listener in onMounted and removes them in onUnmounted"
    - "navigateToCard preventDefaults, pushes window.scrollY, scrollIntoView smooth block:start, adds .highlight for 2500ms (verbatim index.html:6390-6398)"
    - "goBack pops the stack and window.scrollTo smooth to the prior position (verbatim index.html:6403-6409)"
    - "the delegated click listener intercepts a[href^='#'] ONLY when monById.has(id) (D-02); section links fall through to native anchor jump"
    - "NavPills binds .active reactively per pill via activeSection; BackButton binds .show via canGoBack and @click=goBack — neither shell's markup is restructured"
    - "default state is navStack=[] and activeSection='' so SSR/hydration emit no .active pill and no .show button (zero hydration mismatch)"
  artifacts:
    - path: "app/composables/useCardNavigation.ts"
      provides: "Singleton accessor + effects controller for card navigation (D-05 API for F5 links, F6 search, F7 map)"
      exports: ["useCardNavigation", "useCardNavigationController"]
      contains: "export function useCardNavigationController"
    - path: "app/components/NavPills.vue"
      provides: "F3 shell wired with reactive .active per pill (markup unchanged)"
      contains: ":class"
    - path: "app/components/BackButton.vue"
      provides: "F3 shell wired with reactive .show + @click=goBack (markup unchanged)"
      contains: "@click"
    - path: "app/components/TripView.vue"
      provides: "Page owner invokes useCardNavigationController() exactly once"
      contains: "useCardNavigationController()"
  key_links:
    - from: "app/composables/useCardNavigation.ts"
      to: "app/utils/cardNav.ts"
      via: "delegate computeActiveSection (auto-imported)"
      pattern: "computeActiveSection"
    - from: "app/composables/useCardNavigation.ts"
      to: "app/composables/useTrip.ts"
      via: "monById for ficha-vs-section discrimination"
      pattern: "useTrip"
    - from: "app/components/TripView.vue"
      to: "app/composables/useCardNavigation.ts"
      via: "useCardNavigationController() called once in <script setup>"
      pattern: "useCardNavigationController"
    - from: "app/components/NavPills.vue"
      to: "app/composables/useCardNavigation.ts"
      via: "useCardNavigation() accessor for activeSection"
      pattern: "useCardNavigation"
    - from: "app/components/BackButton.vue"
      to: "app/composables/useCardNavigation.ts"
      via: "useCardNavigation() accessor for canGoBack + goBack"
      pattern: "useCardNavigation"
---

<objective>
Build `app/composables/useCardNavigation.ts` (the singleton accessor + effects controller, copied structurally from `useTripModes`) and wire it into the three F3/F4 shells: `TripView` invokes the controller once; `NavPills` binds the reactive `.active`; `BackButton` binds `.show` + `@click="goBack"`. This is the heart of FEAT-05 — a 1:1 parity port of index.html's navigation/back-stack (6382-6409) and scrollspy (6488-6501), wrapped in the established singleton pattern, with the delegated click listener resolving Pitfall 1 (prose links are NuxtLink).

Purpose: Implement all three behaviors (D-01 event-delegation interception, D-04 scrollspy, the navigate/back stack) and the D-05 stable public API so F6 (search) and F7 (map) plug in without refactor. The composable is the single owner of `navStack`/`activeSection`; the shells only gain reactive bindings (the F3→F4 "shell wired in its phase" pattern), writing ZERO new CSS.
Output: One new composable + three modified components, all consuming the same `useState` singleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-navegaci-n-transversal/05-CONTEXT.md
@.planning/phases/05-navegaci-n-transversal/05-RESEARCH.md
@.planning/phases/05-navegaci-n-transversal/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor uses directly — do NOT explore the codebase to rediscover them. -->

PRECEDENT to copy (read in full) — app/composables/useTripModes.ts:
  - `useTripModes()` = PURE accessor: only reads `useState` keys and returns `{ pace, light, resumen, isVisible }`. Idempotent, callable from any component.
  - `useTripModesController()` = EFFECTS, invoked ONCE in TheHero: `watch`, `useHead({bodyAttrs})`, and `onMounted` (restore + register persist watches).
  - DEFAULT = prerendered HTML: `useState('pace', () => 'optimistic')` — the initial value equals what SSR emits → zero hydration mismatch.
  - `isVisible` delegates to the pure `isVisible` from `~/utils/pace` (the delegation precedent; cardNav delegates the same way).

DATA SOURCE (read-only) — app/composables/useTrip.ts:
  - `const { monById } = await useTrip(slug)` → `monById` is `ComputedRef<Map<slug, Monument>>`, keyed by slug (= the `#fragment` of links). `useTrip` is `async` and uses `useAsyncData`.

PURE LOGIC (from Plan 05-01, auto-imported) — app/utils/cardNav.ts:
  - `computeActiveSection(scrollY: number, sections: { id: string, offsetTop: number }[]): string` (verbatim +130 last-wins)
  - `isFichaTarget(id: string, monById: Map<string, unknown>): boolean`
  - type `Section = { id: string; offsetTop: number }`

SHELLS to wire (F3, markup is LOCKED — append bindings only):
  - app/components/NavPills.vue — `<nav id="nav-pills">` with 7 structural `<a class="nav-pill" href="#inicio|#mapa|#reservas|#gastronomia|#practica|#arte|#arquitectura">` + a `v-for="d in props.days"` emitting `<a :href="`#${d.slug}`" class="nav-pill">`. `<script setup>` currently only `defineProps<{ days: Day[] }>()`.
  - app/components/BackButton.vue — `<button id="back-btn" class="back-btn" aria-label="Volver"><span class="back-btn-arrow">←</span> Volver</button>`. Currently has NO `<script>` block.
  - app/components/TripView.vue — page owner; `<script setup>` has `const props = defineProps<{ slug: string }>()` then `const { trip, days, monById, food, artists, refById } = await useTrip(props.slug)`. Add the controller call here (TheHero is the precedent: it calls `useTripModesController()` once).

CONTROLLER-INVOCATION precedent — TheHero.vue calls `useTripModesController()` then `const { ... } = useTripModes()` in <script setup>.

SOURCE OF TRUTH (index.html, port 1:1):
  - 6382-6409: navStack/updateBackBtn/navigateToCard/goBack. `navigateToCard(id, event)`: `if (event) event.preventDefault(); navStack.push(window.scrollY); const el = document.getElementById(id); if (el) { el.scrollIntoView({behavior:'smooth',block:'start'}); el.classList.add('highlight'); setTimeout(() => el.classList.remove('highlight'), 2500); }`. `goBack()`: `const prev = navStack.pop(); if (typeof prev === 'number') window.scrollTo({top: prev, behavior:'smooth'});`.
  - 6420-6429: `bindCardLinks` (the DOM-scan being REPLACED by delegation — D-01).
  - 6488-6501: `updateActivePill` (`const scrollY = window.scrollY + 130; ... sections.forEach(s => { if (scrollY >= s.offsetTop) current = s.id }); ...`) + `window.addEventListener('scroll', updateActivePill, { passive: true })`.
  - 6649-6659: `init()` calls `updateActivePill()` then `bindCardLinks()` — the controller's onMounted mirrors this (initial `updateActivePill()` + register listeners).
  - DO NOT port `highlightCard` (6411-6417, legacy alias — confirmed dropped).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useCardNavigation.ts — singleton accessor + effects controller</name>
  <files>app/composables/useCardNavigation.ts</files>
  <read_first>
    - app/composables/useTripModes.ts (the EXACT structural precedent — read whole: accessor/controller split, useState defaults = prerendered, onMounted/onUnmounted lifecycle, the header rationale on why effects run once)
    - app/composables/useTrip.ts (provides `monById`; note it is `async` and returns a computed Map keyed by slug)
    - app/utils/cardNav.ts (Plan 05-01 — `computeActiveSection`, `isFichaTarget`, type `Section`; delegate to these)
    - .planning/phases/05-navegaci-n-transversal/05-RESEARCH.md §Common Pitfalls Pitfall 1 (NuxtLink onClick), §Pattern 3 (native listener register + cleanup), §Code Examples
    - .planning/phases/05-navegaci-n-transversal/05-PATTERNS.md §"useCardNavigation.ts (NEW)" and §"the two listeners (controller body)"
    - index.html lines 6382-6409, 6488-6501, 6649-6659 (parity source of truth)
  </read_first>
  <behavior>
    - Accessor purity: `useCardNavigation()` returns `{ navStack, activeSection, canGoBack, navigateToCard, goBack }` reading the same `useState` keys; calling it from two components yields the SAME singleton state (no per-call listeners, no onMounted).
    - Default = prerendered: `navStack` defaults to `[]` and `activeSection` to `''` so a freshly mounted (un-scrolled, un-clicked) app has `canGoBack === false` and no active section.
    - navigateToCard: after `navigateToCard('vaticano', evt)` with `evt` a real Event, `evt.preventDefault()` was called, `navStack.value.length` increased by 1, and (in a DOM) the `#vaticano` element gains class `highlight` which is removed ~2500ms later. The `if (el)` guard is present (no-op when getElementById returns null — Pitfall 2).
    - goBack: after a push then `goBack()`, the stack length decreases by 1 and `window.scrollTo` is called with the popped `top` and `behavior:'smooth'`; `goBack()` on an empty stack is a no-op (the `typeof prev === 'number'` guard).
    - canGoBack reactivity: `canGoBack` is a computed that is `true` immediately after the first `navigateToCard` push and `false` after the matching `goBack`.
    - Delegated click: a native `document` click listener calls `closest('a[href^="#"]')`; if the href fragment satisfies `isFichaTarget(id, monById.value)` it `preventDefault()`s and calls `navigateToCard(id, e)`; otherwise it returns early (section link → native jump, D-02).
    - Scrollspy: a passive `scroll` listener reads all `document.querySelectorAll('section')` into `{id, offsetTop}` and sets `activeSection.value = computeActiveSection(window.scrollY, sections)`.
    - Listener count: the controller registers exactly one click + one scroll listener (Pitfall 4) and removes both in onUnmounted with the same function references.
  </behavior>
  <action>
    Create `app/composables/useCardNavigation.ts` with the two-part structure copied from `useTripModes.ts`.

    ACCESSOR `useCardNavigation()` (pure, no effects): declare `const navStack = useState<number[]>('cardNav:stack', () => [])` and `const activeSection = useState<string>('cardNav:activeSection', () => '')` — the `[]`/`''` defaults are load-bearing for hydration parity (they equal the F3 shells' SSR markup: no `.show`, no `.active`). Declare `const canGoBack = computed(() => navStack.value.length > 0)`. Define `navigateToCard(id: string, event?: Event)` as the verbatim port of index.html:6390-6398: `if (event) event.preventDefault()` (D-03 — the URL does NOT change); `navStack.value.push(window.scrollY)`; `const el = document.getElementById(id)`; inside `if (el)`: `el.scrollIntoView({ behavior: 'smooth', block: 'start' })`, `el.classList.add('highlight')`, `setTimeout(() => el.classList.remove('highlight'), 2500)` (the literal 2500 ms; rely on `scroll-padding-top:124px` for the fixed-header offset — Pitfall 5, add NO manual offset). Define `goBack()` as the verbatim port of 6403-6409: `const prev = navStack.value.pop(); if (typeof prev === 'number') window.scrollTo({ top: prev, behavior: 'smooth' })`. Return `{ navStack, activeSection, canGoBack, navigateToCard, goBack }`. Do NOT port `highlightCard`. Do NOT register any listener or call `onMounted` in the accessor.

    CONTROLLER `useCardNavigationController()` (effects, called once): inside it call `const { navigateToCard, activeSection } = useCardNavigation()` and `const { monById } = await useTrip('roma')` — confirm the slug matches what TripView uses (Pitfall 2; `useTrip` is async, so the controller awaits it, and TripView already awaits useTrip so calling the controller before/after is fine in `<script setup>` with top-level await). Define `function onDelegatedClick(e: MouseEvent)`: `const a = (e.target as HTMLElement).closest('a[href^="#"]'); if (!a) return; const id = a.getAttribute('href')!.slice(1); if (!isFichaTarget(id, monById.value)) return; e.preventDefault(); navigateToCard(id, e)`. Define `function updateActivePill()`: build `const sections = Array.from(document.querySelectorAll('section')).map(s => ({ id: s.id, offsetTop: (s as HTMLElement).offsetTop }))` then `activeSection.value = computeActiveSection(window.scrollY, sections)`. In `onMounted`: `document.addEventListener('click', onDelegatedClick)` (NATIVE listener on `document`, NOT a Vue `@click` — Pitfall 1; default bubble phase for now, Plan 05-03 empirically decides whether capture phase is needed), `window.addEventListener('scroll', updateActivePill, { passive: true })` (the `{ passive: true }` is verbatim parity with index.html:6501, not just perf), then call `updateActivePill()` once (mirrors `init()` index.html:6655). In `onUnmounted`: `document.removeEventListener('click', onDelegatedClick)` and `window.removeEventListener('scroll', updateActivePill)` (same function references; defensive hygiene for HMR / future `/trips/[slug]` nav — the one delta from `useTripModes` which only uses onMounted).

    Header JSDoc (in the `useTripModes.ts` style): cite index.html:6382-6409 / 6488-6501 / 6649-6659, name D-01..D-05, note the accessor/controller split exists to register listeners exactly once (Pitfall 4), the `useState` singleton is SSR-safe, and the default empty state = prerendered HTML. State that `useState`/`computed`/`onMounted`/`onUnmounted` are Nuxt auto-imports and `computeActiveSection`/`isFichaTarget` are auto-imported from `app/utils/cardNav.ts`. Do NOT add `<style>` (this is a .ts file) and do NOT install any dependency.
  </action>
  <verify>
    <automated>pnpm test:unit >/dev/null 2>&1 && pnpm typecheck 2>&1 | tail -3 && pnpm typecheck >/dev/null 2>&1 && echo "typecheck OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/composables/useCardNavigation.ts` exports both `useCardNavigation` and `useCardNavigationController` (source assertion: file contains `export function useCardNavigation(` and `export function useCardNavigationController(`).
    - The accessor declares `useState<number[]>('cardNav:stack', () => [])` and `useState<string>('cardNav:activeSection', () => '')` (source assertion: both literal keys + empty defaults present).
    - `navigateToCard` contains the verbatim tokens `event.preventDefault()`, `window.scrollY`, `scrollIntoView({ behavior: 'smooth', block: 'start' })`, `classList.add('highlight')`, and `2500` (source assertion via grep).
    - `goBack` contains `navStack.value.pop()` and `window.scrollTo({ top: prev, behavior: 'smooth' })` (source assertion).
    - The controller registers the click listener via `document.addEventListener('click'` and the scroll listener via `window.addEventListener('scroll', updateActivePill, { passive: true }` and removes both in `onUnmounted` (source assertion: both `addEventListener` and matching `removeEventListener` present; `{ passive: true }` present).
    - The delegated handler gates on `isFichaTarget(` and calls `closest('a[href^="#"]')` (source assertion).
    - The file contains NO Vue `@click` template handler, NO `<style>`, and does NOT port `highlightCard` (source assertion: `grep highlightCard app/composables/useCardNavigation.ts` returns nothing).
    - `pnpm typecheck` exits 0 and `pnpm test:unit` exits 0.
  </acceptance_criteria>
  <done>The composable exists with a pure accessor (useState singleton, defaults `[]`/`''`, verbatim navigateToCard/goBack) and an effects controller (one native document click listener gated by isFichaTarget + one passive scroll listener delegating to computeActiveSection, registered in onMounted, cleaned up in onUnmounted); typecheck and unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TripView (invoke controller once) + NavPills (.active) + BackButton (.show + @click)</name>
  <files>app/components/TripView.vue, app/components/NavPills.vue, app/components/BackButton.vue</files>
  <read_first>
    - app/components/TripView.vue (current state — the page owner; confirm `<script setup>` already awaits `useTrip(props.slug)` and that the controller call goes alongside, mirroring TheHero)
    - app/components/NavPills.vue (current shell — markup is LOCKED; only `:class` is appended; note the 7 structural pills + the `v-for` day pills compare against `d.slug`)
    - app/components/BackButton.vue (current shell — currently has NO `<script>` block; add one; markup/aria-label/glyph are LOCKED)
    - app/components/TheHero.vue (the controller-invocation precedent: `useTripModesController()` once + `useTripModes()` accessor in `<script setup>`; the "add reactive bindings to a mounted shell" pattern)
    - app/composables/useCardNavigation.ts (the API from Task 1 — `activeSection`, `canGoBack`, `goBack`)
    - app/assets/css/base.css lines 3, 90, 694, 1025 (verbatim `.nav-pill.active`, `.card.highlight`, `.back-btn.show`, `scroll-padding-top:124px` — confirm F5 only TOGGLES these; writes none)
    - CLAUDE.md §"CSS verbatim" / §"What NOT to Use" (no `<style scoped>`, no new CSS)
  </read_first>
  <action>
    Three surgical edits, appending bindings only — NO markup restructuring, NO `<style scoped>`, ZERO new CSS.

    TripView.vue: in `<script setup>`, add a single call `useCardNavigationController()` (the effects host — TripView is the page owner mounted once, exactly as TheHero hosts `useTripModesController()`). Place it after the `await useTrip(props.slug)` line so the controller's own `await useTrip('roma')` resolves consistently. Do NOT add a wrapper `<div>` or any `@click` to `<main>` (the controller uses a native `document` listener — Pitfall 1). `<template>` is otherwise unchanged.

    NavPills.vue: in `<script setup>`, add `const { activeSection } = useCardNavigation()` (pure accessor, no effects). On EACH `<a class="nav-pill">`, append a reactive `:class` that adds `active` when the pill matches the active section — for the 7 structural pills use the literal id (e.g. `:class="{ active: activeSection === 'inicio' }"` for `#inicio`, `'mapa'` for `#mapa`, `'reservas'`, `'gastronomia'`, `'practica'`, `'arte'`, `'arquitectura'`); for the `v-for` day pills use `:class="{ active: activeSection === d.slug }"`. The static `class="nav-pill"` stays — the `:class` object merges with it. Do NOT change `id="nav-pills"`, the pill order, the grafías (incl. "Pratica"), or any `href`.

    BackButton.vue: add a `<script setup lang="ts">` block with `const { canGoBack, goBack } = useCardNavigation()` (pure accessor). On the `<button id="back-btn" class="back-btn">`, append `:class="{ show: canGoBack }"` and `@click="goBack"`. Keep `id="back-btn"`, `aria-label="Volver"`, the literal `←` glyph in `.back-btn-arrow`, and the text "Volver" verbatim. The static `class="back-btn"` stays; the `:class` merges.

    Update each file's header comment to note F5 wired the reactive binding (consistent with the existing F3 header prose) without restructuring the shell.
  </action>
  <verify>
    <automated>pnpm typecheck >/dev/null 2>&1 && pnpm lint >/dev/null 2>&1 && pnpm generate >/dev/null 2>&1 && echo "typecheck+lint+generate OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/TripView.vue` contains exactly one `useCardNavigationController()` call and no `@click` on `<main>` (source assertion: `grep -c "useCardNavigationController()" app/components/TripView.vue` returns 1).
    - `app/components/NavPills.vue` contains `const { activeSection } = useCardNavigation()` and a `:class="{ active:` binding on the pills, including `activeSection === d.slug` for the day `v-for` (source assertion).
    - `app/components/NavPills.vue` markup is otherwise unchanged: still has `id="nav-pills"`, the 7 structural `href`s, and "Pratica" verbatim (source assertion: those tokens still present).
    - `app/components/BackButton.vue` contains `@click="goBack"` and `:class="{ show: canGoBack }"` and a `<script setup` block with `const { canGoBack, goBack } = useCardNavigation()` (source assertion).
    - No modified component contains `<style` (source assertion: `grep -L "<style" app/components/NavPills.vue app/components/BackButton.vue app/components/TripView.vue` lists all three).
    - `app/assets/css/base.css` is unchanged (CSS verbatim — zero new CSS; not in this plan's files_modified).
    - `pnpm typecheck` exits 0, `pnpm lint` exits 0, and `pnpm generate` exits 0 (the static build succeeds with the wired components).
  </acceptance_criteria>
  <done>TripView invokes the controller once; NavPills binds reactive `.active` per pill against `activeSection`; BackButton binds `.show` via `canGoBack` and `@click="goBack"`; no shell markup is restructured, no `<style>` is added, base.css is untouched; typecheck, lint, and `pnpm generate` all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM anchors → delegated click listener | The native `document` click handler reads `href` from `<a>` elements (MDC prose links, timeline `tl-title`) and calls `getElementById(href.slice(1))` + `scrollIntoView`. The hrefs originate from F2 content (zod-validated YAML), not runtime user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | delegated click → `getElementById(href.slice(1))` | mitigate | Bounded by `isFichaTarget(id, monById.value)` (D-02): only ids present in the zod-validated `monById` are acted on; an arbitrary/injected id falls through to a native section jump. `getElementById` matches an element by id — it is NOT an injection sink (not `innerHTML`, not `eval`, not a CSS selector). `event.preventDefault()` cancels default navigation. Content is build-time YAML, not runtime input. Severity: LOW. |
| T-05-02 | Denial of Service | `navStack` growth + repeated `scrollIntoView` | accept | `navStack` is an in-memory array bounded by user clicks; unbounded growth requires thousands of deliberate clicks with no security impact on a read-only static site. No mitigation needed (RESEARCH §Security Domain). |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs NOTHING (RESEARCH §Package Legitimacy Audit: zero new packages). No slopcheck applies; the phase uses only nuxt/@nuxt-content/vitest/@playwright already audited in F1/F2. |

Static SSG; no server, no auth, no persisted user input. The single real surface (reading `href` and calling `getElementById`/`scrollIntoView`) is bounded by the `monById.has(id)` predicate + `preventDefault`. Expected severity LOW — documented honestly per the phase security note; no server/auth threats invented.
</threat_model>

<verification>
- `pnpm test:unit` green (pure logic from 05-01 still passing).
- `pnpm typecheck` and `pnpm lint` clean (no eslint config change — composable is not a single-word component; PATTERNS.md confirms the allowlist is unaffected).
- `pnpm generate` produces the static site with the wired components (SSR/hydration default = empty stack + no active pill).
- Behavioral parity (smooth scroll, `.highlight`, `.active`, `.show`, prose-interception vs native section jump) is verified in Plan 05-03's Playwright spec.
</verification>

<success_criteria>
- SC#1: `navigateToCard` scrolls smoothly to the ficha, adds `.highlight` for 2500ms, and pushes scrollY; `goBack` restores the prior scroll position; `BackButton` shows `.show` exactly when the stack is non-empty (implemented; behavior asserted in 05-03).
- SC#2: the scrollspy sets `activeSection` via the verbatim `scrollY+130` last-wins selector through a passive scroll listener (no IntersectionObserver); `NavPills` reflects it as `.active`.
- SC#3: prose `a[href^="#"]` whose target is a ficha are intercepted by the native delegated listener → `navigateToCard` (D-01); section links fall through to native anchor jumps (D-02/D-03).
- D-05: the public API (`navigateToCard`, `goBack`, `activeSection`, `canGoBack`) is stable for F6 (search) and F7 (map) to consume without refactor.
</success_criteria>

<output>
Create `.planning/phases/05-navegaci-n-transversal/05-02-SUMMARY.md` when done.
</output>
