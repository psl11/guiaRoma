---
phase: 02-esquema-de-datos-migraci-n-del-contenido
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - content/trips/roma/trip.yml
  - content/trips/roma/days/viernes.yml
  - content/trips/roma/days/sabado.yml
  - content/trips/roma/days/domingo.yml
  - content/trips/roma/days/lunes.yml
  - content/trips/roma/days/martes.yml
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "Cada dia codifica su timeline como array ordenado con kind y pace por fila (DATA-02)"
    - "Cada dia declara cards como string array en el ORDEN del DOM de su seccion, no del array places (DATA-03)"
    - "El trip codifica hero, info-grid, como usar y el center/zoom del mapa"
    - "Cada day.cards y cada timeline.ref resuelven a monumentos/food existentes"
  artifacts:
    - path: "content/trips/roma/trip.yml"
      provides: "Documento trip de Roma (hero, infoCards, howTo, map)"
      contains: "slug: roma"
    - path: "content/trips/roma/days/viernes.yml"
      provides: "Dia viernes: timeline discriminado + cards en orden de DOM"
      contains: "timeline"
    - path: "content/trips/roma/days/martes.yml"
      provides: "Dia martes: timeline + cards"
      contains: "cards"
  key_links:
    - from: "content/trips/roma/days/viernes.yml"
      to: "content/trips/roma/monuments"
      via: "cards[] referencia slugs de monumento en orden de visita"
      pattern: "cards"
    - from: "content/trips/roma/days/sabado.yml"
      to: "content/trips/roma/monuments"
      via: "timeline stop.ref a slug de monumento"
      pattern: "ref"
---

<objective>
Migrar el documento trip de Roma y los 5 dias (viernes, sabado, domingo, lunes, martes), que son las dos piezas mas criticas del modelo: el timeline ordenado con kind+pace por fila (DATA-02) y, sobre todo, cards como string array en el ORDEN exacto del DOM de cada seccion de dia (DATA-03) — la pieza de la que la Fase 6 reproduce la ruta del dia (hoy depende del orden del DOM, NO del array places).

Purpose: cards[] mal ordenado (p.ej. tomado del array places, que va por numeral romano con saltos) desordenaria la ruta del dia. El timeline es la estructura mas rica del HTML y su discriminacion por kind habilita el render de Fase 4.
Output: trip.yml + 5 days/*.yml validados por schema.spec e invariants.spec del Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-esquema-de-datos-migraci-n-del-contenido/02-RESEARCH.md
@.planning/phases/02-esquema-de-datos-migraci-n-del-contenido/02-PATTERNS.md
@shared/schemas.ts

<interfaces>
DaySchema y TripSchema viven en shared/schemas.ts (Plan 01). El executor escribe YAML que valida
contra ellos. NO redefinir el esquema; usar exactamente sus nombres de campo.

Fronteras de seccion de dia en index.html (verificado): viernes 2375-2840, sabado 2840-3445,
domingo 3445-4002, lunes 4002-4736, martes 4736-5260. Timeline de viernes leido a fondo 2403-2446.

cards[] = orden de aparicion de los article.card DENTRO de la seccion del dia (Pitfall 3, RESEARCH
252-256), NUNCA el orden del array places (que va por numeral romano con saltos: minerva XXXV y
san-luigi XXXVI estan entre los del lunes). Leer el DOM de cada seccion para el orden visual.

Sutileza auditorium: el article.card auditorium esta maquetado en la seccion de sabado (linea 3381)
pero places dice day Domingo noche. cards[] sigue el DOM (donde aparece la ficha en la seccion);
day del monumento (campo de monument, Plan 04) viene de places. No confundir.

Las 6 kinds del timeline (02-PATTERNS.md 149-159 / RESEARCH 349-376): stop (tl-item, con ref/tag/
note/disabled/reservedEvent), transport (tl-transport taxi/walk/train con modes), meta (tl-meta
items con level ok/warn/plain), food (tl-food entries), reservation (tl-resv-meta). data-pace de
cada fila a pace en all/medium/slow-only.

trip.yml (RESEARCH 482-491): title (h1 con em), decoration, meta, quote+quoteAttr, infoCards
(info-grid label/value), howTo (2 parrafos), map center/zoom (setView 41.8989,12.477 zoom 14).
Fuente: index.html 2283-2357 + 2361 (mapa).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar trip.yml (hero, info-grid, como usar, mapa)</name>
  <files>content/trips/roma/trip.yml</files>
  <read_first>
    - index.html 2283-2357 (seccion inicio: h1 con em, hero-decoration, hero-meta, hero-quote + attr, info-grid, Como usar esta guia) y 2361 (seccion mapa) y 6316-6320 (setView del mapa: center/zoom)
    - shared/schemas.ts (TripSchema: nombres de campo exactos)
    - 02-RESEARCH.md (TripSchema 482-491; ejemplo de YAML multilinea 516-550 para el estilo de bloques)
  </read_first>
  <action>
    Crear content/trips/roma/trip.yml como UN objeto YAML (nunca array raiz; Pitfall RESEARCH 221). Campos: slug roma; title (h1, con el em en markdown-inline); decoration; meta; quote y quoteAttr; infoCards como array de label/value (value en Md, conserva strong/br como markdown); howTo como array de Md (los 2 parrafos de Como usar esta guia); map con center (lat/lng del setView, 41.8989 y 12.477) y zoom (14). Transcribir el texto 1:1 desde index.html 2283-2357; usar bloques YAML legibles (folded > o literal |) para la prosa.
    Tras escribir, validar con el schema.spec del Plan 01.
  </action>
  <verify>
    <automated>test -f content/trips/roma/trip.yml; grep -q "slug: roma" content/trips/roma/trip.yml; pnpm exec vitest run tests/data/schema.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - content/trips/roma/trip.yml existe y es un unico objeto YAML (no array raiz)
    - Declara slug roma, title, decoration, meta, quote, quoteAttr, infoCards, howTo, map con center y zoom
    - map.center coincide con el setView de index.html (lat 41.8989, lng 12.477) y zoom 14
    - El fichero valida contra TripSchema (schema.spec en verde)
    - El texto de hero/info-grid/como usar coincide 1:1 con index.html 2283-2357
  </acceptance_criteria>
  <done>trip.yml migrado y validado: hero, info-grid, como usar y center/zoom del mapa, 1:1 con el original.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar viernes, sabado y domingo (timeline discriminado + cards en orden de DOM)</name>
  <files>content/trips/roma/days/viernes.yml, content/trips/roma/days/sabado.yml, content/trips/roma/days/domingo.yml</files>
  <read_first>
    - index.html seccion viernes 2375-2840 (timeline a fondo 2403-2446) y los article.card del viernes (galleria-sciarra 2450, fontana-trevi 2513, santignazio 2579, pantheon 2643, piazza-navona 2708, campo-fiori 2772)
    - index.html seccion sabado 2840-3445 (incluye el card auditorium maquetado en 3381, sutileza de orden) y domingo 3445-4002
    - index.html 6269-6314 (places: para confirmar que NO se usa su orden en cards)
    - shared/schemas.ts (DaySchema, TimelineRow discriminado, los kinds y pace)
    - 02-RESEARCH.md (timeline 349-396; Pitfall 3 252-256 orden de cards) y 02-PATTERNS.md (days 145-164)
  </read_first>
  <action>
    Crear content/trips/roma/days/viernes.yml, sabado.yml y domingo.yml, un objeto cada uno. Por dia: slug (viernes/sabado/domingo), trip roma, order (1/2/3), roman, eyebrow (section-eyebrow), title, subtitle, stats (day-stats walk/train/taxi/metro), light (dia-ligera: items see/move/skip/rest) si existe, timeline y cards.
    timeline: transcribir cada fila del DOM del dia en orden, mapeando a su kind (stop, transport, meta, food, reservation) segun el shape de TimelineRow. Cada fila con data-pace a pace (all/medium/slow-only); stop con ref a la ancla cuando el tl-title es un enlace, tag/note/disabled/reservedEvent segun el HTML; transport con variant taxi/walk/train, modes (icon, recommended, desc, tag, meta) y footnote; meta con items (level ok/warn/plain + text); food con entries (ref o href, name, reserved, badge, time, desc) y footnote; reservation con text. Conservar strong/em como markdown.
    cards: el ORDEN de aparicion de los article.card dentro de la seccion del dia (leer el DOM, NO places; Pitfall 3). Para sabado, incluir auditorium en la posicion donde su article.card aparece en la seccion (linea 3381) aunque places lo etiquete Domingo noche. pantheon aparece en viernes; si su ficha tambien se referencia en sabado, incluirlo segun el DOM de cada seccion.
    Validar con schema.spec (forma) tras escribir. invariants.spec resolvera cards/refs cuando los monumentos existan (Plan 04/05).
  </action>
  <verify>
    <automated>test -f content/trips/roma/days/viernes.yml; test -f content/trips/roma/days/sabado.yml; test -f content/trips/roma/days/domingo.yml; pnpm exec vitest run tests/data/schema.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Los 3 ficheros existen, cada uno un objeto YAML con slug, trip, order, timeline y cards
    - timeline es un array ordenado cuyas filas usan kind en stop/transport/meta/food/reservation y cada fila aplicable lleva pace en all/medium/slow-only (DATA-02)
    - cards es un string array; su orden coincide con el orden de los article.card en la seccion del dia (NO con el orden de places) (DATA-03)
    - sabado.cards incluye auditorium en la posicion del DOM de su seccion
    - Los 3 ficheros validan contra DaySchema (schema.spec en verde)
  </acceptance_criteria>
  <done>viernes, sabado y domingo migrados: timeline discriminado con pace por fila y cards en orden de DOM, validados por schema.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar lunes y martes (timeline discriminado + cards en orden de DOM)</name>
  <files>content/trips/roma/days/lunes.yml, content/trips/roma/days/martes.yml</files>
  <read_first>
    - index.html seccion lunes 4002-4736 (article.card: minerva 4096, san-luigi 4155, casanatense 4213, torre-scimmia 4275, popolo 4337, spagna 4401, tazza-doro 4471, laterano 4544, san-clemente 4611, san-pietro-vincoli 4672) — nota: minerva XXXV y san-luigi XXXVI van por DOM aqui, no por numeral
    - index.html seccion martes 4736-5260 (article.card: galleria-borghese 4809, palazzo-barberini 4889, cappuccini 4948, smm 5012, santa-teresa 5073, palazzo-massimo 5142, angeli 5199)
    - shared/schemas.ts (DaySchema) y 02-RESEARCH.md (timeline 349-396; Pitfall 3) y 02-PATTERNS.md (days 145-164)
  </read_first>
  <action>
    Crear content/trips/roma/days/lunes.yml y martes.yml con la misma estructura y reglas que Task 2 (slug, trip, order 4/5, roman, eyebrow, title, subtitle, stats, light si existe, timeline, cards). Transcribir el timeline de cada seccion fila a fila con su kind y pace; cards en el ORDEN del DOM de cada seccion (leer el orden visual; recordar que minerva y san-luigi salen en el DOM del lunes aunque sus numerales sean XXXV/XXXVI). Conservar strong/em como markdown.
    Validar con schema.spec tras escribir.
  </action>
  <verify>
    <automated>test -f content/trips/roma/days/lunes.yml; test -f content/trips/roma/days/martes.yml; pnpm exec vitest run tests/data/schema.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - lunes.yml y martes.yml existen, cada uno un objeto YAML con slug, trip, order, timeline y cards
    - timeline ordenado con kind y pace por fila (DATA-02); cards string array en orden de DOM (DATA-03)
    - lunes.cards refleja el orden visual de su seccion (incluyendo minerva y san-luigi donde aparecen en el DOM, no por numeral)
    - Ambos validan contra DaySchema (schema.spec en verde)
  </acceptance_criteria>
  <done>lunes y martes migrados con timeline discriminado y cards en orden de DOM, validados por schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html (local) hacia YAML | Transcripcion de contenido; fuente del repo, no entrada externa |

Nota: creacion de datos para un sitio estatico. Sin runtime, red, entrada de usuario ni secretos. Vector unico: integridad de la transcripcion (orden de cards y de timeline), cubierto por schema.spec/invariants.spec (Plan 01) y migration-diff (Plan 02).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Orden de cards (ruta del dia) e integridad del timeline | mitigate | invariants.spec resuelve cards/refs; schema.spec valida discriminatedUnion + pace; migration-diff valida texto/enlaces del dia. El orden de DOM se transcribe leyendo las secciones (Pitfall 3). |
| T-02-10 | Information Disclosure | Datos del dia (YAML) | accept | Contenido publico de guia; sin PII ni secretos. ASVS L1 confidencialidad: N/A. |
| T-02-11 | Spoofing/Repudiation/DoS/Elevation | — | accept | N/A: sin runtime, identidad, servicio ni privilegios. |

ASVS L1: categorias web N/A en build-time de datos estaticos. La integridad de orden/contenido la garantizan los tests del Plan 01/02.
</threat_model>

<verification>
- Los 6 ficheros (trip + 5 days) existen y son objetos YAML unicos.
- pnpm exec vitest run tests/data/schema.spec.ts en verde (forma + discriminatedUnion + pace).
- cards en orden de DOM (no places); auditorium en sabado por DOM; minerva/san-luigi en lunes por DOM.
- invariants.spec resolvera cards/refs cuando Plan 04/05 migren los monumentos.
</verification>

<success_criteria>
- trip.yml migrado 1:1 con center/zoom del mapa.
- Los 5 days con timeline discriminado por kind y pace por fila (DATA-02) y cards string array en orden de DOM (DATA-03).
- Todos validan contra DaySchema/TripSchema.
</success_criteria>

<output>
Crear .planning/phases/02-esquema-de-datos-migraci-n-del-contenido/02-03-SUMMARY.md al terminar.
</output>
