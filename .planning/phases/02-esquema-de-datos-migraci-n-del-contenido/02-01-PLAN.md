---
phase: 02-esquema-de-datos-migraci-n-del-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - shared/schemas.ts
  - content.config.ts
  - vitest.config.ts
  - eslint.config.mjs
  - tests/data/schema.spec.ts
  - tests/data/invariants.spec.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-05, DATA-06]
must_haves:
  truths:
    - "Las 6 colecciones (trip, day, monument, food, artist, reference) estan definidas y nuxt build genera sus tipos sin romper"
    - "Un dato invalido (enum/required/tipo) hace fallar el test de esquema (puerta DATA-05)"
    - "El esquema zod vive en un unico modulo importado tanto por content.config.ts como por los tests"
    - "El test de invariantes detecta ids duplicados y cross-refs que no resuelven"
    - "El esquema codifica las decisiones de modelado: prosa de monument/artist como array de secciones {heading, body} (D-01), coleccion reference bespoke reservas+practica via z.union (D-03), arquitectura dentro de artist con discriminador kind y glosario como sub-shape (D-04), y validacion en dos capas zod-enums + test de invariantes (D-06)"
  artifacts:
    - path: "shared/schemas.ts"
      provides: "Las 6 colecciones zod + sub-esquemas reutilizables, fuente unica de verdad"
      exports: ["MonumentSchema", "DaySchema", "FoodSchema", "ArtistSchema", "ReferenceSchema", "TripSchema", "Motif", "Pace", "PlaceType"]
    - path: "content.config.ts"
      provides: "Registro de las 6 colecciones type data con globs anidados, importando shared/schemas"
      contains: "defineCollection"
    - path: "vitest.config.ts"
      provides: "Runner Vitest apuntando a tests/data"
      contains: "tests/data"
    - path: "tests/data/schema.spec.ts"
      provides: "Puerta DATA-05: Schema.safeParse por fichero YAML"
    - path: "tests/data/invariants.spec.ts"
      provides: "Puerta SC4: ids unicos + cross-refs resuelven + basename igual a slug"
  key_links:
    - from: "content.config.ts"
      to: "shared/schemas.ts"
      via: "import de los 6 schemas"
      pattern: "shared/schemas"
    - from: "tests/data/schema.spec.ts"
      to: "shared/schemas.ts"
      via: "import del mismo schema (fuente unica)"
      pattern: "shared/schemas"
---

<objective>
Establecer el contrato de datos de la Fase 2: las 6 colecciones zod en un modulo compartido (shared/schemas.ts), su registro en content.config.ts (reemplazando el stub vacio de Fase 1), el tooling de test (Vitest + yaml + cheerio), y las dos puertas de validacion Node-puras (schema.spec.ts + invariants.spec.ts) que hacen cumplir DATA-05/SC4 — porque Content v3 NO valida los type data contra zod en build (issue nuxt/content#3351, confirmado en el codigo instalado).

Este plan es la raiz de la que dependen TODOS los planes de migracion (Wave 2): definen los nombres de campo, enums y cross-refs que los YAML deben cumplir, y los tests son el feedback inmediato de cada fichero migrado.

Purpose: Sin el esquema exportado + los tests, DATA-05 no se cumple (un motif mal escrito o un campo ausente llegaria a produccion silenciosamente) y los planes de migracion no tendrian contrato contra el que escribir.
Output: shared/schemas.ts, content.config.ts poblado, vitest.config.ts, scripts npm de test, las dos specs de validacion, deps de tooling instaladas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-esquema-de-datos-migraci-n-del-contenido/02-RESEARCH.md
@.planning/phases/02-esquema-de-datos-migraci-n-del-contenido/02-PATTERNS.md
@.planning/phases/02-esquema-de-datos-migraci-n-del-contenido/02-VALIDATION.md

<interfaces>
El blueprint LITERAL del esquema esta en 02-RESEARCH.md seccion Code Examples (lineas 282-491),
derivado 1:1 de index.html. El executor lo usa como fuente, ajustando solo lo que CONTEXT
marca como Claude's Discretion. NO re-explorar el codebase: estos son los contratos.

Sub-esquemas reutilizables (02-RESEARCH.md 286-298):
  Coords = z.object({ lat, lng }). Fact = z.object({ label, value }). Md = z.string().
  Link = z.object({ ref, label: Md, note: Md.optional() }).
  Motif = z.enum([19 valores verbatim de CARD_TO_MOTIF index.html:2213]).
  Pace = z.enum(['all','medium','slow-only']). PlaceType = z.enum(['card','guided','concert']).

Colecciones (02-RESEARCH.md 305-491):
  MonumentSchema  incl. artists: z.array(Link).optional() Y arch: z.array(Link).optional()
  DaySchema       timeline: z.array(z.discriminatedUnion('kind', 6 kinds)) + cards: z.array(z.string())
  FoodSchema      group, badge/badgeKind, name, address, desc, plato?, footer, mapsQuery
  ArtistSchema    z.discriminatedUnion('kind', artist | arquitectura | glossary)
  ReferenceSchema union de ReservasSchema + PracticaSchema
  TripSchema      title, decoration, meta, quote, infoCards, howTo, map{center,zoom}

content.config.ts (02-RESEARCH.md 495-513): 6 defineCollection type data con source globs anidados
  trip trips/*/trip.yml . day trips/*/days/*.yml . monument trips/*/monuments/*.yml
  food trips/*/food/*.yml . artist trips/*/artists/*.yml . reference trips/*/reference/*.yml

Patron del test Node-puro (02-RESEARCH.md 596-616 / 02-PATTERNS.md 235-249):
  glob YAML, parseYaml(readFileSync), Schema.safeParse(data), expect(r.success).toBe(true)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar tooling de test y configurar Vitest + scripts</name>
  <files>package.json, pnpm-lock.yaml, vitest.config.ts, eslint.config.mjs</files>
  <read_first>
    - package.json (deps @nuxt/content@3.14.0 + zod@4.4.3 ya presentes; devDeps better-sqlite3/prettier/@playwright/test; scripts test:golden/test:subpath en lineas 14-16)
    - pnpm-lock.yaml (existe; pnpm@10.32.1 es el packageManager)
    - playwright.config.ts (analog de convencion: defineConfig + cabecera en espanol citando el requisito; replicar el ESTILO, no la API; testDir tests/parity debe quedar intacto)
    - eslint.config.mjs (lineas 12-14: ignores app/assets/css y tests/parity; decidir cobertura de tests/data)
    - 02-RESEARCH.md (Standard Stack 87-105; Validation Architecture 570-577)
  </read_first>
  <action>
    Asegurar deps instaladas y anadir tooling con pnpm (NO npm, NO yarn): ejecutar pnpm install (asegura @nuxt/content/zod en node_modules), luego pnpm add -D con vitest fijado a 4.1.9 mas yaml y cheerio. Las tres son devDependencies. vitest se fija a 4.1.9 (latest compatible verificado en research); yaml y cheerio a latest. NO instalar @nuxt/test-utils ni happy-dom (el test es Node puro, no usa runtime Nuxt; 02-RESEARCH.md 91/617).
    Crear vitest.config.ts en la raiz: default export de defineConfig de vitest/config, con cabecera-comentario en espanol (estilo playwright.config.ts 3-6) explicando que es la puerta de validacion de datos (DATA-05/DATA-06/SC4) Node-pura. Configurar test.include apuntando a los spec de tests/data. NO incluir tests/parity (eso queda en Playwright; separacion de runners 02-PATTERNS.md 350-352). NO usar modo watch.
    Anadir a package.json scripts el comando test:data con valor vitest run tests/data, conservando los scripts existentes.
    En eslint.config.mjs: NO anadir tests/data a ignores (es TS fuente, debe lintarse; 02-PATTERNS.md 38/352). Verificar que tests/data no queda ignorado; no tocar el resto del bloque.
  </action>
  <verify>
    <automated>test -f vitest.config.ts && grep -q test:data package.json && grep -q tests/data vitest.config.ts && pnpm ls vitest yaml cheerio && echo verify-ok</automated>
  </verify>
  <acceptance_criteria>
    - vitest, yaml y cheerio aparecen en package.json devDependencies y en pnpm ls
    - pnpm-lock.yaml actualizado con las 3 nuevas devDeps
    - vitest.config.ts existe, exporta defineConfig por defecto y su test.include apunta a tests/data (NO a tests/parity)
    - package.json scripts contiene test:data con valor vitest run tests/data y conserva test:golden y test:subpath
    - eslint.config.mjs NO lista tests/data en ignores
    - playwright.config.ts testDir sigue siendo ./tests/parity (sin cambios)
  </acceptance_criteria>
  <done>Tooling de test instalado, Vitest configurado para tests/data, script test:data disponible, runners disjuntos Vitest y Playwright.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Definir las 6 colecciones zod en shared/schemas.ts y poblar content.config.ts</name>
  <files>shared/schemas.ts, content.config.ts</files>
  <read_first>
    - content.config.ts (stub actual: import defineContentConfig + collections vacio; conservar wrapper + estilo de comentario)
    - 02-RESEARCH.md (Code Examples 280-513 blueprint LITERAL; Pattern 1 188-203 slug-no-id; Pattern 2 205-208 timeline; Pattern 3 210-213 sections; Pitfall 2 246-250 artists+arch; Pitfall 6 270-274 no refine; Open Q 668-680)
    - 02-PATTERNS.md (shared/schemas.ts 80-101; Shared Patterns motif 326-332, slug 342-344, zod import 346-348)
    - 02-CONTEXT.md (decisions D-01..D-04 + Claude's Discretion 53-58)
    - index.html linea 2213 (CARD_TO_MOTIF, los 19 valores verbatim) y 6269-6314 (places: type card/guided/concert)
  </read_first>
  <behavior>
    El esquema es correcto cuando estas aserciones (escritas en Task 3) se cumplen:
    - Un objeto monumento valido (slug, roman, name, coords, type en card/guided/concert, motif en enum, sections, facts, mapsQuery) pasa MonumentSchema.safeParse con success
    - Un monumento con motif foo (fuera del enum) hace fallar safeParse (DATA-05)
    - Un monumento sin coords hace fallar safeParse (campo requerido)
    - MonumentSchema acepta artists y arch como arrays opcionales de Link (Pitfall 2)
    - Un dia con timeline heterogeneo (kinds stop/transport/meta/food/reservation) y cada stop con pace en all/medium/slow-only pasa DaySchema.safeParse; cards es string array
    - ArtistSchema discrimina por kind y acepta artist, arquitectura y glossary
    - ReferenceSchema acepta tanto el shape reservas como el practica
  </behavior>
  <action>
    Crear shared/schemas.ts (el directorio shared/ no existe, crearlo). Importar zod con import z desde zod, NUNCA desde @nuxt/content (re-export deprecado, CLAUDE.md What NOT to Use). Exportar los sub-esquemas reutilizables (Coords, Fact, Md, Link, Motif, Pace, PlaceType) y las 6 colecciones (TripSchema, DaySchema, MonumentSchema, FoodSchema, ArtistSchema, ReferenceSchema) transcribiendo desde el blueprint de 02-RESEARCH.md Code Examples (fuente literal de campos/enums; no inventar).
    Decisiones obligatorias (no reabrir): (1) campo estable slug z.string() en cada coleccion igual al ancla id de index.html, NUNCA un campo id (reservado por Content; Pattern 1). (2) Motif enum con los 19 valores distintos de CARD_TO_MOTIF (dome, pantheon, arch, fountain, obelisk, statue, painting, church, fortress, temple, garden, keyhole, mask, monument, rooftops, library, tower, stairs, coffee), verbatim de index.html:2213. (3) MonumentSchema con artists z.array(Link).optional() (hacia art) Y arch z.array(Link).optional() (hacia arq), ambos multi-enlace (Pitfall 2). (4) prosa como sections z.array de objetos heading/body (heading libre, body Md), array ordenado, NUNCA campos fijos por seccion (D-01/Pattern 3). (5) DaySchema.timeline z.array de z.discriminatedUnion por kind con stop/transport/meta/food/reservation, pace por fila donde aplica, mas cards z.array(z.string()). (6) ArtistSchema z.discriminatedUnion por kind con tres valores artist, arquitectura, glossary (glosario como 3er kind, research 186). (7) ReferenceSchema union de ReservasSchema + PracticaSchema (bespoke por seccion, D-03). NO usar refine ni superRefine para cross-refs entre ficheros (se pierden al convertir a JSON-Schema; las cross-refs van en el test de invariantes Task 4).
    Decisiones de discrecion a fijar y documentar en comentario: nombres exactos de campo, modelado de group/groupIntro de gastronomia, ubicacion de eyebrows/intros. Seguir las recomendaciones del research salvo mejor criterio; el shape debe reproducir lo que las clases CSS de Fase 1 esperan (checklist 02-CONTEXT.md 99).
    Reemplazar content.config.ts: importar defineCollection ademas de defineContentConfig, importar los 6 schemas desde ./shared/schemas, poblar collections con 6 defineCollection type data, source y schema usando los globs anidados de 02-RESEARCH.md 502-507. Cabecera-comentario en espanol explicando que el esquema vive en shared/ porque lo comparten config y tests (DATA-05).
    Tras escribir: pnpm typecheck debe pasar y pnpm exec nuxt build completar sin error de tipos de coleccion (DATA-01). Si build falla por el campo slug (Open Q 668-671), confirmar que no colisiona con ningun reservado y ajustar.
  </action>
  <verify>
    <automated>grep -q "from 'zod'" shared/schemas.ts && grep -q defineCollection content.config.ts && pnpm typecheck && echo verify-ok</automated>
  </verify>
  <acceptance_criteria>
    - shared/schemas.ts importa z desde zod y NO desde @nuxt/content
    - shared/schemas.ts exporta MonumentSchema, DaySchema, FoodSchema, ArtistSchema, ReferenceSchema, TripSchema, Motif, Pace, PlaceType
    - Motif tiene exactamente 19 valores; MonumentSchema declara artists y arch (ambos arrays opcionales)
    - DaySchema.timeline es z.array de z.discriminatedUnion por kind; DaySchema.cards es z.array(z.string())
    - ArtistSchema es z.discriminatedUnion con kind en artist, arquitectura, glossary
    - content.config.ts importa los 6 schemas desde ./shared/schemas y registra 6 defineCollection type data con los globs anidados
    - pnpm typecheck pasa; pnpm exec nuxt build genera los tipos de las 6 colecciones sin romper (DATA-01)
    - Ningun schema usa refine ni superRefine para validar referencias entre ficheros
  </acceptance_criteria>
  <done>Las 6 colecciones zod existen como fuente unica en shared/schemas.ts, content.config.ts las registra, los tipos generan y el build no rompe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test de validacion de esquema (puerta DATA-05) en tests/data/schema.spec.ts</name>
  <files>tests/data/schema.spec.ts</files>
  <read_first>
    - shared/schemas.ts (creado en Task 2; los schemas que este test importa y ejecuta)
    - tests/parity/subpath.spec.ts (convencion SOLO: imports node:, cabecera-doc en espanol con el requisito; framework difiere, aqui es vitest)
    - 02-RESEARCH.md (Patron del test 594-617 cuerpo blueprint; Pitfall 1 240-244 por que Node puro y no queryCollection)
    - 02-PATTERNS.md (schema.spec.ts 220-253)
    - 02-VALIDATION.md (Per-Task Verification Map 42-49)
  </read_first>
  <behavior>
    - Para cada .yml de cada coleccion, parseYaml + Schema.safeParse del schema correspondiente da success (un it por fichero, locus de fallo claro)
    - Si no hay ficheros aun (Wave 2 no ejecutada), el test sigue siendo verde de forma no vacia: casos fixture in-line donde un objeto valido pasa y un objeto con motif invalido o sin campo requerido FALLA safeParse (prueba la PUERTA DATA-05 sin depender de datos)
    - Cuando los datos existan, el conteo por coleccion es el esperado: 38 monuments, 26 food, 13 artists, 5 days, 2 reference, 1 trip (SC1)
  </behavior>
  <action>
    Crear tests/data/schema.spec.ts (Vitest, Node puro). Cabecera-doc en espanol citando DATA-05, DATA-02 y DATA-06 (estilo subpath.spec.ts 7-23). Imports: describe, it, expect de vitest; readFileSync y un glob (node:fs globSync o fast-glob, elegir uno) y parse de yaml; los schemas desde ../../shared/schemas.
    Por cada coleccion: globear sus .yml (p.ej. content/trips/roma/monuments con patron *.yml), y por cada fichero un it que hace Schema.safeParse(parseYaml(readFileSync(f utf8))) y expect del r.success (con r.error.issues en el mensaje) igual a true. NO usar queryCollection (Content limpia los datos invalidos y ocultaria los fallos que DATA-05 debe atrapar; Pitfall 1 / RESEARCH 617).
    Anadir casos fixture in-line que NO dependen de ficheros: un objeto monumento minimo valido pasa; el mismo con motif foo falla; el mismo sin coords falla. Garantizan que la PUERTA existe aunque Wave 2 aun no haya migrado (evita un test vacuo verde).
    Anadir un describe de conteo: cuando existan ficheros, expect del numero de ficheros por coleccion igual a 38/26/13/5/2/1. Mientras no haya datos, tolerar 0 (it.skip o guard). Cubre tambien DATA-02 (un day valido con timeline discriminado pasa) y DATA-06 (los campos Md son strings).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/data/schema.spec.ts && echo verify-ok</automated>
  </verify>
  <acceptance_criteria>
    - tests/data/schema.spec.ts importa los schemas desde shared/schemas (misma fuente que content.config.ts)
    - Contiene casos fixture in-line: objeto valido pasa, motif invalido falla, campo requerido ausente falla, y corren aunque no exista ningun .yml
    - El comando de vitest sobre schema.spec.ts termina con exit 0
    - El test NO usa queryCollection ni runtime Nuxt (Node puro: readFileSync + parseYaml + safeParse)
    - Existe un describe de conteo por coleccion con los numeros esperados (38/26/13/5/2/1), tolerante a 0 mientras Wave 2 no haya migrado
  </acceptance_criteria>
  <done>La puerta DATA-05 existe y es verde: un dato invalido haria fallar el test; los casos fixture lo demuestran antes de migrar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test de invariantes (ids unicos + cross-refs) en tests/data/invariants.spec.ts</name>
  <files>tests/data/invariants.spec.ts</files>
  <read_first>
    - shared/schemas.ts (los tipos/colecciones cuyos cross-refs se resuelven)
    - tests/parity/subpath.spec.ts (convencion: imports node:, cabecera-doc en espanol con el requisito)
    - 02-RESEARCH.md (invariants blueprint 613-616; Open Q 673-676 escanear ancla id dentro de Md; Pitfall 6 270-274 cross-refs NO en zod; Pattern 1 200-201 basename igual a slug)
    - 02-PATTERNS.md (invariants.spec.ts 257-269, lista exacta de cross-refs)
    - 02-CONTEXT.md (cross-refs a validar 107: day.cards, timeline.ref, artist.seenIn, archLink)
  </read_first>
  <behavior>
    - Cargar TODAS las colecciones, construir un Set de slugs y aseverar que no hay duplicados (igual al numero de ficheros)
    - Toda cross-ref resuelve a un slug existente: day.cards hacia monument; timeline kind stop/food ref hacia monument o food; monument.artists ref hacia artist kind artist; monument.arch ref hacia artist kind arquitectura; artist.seenIn ref hacia monument; reservas.table ref hacia monument o food
    - Cada ancla id encontrada dentro de los campos Md de prosa resuelve a un slug existente (cubre archLink inline y cualquier enlace markdown a id; Open Q 676)
    - basename del fichero sin extension coincide con el campo slug del objeto (Pattern 1)
    - Una cross-ref rota (ref a slug inexistente) hace fallar el test (probarlo con un caso fixture in-line)
  </behavior>
  <action>
    Crear tests/data/invariants.spec.ts (Vitest, Node puro). Cabecera-doc en espanol citando DATA-03 y SC4. Cargar todas las colecciones leyendo los .yml de cada directorio (mismo patron de glob que schema.spec). Construir allSlugs como Set de todos los slugs; aseverar que el tamano del Set es igual al numero de ficheros (sin duplicados).
    Para cada cross-ref de la lista (02-PATTERNS.md 261-267): expect del allSlugs.has(ref) igual a true. Implementar el escaneo de anclas en prosa: por cada campo Md (sections body, etc.) extraer las anclas id con un regex sobre el string Markdown y aseverar que cada id resuelve (cubre archLink inline). NO meter esta logica en zod (refine se pierde y no ve las otras colecciones; Pitfall 6).
    Aseverar basename del fichero igual al slug para cada documento (Pattern 1; mantiene resolubles las anclas id y las claves roma-note-id de Fase 7).
    Anadir un caso fixture in-line: dado un Set de slugs y una ref inexistente, el aserto falla (garantiza que la puerta detecta cross-refs rotas aunque Wave 2 aun no exista). Mientras no haya datos, los asertos sobre ficheros toleran conjuntos vacios; el fixture siempre corre. Tras Wave 2, este test es la puerta dura de SC4.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/data/invariants.spec.ts && echo verify-ok</automated>
  </verify>
  <acceptance_criteria>
    - tests/data/invariants.spec.ts carga todas las colecciones y construye un Set de slugs con aserto de unicidad
    - Resuelve explicitamente: day.cards, timeline.ref (stop/food), monument.artists, monument.arch, artist.seenIn, reservas.table.ref
    - Escanea las anclas id dentro de los campos Md de prosa y asevera que resuelven (cubre archLink inline)
    - Asevera basename del fichero igual al slug
    - Incluye un caso fixture in-line donde una ref rota hace fallar el aserto (la puerta funciona sin datos)
    - El comando de vitest sobre invariants.spec.ts termina con exit 0
    - La resolucion de cross-refs vive en el test, NO en zod
  </acceptance_criteria>
  <done>La puerta de invariantes existe: ids unicos, todas las cross-refs (incl. anclas inline) resuelven, basename igual a slug; un fixture prueba que detecta refs rotas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm registry hacia repo (devDeps) | Se anaden vitest/yaml/cheerio como devDependencies (cadena de suministro build-time) |
| index.html (fichero local) hacia harness/datos | Fuente de la migracion; fichero del propio repo, no entrada externa |

Nota: fase de modelado de datos para un sitio ESTATICO. Sin entrada de usuario en runtime, sin auth, sin endpoints de red, sin secretos. Los unicos vectores reales son integridad de datos y cadena de suministro (devDeps), no amenazas web clasicas.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | devDeps vitest/yaml/cheerio | mitigate | Versiones fijadas (vitest 4.1.9) + pnpm-lock.yaml commiteado; las 3 son paquetes canonicos verificados en el registro (02-RESEARCH.md Package Legitimacy Audit 110-122). Sin ASSUMED/SUS/SLOP, sin checkpoint humano requerido. |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | Lockfile + auditoria de legitimidad en research (todas Aprobado). Ningun paquete SLOP/SUS. |
| T-02-02 | Information Disclosure | Datos del viaje (YAML) | accept | Contenido publico de una guia de viaje; sin PII, sin secretos. ASVS L1 de confidencialidad: N/A. |
| T-02-03 | Spoofing/Repudiation/Elevation | — | accept | N/A en build-time de sitio estatico (sin identidad, sesiones ni privilegios). |
| T-02-04 | Denial of Service | — | accept | N/A: no hay servicio en runtime (salida estatica). |

ASVS L1: autenticacion, sesion, control de acceso y criptografia son N/A en esta fase (sin runtime, sin usuarios, sin secretos). La integridad de datos se cubre con los tests de esquema/invariantes (DATA-05/SC4), no con controles web.
</threat_model>

<verification>
- pnpm install + pnpm add -D (vitest 4.1.9, yaml, cheerio) completan; pnpm-lock.yaml actualizado.
- pnpm typecheck pasa; pnpm exec nuxt build genera los tipos de las 6 colecciones sin romper (DATA-01).
- pnpm vitest run tests/data termina en verde con los casos fixture (puertas DATA-05 + SC4 demostradas antes de migrar).
- shared/schemas.ts importado por content.config.ts Y por ambos specs (fuente unica).
</verification>

<success_criteria>
- Las 6 colecciones zod existen en shared/schemas.ts (fuente unica) y content.config.ts las registra con type data + globs anidados.
- Vitest configurado y disjunto de Playwright; script test:data disponible.
- schema.spec.ts hace cumplir DATA-05 (un dato invalido falla) e invariants.spec.ts hace cumplir SC4 (ids unicos + cross-refs resuelven), ambos con casos fixture que pasan aunque Wave 2 no haya migrado.
- nuxt build genera los tipos sin error (DATA-01).
</success_criteria>

<output>
Crear .planning/phases/02-esquema-de-datos-migraci-n-del-contenido/02-01-SUMMARY.md al terminar.
</output>
</content>
</invoke>
